[
 {
  "domain": "text-model",
  "title": "EOL detection, normalization, and BOM stripping at buffer-construction time",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeTextBufferBuilder.ts:27-40 (_getEOL majority vote), :44-56 (normalize all chunks to one EOL), :99-119 (acceptChunk: BOM strip + hold back trailing \\r / high surrogate so a CRLF or surrogate pair is never split across chunks), :156-186 (_finish re-attaches held char, counts a trailing lone \\r); pieceTreeBase.ts:64-99 (createLineStarts counts cr/lf/crlf separately and tracks isBasicASCII in one pass), :320-353 (normalizeEOL re-chunks the whole tree at AverageBufferSize), :555-585 (getLineRawContent strips a trailing \\r when _EOLNormalized is false)",
  "mechanism": "The builder counts cr, lf and crlf occurrences while ingesting chunks. `_getEOL` picks '\\r\\n' if `cr + crlf > totalEOL/2`, else '\\n'. If the chosen EOL disagrees with what's in the text, every chunk is rewritten with `str.replace(/\\r\\n|\\r|\\n/g, eol)` and its lineStarts array rebuilt, so the tree is internally single-EOL. The BOM is stripped off the first chunk and stored separately on the buffer (`getBOM()`), then re-prepended only by `createSnapshot(preserveBOM)` / `getValue(eol, preserveBOM)`. `acceptChunk` refuses to end a chunk on a lone '\\r' or a high surrogate, holding the char back so no chunk boundary ever falls inside a CRLF pair or a surrogate pair. When normalization is off, `_EOLNormalized === false` and every line read strips a trailing CR with an explicit regex. `setEOL` re-runs `normalizeEOL`, and the EOL is recorded in the undo stack (beforeEOL/afterEOL in editStack.ts:41-47) so undo restores it.",
  "whyItMatters": "Open any file written on Windows or any git checkout with `core.autocrlf=true` and our editor silently keeps a '\\r' as the last character of every line. Because our view computes line ends as `nextLineStart - 1`, that CR is part of the rendered line text: the caret can be placed after it, End goes one column too far, word/subword navigation sees a spurious character, find/replace regexes with `$` misbehave, and the character is round-tripped back on save. There is also no BOM handling, so a UTF-8 BOM becomes a visible zero-width character at offset 0 of line 1 that all offsets are shifted by.",
  "ourStatus": "missing",
  "ourEvidence": "grep for `\\\\r` across packages/*/src returns only 5 unrelated hits (shiki tokenizer, merge-conflict marker regex, a paste heuristic, one LSP position probe); grep for BOM/\ufeff/feff across packages/*/src returns nothing. /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/buffers.ts:131-141 `countLineBreaks` and :167-175 `extendBufferLineIndex` index '\\n' only. /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewHelpers.ts:233-243 and virtualizedTextViewLayout.ts:327-336 build lineStarts from '\\n' only. /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewModel.ts:119-124 `lineEndOffset` returns `nextLineStart - 1`, so the CR is inside the line. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/Editor.ts:667-684 `setText` passes the raw string straight to `createEditorDocumentSession` with no normalization.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add an ingestion step in front of `createPieceTableSnapshot` (packages/editor/src/pieceTable/snapshot.ts:43): scan the source once counting cr/lf/crlf plus isBasicASCII/containsRTL, strip a leading \ufeff into a `bom` field, pick the document EOL by majority, and if it differs, `replace(/\\r\\n|\\r|\\n/g, eol)` before constructing buffers. Store `{ eol, bom }` on `PieceTableBuffers` (it is already a plain object spread on every edit, so this is free). Keep the internal representation LF-only \u2014 that keeps every existing '\\n' index in buffers.ts, virtualizedTextViewHelpers.ts and the fold/inline maps correct with zero changes \u2014 and re-expand to the document EOL only in `materializeFullText`/`readRange` when an explicit `eol` preference is passed, plus in whatever host code saves. Also normalize on the insert path: `insertIntoPieceTable` should normalize `text` (paste of CRLF content into an LF document is the common case today). Record `eol` in `DocumentTransaction.metadata` so an EOL change is undoable like Monaco does.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. `grep -rn '\\\\r' packages --include='*.ts'` (excluding node_modules) hits exactly 6 files, none in the buffer layer: /Users/shaul/Desktop/D/Editor/packages/lsp/src/positions.ts:275 (`lineBreakProbe(...).endsWith('\\r\\n')` \u2014 an LSP-position-only CRLF probe), /Users/shaul/Desktop/D/Editor/packages/editor/src/mergeConflicts.ts:278 (path sanitizer), /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/inputSelectionController.ts:1581 (paste reveal heuristic), plus tokenizer/test files. `grep -rni 'feff|byte order mark|\\bbom\\b'` across packages/*/src: zero hits. /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/buffers.ts:130-140 `countLineBreaks` uses `text.indexOf('\\n')` only; :165-178 `extendBufferLineIndex` likewise. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/Editor.ts:666-682 `setText` hands the raw string to `resetOwnedDocument` with no normalization pass. Reference verified: /Users/shaul/Desktop/D/Editor/references/vscode/src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeTextBufferBuilder.ts:27-58 (`_getEOL`, the `_cr>0||_lf>0` re-normalize condition, BOM captured at :104-105 and passed to the buffer at :58).",
  "verifierNotes": "Fully confirmed \u2014 no false-gap defence found. Worth flagging that the damage is bigger than a cosmetic one: because /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewModel.ts computes lineEndOffset as nextLineStart-1, a CR sits INSIDE the rendered line, so on a CRLF file every End key, every selection-to-line-end, every column measurement, and every gutter width is off by one, and the stray CR reaches the DOM. Our LSP layer already had to grow a CRLF workaround (packages/lsp/src/positions.ts:275), which is the classic symptom of the missing model-level invariant \u2014 each consumer re-solves it. Implementation cost for us is low and localized: normalize + strip BOM once in the buffer constructor path and store the detected EOL on the snapshot, then delete the LSP probe. Highest value-per-effort item in this batch."
 },
 {
  "domain": "text-model",
  "title": "Undo coalescing keyed on edit-operation type, so consecutive deletes merge and an explicit undo-stop API exists",
  "kind": "feature",
  "source": "both",
  "referenceLocation": "references/vscode/src/vs/editor/common/cursor/cursorTypeEditOperations.ts:942-976 (getTypingOperation / shouldPushStackElementBetween / normalizeOperationType); cursorCommon.ts:34-41 (EditOperationType: Other, DeletingLeft, DeletingRight, TypingOther, TypingFirstSpace, TypingConsecutiveSpace); cursorDeleteOperations.ts:22 and :172 (`shouldPushStackElementBefore = prevEditOperationType !== EditOperationType.DeletingLeft/Right`, forced true at :35/:184 when the selection was non-empty); editStack.ts:412-420 (_getOrCreateEditStackElement appends into the open element), :390-408 (pushStackElement/popStackElement close and reopen it); references/codemirror5/src/model/history.js:61-99 (addChangeToHistory: merge if same opId, or same `origin` where '+'-prefixed origins merge within historyEventDelay and '*'-prefixed always merge) and src/edit/options.js:155 (historyEventDelay 1250)",
  "mechanism": "Monaco keeps one *open* edit-stack element and appends every subsequent edit into it (`compressConsecutiveTextChanges`, editStack.ts:50-56) until something closes it. The cursor decides: before each operation it computes `shouldPushStackElementBetween(previousOpType, newOpType)`. The rules are (a) always stop before a non-typing operation, (b) `TypingFirstSpace` never forces a stop \u2014 so a single space glues the preceding word to the following one \u2014 while a *second* space does, and (c) otherwise stop whenever the normalized operation type changes. Deletes have their own types: consecutive Backspaces are all `DeletingLeft` so they collapse into one undo step, consecutive Deletes are all `DeletingRight`, and switching between them, or deleting a non-empty selection, forces a stop. Any command can also call `pushStackElement()` to force a boundary. CM5 reaches a similar place differently: everything inside one `operation` (same `opId`) merges unconditionally, and across operations it merges by a caller-supplied `origin` string protocol \u2014 `'+input'`/`'+delete'` merge if within `historyEventDelay` ms, `'*'`-prefixed always merge, anything else never merges. That origin string is the public extension point for undo grouping.",
  "whyItMatters": "Today every Backspace is its own undo step in our editor. Hold Backspace over a word, then press Cmd-Z: Monaco and CM5 restore the whole word in one stroke; we restore one character at a time. The same applies to Delete, to indent/outdent runs, and to any repeated programmatic edit. Our typing coalescing also has no time component, so a run typed 10 minutes apart at the same offset still merges into one undo entry. And plugins have no way to say 'these three edits are one user action' \u2014 `undoGroup` is declared in the metadata type but is never written by any caller.",
  "ourStatus": "partial",
  "ourEvidence": "/Users/shaul/Desktop/D/Editor/packages/editor/src/documentSession.ts:552-565 `shouldAmendTypingRun` returns false unless `options.metadata.intent === 'insert-text'`, so backspace/delete/indent/outdent transactions (intents at :208-217) always push a fresh history entry via :544-549; :567-584 `updateTypingRun` nulls the run for any non-insert intent; :217 `readonly undoGroup?: string` \u2014 grep across packages shows the only other reference is Editor.ts:2482 reading it, never writing it; :1178-1182 `startsNewWordAfterWhitespace` is our only boundary heuristic and it puts the stop *after* whitespace rather than before it.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Generalise `TypingRun` (documentSession.ts:243-246) into `{ opType: 'typing' | 'typing-space' | 'delete-left' | 'delete-right' | 'other', endOffset, startOffset }` and replace `shouldAmendTypingRun` with a `shouldContinueRun(prevOpType, nextOpType)` port of Monaco's table. Backspace/delete already flow through `commitRecordedEdit`, so amending them is the same `amendEditorHistory` call already used for typing \u2014 the only new piece is a delete-aware `createAmendedTypingTransaction` that extends the recorded range leftward (for backspace) or rightward (for delete) and re-derives `inverseEdits` by concatenating the removed text. Add a public `breakUndoGroup()` on DocumentSession (we already have `breakTypingRun`) and start honouring `metadata.undoGroup` as CM5 honours `origin`: same non-empty group => always amend, different group => always break. Add a wall-clock guard (`Date.now() - runStartedAt > 1250`) to match historyEventDelay.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Split verdict. The undo-stop API DOES exist and the claim missed it: `breakTypingRun()` is declared on both session interfaces at /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSession.ts:89 and :152, implemented at :486-488 (`this.typingRun = null`), forwarded at :898-899 and :1058, and already called around paste at /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/inputSelectionController.ts:1585-1589 (`applyPasteText` brackets the edit with a break on both sides) and at documentSession.ts:681, :692, :711. The coalescing gap is real and confirmed: :552-564 `shouldAmendTypingRun` returns false unless `options.metadata.intent === 'insert-text'` (:556) and :1184-1189 `canAmendTypingTransaction` re-asserts the same; :566-583 `updateTypingRun` nulls the run for any other intent. The intent union at :206-218 lists 'backspace' and 'delete' as first-class, so both always take the fresh-entry path at :544-549. `undoGroup` at :217 is confirmed write-never: the only other reference in the repo is a read at Editor.ts:2482.",
  "verifierNotes": "Keep, but narrow the scope to the half that is actually missing. Do NOT build an undo-stop API \u2014 we have one. The real defect is that holding Backspace over a word produces one undo entry per character, so Cmd-Z restores one character at a time; Monaco merges the whole deletion run. The fix is small and local: generalize `shouldAmendTypingRun`/`updateTypingRun` (documentSession.ts:552-583) from a single 'insert-text' intent to a typing-run *kind* ('insert' | 'backspace' | 'delete'), amend only when kind matches and the edit is contiguous with the run (for backspace that means `edit.to === typingRun.startOffset` rather than `edit.from === endOffset`), and keep the existing `startsNewWordAfterWhitespace` boundary. Monaco's `TypingFirstSpace` nuance \u2014 a single space does not force a stop but a second one does \u2014 is worth copying since our current heuristic puts the stop after whitespace rather than before it."
 },
 {
  "domain": "text-model",
  "title": "Bounded undo history \u2014 depth cap, and serializing closed stack elements out of the live heap",
  "kind": "performance",
  "source": "both",
  "referenceLocation": "references/codemirror5/src/model/history.js:16 (undoDepth), :90-94 (`while (hist.done.length > hist.undoDepth) { hist.done.shift(); if (!hist.done[0].ranges) hist.done.shift() }` \u2014 pops the change *and* its paired selection entry) and src/edit/options.js:154 (`option(\"undoDepth\", 200, ...)`); references/vscode/src/vs/editor/common/model/editStack.ts:82-140 (SingleModelEditStackData.serialize/deserialize into a flat ArrayBuffer with fixed-width selections and length-prefixed text), :200-210 (close() serializes when the element stops being appendable, open() rehydrates), :235-240 (heapSize() = byteLength + 168 overhead); vs/editor/common/services/modelService.ts:502-542 (sums element.heapSize() and drops the retained stack when it exceeds maxMemory)",
  "mechanism": "CM5 hard-caps the number of undo events at 200 and shifts the oldest off, carefully removing the selection entry that pairs with it. Monaco does not cap count but converts each undo element to a compact binary blob as soon as it stops being the append target (`close()` \u2014 which is exactly the moment the undo group ends), so the *live* representation of history is a byte buffer rather than a graph of JS objects, and `heapSize()` gives the service a real number to budget against.",
  "whyItMatters": "Our undo stack is an unbounded persistent linked list where each entry retains a whole `PieceTableSnapshot` **and** a `DocumentTransaction` containing `snapshotBefore`, `snapshotAfter`, `edits` and `inverseEdits` (the inverse edits hold the full deleted text as strings). Structural sharing makes the tree cheap, but two things are not shared: the inverse-edit strings, and \u2014 more importantly \u2014 deleted pieces are never removed from the tree. `deleteFromPieceTable` marks pieces invisible (tombstones) instead of unlinking them, so `pieceCount` only ever grows, and every retained historical root pins every tombstone that was ever created. A long editing session on a large file therefore grows monotonically in both node count (slowing every O(log n) descent) and string retention, with no eviction point anywhere. The tombstone-compaction TODO is already acknowledged in the code but has nothing to trigger it.",
  "ourStatus": "missing",
  "ourEvidence": "/Users/shaul/Desktop/D/Editor/packages/editor/src/history.ts:30-53 `pushHistoryEntry`/`commitEditorHistory` push unconditionally; `size` is tracked but never compared to a limit and nothing ever truncates `undo`. /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/edits.ts:112-141 `deleteFromPieceTable` calls `markTreeInvisible` (tree.ts:402-418) which keeps every node. /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/walker.ts:8-9 comments reference an unimplemented 'tombstone-compaction TODO'. /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSession.ts:220-228 DocumentTransaction retains both snapshots plus edits and inverseEdits.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `maxUndoDepth` (default ~200) to `createEditorHistory` and truncate in `pushHistoryEntry` \u2014 since the stack is a persistent cons list, truncation means walking to the tail once and rebuilding, so instead track depth and rebuild only every N pushes, or switch `EditorHistoryStack` to a plain array with `shift()` like CM5. Truncation is also the natural trigger for tombstone compaction: once the oldest retained root is dropped, run a rebuild pass (flattenPieces filtered on `visible`, then `createTreeFromPieces` + `buildReverseIndex`) on the *current* snapshot only, and invalidate any anchors resolving to now-dropped pieces via the existing `liveness: 'deleted'` path in anchors.ts:113-135. Separately, stop retaining `snapshotBefore`/`snapshotAfter` on transactions that are already in the history stack \u2014 the stack holds them.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed absent. `grep -rn 'maxUndo|historyLimit|MAX_HISTORY|undoDepth|trimHistory|historySize|maxEntries'` across packages/*/src returns nothing. /Users/shaul/Desktop/D/Editor/packages/editor/src/history.ts:429-435 `pushHistoryEntry` conses unconditionally; `size` is computed at :434 but never read against a bound anywhere. Tombstones are permanent: /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/edits.ts:128 calls `markTreeInvisible`, which at /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/tree.ts:402-418 clones each node and flips `visible:false` while keeping every node in the tree; the acknowledged TODO is at /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/walker.ts:8-9. Each retained `DocumentTransaction` (documentSession.ts:220-228) holds snapshotBefore, snapshotAfter, edits, inverseEdits and both selection sets.",
  "verifierNotes": "Confirmed, but lower the priority and drop Monaco's serialization half. Monaco binary-serializes closed elements because its undo elements own full text strings; ours do not \u2014 text lives in the append-only piece buffers regardless of history, and snapshots share tree nodes structurally, so a retained entry costs a handful of objects, not a copy of the document. The genuine cost is second-order: because history pins every historical snapshot, no tombstone can ever be compacted, so the walker's O(k) boundary cost (walker.ts:8-9) grows without bound in a long editing session \u2014 a day-long session on a hot file degrades sequential reads. So the item to keep is a depth cap that lets the tail of history be dropped and thereby unblocks the tombstone-compaction TODO, not blob serialization. CM5's cap is 200 events; note it also has to evict the paired selection entry, which we would not, since our entries carry selections inline (history.ts:400-403)."
 },
 {
  "domain": "text-model",
  "title": "Huge-file guards decided once at construction and permanently respected",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/textModel.ts:188-191 (_MODEL_SYNC_LIMIT 50MB, LARGE_FILE_SIZE_THRESHOLD 20MB, LARGE_FILE_LINE_COUNT_THRESHOLD 300k lines, LARGE_FILE_HEAP_OPERATION_THRESHOLD 256M chars), :334-348 (the decision, with the comment '!!! Make a decision in the ctor and permanently respect this decision !!!'), :368 (_isTooLargeForSyncing), :633-643 (the three predicates), :651 (tokenization refuses), :801-803 and :871-873 (getValue / getLinesContent throw BugIndicatingError when isTooLargeForHeapOperation)",
  "mechanism": "Three independent budgets, all resolved once in the constructor from the buffer's length and line count so no later edit can flip the answer mid-session: tokenization is refused above 20MB *or* 300k lines; worker syncing is refused above 50MB; and whole-document string materialization (`getValue`, `getLinesContent`) throws above 256M characters rather than OOMing the renderer. The line-count threshold matters as much as the byte threshold \u2014 a 5MB minified bundle is one line and tokenizes fine, a 5MB log file with 400k lines does not.",
  "whyItMatters": "Nothing in our editor asks how big the document is before doing something proportional to its size. Concretely: `findController` calls `host.materializeFullText()` on every search input change, and after any edit that string is not retained, so each call walks the piece tree and joins a fresh full-size string. The diff plugin, decode plugin, merge-conflict plugin and the shiki worker client all do the same. On a 50MB file, opening find allocates 50MB per keystroke. There is also no point at which we decline to tokenize or decline to ship the document to a worker.",
  "ourStatus": "partial",
  "ourEvidence": "grep for `1024 * 1024|MAX_FILE|tooLarge|LARGE_|maxFileSize|sizeLimit` across packages/*/src returns nothing. /Users/shaul/Desktop/D/Editor/packages/find/src/findController.ts:375 and :449 call `host.materializeFullText()` unconditionally; /Users/shaul/Desktop/D/Editor/packages/editor/src/mergeConflictPlugin.ts:156,195; /Users/shaul/Desktop/D/Editor/packages/diff/src/editorDiffPlugin.ts:156; /Users/shaul/Desktop/D/Editor/packages/decode/src/index.ts:73; /Users/shaul/Desktop/D/Editor/packages/editor/src/shiki/workerClient.ts:262,327-329. /Users/shaul/Desktop/D/Editor/packages/editor/src/documentTextSnapshot.ts:42 only retains the string for the *initial* snapshot (`materializedText?.length === snapshot.length`), so every post-edit call re-materializes.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Compute `{ length, lineCount }` once in `createEditorTextBuffer` (documentSession.ts:258-269) \u2014 line count is already `getSubtreeLineBreaks(root) + 1`, free from the tree \u2014 and expose `isTooLargeForTokenization()`, `isTooLargeForSyncing()`, `isTooLargeForHeapOperation()` on `EditorTextBuffer` / the plugin host context (packages/editor/src/plugins.ts:275 already declares `materializeFullText` on the host interface, so add the predicates beside it). Gate `refreshSyntax` (Editor.ts) and the worker sync paths on them. For find specifically, the bigger win is not a guard but a chunked search: `TextSnapshot.forEachTextChunk` already exists and streams piece text without joining \u2014 port Monaco's line-by-line search shape (textModelSearch.ts:229-253) on top of it, keeping full materialization only for multiline regex sources (`isMultilineRegexSource`, textModelSearch.ts:69).",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "The claim's grep was too narrow \u2014 it searched for size constants and missed our time-and-window budgets, which cover the tokenization case by a different mechanism. Present: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/Editor.ts:144-148 defines VISIBLE_SYNTAX_OVERSCAN_CHARS=20_000, VISIBLE_SYNTAX_TRAILING_CHARS=50_000, VISIBLE_SYNTAX_LEAD_CHARS=250_000, VISIBLE_SYNTAX_MAX_LEAD_CHARS=750_000, consumed by `visibleSyntaxLeadChars` at :2374-2386 which scales the lead by scroll velocity and clamps to `Math.min(750_000, textSnapshot.length)` \u2014 i.e. we never tokenize the whole document at all, we tokenize a velocity-adaptive window, which strictly dominates Monaco's 20MB/300k-line all-or-nothing refusal. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/syntaxController.ts:97 BACKGROUND_SYNTAX_TILE_CHARS=120_000 tiles the background warm pass, and :626 sets a 'degraded' status. /Users/shaul/Desktop/D/Editor/packages/tree-sitter/src/treeSitter/treeSitter.worker.ts:134-138 caps MAX_RETAINED_SNAPSHOTS=6, MAX_RETAINED_SOURCE_UNITS=8_000_000, PARSE_BUDGET_MS/QUERY_BUDGET_MS=20_000. Genuinely missing: any guard on whole-document materialization. `materializeFullText()` is called unconditionally at /Users/shaul/Desktop/D/Editor/packages/find/src/findController.ts:375,449; /Users/shaul/Desktop/D/Editor/packages/diff/src/editorDiffPlugin.ts:156; /Users/shaul/Desktop/D/Editor/packages/editor/src/mergeConflictPlugin.ts:156,195; /Users/shaul/Desktop/D/Editor/packages/minimap/src/workerClient.ts:978,988,1048,1068; /Users/shaul/Desktop/D/Editor/packages/lsp/src/workspace.ts:157,170 and positions.ts:301; /Users/shaul/Desktop/D/Editor/packages/decode/src/index.ts:73. /Users/shaul/Desktop/D/Editor/packages/editor/src/documentTextSnapshot.ts:38-56 retains the string only when `materializedText?.length === snapshot.length` (initial load), so every post-edit call rebuilds it.",
  "verifierNotes": "Downgrade from missing to partial and re-scope. The syntax budget Monaco wants is already ours and better \u2014 do not add a size cutoff there, it would replace graceful windowing with a cliff. What to keep is the third budget only: a materialization guard. The strong signal that we already know this is a problem is documentTextSnapshot.ts:44-52, which instruments every full-text read via `recordFullTextSnapshotRead`/`measureEditorPerformance` with a hit/miss flag \u2014 we measure the hazard but never refuse it. Cheapest correct shape for us is not Monaco's throw-above-256M but making `materializeFullText()` size-gated and pushing find/diff/merge-conflict onto the chunk API that already exists on the same object (`forEachTextChunk`, documentTextSnapshot.ts:15) \u2014 find in particular is a streaming scan that never needed the string."
 },
 {
  "domain": "text-model",
  "title": "Position<->offset conversion caches: a node search cache, a last-visited-line cache, and a remainder trick that avoids the second tree descent",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeBase.ts:200-265 (PieceTreeSearchCache with get(offset) / get2(lineNumber) / validate(offset) which drops entries at or after an edit), :277-278 and :315-316 (instantiated with limit 1 plus a _lastVisitedLine slot), :395-416 (getOffsetAt: one descent using lf_left + getAccumulatedValue), :418-456 (getPositionAt: `out.remainder + 1` is the column directly, so a second descent via getOffsetAt only happens when the offset lands on a piece's first line), :610-630 (getLineContent memoizes the last line requested), :849-853 and :946-950 (both insert and delete reset _lastVisitedLine), :1257-1290 (nodeAt2 consults and populates the cache with nodeStartLineNumber)",
  "mechanism": "Three layers. (1) The augmented tree stores `lf_left` (line feeds in the left subtree) and `size_left`, so both directions are a single root-to-node walk. (2) `getPositionAt` gets the column for free from the in-piece remainder returned by `getIndexOf`, and only falls back to a second `getOffsetAt` descent in the boundary case where the offset sits on the piece's first line. (3) A tiny LRU (limit 1) of `{node, nodeStartOffset, nodeStartLineNumber}` short-circuits repeated lookups in the same neighbourhood \u2014 which is what actually happens during typing, multi-cursor edits, and viewport rendering \u2014 and `validate(offset)` invalidates only entries at or past the edit rather than clearing everything.",
  "whyItMatters": "Our `offsetToPoint` does two independent full descents: `countLineBreaksBeforeOffset` and then `lineStartOffset` (which is a third helper doing its own descent to find the row-th line break). `pointToOffset` does two descents (line start + line end). These run per selection per line in indent/outdent (documentSelectionEdits.ts:237-250), and per fold and per edit in foldMap/inlineMap. With 500 cursors, an indent is ~2000 tree descents, all in the same handful of pieces. There is no cache anywhere at this layer \u2014 the good `lineStarts` index we do maintain lives in the *view* (virtualization), so model-side consumers like the fold map and selection edits get none of it.",
  "ourStatus": "partial",
  "ourEvidence": "/Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/positions.ts:113-121 `offsetToPoint` calls `countLineBreaksBeforeOffset` then `lineStartOffset`; :98-103 `lineStartOffset` runs `findOffsetAfterLineBreak`, a full descent; :123-129 `pointToOffset` runs both `lineStartOffset` and `lineEndOffset`. No memoization exists in the file and `PieceTableTreeSnapshot` (pieceTableTypes.ts:87-93) has no cache slot. Callers: documentSelectionEdits.ts:237-250, foldMap.ts:120-121,161-162,262-271, inlineMap.ts:176-177,239-240. The view-level index is separate: /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/lineStartIndex.ts.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Two cheap steps. First, make `offsetToPoint` return the column from the same descent: change `countLineBreaksBeforeOffset` to also return the offset of the last line break it passed (it already computes `countPiecePrefixLineBreaks`; have `findOffsetAfterPieceLineBreak`-style bookkeeping carry the running prefix), eliminating the `lineStartOffset` call for the common case. Second, add a per-snapshot mutable cache slot to `PieceTableTreeSnapshot` (the type is already an object created per edit in snapshot.ts:13-23, and buffers.ts:147-165 already establishes the precedent of a lazily-populated mutable side-cache on a nominally readonly object): `{ lastRow, lastRowStartOffset }`, populated by `lineStartOffset` and consulted when the requested row is >= lastRow so the descent can start from a known prefix. Because snapshots are immutable and one is created per edit, no invalidation logic is needed \u2014 the cache dies with the snapshot.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Layer 1 is present and the claim understated it: /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/pieceTableTypes.ts:63-73 `PieceTreeNode` carries `subtreeLineBreaks`, `subtreeVisibleLength`, `subtreeLength`, `subtreePieces`, `subtreeMinOrder/MaxOrder` \u2014 the equivalent of Monaco's lf_left/size_left, and both directions are single root-to-node walks (/Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/positions.ts:39-61 `countLineBreaksBeforeOffset`, :63-93 `findOffsetAfterLineBreak`). We also have a caching layer Monaco lacks entirely: /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/buffers.ts:141-174 `bufferLineIndex` builds a lazily extended per-buffer '\\n' offset array, valid across every snapshot because buffers are append-only, turning per-piece line-break scans into a binary search (`firstLineBreakAtOrAfter`, :176-186). Layers 2 and 3 confirmed absent: positions.ts:113-121 `offsetToPoint` does two independent descents (`countLineBreaksBeforeOffset` then `lineStartOffset`) and :123-129 `pointToOffset` does two more (`lineStartOffset` + `lineEndOffset`); no memo slot exists on `PieceTableTreeSnapshot` (pieceTableTypes.ts:87-93). Separate view-level index at /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/lineStartIndex.ts (`LineStartOffsetIndex`, with a segment-wise `materialize` at :44-62 explicitly written for million-line documents).",
  "verifierNotes": "Correctly partial, but value is lower than it looks and only one of the three sub-items is worth doing. The hot render path never touches positions.ts \u2014 it goes through LineStartOffsetIndex \u2014 so Monaco's `_lastVisitedLine` cache (references/vscode/.../pieceTreeBase.ts:614-623) buys us nothing we don't have. The `_searchCache` (pieceTreeBase.ts:207, :277, sized 1 at :315) is a one-entry LRU and is awkward for us anyway since our snapshots are immutable and shared across history, so a mutable cache would need to hang off the snapshot object keyed by identity. The one clean win is the remainder trick: have `countLineBreaksBeforeOffset` also return the in-piece prefix remainder so `offsetToPoint` derives the column without the second `lineStartOffset` descent, falling back only when the offset lands on the piece's first line. That halves the cost of the callers that actually run per-keystroke \u2014 documentSelectionEdits.ts:237-250, foldMap.ts:120-121,161-162,262-271, inlineMap.ts:176-177,239-240."
 },
 {
  "domain": "text-model",
  "title": "Decorations as an interval tree with lazy subtree deltas and a four-way stickiness taxonomy",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/intervalTree.ts:154-177 (node.delta \u2014 every node's start/end is relative to its parent chain), :56-64 (the comment on why deltas must be normalized: they grow/shrink until 30-bit overflow), :306-332 (acceptReplace: pull out the nodes intersecting the edit, shift everything else by a single delta on O(log n) subtree roots via noOverlapReplace, then re-insert the touched nodes), :339-345 and :350-380 (_normalizeDeltaIfNecessary / normalizeDelta), :396-411 (adjustMarkerBeforeColumn with MarkerDefined/ForceMove/ForceStay), :416-489 (nodeAcceptEdit: the full start/end stickiness resolution including collapse-on-replace and the commonLength phase for replacements), :492-520 (searchForEditing using maxEnd to prune)",
  "mechanism": "Decorations live in an augmented red-black tree keyed by absolute offset, but each node stores its position *relative* to the accumulated `delta` of its ancestors. An edit therefore does not touch every decoration after it: it adds one delta to O(log n) subtree roots. `maxEnd` augmentation makes 'all decorations overlapping [a,b)' an O(log n + k) query, which is what the renderer needs per viewport. Per-decoration stickiness is a real taxonomy \u2014 AlwaysGrowsWhenTypingAtEdges, NeverGrowsWhenTypingAtEdges, GrowsOnlyWhenTypingBefore, GrowsOnlyWhenTypingAfter \u2014 decomposed into two independent booleans (does the start stick to the previous char, does the end), plus `forceMoveMarkers` for 'push this decoration even if it would normally stay' and `collapseOnReplaceEdit` for 'if an edit swallows me whole, become empty at the edit start'. The `commonLength` phase handles replacements correctly: for `replace(3 chars, 5 chars)` the first 3 chars are treated as overwritten in place, so a decoration inside them doesn't get flung to the end.",
  "whyItMatters": "Our range decorations are plain `{start, end}` arrays that plugins recompute and hand over wholesale \u2014 there is no persistent decoration store, so nothing survives an edit on its own, every plugin re-derives its ranges after every change, and the view has no way to ask 'what decorations touch rows 400-450' other than scanning the whole array. Our anchors are the right primitive for tracking (they already survive edits and report liveness), but they expose only `bias: 'left' | 'right'` \u2014 one bit where Monaco has two plus two override flags \u2014 so a plugin cannot express 'this squiggly should grow when I type inside it but not when I type at its edges', which is exactly the difference between a diagnostic that stays sensible while you edit and one that doesn't.",
  "ourStatus": "partial",
  "ourEvidence": "/Users/shaul/Desktop/D/Editor/packages/editor/src/editor/types.ts:37-42 `EditorRangeDecoration` is `{className?, start, end, style?}` \u2014 plain offsets, no identity, no stickiness. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/rangeDecorations.ts is 126 lines of grouping/equality helpers over arrays, no index. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/Editor.ts:2101-2110 `composedRangeDecorations` concatenates every projection's array on each read. Tracking primitive exists: /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/pieceTableTypes.ts:12 `AnchorBias = 'left' | 'right'` and anchors.ts:113-135 resolve with liveness.",
  "value": "medium",
  "effort": "L",
  "implementationSketch": "Don't port the interval tree wholesale \u2014 our anchors already solve the tracking half, and a persistent treap keyed on resolved offset would fight the immutable-snapshot design. Two targeted steps instead. (1) Widen `AnchorBias` to Monaco's four-way stickiness by making it a pair of biases (startSticksToPrevious, endSticksToPrevious) at the *range* level rather than the anchor level, and port `nodeAcceptEdit`'s commonLength phase into a `TextRange.applyEdit` helper in packages/editor/src/textRanges.ts (257 lines, already the home for range math) so plugins that keep plain offsets can shift them correctly without re-deriving. (2) Give the view an interval index over the composed decoration array \u2014 since it is rebuilt per frame anyway, a sorted array plus a running `maxEnd` prefix gives the same O(log n + k) viewport query for a few dozen lines of code, replacing the current full scan in virtualizedTextViewHighlights.ts.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "The decoration API itself is as claimed \u2014 /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/types.ts:37-42 `EditorRangeDecoration` is `{className?, end, start, style?}` with no id and no stickiness; /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/rangeDecorations.ts is array grouping only; Editor.ts:2101-2110 `composedRangeDecorations` concatenates every projection on each read. But both halves of the mechanism already exist elsewhere in our repo and the claim missed them. (a) The maxEnd-augmented interval index exists for tokens: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/tokenIndex.ts defines `EditorTokenIndex { maxEnds: number[] }`, built at /Users/shaul/Desktop/D/Editor/packages/editor/src/syntax/packedTokens.ts:59-74 (over Uint32Array starts/ends/styleIds), and queried in O(log n) by `firstIndexedTokenEndingAfter` at /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewHighlights.ts:695-714 \u2014 a textbook maxEnd binary search over the exact 'everything overlapping the viewport' query. (b) The lazy-delta rebasing exists as `projectTokensThroughEdit` at /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/tokenProjection.ts:34-51, with an explicit lazy suffix path (`lazyProjectedMaxEnds` :329, :390-402, LAZY_PROJECTED_SUFFIX_THRESHOLD=64 at :30) that adds a delta on read rather than rewriting the suffix, plus a prefix/middle/suffix concat at :218-242 \u2014 structurally the same trick as Monaco's subtree delta. (c) Stickiness: /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/pieceTableTypes.ts:12 `AnchorBias = 'left' | 'right'`, resolved with liveness at anchors.ts:113-135 and used for selections at /Users/shaul/Desktop/D/Editor/packages/editor/src/selections.ts:58,113. Also a second delta index at /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/lineStartIndex.ts (suffix deltas, revision counter).",
  "verifierNotes": "Correctly partial, but reframe the work as *wiring*, not invention. We do not need to build an interval tree or design a stickiness taxonomy \u2014 a per-endpoint AnchorBias of left|right already spans exactly Monaco's four TrackedRangeStickiness values (verified at references/vscode/src/vs/editor/common/model.ts:676-677), since start-bias x end-bias = the same four growth behaviours. The missing piece is that `EditorRangeDecoration` is a value object with no identity, so a plugin cannot say 'this decoration, follow the text' \u2014 every plugin re-derives offsets after each edit, which is why composedRangeDecorations rebuilds by concatenation. The high-value change is giving decorations an id plus start/end anchors, and reusing the tokenIndex maxEnds machinery (tokenIndex.ts + packedTokens.ts:59-74) for the viewport query rather than writing a new red-black tree. That is a contained refactor with a clear payoff for find-highlights, LSP diagnostics, document-highlights and inline replacements, all of which currently churn full arrays."
 },
 {
  "domain": "text-model",
  "title": "Surrogate-pair-aware range validation before an edit is applied",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/textModel.ts:175-184 (StringOffsetValidationType.Relaxed vs SurrogatePairs), :1028-1062 (_validatePosition moves a position off the low half of a surrogate pair), :1119-1160 (_validateRange: if the start is mid-pair expand left, if the end is mid-pair expand right, if both expand both, and if the range is *collapsed* mid-pair move it left instead of expanding it), :1064-1078 (validateRange/validatePosition always use SurrogatePairs), :925-984 (_validateRangeRelaxedNoAllocations, the fast path used for decorations)",
  "mechanism": "Before any edit or decoration range reaches the buffer it is snapped so that neither endpoint falls between the high and low halves of a surrogate pair. The rules are asymmetric on purpose: a non-empty range *expands* to swallow the whole code point (so deleting 'half an emoji' deletes the emoji), while an empty range (a cursor) *moves left* rather than expanding, because expanding a cursor would turn it into a selection.",
  "whyItMatters": "We validate code-point boundaries only when creating an anchor, and we do it by throwing. That means a caret can be created mid-pair only via a crash, but an *edit* whose range splits a surrogate pair is accepted silently and produces a lone surrogate in the buffer \u2014 permanently corrupt text that then breaks every downstream `slice`, the tree-sitter worker's UTF-8 conversion, and LSP position mapping. Reachable paths: an LSP server returning a UTF-16 offset computed against a different encoding, a regex replace whose match boundary lands mid-pair, and our own commands doing offset arithmetic (`offset - 1` for backspace) near an emoji or a CJK extension character.",
  "ourStatus": "partial",
  "ourEvidence": "/Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/anchors.ts:33-45 `ensureCodePointBoundary` throws `RangeError('anchor offset must be a code-point boundary')` \u2014 it is called only from `anchorAt` (:200), never from the edit path. /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/edits.ts:31-43 `validateBatchEdits` checks only ordering and `ensureValidRange` (reads.ts:11-15 = bounds only). /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSession.ts:1234-1238 `normalizeTextEdits` only sorts.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Add `snapEditRangeToCodePointBoundaries(snapshot, from, to)` next to `ensureValidRange` in packages/editor/src/pieceTable/reads.ts, reusing the two-char probe already written in anchors.ts:33-45. Apply Monaco's exact rules: if `from === to` and mid-pair, move both to `from - 1`; otherwise expand `from` left and `to` right as needed. Call it from `validateBatchEdits` (edits.ts:31) so every path \u2014 session applyEdits, insert, delete \u2014 is covered, and change `ensureCodePointBoundary` in `anchorAt` from throwing to snapping so a stray offset degrades instead of crashing the editor.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed partial, and our coverage is wider than the claim credits. `ensureCodePointBoundary` at /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/anchors.ts:33-45 throws on a mid-surrogate offset but is reachable only from `anchorAt`. Beyond that, code-point awareness is pervasive in the paths a *user* can reach: /Users/shaul/Desktop/D/Editor/packages/editor/src/textRanges.ts:58-110 (`previousCodePointStart`, `nextCodePointOffset`, `codePointSizeAt`, `isWordCodePointAt`, `characterClassAt`, `previousWordOffset`/`nextWordOffset` all operate on code points); /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/walker.ts exposes `codePoint()` that reassembles pairs across piece boundaries (tested at /Users/shaul/Desktop/D/Editor/packages/editor/test/pieceTable-walker.test.ts:152-177, including unpaired surrogates and pairs at document end); /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/tokenProjection.ts:833-870 handles high-surrogate lookbehind; geometry segments by grapheme with combining-mark and variation-selector handling at /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:840-950. /Users/shaul/Desktop/D/Editor/packages/editor/src/inlineMap.ts:193 even documents the interaction. Genuinely missing: the edit path itself \u2014 /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/edits.ts:31-43 `validateBatchEdits` checks ordering and bounds only, and documentSession.ts:1234-1238 `normalizeTextEdits` only sorts.",
  "verifierNotes": "Keep, but as cheap defence-in-depth rather than a user-visible bug fix. Our own cursor and word-motion code cannot produce a mid-surrogate offset \u2014 that is well covered and tested (packages/editor/test/editor-helpers.test.ts:250,664; packages/editor/test/textRanges.test.ts:19). The exposure is entirely *external* offsets: LSP text edits converted through packages/lsp/src/positions.ts (UTF-16 code-unit positions from a server that may split a pair), find-and-replace ranges, and plugin-supplied edits. Today those either silently corrupt the buffer into a lone surrogate or, if they happen to route through anchorAt, throw a RangeError at an unhelpful place. Adopt Monaco's asymmetry precisely \u2014 non-empty range expands outward to swallow the code point, empty range moves left \u2014 and put it in `validateBatchEdits` (edits.ts:31-43) so every entry point inherits it. Small, contained, no architectural impact."
 },
 {
  "domain": "text-model",
  "title": "Buffer-level `mightContainRTL` / `mightContainNonBasicASCII` flags that unlock a per-line rendering fast path",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeBase.ts:64-99 (createLineStarts computes isBasicASCII in the same pass that finds line starts, so it is free at load); pieceTreeTextBufferBuilder.ts:144-153 (only when a chunk is non-ASCII does it pay for containsRTL / containsUnusualLineTerminators); pieceTreeTextBuffer.ts:37-48, :66-76, :243-266 (applyEdits updates the flags monotonically from inserted text only, and again only checks RTL/unusual terminators when the text is already known non-ASCII), :377-379; references/vscode/src/vs/editor/common/viewModel.ts:357-372 (ViewLineRenderingData derives per-line isBasicASCII/containsRTL from the model flags); references/vscode/src/vs/editor/common/model/textModel.ts:750-758 (removeUnusualLineTerminators + resetMightContainUnusualLineTerminators)",
  "mechanism": "Three monotone booleans maintained on the buffer, computed for free during the line-start scan at load and updated only from newly inserted text (never re-derived from the whole document). They are cheap negatives: `mightContainRTL === false` is a proof, so the view can skip bidi handling entirely; `isBasicASCII` per line lets the renderer skip grapheme/width analysis. The layering matters \u2014 the expensive `containsRTL` regex is only run on chunks already known to be non-ASCII. Separately, U+2028/U+2029 are detected and the model offers to strip them (they are line terminators to the JS engine but not to the editor, a classic source of 'the file looks fine but breaks').",
  "whyItMatters": "We do no ASCII/RTL/unusual-terminator classification at all, so every line goes down the same rendering and measurement path. For the overwhelmingly common case \u2014 a source file that is pure ASCII \u2014 a proof of that fact is the cheapest possible optimization for text measurement, caret geometry and the minimap, and it costs nothing to compute because we already scan the whole document for '\\n' at load. U+2028/U+2029 also currently pass straight through: they are not line breaks to us but they are to `String.prototype.split` and to any JS the content might become.",
  "ourStatus": "partial",
  "ourEvidence": "grep for `ascii|ASCII|containsRTL|bidi` across packages/editor/src and packages/minimap/src returns nothing. grep for `unusualLineTerminator|u2028|u2029` across packages/*/src returns nothing. The load-time scan that could carry the flags for free: /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewHelpers.ts:233-243 `computeLineStarts` and virtualizedTextViewLayout.ts:327-336 `computeLineStartsFromSnapshot`.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Fold the classification into the EOL/BOM ingestion pass proposed above (one scan produces lineStarts, cr/lf/crlf counts, isBasicASCII, containsRTL and containsUnusualLineTerminators together \u2014 Monaco's createLineStarts is the model). Store the flags on `PieceTableBuffers` and update them monotonically in `insertIntoPieceTable` from the inserted text only. Then thread `mightContainNonBasicASCII` into the row renderer (packages/editor/src/virtualization/virtualizedTextViewRows.ts) as the gate for any per-grapheme work, and into the minimap renderer. Note our `computeLineStartsFromSnapshot` currently scans char-by-char with `text[index] !== '\\n'`, which allocates a one-character string per character; switching it to `indexOf('\\n')` (as the sibling `computeLineStarts` already does) is an independent, free win on large files.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "The fast path the claim says we lack is one of the more developed parts of our renderer. /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:753-756 `isSimpleRowCodeUnit` (tab, or 0x20-0x7E) and :81-87 `isSimpleRowText` are exactly Monaco's per-line `isBasicASCII`. They gate more than rendering: :373-376 `rowUsesCalculatedGeometry` routes simple rows to :378+ `buildCalculatedRowGeometry`, which multiplies `view.metrics.characterWidth` arithmetically instead of measuring the DOM, and :364-365 `buildRowGeometry` picks between calculated and measured. Same predicate is reused at :223 and at /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewRows.ts:816, :1050, :1123, :2001. Results are cached per row (`geometryCache` on the row type, virtualizedTextViewTypes.ts:253; invalidated by `clearRowGeometryCaches`, geometry.ts:150-153). Non-simple rows fall through to Intl.Segmenter grapheme segmentation (:70-79, :840-862 `fallbackTextSegments`/`consumeCombiningSuffix`) with wide-CJK and combining-mark width logic at :898-919. What IS absent: any document-level monotone flag; any bidi handling at all \u2014 there is an explicit TODO at geometry.ts:161-163 ('Add BiDi/RTL-aware geometry... breaks caret positions, hit testing, and selection widths'); and any U+2028/U+2029 handling (grep for `unusualLineTerminator|u2028|u2029` across packages: zero hits).",
  "verifierNotes": "Upgrade from missing to partial and re-scope hard, because two of the three sub-items are worthless to us. (1) The ASCII fast path exists and is applied more aggressively than Monaco's \u2014 do not rebuild it. (2) `mightContainRTL === false` is only useful as a proof that lets you *skip* bidi work; we have no bidi work to skip (geometry.ts:161 TODO), so the flag would gate nothing. The prerequisite is BiDi geometry itself, which is a large independent project and the actual finding hiding behind this claim. (3) The one genuinely cheap, genuinely broken item is unusual line terminators: U+2028/U+2029 are line breaks to the browser's layout engine but not to our model, so a document containing one renders with a visual line our model does not know exists, desynchronizing row geometry from row indices \u2014 the same class of bug as the CRLF issue in claim 1, and fixable in the same load-time scan (virtualizedTextViewHelpers.ts:233-243 `computeLineStarts`). Keep only that, and log BiDi separately."
 },
 {
  "domain": "text-model",
  "title": "Model-level indentation guessing (tabSize + insertSpaces) with an alignment-vs-indentation heuristic",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/indentationGuesser.ts:105-215 (guessIndentation: samples at most 10k lines, ALLOWED_TAB_SIZE_GUESSES = [2,4,6,8,3,5,7] preferring even sizes, a spacesDiffCount score array indexed by candidate tab size, tab-vs-space vote from linesIndentedWithTabsCount vs linesIndentedWithSpacesCount), :17-90 (spacesDiff: compares consecutive non-blank lines' indentation, bails when a line mixes tabs and spaces, divides spacesDiff by tabsDiff when both changed, and flags `looksLikeAlignment` when the previous line ends in a comma and the extra spaces line up under a token \u2014 so `const a = b,\\n      d = c;` does not vote for tabSize 6), :123-125 (`useCurrentLineText = currentLineLength <= 65536` \u2014 for very long lines it reads char codes off the buffer instead of forcing V8 to flatten a cons-string)",
  "mechanism": "Rather than reading a config value, the model derives indentation from the content: for each pair of consecutive non-blank lines it computes the indentation delta, discards deltas that look like column alignment rather than nesting, scores each candidate tab size by how often that delta appears, and picks the highest-scoring one with a tie-break preferring even sizes. The tabs-vs-spaces decision is a separate majority vote over lines whose indentation contains a tab vs lines with >1 space. The 10k-line sample cap keeps it O(1)-ish on huge files, and the cons-string note is a real V8 detail: `charCodeAt` on a rope forces flattening, so for lines over 64KB it reads through the buffer's own accessor.",
  "whyItMatters": "Our indentation unit is whatever `tabSize` the host passes in, so opening a 2-space file in a 4-space-configured editor silently indents new lines wrong and our indent/outdent commands operate on the wrong unit. This is the single most visible 'the editor doesn't understand my file' behaviour, and it is self-contained \u2014 no architecture change, one function over the text.",
  "ourStatus": "partial",
  "ourEvidence": "/Users/shaul/Desktop/D/Editor/packages/editor/src/editor/indentation.ts:67-71 `indentUnit(base, tabSize)` returns `'\\t'` or `' '.repeat(tabSize)` from the passed option; :29 `readonly tabSize: number` is an input. grep for `detectIndent|guessIndent` across packages returns nothing.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Port `guessIndentation` almost verbatim against our `TextSnapshot` \u2014 it needs only `getLineCount`, `getLineLength` and `getLineContent`, all of which the virtualization model already exposes (virtualizedTextViewModel.ts:47-49 `lineText` / `lineStartOffset` / `lineEndOffset`), and the view already holds the `lineStarts` array so line access is O(1). Run it once when a document is loaded (Editor.setText, Editor.ts:667) and feed the result into the existing `IndentationOptions` as a default that an explicit host setting overrides. Keep the 10k-line sample cap and the `looksLikeAlignment` rule \u2014 without the latter the guesser mis-fires badly on aligned declaration lists and on markdown bullet lists.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "The tabs-vs-spaces half exists, inferred rather than configured, and the code says so explicitly. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/indentation.ts:61-70: `indentUnit(base, tabSize)` returns '\\t' when the current line's leading whitespace contains a tab, else spaces \u2014 with the doc comment at :62-65 'One indent level, inferred from the line rather than configured. A file indented with tabs keeps getting tabs... Inferring beats a setting here because the wrong choice is visible on the very next line.' It is driven by `leadingWhitespace` (:56-59) from the caller's line, via `lineBreakIndent` (:41-53). So insertSpaces is derived from content, just per-line rather than per-document. The tabSize half is confirmed absent: indentation.ts:29 `readonly tabSize: number` is an input, and `grep -rn 'detectIndent|guessIndent|inferIndent|indentGuess'` across all packages returns nothing. tabSize flows in from options everywhere \u2014 /Users/shaul/Desktop/D/Editor/packages/scope-lines/src/index.ts:365-385, /Users/shaul/Desktop/D/Editor/packages/lsp-plugin/src/formatting.ts:46-50 (which hardcodes `insertSpaces: true` when talking to language servers), /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSelectionEdits.ts:132-134,255-262.",
  "verifierNotes": "Upgrade to partial: we already infer tabs-vs-spaces, just locally. That local rule is arguably better than Monaco's document-wide vote for the common case (it does the right thing in a mixed file), so the gap is narrower than claimed and the fix is smaller: derive tabSize from content at load, and keep indentUnit's local rule as-is. Two concrete payoffs beyond indentUnit. First, outdent and scope-guide levels currently use a possibly-wrong configured tabSize (documentSelectionEdits.ts:261-262 reads exactly `tabSize` characters of prefix; scope-lines/src/index.ts:384-385 floors column/tabSize) \u2014 on a 2-space file with tabSize 4 the indent guides land on the wrong columns. Second, lsp-plugin/src/formatting.ts:48 sends `insertSpaces: true` unconditionally to every language server, so formatting a tab-indented file silently converts it. Monaco's alignment-vs-indentation discard step is the non-obvious part worth copying verbatim; without it, aligned function arguments poison the histogram."
 },
 {
  "domain": "text-model",
  "title": "Chunked change buffer with re-chunking, and typed-array line-start indexes",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeBase.ts:14 (AverageBufferSize = 65535), :1164-1200 (createNewPieces splits any inserted text over 64KB into chunks, and refuses to split on a trailing \\r or high surrogate), :855-868 (the append fast path requires `value.length < AverageBufferSize` and that the insert continues exactly where the last change ended, `_lastChangeBufferPos`), :320-353 (normalizeEOL re-chunks the entire tree into chunks between `min = size - size/3` and `max = 2*min`, i.e. it *rebalances* rather than preserving the historical chunking), :37-62 (createLineStartsFast returns a Uint32Array/Uint16Array via createUintArray, not a number[]); references/codemirror5/src/model/chunk.js:110-120 (splices are avoided on huge leaf arrays \u2014 'small slices are taken... in order because sequential memory accesses are fastest')",
  "mechanism": "Two ideas. Chunk sizing: 64KB is chosen because `substring`/`charCodeAt` degrade on very large strings and because V8 cons-strings from repeated appends must be flattened before indexed access; splits deliberately never land inside a CRLF or a surrogate pair. Re-chunking: whenever the buffer is normalized, the whole tree is walked and re-emitted into evenly sized chunks in the `[min, 2*min]` band, which collapses the fragmentation that accumulates from thousands of small inserts. Index representation: line-start arrays are `Uint32Array` (or `Uint16Array` when the chunk is small), roughly half the memory of a V8 `number[]` and with better cache behaviour on the binary searches that run per position lookup.",
  "whyItMatters": "Our initial document is one single unchunked string (`PieceBufferChunkStore.from([original])`), and our per-buffer line index is a plain `number[]` covering the *whole* file. For a 300k-line file that is a 300k-element JS array (~2.4MB as doubles, vs 1.2MB as Uint32Array) built eagerly on the first line-break query, and the same shape is duplicated in the view's `lineStarts`. Our insert path does chunk at 16KB and coalesces into the tail chunk, which is good, but there is no re-chunking pass ever, so a long session leaves a tree of thousands of small pieces that never consolidate \u2014 compounding the tombstone problem, since neither pieces nor tombstones are ever merged back together.",
  "ourStatus": "partial",
  "ourEvidence": "/Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/buffers.ts:11 `BUFFER_CHUNK_SIZE = 16 * 1024` (used only for inserts, :244-277), :320-332 `createInitialBuffers` does `PieceBufferChunkStore.from([original])` \u2014 one chunk for the entire file; :52-55 `PieceBufferLineIndex = { offsets: number[]; scannedLength: number }`; :81-110 in edits.ts `tryCoalesceInsert` correctly caps the tail at BUFFER_CHUNK_SIZE and refuses to coalesce into the original buffer. No re-chunking or piece-merging pass exists anywhere in packages/editor/src/pieceTable/.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Three independent pieces. (1) Change `PieceBufferLineIndex.offsets` to a growable Uint32Array (with a documented fallback to number[] above 4GB, which we will never hit) \u2014 the only consumers are the binary search helpers in buffers.ts:177-215, so the change is local. (2) Chunk the original text in `createInitialBuffers` at the same 16KB (or raise to 64KB to match Monaco's reasoning about substring cost) and split on '\\n' boundaries where possible so per-chunk line indexes stay small; `createOriginalPiece` becomes a loop like `appendChunksToBuffers`. (3) Add a `compactPieceTable(snapshot)` that walks visible pieces in order, merges adjacent pieces that are contiguous in the same buffer, drops tombstones, and re-emits evenly sized chunks \u2014 then call it from the undo-truncation point proposed in the bounded-history finding, so compaction has a safe trigger where no historical snapshot can still reference the dropped pieces.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed on both halves, with one mitigation the claim missed. Chunking: /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/buffers.ts:11 `BUFFER_CHUNK_SIZE = 16 * 1024`, applied only to appended text (:244-277 `appendChunksToBuffers` slices at that size); :320-332 `createInitialBuffers` does `PieceBufferChunkStore.from([original])` \u2014 one chunk for the whole file. Splits are naive `text.slice(offset, offset + BUFFER_CHUNK_SIZE)` with no CRLF or surrogate-pair guard (:251). No re-chunking or piece-merging pass exists in packages/editor/src/pieceTable/. Cons-string risk is real but capped: :280-289 `extendTailChunk` -> :306-317 `extendTailChunkText` does `previous + text`, and /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/edits.ts:81-110 `tryCoalesceInsert` caps the tail at BUFFER_CHUNK_SIZE and refuses to coalesce into the original buffer. Mitigation the claim missed: the lazy per-buffer line index at buffers.ts:141-186 means we never rescan the original for line breaks \u2014 it is scanned once, incrementally, and the result is shared across all snapshots and all history. Typed arrays: `PieceBufferLineIndex = { offsets: number[]; scannedLength: number }` (pieceTableTypes.ts:51-54) and `LineStartOffsetIndex.materialize` returns `number[]` (lineStartIndex.ts:44-62) \u2014 but we already use Uint32Array elsewhere, at /Users/shaul/Desktop/D/Editor/packages/editor/src/syntax/packedTokens.ts:9-21 (starts/ends/styleIds).",
  "verifierNotes": "Correctly partial, but split it: three sub-items with very different value. (a) Re-chunking the original buffer: low value for us, because the lazy per-buffer line index (buffers.ts:141-174) already removes the main reason Monaco re-chunks, and V8 sliced strings make substring on a large original cheap. Skip. (b) Split-point safety: genuinely wrong today \u2014 buffers.ts:251 can split a surrogate pair across two chunks on a large paste, and while the walker reassembles code points across piece boundaries (walker.ts, tested), anything reading a single chunk directly sees a lone surrogate. Cheap one-line guard, worth doing. (c) Typed-array line indexes: real memory win at scale (offsets as number[] on a 5M-line file is ~40MB of boxed doubles vs 20MB as Uint32Array), and we already have the pattern in packedTokens.ts to copy, so the cost is low. Do (b) and (c), skip (a)."
 },
 {
  "domain": "rendering",
  "title": "Lazy, memoized per-column pixel offsets with a shared Range and one cached container rect",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLine.ts:494-511 (_pixelOffsetCache: Float32Array), :612-626 (_readPixelOffset memoization), :628-658 (_actualReadPixelOffset); references/vscode/src/vs/editor/browser/viewParts/viewLines/rangeUtil.ts:10-45 (single static reused Range + _detachRange), :74-89 (_createHorizontalRangesFromClientRects with clientRectDeltaLeft/clientRectScale + _mergeAdjacentRanges); references/vscode/src/vs/editor/browser/viewParts/viewLines/domReadingContext.ts:6-51",
  "mechanism": "Monaco never measures a line eagerly. `RenderedViewLine` allocates one `Float32Array(len+1)` of -1 sentinels (viewLine.ts:506-509) and fills a slot only when someone asks for that exact column. The DOM read itself goes through `RangeUtil.readHorizontalRanges`, which (a) reuses ONE process-wide `Range` object rather than allocating per measurement, and parks it on a scratch `endNode` afterwards (rangeUtil.ts:17-30), (b) converts client rects to line-relative coordinates using `DomReadingContext.clientRectDeltaLeft`/`clientRectScale`, which are read from the *lines container* exactly once per render pass and cached (domReadingContext.ts:17-40) \u2014 including a `rect.width / offsetWidth` scale factor so measurements stay correct when the editor sits inside a CSS `transform: scale()`, and (c) merges adjacent rects with a 0.9px tolerance for browser rounding (rangeUtil.ts:61). It also snaps back to the predicted monospace value when the DOM read is within 1px of `spaceWidth * horizontalOffset` for basic-ASCII lines (viewLine.ts:650-656), which kills sub-pixel caret jitter between lines.",
  "whyItMatters": "Caret placement, selection rectangles, hit-testing and any overlay that needs an x-coordinate all go through this path. Making it lazy + memoized means a click on column 4 of a CJK line costs one client-rect read, not one per grapheme in the line; and the cached container rect + reused Range removes per-call allocation and per-call layout flushes. The `clientRectScale` correction additionally fixes measurement inside scaled/zoomed containers, which is a silent-wrong-answer class of bug.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:434-522 \u2014 `buildMeasuredRowGeometry` walks every chunk, every part, and every grapheme segment (`textPartSegments`) and calls `measuredTextSegmentRect` for each one, building the FULL boundary array before answering a single `offsetToX`. :668-691 `measuredTextSegmentRect` calls `node.ownerDocument.createRange()` fresh per segment, and `measuredRangeRect` calls `row.element.getBoundingClientRect()` per segment (so 2 rect reads and 1 Range allocation per grapheme). There is a per-row cache (:327-338 `ensureRowGeometry`) but it is all-or-nothing: the first query for any column pays for the whole row. No `clientRectScale` equivalent \u2014 we subtract `rowRect.left` but never divide by a scale factor, so measurements are wrong inside a CSS-transformed host.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Change `RowGeometry` from an eagerly-built `boundaries` array into a lazy structure: keep the `chunks`/`parts` layout (which we already compute cheaply and without DOM), and add a `Float32Array` of per-local-offset x values initialised to NaN plus a `resolve(localOffset)` that measures just the containing segment on demand and fills its two endpoints. Hoist a module-level `const sharedRange = document.createRange()` in virtualizedTextViewGeometry.ts to replace the per-call `createRange()` in `measuredTextSegmentRect`. Introduce a `RowReadingContext` created once per render pass in `renderSnapshot` (virtualizedTextView.ts:806) holding the cached `view.spacer.getBoundingClientRect()` and a `scale = rect.width / spacer.offsetWidth`, threaded into `measuredRangeRect`/`measuredElementRect` in place of the per-segment `row.element.getBoundingClientRect()`. Keep `rangeSegments` (:181-197) working by resolving only the two endpoints per chunk instead of every boundary.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed line by line. /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:434-446 `buildMeasuredRowGeometry` iterates every chunk \u2192 every part \u2192 every grapheme (`textPartSegments`, :830) before answering any query; :668-680 `measuredTextSegmentRect` does `node.ownerDocument.createRange()` per segment; :682-691 `measuredRangeRect` reads `row.element.getBoundingClientRect()` per segment (2 rect reads + 1 Range alloc per grapheme). Cache is real but all-or-nothing (:327-338 `ensureRowGeometry` + :340-358 `rowGeometryCacheKey`). No `clientRectScale` analogue: :690 does `rect.left - rowRect.left` with no division by a scale factor \u2014 Monaco's /Users/shaul/Desktop/D/Editor/references/vscode/src/vs/editor/browser/viewParts/viewLines/domReadingContext.ts:19-25 computes `rect.width / offsetWidth` once per frame and rangeUtil.ts:85 divides by it. Monaco's single shared Range is at rangeUtil.ts:17-23 `_handyReadyRange`.",
  "verifierNotes": "Real, but scope it correctly. `rowUsesCalculatedGeometry` (:373-376) routes every simple-ASCII row without an inline mapping to the pure-arithmetic path, so plain code never touches this. The eager path fires on (a) any row with non-ASCII text and (b) EVERY row carrying an inline mapping \u2014 which per our markdown-view architecture is the entire WYSIWYG document. So this is a markdown-mode cost, not a code-editing cost, and that raises its value rather than lowering it. Three separable wins, ordered by effort: (1) hoist `row.element.getBoundingClientRect()` to one read per geometry build (currently once per grapheme) \u2014 ~10 lines, immediate; (2) module-level reusable Range instead of `createRange()` per segment; (3) the lazy Float32Array-with-sentinel scheme, which is the big one but requires reworking `boundaries` from an eager array into a fill-on-demand cache and touches `xToOffset`/`rangeSegments`. Also add `clientRectScale` \u2014 without it we measure wrong under any CSS-transformed host (zoom UIs, Electron scaling), and that is a correctness bug, not a perf one."
 },
 {
  "domain": "rendering",
  "title": "Key-column anchoring: bounded float drift on long lines without per-character measurement",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLine.ts:342-351 (Constants.MaxMonospaceDistance = 300, with the rationale comment), :366-377 (_keyColumnPixelOffsetCache sizing), :429-455 (_getColumnPixelOffset)",
  "mechanism": "For a monospace ASCII line, `charWidth * column` is exact enough for the first 300 columns. Beyond that, accumulated float error from a non-integral `charWidth` (e.g. 7.2266px) visibly desyncs the caret from the glyphs \u2014 Monaco cites microsoft/vscode#33178. So `FastRenderedViewLine` keeps a `Float32Array` of 'key column' pixel offsets, one per 300 columns. `_getColumnPixelOffset(column)` computes `keyColumnOrdinal = floor((column-1)/300) - 1`, does ONE real DOM read at that key column (memoized forever), and then extrapolates: `keyColumnPixelOffset + charWidth * (horizontalOffset(column) - horizontalOffset(keyColumn))`. Error is therefore bounded by at most 300 characters of accumulation regardless of line length, at a cost of ceil(len/300) DOM reads per line. `getWidth` uses the same threshold: pure arithmetic under 300 chars, `offsetWidth` above it (viewLine.ts:383-393). A background `RunOnceScheduler` re-validates the assumption on the longest visible line and downgrades every visible line to the slow path if the measured width is >2px off the predicted one (viewLine.ts:403-417; viewLines.ts:574-603, scheduled only on Linux at viewLines.ts:659-669).",
  "whyItMatters": "Our calculated (monospace) geometry path is `column * characterWidth` with no anchoring, so on a 5000-character line the caret and selection edges drift progressively away from the glyphs. Users notice this as 'clicking puts the cursor a character or two off, and it gets worse the further right I go' \u2014 precisely the class of bug that makes an editor feel unreliable.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:378-391 `buildCalculatedRowGeometry` and :417-432 `appendCalculatedChunkBoundaries` compute `rowTextInsetLeft(row) + column * cellWidth` for every offset with no DOM anchor at any interval. :393-402 `calculatedXToOffset` inverts the same pure multiplication. `rowUsesCalculatedGeometry` (:373-376) selects this path for every simple-ASCII row with no inline mapping, i.e. the common case, at any length. There is no equivalent of `monospaceAssumptionsAreValid` \u2014 no runtime check that `view.metrics.characterWidth` still matches reality after a zoom or font-load.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "In `buildCalculatedRowGeometry`, when `row.text.length > KEY_COLUMN_DISTANCE` (300), do one `measuredTextSegmentRect` read at each multiple of 300 and use those as anchors, offsetting subsequent boundaries from the nearest preceding anchor instead of from 0. Cache the anchors on the row alongside `geometryCache` so they survive re-queries; they are already invalidated correctly because `rowGeometryCacheKey` (:340-358) includes `view.metrics.characterWidth`. Separately, add a cheap validity probe: after `renderRows`, if any visible simple row is longer than 300 chars, compare `element.firstChild` offsetWidth against the predicted width once (deferred to an idle callback, as viewLines.ts:659 does) and set a `view.monospaceValid = false` flag that forces `rowUsesCalculatedGeometry` to return false \u2014 this also covers the font-loading race where `characterWidth` was measured against a fallback font.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. virtualizedTextViewGeometry.ts:417-432 `appendCalculatedChunkBoundaries` emits `rowTextInsetLeft(row) + column * cellWidth` for every local index with no DOM anchor; :393-402 `calculatedXToOffset` inverts the same multiplication; :373-376 selects this path for every simple-ASCII row at any length. No runtime validity check anywhere \u2014 grep for `monospaceAssumptions` across packages returns nothing, and browserMetrics.ts caches metrics in a module-level WeakMap keyed on computed font properties with no invalidation on `document.fonts.ready` (grep for `document.fonts` across packages: zero hits outside tests). Monaco's counterparts: references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLine.ts:350 `MaxMonospaceDistance = 300`, :366-381 the `Float32Array` key-column cache, :429-455 `_getColumnPixelOffset`, :403-417 `monospaceAssumptionsAreValid` (disables the optimization process-wide when a measured line is >2px off).",
  "verifierNotes": "Genuinely absent and directly portable \u2014 our calculated path is structurally identical to Monaco's `FastRenderedViewLine`, so the key-column array drops straight into `buildCalculatedRowGeometry`. One mitigation the claim missed: browserMetrics.ts:8 probes with a 16-char string ('mmmmmmmmmmmmmmmm') and divides by 16 (:50), so our `characterWidth` already carries ~1/16th the quantisation error of a single-char probe. Drift is therefore slower than Monaco's but still unbounded \u2014 at 7.2266px/char a 1/16-px error accumulates past a pixel by column ~5000. Value is confined to very long lines (minified JS, CSV, log files); moderate. The cheaper companion fix is the missing `monospaceAssumptionsAreValid` check: a stale cached `characterWidth` after a webfont swap desyncs the caret on EVERY line, not just long ones, and one `offsetWidth` comparison per frame catches it."
 },
 {
  "domain": "rendering",
  "title": "Deferred true line-width measurement to fix the horizontal scroll extent",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLines.ts:521-572 (_updateLineWidthsFast / _updateLineWidthsSlow / _updateLineWidths), :533-544 (_updateLineWidthsSlowIfDomDidLayout), :652-657 (RunOnceScheduler dispatch in renderText); references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLine.ts:395-397 (getWidthIsFast), :383-393 (getWidth); references/vscode/src/vs/editor/browser/view/domReadingContext.ts:8-15,48-50 (didDomLayout)",
  "mechanism": "Three-tier strategy. (1) During `renderText`, `_updateLineWidthsFast()` asks each visible line for its width but only if `getWidthIsFast()` \u2014 true when the line is short-and-monospace (pure arithmetic) or its `offsetWidth` is already cached. (2) If any line couldn't answer cheaply, a `RunOnceScheduler` is armed; when it fires (off the critical path) `_updateLineWidthsSlow()` reads real `offsetWidth` for every visible line. (3) The clever part: `DomReadingContext` tracks a `didDomLayout` flag that any code path sets when it forced a layout for some *other* reason (a caret measurement, a widget position). `_updateLineWidthsSlowIfDomDidLayout` (viewLines.ts:533-544) then cancels the scheduled slow pass and does it immediately \u2014 because the layout is already clean, the reads are free. Widths only ever grow (`_ensureMaxLineWidth`), except when the whole document is rendered, where `_maxLineWidth` is reset to 0 so the scrollbar can shrink (viewLines.ts:564-567).",
  "whyItMatters": "Our horizontal scroll extent is a pure character-cell estimate, so any line whose real width exceeds the estimate cannot be scrolled to its end. This bites exactly where our editor is differentiated: markdown inline replacements render headings in a larger/bold font, and CJK/emoji/ligature lines are wider than `visualColumnLength * characterWidth`. Our own CSS comment already documents this as a known regression.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewRows.ts:1717-1780 \u2014 `updateContentWidth` \u2192 `scanVisualColumns` \u2192 `estimatedDisplayRowWidthPx(view, row) / characterWidth(view)`. Purely arithmetic; no DOM width is ever consulted, and there is no deferred correction pass. packages/editor/src/style.css:166-177 states the tradeoff explicitly: \"horizontal scroll extent now trusts the JS column estimate (visualColumnLength), which undercounts double-width glyphs.\" `maxVisualColumnsSeen` is monotonic and only reset via `resetContentWidthScan` (virtualizedTextViewRows.ts:1621).",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `rowWidthIsFast(row)` returning true when `rowUsesCalculatedGeometry(row)` holds (pure math) or a cached `row.measuredWidth` exists. In `updateContentWidth`, use the fast value where available and, when any visible row can't answer, arm a debounced idle task (we already have `packages/editor/src/editor/secondaryWorkScheduler.ts` and `workScheduler.ts` for this) that reads `row.element.scrollWidth`/`offsetWidth` for the visible rows and calls `applyContentWidth` with the real maximum. Note our `.editor-virtualized-row` uses `contain: layout paint style size` (style.css:177), so measure the inner text span rather than the row box, or temporarily drop `size` containment for the measured row. Mirror Monaco's reset rule: when the visible range covers the entire document, clear `maxVisualColumnsSeen` first so the scrollbar can shrink after a long line is deleted.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and deliberate. virtualizedTextViewRows.ts:1717-1772 `updateContentWidth` \u2192 `scanVisualWidthRange` \u2192 `scanVisualColumns` \u2192 `estimatedDisplayRowWidthPx(view, row) / characterWidth(view)`; that resolves to virtualizedTextViewBlockLanes.ts:92-100 \u2192 :379-381 `estimatedTextWidthPx` = `visualColumnLength(text, tabSize) * characterWidth` \u2014 pure arithmetic. Grep for `offsetWidth`/`scrollWidth` across packages/editor/src returns zero non-test hits. packages/editor/src/style.css:166-177 documents the tradeoff and its cause: `contain: layout paint style size` on `.editor-virtualized-row` makes rows relayout boundaries, which is why no `min-width: max-content` and why the extent 'trusts the JS column estimate (visualColumnLength), which undercounts double-width glyphs'. Monaco's three-tier version: references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLines.ts:120,160-161,521-546,652-656 (`_updateLineWidthsFast` \u2192 `RunOnceScheduler` \u2192 `_updateLineWidthsSlow`), backed by `getWidthIsFast()` at viewLine.ts:395-397.",
  "verifierNotes": "Real user-visible bug: a line of CJK or emoji cannot be scrolled to its end, because every wide glyph counts as one cell. But the implementation is cheaper for us than the claim implies \u2014 we already compute a true measured width and then throw it away. virtualizedTextViewGeometry.ts:441-445 sets `RowGeometry.width` from measured boundaries, and grep shows `width` is never read off `RowGeometry` by any caller. So the deferred pass is: after render, for rows where a geometry cache already exists (i.e. width is free, Monaco's `getWidthIsFast()`), feed `geometry.width` into `maxVisualColumnsSeen`; for the rest, schedule an idle pass. Note `contain: size` means row `offsetWidth` is useless to us \u2014 measure the chunk spans or the Range, not the row \u2014 so do not port Monaco's `_getReadingTarget().offsetWidth` literally. Also `maxVisualColumnsSeen` is monotonic (only reset by `resetContentWidthScan`, :1621), so a corrected-upward width sticks correctly without further plumbing."
 },
 {
  "domain": "rendering",
  "title": "Type-enforced read/write phase separation (RenderingContext vs RestrictedRenderingContext)",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/view/viewPart.ts:27-32; references/vscode/src/vs/editor/browser/view/renderingContext.ts:17-73 (RestrictedRenderingContext) and :75-105 (RenderingContext adds linesVisibleRangesForRange/visibleRangeForPosition); references/vscode/src/vs/editor/browser/view.ts:567-580 (_flushAccumulatedAndRenderNow) and :593-661 (_createCoordinatedRendering)",
  "mechanism": "Every frame runs exactly four ordered phases: `prepareRenderText` (pure model work, computes ViewportData), `renderText` (writes line DOM only), `prepareRender` (the ONLY phase allowed to read geometry \u2014 each view part is handed a full `RenderingContext` and stashes computed strings/positions in local fields), `render` (writes only \u2014 each part gets a `RestrictedRenderingContext`, whose type simply does not have `visibleRangeForPosition` or `linesVisibleRangesForRange`). Because the restriction is expressed in the type signature `render(ctx: RestrictedRenderingContext)` (viewPart.ts:31), a view part physically cannot measure during the write phase; the compiler enforces the read/write batching that everyone else enforces by convention and gets wrong. `renderText` also re-collects `_getViewPartsToRender()` afterwards (view.ts:644-646) because writing lines may itself fire scroll events that dirty more parts. The whole thing is wrapped so that `contain: strict` and layer hinting are applied to the lines container once per frame (viewLines.ts:672-676).",
  "whyItMatters": "This is the single structural mechanism that prevents layout thrash. Without it, every new overlay someone adds (whitespace dots, scope lines, inline hints, diagnostics squiggles) silently interleaves a read between two writes and turns one forced layout per frame into N.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextView.ts:806-825 `renderSnapshot` runs write \u2192 read \u2192 write in one synchronous pass: `renderRows` writes row DOM (virtualizedTextViewRows.ts:111-121) and then, at :120, calls `renderHiddenCharacters` which calls `offsetToX` per whitespace character (virtualizedTextViewHiddenCharacters.ts:102-103) \u2014 a geometry read that for measured rows hits `getBoundingClientRect` \u2014 and immediately writes marker spans back (:55-58). Control then returns to `renderSnapshot`, which writes `renderBlockLanes`, then reads/writes highlights. There is no phase concept and no restricted context type; `packages/scope-lines/src/index.ts` and other plugin contributions render off the same snapshot with no read/write discipline.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Introduce two types in virtualizedTextViewTypes.ts: `EditorRenderReadContext` (exposing `offsetToX`, `rangeSegments`, row lookup, viewport data) and `EditorRenderWriteContext` (viewport data + scroll geometry only, no measurement functions). Restructure `renderSnapshot` into: (1) write rows and block lanes; (2) a read loop where hidden characters, scope lines, and any plugin `viewContribution` compute their layout into plain data (arrays of {left,width,top}); (3) a write loop that applies that data to the DOM. Change `renderHiddenCharacters` to split into `prepareHiddenCharacters(view): MarkerData[][]` and `applyHiddenCharacters(view, data)`, moving the `offsetToX` calls out of `renderRows`. Extend `packages/editor/src/editor/viewContributions.ts` to expose the same two-phase hook so plugins inherit the discipline. Pair this with the shared `RowReadingContext` from the first finding so the read phase pays exactly one container-rect read.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. virtualizedTextView.ts:806-825 `renderSnapshot` runs one synchronous pass: `renderRows` \u2192 `renderBlockLanes` \u2192 `renderTokenHighlights` \u2192 `renderRangeHighlight` \u2192 `renderSelectionHighlight`. Inside `renderRows` (virtualizedTextViewRows.ts:111-121), `reconcileRows` writes row DOM and then :120 calls `renderHiddenCharacters`, which for each row calls `offsetToX` twice per whitespace character (virtualizedTextViewHiddenCharacters.ts:102-103) and immediately writes marker spans via `replaceChildren` (:55-58). No phase concept, no restricted context type; grep confirms no `RenderingContext` analogue anywhere in packages/*.",
  "verifierNotes": "True gap, but its value is much lower for us than for Monaco, and the claim overstates the thrash. `contain: layout paint style size` on `.editor-virtualized-row` (style.css:166-177) makes each row its own relayout boundary, so writing marker spans into row N does not dirty row N+1's layout \u2014 the interleaving costs far less here than in Monaco's uncontained line DOM. On top of that `offsetToX` only touches the DOM for measured rows; simple-ASCII rows resolve arithmetically, and default `hiddenCharacters` is 'show-on-selection'. Recommend the lightweight version and skip the type hierarchy: (a) split `renderHiddenCharacters` into a marker-computation pass over all rows followed by a DOM-write pass, and (b) document the read-then-write contract for plugin render hooks (packages/scope-lines, packages/minimap, packages/find, packages/diff all render off the same snapshot with no discipline today). The `RenderingContext`/`RestrictedRenderingContext` class split is a large refactor whose main payoff \u2014 preventing accidental reads in write phase \u2014 is what the doc comment plus the two-pass split already buys."
 },
 {
  "domain": "rendering",
  "title": "Measure-back-and-correct loop with scroll-position compensation for variable-height rows",
  "kind": "correctness",
  "source": "codemirror5",
  "referenceLocation": "references/codemirror5/src/display/update_lines.js:8-49 (updateHeightsInViewport, `mustScroll` accumulation at :33 and application at :48; updateWidgetHeight at :53-58); references/codemirror5/src/display/update_display.js:165-195 (postUpdateDisplay re-render loop until the drawn range covers the viewport)",
  "mechanism": "CM5 renders lines into normal document flow inside a `mover` div whose `top` is set to `viewOffset` (update_display.js:128-130) \u2014 so it does NOT need to know a line's height before rendering it. After the DOM update, `updateHeightsInViewport` reads each rendered line's real `getBoundingClientRect()` height, compares it to the stored estimate, and calls `updateLineHeight` on mismatch. Crucially it accumulates `mustScroll -= diff` for every corrected line whose *bottom is above the viewport top* (`if (oldHeight < viewTop)`), and at the end applies `display.scroller.scrollTop += mustScroll` when the drift exceeds 2px. That is scroll anchoring: correcting an estimate above the fold no longer yanks the content the user is reading. Then `postUpdateDisplay` loops \u2014 recompute `visibleLines` from the corrected heights, and if the drawn range no longer covers the viewport, render again \u2014 until it converges.",
  "whyItMatters": "Any editor with block widgets or measured-height rows has this bug latently: a widget above the viewport reports its real height, the height index shifts, and the text under the user's cursor jumps by that delta. It is one of the most visceral 'this editor is janky' symptoms, and it shows up exactly during async work (image loads, a diff widget expanding, a markdown block re-laying out).",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/virtualization/rowHeightIndex.ts:7-19 rebuilds the prefix array from `rowSizes` with no notion of a scroll correction. packages/editor/src/virtualization/virtualizedTextViewRows.ts:681 passes `blockRow.heightMeasured` down to `syncBlockContainerHeight` (:687-695), which clears the inline height and lets the DOM decide \u2014 but the virtualizer's own `item.size` still comes from `heightRows`/`heightPx` (virtualizedTextViewLayout.ts:648-655), and nothing reads the resulting real height back or compensates `scrollTop`. Grep for `scrollTop +=` and `anchor` across src/virtualization returns no scroll-anchoring code. `renderSnapshot` (virtualizedTextView.ts:806-825) renders once and returns; there is no convergence loop equivalent to `postUpdateDisplay`.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "After `renderRows` in `renderSnapshot`, add a `reconcileMeasuredHeights(view, snapshot)` pass: for each mounted block row with `heightMeasured`, read `row.blockContainerElement.getBoundingClientRect().height` (this belongs in the read phase from the previous finding), diff against `item.size`, and accumulate `mustScroll` for rows whose bottom is above `snapshot.scrollTop`. Feed corrected sizes back through `createRowHeightIndex` (or the incremental version from the sparse-index finding), then apply `view.scrollElement.scrollTop += mustScroll` when `|mustScroll| > 2`. Wrap `renderSnapshot` in a bounded loop (CM5 loops unbounded; 3 iterations is enough in practice) that re-runs while the corrected height index means the drawn range no longer covers the viewport. Our existing `ResizeObserver` in fixedRowVirtualizer.ts:194,454 gives an async path for widgets that change size later \u2014 route it through the same compensation function so async growth above the fold is also anchored.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "REFUTED on the measure-back half. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/blockSurfaceController.ts implements exactly this loop: :327-352 `mountEditorBlockSurface` installs a `ResizeObserver` on the block container and calls `measureEditorBlockSurface` immediately; :354-368 reads the real `getBoundingClientRect()` height (via editorBlockSurfaces.ts:134-143 `elementMeasuredEditorBlockSize`, falling back to `scrollHeight`); :370-380 `setMeasuredEditorBlockSize` clamps to min/max and, on change, arms `scheduleEditorBlockMeasurementUpdate` \u2014 a `setTimeout(\u2026, 0)` coalescer (:382-388); :394-421 `applyMeasuredEditorBlockSizes` rewrites `heightPx` on the affected `BlockRow`s and calls `applyBlockRows` + `notifyLayout`, which flows into `variableRowHeightIndex` (virtualizedTextViewLayout.ts:658-668) and rebuilds the layout. Blocks also expose `requestMeasure()` to their mounted content (blockSurfaceController.ts:322). What IS absent is the scroll compensation: grep for `scrollTop +=` / scroll-anchoring across packages/editor/src/virtualization returns nothing, and fixedRowVirtualizer.ts:241-273 only ever normalizes `scrollTop` against geometry \u2014 it never shifts it to keep the viewport anchored when a row above the viewport changes height. CM5's counterpart is references/codemirror5/src/display/update_lines.js:8-40, where `updateHeightsInViewport` accumulates `mustScroll -= diff` for lines whose `oldHeight < viewTop`, driven from src/display/operations.js:90 and src/display/update_display.js:165-182 `postUpdateDisplay`.",
  "verifierNotes": "Status corrected missing \u2192 partial: we have the measure-and-correct convergence loop, we lack scroll anchoring. Retarget the recommendation entirely at the missing half, which is small and high-value: when `applyMeasuredEditorBlockSizes` changes a block row whose document position is above the current viewport top, add the delta to `scrollTop` before the next emit, so a markdown image or preview block resolving its height does not shove the user's cursor down the screen. This is the single most user-visible defect in the area and it is maybe 30 lines in blockSurfaceController.ts + fixedRowVirtualizer. The generalised CM5 'render into flow, measure everything, converge' architecture is NOT worth porting \u2014 it is incompatible with absolute row positioning and our virtualizer already converges via the ResizeObserver path."
 },
 {
  "domain": "rendering",
  "title": "CharacterMapping: a packed bidirectional column\u2194DOM-position index",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/viewLayout/viewLineRenderer.ts:221-354 \u2014 _data: Uint32Array packing (partIndex, charIndex) per column, _horizontalOffset: Uint32Array; :241-248 setColumnInfo; :250-256 getHorizontalOffset; :271-276 getDomPosition; :278-341 getColumn/partDataToCharOffset (binary search over the packed entries, with a tie-break for positions sitting between parts); references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLine.ts:724-734 getColumnOfNodeOffset",
  "mechanism": "The line renderer emits HTML and, in the same pass, records for every column: which `<span>` index it landed in, the char offset within that span, and its *visual* (tab-expanded, full-width-aware) horizontal offset. Both go into flat `Uint32Array`s \u2014 one 32-bit word per column, no objects. Column\u2192DOM is O(1) array indexing. DOM\u2192column inverts by binary search over the monotonically-increasing packed `(partIndex<<offset | charIndex)` words, and `getColumnOfNodeOffset` recovers the span index by walking `previousSibling` from the hit node. `getHorizontalOffset(column)` is the visual-column accumulator that both the monospace fast path and the key-column extrapolation multiply by `charWidth`.",
  "whyItMatters": "It collapses three separate concerns \u2014 tab expansion, DOM node identity, and pixel prediction \u2014 into one array built once per line render, with zero allocation per query. Our equivalent is an array of `{offset, x}` objects plus a second sorted copy for hit-testing, which is both heavier and rebuilt wholesale on invalidation.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:524-537 `geometryFromBoundaries` produces `boundaries: GeometryBoundary[]` (objects) plus `hitBoundaries: boundaries.toSorted(compareBoundaryX)` \u2014 a full second array copy per row purely for x\u2192offset lookup. :561-666 does linear/binary scans over these object arrays. Our DOM\u2192offset direction is handled separately and structurally: :1033-1145 (`offsetFromTextPartBoundary`, `offsetFromControlPartBoundary`, `partForNode`, `partContainsNode`) walk part lists to identify which node a boundary belongs to, rather than indexing. Tab expansion lives in a third place (`bufferColumnToVisualColumn`, `estimatedDisplayCellForColumn` at :205-215).",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Replace `RowGeometry.boundaries`/`hitBoundaries` with two typed arrays sized to `row.text.length + 1`: a `Float32Array` of x positions (indexed by local offset, so offset\u2192x is O(1) with no search, and NaN doubles as the 'not yet measured' sentinel needed by the lazy-measurement finding) and a `Uint32Array` packing `(partIndex, localOffsetWithinPart)`. x\u2192offset becomes a binary search over the Float32Array, which is monotonic for LTR rows \u2014 dropping the `toSorted` copy entirely. Keep the existing part-walking DOM\u2192offset code as the fallback for control/inline-replacement parts, but use the packed array for plain text parts. Fold `bufferColumnToVisualColumn` into the same fill loop so tab expansion is computed once per row instead of per boundary.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and the cost is worse than stated. virtualizedTextViewGeometry.ts:524-537 `geometryFromBoundaries` keeps `boundaries: GeometryBoundary[]` (one heap object per boundary) plus `hitBoundaries: boundaries.toSorted(compareBoundaryX)` \u2014 a full second array per row purely for x\u2192offset. Crucially this is not confined to measured rows: :417-432 `appendCalculatedChunkBoundaries` loops `for (let local = chunk.localStart; local <= chunk.localEnd; local += 1)` and pushes one object per character, so even the fast ASCII path allocates 2N objects per row geometry build. Lookups at :561-666 binary-search those object arrays. DOM\u2192offset is structural rather than indexed: :1033-1148 (`offsetFromTextPartBoundary`, `controlPartForNode`, `partForNode`, `partContainsNode`) linear-scan chunk.parts. Tab expansion lives separately (`bufferColumnToVisualColumn`, `estimatedDisplayCellForColumn` :205-215). We do have a real bidirectional column mapping for inline replacements \u2014 virtualizedTextViewInlineMapping.ts:64-86 `localIndexForOffset`/`offsetForLocalIndex` over `InlineRow` segments \u2014 and a packed SoA elsewhere (packages/editor/src/syntax/packedTokens.ts:9-49, Uint32Array starts/ends/styleIds with a de-duplicated style palette), so the technique is already in the codebase. Monaco's version: references/vscode/src/vs/editor/common/viewLayout/viewLineRenderer.ts:221-281, two `Uint32Array`s, part index in the high 16 bits and char index in the low 16 (`CharacterMappingConstants`, :203-209).",
  "verifierNotes": "Partial is right, and the strongest sub-finding is one the survey missed: the per-character object allocation applies to the CALCULATED path too, i.e. plain ASCII code, the common case. Two objects and a sort per row on every geometry rebuild is pure garbage pressure during scroll. Highest-value, lowest-risk change: replace `GeometryBoundary[]` + `hitBoundaries` with two parallel `Float64Array`s (offsets, x) \u2014 offsets are already monotonic so the second sorted copy is unnecessary except where inline replacements reorder, which can be detected in one pass. That is contained to virtualizedTextViewGeometry.ts and preserves both public functions' signatures. The DOM\u2192offset direction is a separate, smaller win: `partForNode` is O(chunks \u00d7 parts) and runs on every `selectionchange`; a WeakMap<Node, part> built at chunk-creation time fixes it without a packed index. packedTokens.ts is the in-repo precedent to follow for style."
 },
 {
  "domain": "rendering",
  "title": "Sparse, lazily-summed layout index instead of a dense per-line prefix array",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/viewLayout/linesLayout.ts:385-399 (getWhitespacesAccumulatedHeight with _prefixSumValidIndex), :271/:277/:297/:317 (mutations only invalidate from the touched index backwards), :430-455 (_findLastWhitespaceBeforeLineNumber binary search), :486-499 (getVerticalOffsetForLineNumber = uniform line arithmetic + whitespace prefix sum), :115-140 (findInsertionIndex ordered by afterLineNumber then ordinal)",
  "mechanism": "Monaco does not store a height per line. Uniform lines are pure arithmetic; only *deviations* \u2014 view zones (`EditorWhitespace`) and explicitly customised line heights \u2014 live in a sorted array of W entries. `getVerticalOffsetForLineNumber` is `accumulatedLineHeights(n-1) + whitespacePrefixSum(binarySearch(n)) + paddingTop`. The prefix sums over that array are computed lazily and incrementally: `_prefixSumValidIndex` records how far the sums are valid, an insertion at index i only sets it back to i-1, and `getWhitespacesAccumulatedHeight(index)` extends the sums forward only as far as the caller actually asked. Insert order is stabilised by `(afterLineNumber, ordinal)` so two zones anchored to the same line keep a deterministic order.",
  "whyItMatters": "With a dense array the memory and the rebuild cost are both O(document), so a 2M-line file costs ~32MB of layout index and a full re-walk every time one block widget changes height. With the sparse form both are O(number of deviations), which is typically single digits \u2014 a file with no widgets costs zero.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/virtualization/rowHeightIndex.ts:7-19 `createRowHeightIndex` allocates `rowStarts` of length n+1 and walks all n rows on every construction \u2014 an O(n) rebuild with O(n) memory, with no incremental invalidation and no uniform-height fast path. Lookups (:25-44) then binary-search that dense array. fixedRowVirtualizer.ts:752 (`if (options.rowHeightIndex) return options.rowHeightIndex.totalSize`) shows the index is optional, so the uniform path does exist when no index is supplied \u2014 but as soon as one block row needs a custom height, the whole document pays for a dense index.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Rewrite `RowHeightIndex` as `{ defaultRowHeight, deviations: Array<{ row, height, prefixSum }>, prefixSumValidIndex }`. `startForRow(r)` = `r * defaultRowHeight + accumulatedDeviation(lastDeviationBefore(r))`; `rowAtOffset` binary-searches the deviation array then solves the uniform remainder arithmetically. Port `_prefixSumValidIndex` verbatim from linesLayout.ts:385-399 so mutating one block height is O(1) plus a lazy forward extension. This also makes the height corrections from the scroll-anchoring finding cheap enough to run every frame, and it removes the O(n) rebuild that currently sits between a block widget resizing and the next paint.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed with an important mitigation the claim understated. rowHeightIndex.ts:7-19 `createRowHeightIndex` allocates `rowStarts` length n+1 and walks all n rows, no incremental invalidation; lookups :21-44 binary-search it. The uniform fast path is broader than 'the index is optional': virtualizedTextViewLayout.ts:658-668 `variableRowHeightIndex` returns null unless `hasVariableRows(view)`, and :670-676 `cachedRowHeightIndexValid` memoizes on `view.model.rows` identity + rowHeight + rowGap, so a pure-text document never builds one at all. Monaco's sparse version: references/vscode/src/vs/editor/common/viewLayout/linesLayout.ts:83-103 (`_prefixSumValidIndex`), :271-317 (suffix invalidation on insert/change/remove), :388-397 (lazy incremental fill), :418 `getWhitespaceAccumulatedHeightBeforeLineNumber`.",
  "verifierNotes": "Partial confirmed. Reframe the recommendation, though: Monaco's headline win \u2014 storing zero per-line data \u2014 is unavailable to us, because `view.model.rows` is already a materialised dense display-row array, so `rowStarts` is a constant-factor addition to something O(n) we already pay. The win that DOES apply is `_prefixSumValidIndex`-style suffix invalidation. Concretely: `applyMeasuredEditorBlockSizes` (blockSurfaceController.ts:394-421) produces a new `rows` array identity on every ResizeObserver tick, which fails `cachedRowHeightIndexValid`'s identity check and triggers a full O(n) `createRowHeightIndex`. On a 100k-line document with one resizing block, that is a 100k-element rebuild per animation frame while the block settles. Tracking a valid-prefix watermark and re-summing only from the changed row onward is a contained change to rowHeightIndex.ts plus a dirty-row hint from the layout caller. Value is high specifically in markdown/block mode, negligible in plain code."
 },
 {
  "domain": "rendering",
  "title": "Semantic render-input equality as the invalidation key, not a scroll-position-derived string",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLine.ts:175-178 (`if (this._renderedViewLine.input.equals(renderLineInput)) return false`), :81-100 (onContentChanged/onTokensChanged/onDecorationsChanged/onSelectionChanged set _isMaybeInvalid, and onSelectionChanged returns false unless whitespace-on-selection or high contrast actually depends on selection); references/vscode/src/vs/editor/common/viewLayout/viewLineRenderer.ts:175-209 (RenderLineInput.equals compares the 20-odd fields that actually affect the rendered HTML)",
  "mechanism": "A line re-renders only when the *inputs that determine its HTML* differ. Everything else \u2014 scroll position, viewport bounds, sibling lines \u2014 is deliberately excluded from the comparison, because it changes the line's `top` (handled by `layoutLine`, a single style write) and nothing else. The dirty flag is additionally gated per event type: `onSelectionChanged` returns false, i.e. does not invalidate, unless the theme is high-contrast or `renderWhitespace === 'selection'` (viewLine.ts:94-100) \u2014 the selection is drawn by a separate overlay, so moving it must not re-render text.",
  "whyItMatters": "An invalidation key that mixes in scroll position turns a smooth scroll into a re-render storm. This is a concrete live cost in our chunked long-line path.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewRows.ts:1081-1090 `rowChunkKey` returns `${window.start}:${window.end}:${snapshot.viewportWidth}:${snapshot.scrollLeft}` \u2014 raw `scrollLeft` is in the key, so the key changes on every pixel of horizontal scroll even though `window.start`/`window.end` are quantised to `longLineChunkSize` (:1111-1113) and usually do NOT change. That key is stored as `row.chunkKey`, and `rowGeometryCacheKey` (virtualizedTextViewGeometry.ts:340-358) includes `row.chunkKey` \u2014 so every 1px of horizontal scroll discards and rebuilds the measured geometry of every chunked row. `horizontalWindowKey` (:1142-1151) likewise floors `scrollLeft` into `rowsKey`, so `renderSnapshot`'s early-out at virtualizedTextView.ts:812 never fires while horizontally scrolling a document containing any long line.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Drop `snapshot.scrollLeft` (and `viewportWidth`, which is already implied by the window bounds) from `rowChunkKey` \u2014 the aligned `window.start:window.end` pair is the complete description of what was rendered. Do the same in `horizontalWindowKey`: key on the quantised window, not on `Math.floor(scrollLeft)`. Then verify the chunk window is still recomputed on scroll (it is \u2014 `horizontalChunkWindow` reads the live snapshot at :1092-1115) so correctness is unaffected while the re-render only fires when the window actually shifts by a chunk. Longer term, replace the string keys with a small `RowRenderInput` object plus an `equals` method mirroring RenderLineInput.equals, which makes it structurally impossible to smuggle a viewport coordinate into an invalidation key.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, with two precision corrections. virtualizedTextViewRows.ts:1081-1090 `rowChunkKey` returns `${window.start}:${window.end}:${snapshot.viewportWidth}:${snapshot.scrollLeft}` \u2014 raw unfloored `scrollLeft` in the key even though `window.start`/`window.end` are quantised to `longLineChunkSize` (:1111-1113). That key lands on `row.chunkKey` (:544) and is a component of `rowGeometryCacheKey` (virtualizedTextViewGeometry.ts:340-358), so a 1px horizontal scroll discards the measured geometry of every chunked row AND fails `isRowCurrent` (:1393), forcing full row re-render. Correction 1: our baseline is stronger than 'partial' suggests \u2014 `isRowCurrent` (:1368-1399) is already a genuine semantic-input comparison over index/top/height/textRevision/bufferRow/displayKind/source/injectedTextRowId/metadata/blockMountKey/blockLaneKey/text/chunkKey/rowDecorationKey/foldMarkerKey/foldCollapsed, i.e. exactly Monaco's `RenderLineInput.equals` discipline. Correction 2: `horizontalWindowKey` (:1142-1151) returns the constant 'direct' unless `hasHorizontalChunkedRows` finds a chunked line among the CURRENTLY VISIBLE items (:1153-1162), so `renderSnapshot`'s early-out (virtualizedTextView.ts:812) does fire during horizontal scroll \u2014 it only stops firing while a long line is on screen, not merely present in the document.",
  "verifierNotes": "Best value-to-effort item in the set. The fix is two lines: drop `snapshot.scrollLeft` from `rowChunkKey` (the window bounds it derives already encode everything that changes the rendered chunks) and floor/quantise it in `horizontalWindowKey` to `longLineChunkSize` boundaries rather than whole pixels. Effect: horizontal scrolling across a long line stops rebuilding measured geometry and stops re-rendering rows on every pixel, and the render early-out starts firing. No architectural change, no new abstraction \u2014 we already have the semantic-equality mechanism, one field is polluting it."
 },
 {
  "domain": "rendering",
  "title": "Sticky scroll: enclosing-scope header from the fold/outline model, with a push-out transition",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/stickyScroll/browser/stickyScrollController.ts:608-639 (findScrollWidgetState); references/vscode/src/vs/editor/contrib/stickyScroll/browser/stickyScrollProvider.ts:164-171,176-200 (getCandidateStickyLinesIntersecting \u2014 binary search over the nested StickyElement tree, then filterHiddenRanges); references/vscode/src/vs/editor/contrib/stickyScroll/browser/stickyScrollWidget.ts:19,465-475 (the widget renders its lines through the SAME renderViewLine/RenderLineInput pipeline as real lines), :322 (per-line top adjusted by _lastLineRelativePosition)",
  "mechanism": "Candidates come from a nested tree (outline model, falling back to folding/indentation \u2014 stickyScrollModelProvider.ts), queried by binary search for the elements intersecting the visible range. For each candidate, if `topOfElement > topOfBeginningLine && topOfElement <= bottomOfEndLine`, the header line belongs on screen. The 'push out' effect is one line of arithmetic: when the widget's own bottom would overlap the end of the scope (`bottomOfElement > bottomOfEndLine`), `lastLineRelativePosition = bottomOfEndLine - bottomOfElement` \u2014 a negative offset applied to every sticky line, so the stack slides up and out rather than popping. Crucially the widget reuses `renderViewLine` with a real `RenderLineInput`, so sticky lines get identical tokens, decorations and font metrics to the lines they mirror; there is no second rendering path to keep in sync.",
  "whyItMatters": "Sticky scroll is the highest-visibility rendering feature we lack, and it is the difference between navigating a 400-line function comfortably and not. The push-out arithmetic is what makes it feel native rather than a jumpy header.",
  "ourStatus": "missing",
  "ourEvidence": "grep -rEl 'stickyScroll|sticky-scroll' over packages/**/*.ts and *.css returns nothing. However the feeders exist: packages/editor/src/editor/folds.ts:23-85 supplies `VirtualizedFoldMarker` ranges from tree-sitter, and packages/scope-lines/src/index.ts:264-320 already implements `createScopeGuides` \u2192 `candidateFoldMarkers` \u2192 `markerIntersectsVisibleRows` \u2192 `visibleTextRowBounds`, i.e. exactly the 'which scopes intersect the viewport' query sticky scroll needs. packages/editor/src/virtualization/virtualizedTextViewBlockLanes.ts and the `blockRowMount` API give us an overlay host.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Build it as a plugin next to scope-lines, reusing `candidateFoldMarkers`/`markerIntersectsVisibleRows` (packages/scope-lines/src/index.ts:277-320) to produce the candidate list, then port `findScrollWidgetState` (stickyScrollController.ts:608-639) verbatim \u2014 the arithmetic needs only `getTopForRow`/`getBottomForRow`, which our virtualizer's `rowHeightIndexStart` provides. Render the sticky lines by calling into the same row-building code as `createRow`/`updateRowTextChunks` (virtualizedTextViewRows.ts:158-228, :791-806) rather than a second renderer, so tokens and inline replacements match; a mounted-but-unmanaged row pool is the natural fit. Position the widget absolutely over the text pane in editor coordinates and apply `lastLineRelativePosition` as a translateY on the whole stack. Filter out candidates inside collapsed folds, as `filterHiddenRanges` does.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed absent: grep for `sticky`/`stickyScroll`/`sticky-scroll` across packages source returns only /Users/shaul/Desktop/D/Editor/packages/editor/src/style.css:112, which is `position: sticky` on `.editor-virtualized-gutter` \u2014 unrelated to sticky scroll, but a useful precedent (see notes). Every feeder the claim names checks out: packages/editor/src/editor/folds.ts:25-37 `foldMarkerFromRange` yields `VirtualizedFoldMarker` with startRow/endRow/startOffset/endOffset, and :55-81 `rejectCrossingFoldRanges` guarantees the markers form a properly nested tree (no crossings) \u2014 which is exactly the precondition a sticky-scroll ancestor query needs. packages/scope-lines/src/index.ts:264-320 already implements the viewport query: `createScopeGuides` \u2192 `candidateFoldMarkers` \u2192 `markerIntersectsVisibleRows` \u2192 `visibleTextRowBounds`, plus `nearestCursorScopeGuides` for innermost-enclosing selection.",
  "verifierNotes": "Genuinely missing and worth building; the hard parts are already done. `rejectCrossingFoldRanges` giving us a non-crossing marker set means the ancestor chain for a viewport top row is a straight filter, no outline model needed \u2014 Monaco falls back to folding/indentation for exactly this and we start there. One correction to the claim's implementation sketch: `blockRowMount` and the block-lane layer are NOT the right host. Block rows live in document flow inside the translated spacer and scroll away with the content; a sticky header must be pinned to the viewport. The right host is a new layer alongside `caretLayerElement`/`blockLaneLayerElement` (virtualizedTextView.ts:201-202, 320-327) using the same `position: sticky` technique the gutter already proves at style.css:107-115. The push-out transition is trivial arithmetic once the header layer exists. Budget the work as: ancestor query (reuse scope-lines logic, ~free) + sticky layer + row-content cloning for the header text."
 },
 {
  "domain": "rendering",
  "title": "Whitespace rendering as an overlay, with boundary/trailing modes and an advance-width-matched dot glyph",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/viewParts/whitespace/whitespace.ts:124-240 (_applyRenderWhitespace overlay), :141-149 (middotWidth vs wsmiddotWidth comparison selecting U+00B7 or U+2E31), :182-202 (trailing and boundary predicates, including the wrapped-line single-trailing-space exception), :214-227 (SVG vs div output), :242-262 (_renderArrow); references/vscode/src/vs/editor/common/viewLayout/viewLineRenderer.ts:752-914 (the in-line variant, incl. the RTL guard at :829-839 that refuses to split whitespace inside RTL tokens because it would change the browser's bidi layout)",
  "mechanism": "Two things worth stealing. (1) Mode semantics: `boundary` shows leading/trailing runs plus interior runs of \u22652 spaces but not single word-separating spaces (:187-194); `trailing` shows only whitespace after the last non-whitespace char, and suppresses itself on wrapped continuations (:128-130); tabs are always shown. (2) Glyph choice: font metrics measure BOTH U+00B7 MIDDLE DOT and U+2E31 WORD SEPARATOR MIDDLE DOT, and the renderer picks whichever advance width is closer to the font's actual space width (`wsmiddotDiff < middotDiff ? 0x2E31 : 0xB7`, :144-149) \u2014 so inserting the dot never nudges the following text. Tabs render as U+FFEB halfwidth rightwards arrow when the font supports it, else U+2192 (:223), or as a hand-drawn SVG path scaled to `spaceWidth` (:242-262). The overlay form is preferable to the in-line form for the same reason our hidden-character layer is: it does not split tokens into extra spans, so ligatures, letter-spacing and bidi layout are untouched, and switching selection under `renderWhitespace: 'selection'` does not re-render any text.",
  "whyItMatters": "We already chose the right architecture (overlay) but implement only two of the four useful modes, and we position markers by width rather than drawing a metric-matched glyph. `boundary` in particular is the mode most users leave on permanently, because it surfaces the indentation and alignment mistakes that matter without visually shredding prose.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewHiddenCharacters.ts implements an overlay with modes 'hidden' | 'show' | 'show-on-selection' (:23-30, :118-126) \u2014 no 'boundary', no 'trailing'. `hiddenCharacterKind` (:112-116) only distinguishes space and tab; the marker is a bare span sized by `left`/`width` (:142-153) with the glyph left to CSS, so there is no advance-width matching and no tab-arrow rendering. The per-row work is also unbatched: `renderHiddenCharactersForRow` builds a `|`-joined string key from every marker (:134-140) and calls `replaceChildren` with freshly created spans on any change (:55-58), for every visible row on every render pass (:32-36).",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Extend `HiddenCharactersMode` in virtualizedTextViewTypes.ts:87 with 'boundary' and 'trailing' and port the predicates from whitespace.ts:182-202 into `shouldShowHiddenCharacterAtOffset` (virtualizedTextViewHiddenCharacters.ts:118-126) \u2014 they need only `firstNonWhitespaceIndex`/`lastNonWhitespaceIndex` of `row.text`, both cheap. Add the middot measurement to packages/editor/src/virtualization/browserMetrics.ts:24-47 (measure U+00B7 and U+2E31 alongside the existing character width) and emit the chosen codepoint as the marker's text content instead of relying on CSS. Render tabs as the arrow glyph. While there, hoist the key computation: markers already carry `left`/`width` derived from `offsetToX`, so the key can be `row.textRevision + ':' + mode + ':' + selectionRevision` rather than a string built from every marker.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. virtualizedTextViewHiddenCharacters.ts:23-29 defines only 'hidden' | 'show' | 'show-on-selection' \u2014 no boundary, no trailing; :110-114 `hiddenCharacterKind` returns only 'space' | 'tab'; :137-147 `createHiddenCharacterMarker` emits a bare span positioned by left/width with the glyph left entirely to CSS, so no advance-width matching and no tab arrow. There IS a per-row early-out the claim glossed: :48-52 skips `replaceChildren` when `row.hiddenCharactersKey` is unchanged. Monaco's counterparts verified: references/vscode/src/vs/editor/common/viewLayout/viewLineRenderer.ts:41 the five-mode enum, :142-148 picks between U+2E31 and U+00B7 by whichever measured width is closer to `spaceWidth`, and :752-812 `_applyRenderWhitespace` implements boundary (leading/trailing plus runs of \u22652 interior spaces, :802-812), trailing, and always-show-tabs.",
  "verifierNotes": "Partial confirmed; two independent items, and the perf one is worth more than the feature one. (1) Perf: the `hiddenCharactersKey` early-out at :48-52 guards only the DOM write. `hiddenCharacterMarkersForRow` (:59-72) still runs for every visible row on every render pass (:32-36) and calls `offsetToX` TWICE per whitespace character (:102-103) to build the key it will then compare. On an indented file that is hundreds of geometry lookups per frame to usually discover nothing changed. Gate marker recomputation on a cheap precondition (row textRevision + chunkKey + selection revision) before touching geometry. (2) Feature: 'boundary' and 'trailing' modes are contained additions to `hiddenCharacterKind`/`shouldShowHiddenCharacterAtOffset` and are what users actually want \u2014 'show' is too noisy to live with, 'show-on-selection' too quiet. The advance-width-matched middot needs a metrics probe alongside browserMetrics.ts's existing one; low effort, and it fixes the visible misalignment when the dot glyph is wider than a space in the user's font."
 },
 {
  "domain": "rendering",
  "title": "Long-line defences: token-span splitting, render truncation with an escape hatch, and bounded coordinate magnitudes",
  "kind": "performance",
  "source": "both",
  "referenceLocation": "references/vscode/src/vs/editor/common/viewLayout/viewLineRenderer.ts:569-645 (splitLargeTokens, Constants.LongToken = 50, with the microsoft/vscode#6885 rationale), :471-479 (stopRenderingLineAfter truncation) and :1205 (renderOverflowingCharCount); references/vscode/src/vs/editor/contrib/longLinesHelper/browser/longLinesHelper.ts (clicking past the truncation point disables it for the session); references/vscode/src/vs/editor/common/viewLayout/linesLayout.ts:642-651 (bigNumbersDelta, quantised to a multiple of defaultLineHeight) and references/vscode/src/vs/editor/browser/viewParts/viewLines/viewLines.ts:618,674-676 (domNode height clamped to 1e6; lines positioned relative to the delta)",
  "mechanism": "Three independent defences. (1) `splitLargeTokens` caps any single `<span>` at 50 characters \u2014 splitting at spaces when ligatures or non-ASCII are involved, blindly otherwise \u2014 because browsers read character positions out of very large text nodes in super-linear time. (2) `stopRenderingLineAfter` truncates the rendered HTML at N columns and appends a rendered overflow count; `LongLinesHelper` then watches for a click at a column beyond the cutoff and flips the option off for that editor, so the truncation is invisible until the user actually needs the tail. (3) `bigNumbersDelta`: when the viewport's vertical offset exceeds 500,000px, all rendered `top` values are expressed relative to a delta that is itself floored to a multiple of the default line height (linesLayout.ts:647-648) \u2014 so the numbers browsers must lay out stay small and the sub-line phase is preserved \u2014 while the lines container's own height is clamped to 1,000,000px (viewLines.ts:618).",
  "whyItMatters": "We chose a better long-line strategy than truncation (horizontal chunk windowing), so (2) is mostly moot for us \u2014 but (1) and (3) are orthogonal to it. Our chunks can still contain very large text nodes, and our absolute row positioning uses raw document coordinates, which for a multi-million-line file exceeds the magnitudes browsers position reliably.",
  "ourStatus": "partial",
  "ourEvidence": "We have the better half: packages/editor/src/virtualization/virtualizedTextViewRows.ts:1076-1115 (`shouldChunkLine` + `horizontalChunkWindow`) renders only the horizontally visible window of a long line, aligned to `longLineChunkSize` \u2014 strictly better than `stopRenderingLineAfter`. What is missing: `createRowChunk` (:1039-1075) does not cap the size of an individual text node within a chunk, and `setDirectRowText`/`isSimpleRowText` (virtualizedTextViewGeometry.ts:81-87) put an entire un-chunked line into a single Text node, so a 9,000-character line under `longLineChunkThreshold` is one enormous text node that Range measurement must scan. On magnitudes: `positionRowElement` (virtualizedTextViewRows.ts:1204-1217) writes `translateY(${top}px)` with `top = item.start`, an absolute document coordinate, and `applyTotalHeight` (:1812) sets the spacer to the full `snapshot.totalSize` with no clamp \u2014 grep for `1000000`, `maxHeight`, `33554` across src/virtualization returns nothing.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "For (1): in `createRowChunks` (virtualizedTextViewRows.ts:1019-1038), subdivide any text part longer than ~50-100 chars into multiple Text nodes \u2014 split at spaces when `!isSimpleRowText`, at fixed stride otherwise, exactly as splitLargeTokens does. This is invisible to `offsetToX` because `appendTextBoundaries` already iterates parts. For (3): thread a `verticalDelta` through the snapshot \u2014 computed in fixedRowVirtualizer's `scrollGeometry` as `floor(scrollTop / 500000) * 500000` re-floored to a multiple of the row height \u2014 subtract it in `positionRowElement` and add it back in the inverse mapping used by hit-testing and `selectionReveal`. Clamp the spacer height (`applyTotalHeight`) to ~1e6 while keeping the logical total in `snapshot.totalSize`, which requires the scroll position to be scaled; alternatively note this as a known ceiling and just add the `verticalDelta`, which fixes precision without the scaling complexity.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Partial is the right status, but one of the three sub-claims is FALSE and must be corrected. TRUE \u2014 we have the better truncation story: virtualizedTextViewRows.ts:1074-1113 `shouldChunkLine` + `horizontalChunkWindow` render only the horizontally visible window aligned to `longLineChunkSize`, strictly better than Monaco's `stopRenderingLineAfter` (references/vscode/src/vs/editor/common/viewLayout/viewLineRenderer.ts:471-474). TRUE \u2014 no text-node cap: `createRowChunk` (:1038-1071) puts a whole chunk's text in one Text node, and virtualizedTextViewGeometry.ts:81-87/`setDirectRowText` put an entire sub-threshold line in a single Text node, so a 9,000-char line below `longLineChunkThreshold` is one node that Range measurement must scan; Monaco caps any span at 50 chars via `splitLargeTokens` (viewLineRenderer.ts:511, 581). FALSE \u2014 'bounded coordinate magnitudes \u2026 no clamp \u2026 grep for 1000000 returns nothing': /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/fixedRowVirtualizer.ts:81 declares `DEFAULT_MAX_SCROLL_HEIGHT = 16_000_000` (the underscore is why the survey's grep missed it), :833-840 `nativeScrollHeightFor` clamps the DOM scroll height to it, and :843-859 `nativeScrollTopForLogical`/`logicalScrollTopForNative` proportionally remap between logical and native scroll space \u2014 Monaco's exact technique. virtualizedTextViewRows.ts:1806-1814 `applyTotalHeight` writes `snapshot.nativeScrollHeight` (the CLAMPED value), not `totalSize`, plus a compensating `translateY(nativeScrollTop - scrollTop)` on the spacer. fixedRowVirtualizer.ts:288-292 even forces immediate emit whenever the capped path is active, precisely so the spacer transform tracks every scroll delta.",
  "verifierNotes": "Status unchanged at partial, but the survey's evidence needs the correction above before anyone acts on it \u2014 we already solved the browser scroll-height ceiling, and a plan that re-implements it would be wasted work. Scope the remaining work to the text-node cap alone: split chunk text into \u226450-char Text nodes inside `createRowChunk`, and apply the same cap on the `setDirectRowText` path for sub-threshold long lines. That directly speeds up `measuredTextSegmentRect`'s Range scans and composes with claim 1's measurement fixes. The residual magnitude concern is minor and I would not chase it: `positionRowElement` (:1204-1217) still writes an unclamped logical `translateY`, so on a document past 16M logical px the transform value exceeds the clamped scroll space; float precision degrades but nothing breaks, and `logicalScrollTopForNative` keeps hit-testing consistent."
 },
 {
  "domain": "cursor-selection",
  "title": "Grapheme-cluster movement and emoji-aware backspace (move \u2260 delete granularity)",
  "kind": "correctness",
  "source": "both",
  "referenceLocation": "references/vscode/src/vs/base/common/strings.ts:586-648 (GraphemeIterator, nextCharLength, prevCharLength), :1007-1090 (breakBetweenGraphemeBreakType, GB3-GB11), :1168-1183 (getLeftDeleteOffset), :1185-1235 (getOffsetBeforeLastEmojiComponent); references/vscode/src/vs/editor/common/cursor/cursorMoveOperations.ts:30-37 (leftPosition uses prevCharLength), :110-118 (rightPosition uses nextCharLength); references/vscode/src/vs/editor/common/cursor/cursorDeleteOperations.ts:222-231 (getPositionAfterDeleteLeft uses getLeftDeleteOffset); references/codemirror5/src/util/misc.js:143-152 (isExtendingChar/skipExtendingChars); references/codemirror5/src/input/movement.js:6-13 (moveCharLogically)",
  "mechanism": "Monaco deliberately uses two different granularities. Arrow-key movement steps a full extended grapheme cluster: `MoveOperations.leftPosition` calls `strings.prevCharLength`, which runs a `GraphemeIterator` over code points and consults a `GraphemeBreakTree` implementing UAX#29 rules GB3-GB11 (no break before Extend/ZWJ/SpacingMark, no break inside Hangul L/V/T sequences, no break inside emoji-modifier or ZWJ sequences, no break between paired regional indicators). Backspace uses a *different* function, `strings.getLeftDeleteOffset`, which first tries `getOffsetBeforeLastEmojiComponent`: it walks back over skin-tone modifiers / VS16 / keycaps, expects an emoji base, then optionally consumes a preceding ZWJ plus its emoji \u2014 so backspace on \ud83d\udc68\u200d\ud83d\udc69\u200d\ud83d\udc67 peels off one family member per press, while backspace on `e`+U+0301 removes only the combining accent. Move-left over the same text jumps the whole cluster. CM5 does a cheaper version of the same idea with a hand-written `extendingChars` regex and `skipExtendingChars`.",
  "whyItMatters": "Today an arrow key in our editor can park the caret between a base character and its combining mark, or between the two halves of a ZWJ emoji sequence. Rendering then shows the caret mid-glyph, selection highlights are half-glyph wide, and Delete/Backspace can leave orphaned combining marks or lone ZWJs in the buffer. Any user writing Hindi, Thai, Korean, Arabic, or just emoji hits this immediately.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/textRanges.ts:43-75 \u2014 `previousCodePointOffset`/`nextCodePointOffset` handle surrogate pairs only (charCode 0xD800-0xDFFF checks) and nothing else; `codePointSizeAt` returns 1 or 2. packages/editor/src/editor/navigationTargets.ts:206-209 `codePointOffset` is what cursorLeft/cursorRight/selectLeft/selectRight use. packages/editor/src/documentSelectionEdits.ts:340-351 `backspaceRangeForSelection` deletes exactly one code point. grep for grapheme/Intl.Segmenter across packages/*/src finds hits only in packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:68-79,831-838 \u2014 i.e. we already segment graphemes for *measurement* but not for *movement*, so measurement and caret placement disagree.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "We already construct an `Intl.Segmenter({granularity:'grapheme'})` in virtualizedTextViewGeometry.ts:835; lift it into `packages/editor/src/textRanges.ts` as `previousGraphemeOffset`/`nextGraphemeOffset` operating on a line-local slice (segmenting the whole document string is not viable \u2014 slice the line via `lineRangeAtOffset`, segment, binary-search the boundary). Point `codePointOffset` in navigationTargets.ts:206 at the grapheme version. Keep backspace on a separate function: code-point delete plus a small `getOffsetBeforeLastEmojiComponent` port (needs only isEmojiModifier / VS16 / keycap / ZWJ code-point tests, ~40 lines) wired into `backspaceRangeForSelection`. Add a fallback path for environments without Intl.Segmenter using CM5's `extendingChars` regex.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed and slightly worse than claimed. /Users/shaul/Desktop/D/Editor/packages/editor/src/textRanges.ts:43-75 `previousCodePointOffset`/`nextCodePointOffset`/`codePointSizeAt` handle surrogate pairs only. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/navigation.ts is a pure re-export of textRanges, so navigationTargets.ts:206-209 `codePointOffset` is the only horizontal granularity in the editor. Deletion: /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSelectionEdits.ts:326-351 has a SECOND private `previousCodePointOffset` implemented directly against the piece table (readPieceTableTextRange(snapshot, offset-2, offset)) \u2014 same surrogate-only logic, duplicated. I also checked the native path: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/inputSelectionController.ts:1128-1159 `handleBeforeInput` early-returns unless `beforeInputText(event)` is non-null, and a repo-wide grep for `getTargetRanges`/`deleteContentBackward` returns zero hits \u2014 so we never fall back to the browser's grapheme-correct deletion ranges either; Backspace always routes keymap.ts:427 -> deleteBackward -> session.backspace(). The only Segmenter in the repo is /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:66-79 (`GraphemeSegmenter` type), :831-838 `textPartSegments`/`createGraphemeSegmenter`, with a hand-rolled `fallbackTextSegments`/`consumeCombiningSuffix` fallback \u2014 measurement only.",
  "verifierNotes": "True gap. Note the duplication: a grapheme fix has to land in BOTH textRanges.ts (string-based, for navigation) and documentSelectionEdits.ts (piece-table-based, for backspace), or move/delete will diverge again. Cheapest correct route given our architecture: extend the existing `fallbackTextSegments`/`consumeCombiningSuffix` machinery in virtualizedTextViewGeometry.ts into a shared grapheme primitive rather than porting Monaco's GraphemeBreakTree \u2014 we already ship the Intl.Segmenter path and a fallback, they are just in the wrong package. Keeping move=grapheme / delete=grapheme-with-partial-emoji-decomposition is the Monaco nuance worth copying."
 },
 {
  "domain": "cursor-selection",
  "title": "Word navigation has three distinct stop policies; ours is a fourth that matches none",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/cursor/cursorWordOperations.ts:42-47 (WordNavigationType enum), :80-115 (_doFindPreviousWordOnLine), :117-141 (_findEndOfWord), :211-261 (moveWordLeft), :306-366 (moveWordRight); references/vscode/src/vs/editor/contrib/wordOperations/browser/wordOperations.ts:123-139 (cursorWordLeft = WordStartFast, bound to Ctrl/Alt+Left), :238-246 + :218-235 (cursorWordEndRight = WordEnd, bound to Ctrl/Alt+Right); references/vscode/src/vs/editor/common/core/wordCharacterClassifier.ts:18-95",
  "mechanism": "Monaco models a word as `{start, end, wordType: Regular|Separator, nextCharClass}` found by scanning one line, where every character is classified Regular / Whitespace / WordSeparator. Crucially a *run of punctuation* is itself a word (`WordType.Separator`), so `foo.bar` has three words. Four navigation policies then sit on top: **WordStart** stops at word starts; **WordStartFast** (the default for Ctrl+Left) is WordStart plus one rule \u2014 if the previous word is a single separator character immediately followed by a Regular character, skip it, so Ctrl+Left from `foo.|bar` goes to `foo` not to `.`; this rule is disabled when `hasMulticursor` is true so multiple carets do not diverge (cursorWordOperations.ts:227-240). **WordEnd** (the default for Ctrl+Right) stops *after* the last character of a word, so Ctrl+Right from `|foo bar` lands at offset 3, not 4. **WordAccessibility** skips separator-only words entirely to match screen-reader expectations. Word characters are not a Unicode property test: a cached `WordCharacterClassifier` (LRU of 10) is built from a configurable `wordSeparators` string, defaulting to ``` `~!@#$%^&*()-=+[{]}\\|;:'\",.<>/? ``` \u2014 everything else, including CJK and emoji, is Regular. Optional `Intl.Segmenter({granularity:'word'})` locales layer real CJK/Thai word segmentation on top (findPrevIntlWordBeforeOrAtOffset / findNextIntlWordAtOrAfterOffset, results cached per line).",
  "whyItMatters": "Concretely: in our editor Ctrl+Shift+Right on `foo bar` selects `foo ` (with the trailing space) because we always consume trailing whitespace, whereas every other editor selects `foo`. Ctrl+Left from `foo.|bar` stops on the `.` instead of jumping to `foo`. Word motion also silently walks across line breaks in ours. These are the highest-frequency keystrokes in an editor, so the mismatch is felt constantly.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/textRanges.ts:98-110 \u2014 `previousWordOffset` = skip spaces backward then skip one class run (\u2248WordStart, no fast rule); `nextWordOffset` = skip spaces, skip one class run, *then skip trailing spaces again* (\u2248WordStart with whitespace eaten, never WordEnd). :88-96 `characterClassAt` classifies with regexes `\\s`, `[\\p{L}\\p{N}_]` \u2014 not a configurable separator set, so `$`, `#`, `@` are punctuation here but word characters under Monaco defaults for some languages. No `\\n` guard anywhere in these loops, so motion crosses lines. packages/editor/src/editor/navigationTargets.ts:79-96 exposes exactly one left/right pair; packages/editor/src/editor/commands.ts:75-78 has only cursorWordLeft/Right + selectWordLeft/Right \u2014 no WordStart/WordEnd/Accessibility variants.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Rewrite `previousWordOffset`/`nextWordOffset` in packages/editor/src/textRanges.ts to first slice the line via the existing `lineRangeAtOffset` (bounding the scan to the line and fixing the newline-crossing bug), then port `_doFindPreviousWordOnLine`/`_doFindNextWordOnLine` returning `{start,end,wordType,nextCharClass}`, and add a `WordNavigationType` parameter. Default `cursorWordLeft` to WordStartFast (passing `hasMulticursor = selections.length > 1`, available at inputSelectionController.ts:617-621) and `cursorWordRight` to WordEnd. Replace the per-character regex in `characterClassAt` with a Uint8Array-backed classifier built once from a configurable separators string (see the perf finding). Add the extra command ids to packages/editor/src/editor/commands.ts so keymaps can pick a policy per platform.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed exactly. /Users/shaul/Desktop/D/Editor/packages/editor/src/textRanges.ts:98-103 `previousWordOffset` = skipBackward(space) then skipBackward(class of preceding char); :105-110 `nextWordOffset` = skipForward(space), skipForward(class), skipForward(space) \u2014 the trailing whitespace skip makes right-motion land past the gap, matching neither WordStart nor WordEnd. :88-96 `characterClassAt` is a hardcoded `/\\s/u` then `/^[\\p{L}\\p{N}_]$/u` (WORD_PATTERN, :8); a repo-wide grep for `wordSeparator|separators` finds only subword-separator hits in textRanges.ts:201-248 and unrelated test names \u2014 there is no configurable separator set anywhere in packages/*. `\\n` classifies as 'space' via `/\\s/u`, and skipBackward/skipForward (:143-164) have no line guard, so Ctrl+Left from column 0 walks into the previous line's text. Surface: navigationTargets.ts:79-96 `wordTarget` is the only word mover; commands.ts:75-78 lists exactly cursorWordLeft/Right + selectWordLeft/Right, plus deleteWordLeft/Right (commands.ts:40-41 -> editActions.ts:194-212 `deleteWordAction`, which reuses the same single policy). No WordStart/WordEnd/Accessibility variants.",
  "verifierNotes": "True. Two separable wins, and the second is much cheaper than the first: (a) a configurable `wordSeparators` string per language replacing the hardcoded WORD_PATTERN \u2014 this is what makes `$foo` in PHP/jQuery or `@media` in CSS behave right, and it is a ~20-line change to characterClassAt; (b) treating a punctuation run as its own word so `foo.bar` is three words, which our `characterClassAt` already almost gives us since 'punctuation' is a distinct class \u2014 `previousWordOffset` already stops at class transitions, so we are closer to Monaco here than the claim implies. The line-crossing bug in (\\n classified as space) is the highest-value single fix in this claim and is a two-line guard."
 },
 {
  "domain": "cursor-selection",
  "title": "Atomic soft-tab movement and tab-stop-aware backspace inside indentation",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/cursor/cursorAtomicMoveOperations.ts:22-50 (whitespaceVisibleColumn), :62-160 (atomicPosition with Left/Right/Nearest); references/vscode/src/vs/editor/common/cursor/cursorMoveOperations.ts:41-51 (leftPositionAtomicSoftTabs), :120-130 (rightPositionAtomicSoftTabs, both gated on config.stickyTabStops and on being inside the line's indent); references/vscode/src/vs/editor/common/cursor/cursorDeleteOperations.ts:193-220 (getDeleteLeftRange, gated on config.useTabStops which is ON by default)",
  "mechanism": "Two related mechanisms. (1) `stickyTabStops`: while the caret is within a line's leading whitespace, Left/Right jump a whole `tabSize` worth of columns instead of one space. `whitespaceVisibleColumn` walks the indent once, returning the previous tab-stop position, its visible column, and the caret's visible column, bailing with -1 on any non-whitespace so the rest of the line is untouched. Going left it then re-scans forward to confirm the indent between the previous tab stop and the caret is a *full* tabSize \u2014 this is why `'      foo'` with tabSize 4 moves 6\u21925 (partial indent, one char) rather than 6\u21924. Going right it walks to `nextRenderTabStop`. `Direction.Nearest` is used by mouse hit-testing so a click inside an indent snaps to a tab stop. (2) `useTabStops` (default true, independent of stickyTabStops): backspace inside indentation deletes back to `prevIndentTabStop(visibleColumn, indentSize)` \u2014 one indent level, not one space.",
  "whyItMatters": "With soft tabs (spaces), our backspace currently removes one space at a time, so unindenting a line takes 2-4 presses instead of one \u2014 this is a daily-friction bug for any spaces-indented codebase. The Left/Right half additionally stops the caret from landing in visually meaningless positions inside indentation.",
  "ourStatus": "missing",
  "ourEvidence": "grep for stickyTabStops|atomicSoftTab|atomicTab across packages/editor/src returns nothing. packages/editor/src/documentSelectionEdits.ts:340-351 `backspaceRangeForSelection` unconditionally deletes one code point back \u2014 no indent check, no tabSize. packages/editor/src/editor/editActions.ts only references tabSize at :831-853 for explicit outdent (`outdentLineEdit`/`outdentLength`), never for backspace. We do have the required primitives: `bufferColumnToVisualColumn`/`visualColumnToBufferColumn` and `view.tabSize` in packages/editor/src/virtualization/virtualizedTextViewLayout.ts:260-297.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Start with the backspace half \u2014 highest value per line of code. In `backspaceRangeForSelection` (documentSelectionEdits.ts:340), when the selection is collapsed, read the line via `lineRangeAtOffset`, compute `firstNonWhitespaceIndex`; if the caret is at or before it, convert caret\u2192visible column with the existing `bufferColumnToVisualColumn`, round down to the previous multiple of indentSize, convert back, and delete that range. Needs `tabSize`/`indentSize` threaded into `SelectionEditResult` options (the shape at documentSelectionEdits.ts:40 already carries `tabSize?`). Then port `AtomicTabMoveOperations.atomicPosition` verbatim (~90 lines, pure string+number, trivially unit-testable) and call it from `horizontalTarget` in navigationTargets.ts:58 behind an option, plus `Direction.Nearest` from `textOffsetFromPoint` in the mouse path.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. Repo-wide grep for `stickyTabStops|atomicSoftTab|atomicTab` returns nothing. Backspace: documentSelectionEdits.ts:340-351 `backspaceRangeForSelection` unconditionally returns {previousCodePointOffset, startOffset} with no indent or tabSize input; the only tabSize consumers in packages/editor/src are documentSession.ts:57/315/804 (outdentSelection), documentSelectionEdits.ts:132/255-271 (`outdentLength`, which already implements 'delete back to the previous tab stop' \u2014 `while (spaces < tabSize && text[spaces] === ' ')`), the virtualization layer, and displayTransforms.ts:212-288. Primitives all exist: displayTransforms.ts:221-253 `bufferColumnToVisualColumn`/`visualColumnToBufferColumn` are tab-aware and pure, and editActions.ts:1191 `firstNonWhitespaceOffset` already computes the indent boundary for line ops. Also confirmed: nothing in the horizontal path (navigationTargets.ts:59-77 `horizontalTarget`) consults tabSize.",
  "verifierNotes": "True, but split it. The backspace-to-previous-tab-stop half is high value / low effort: `outdentLength` in documentSelectionEdits.ts:267-271 is literally the algorithm already, it just is not reachable from backspace \u2014 the change is a caret-is-in-leading-whitespace guard in `backspaceRangeForSelection` reusing it. The `stickyTabStops` arrow-movement half is off by default even in Monaco and is a genuine niche preference; I would implement it only behind an option, or not at all. Worth flagging the deeper design point: our horizontal motion has no access to view/tabSize at all (navigationTargets.ts:19-23 NavigationTargetView exposes offsetAtLineBoundary/offsetByDisplayRows/pageRowDelta/visualColumnForOffset but nothing indentation-related), so the arrow half is a wider plumbing change than the backspace half."
 },
 {
  "domain": "cursor-selection",
  "title": "Multi-cursor merge rules: touching vs overlapping, direction preservation, and last-added priority",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/cursor/cursorCollection.ts:158-252 (normalize), :19-32 (cursors[0] is primary, lastAddedCursorIndex), :140-142 (killSecondaryCursors keeps cursors[0]), :144-148 (_addSecondaryCursor updates lastAddedCursorIndex), :157-160 (_removeSecondaryCursor shifts it)",
  "mechanism": "Monaco's `normalize()` sorts a copy of the cursors by range start and merges neighbours, but the merge predicate is asymmetric: if either selection is empty, merge when they merely *touch* (`nextStart.isBeforeOrEqual(currentEnd)`); if both are non-empty, merge only on strict *overlap* (`isBefore`). Two adjacent non-empty selections therefore survive as two cursors. When a merge does happen, the surviving state is chosen by original cursor index \u2014 the lower-indexed (older) cursor is the 'winner' and its LTR/RTL direction is kept, *except* when the loser is `lastAddedCursorIndex`, in which case the last-added cursor's direction wins and `lastAddedCursorIndex` is transferred to the survivor. Sort order never becomes identity order: `cursors[0]` stays the primary cursor, is what reveal/scroll follows, and is the one that survives Escape (`killSecondaryCursors`).",
  "whyItMatters": "Ours merges any two selections that touch, so Ctrl+D-selecting two adjacent identical tokens (`abab` \u2192 `ab|ab`) collapses them into one selection instead of two cursors, and typing then replaces the whole run. Merging also throws away direction and goal column, so after two cursors collide, Shift+Arrow reverses meaning and Up/Down loses the column. And because we treat `selections[0]` (document order) as primary, Escape from a multi-cursor set keeps the topmost cursor rather than the one the user started from, and reveal jumps to the top of the document.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/selections.ts:209-210 \u2014 `shouldMergeRanges = right.start <= left.end`, a single unconditional touching test with no empty/non-empty distinction. :233-250 `mergeResolvedSelections` hard-codes `reversed: false` and `goal: SelectionGoal.none()`, discarding both. :200-207 `compareResolvedSelections` sorts by offset and ties on `id.localeCompare`, and :275 returns the sorted array, so document order becomes cursor order. There is no `lastAdded` concept anywhere. packages/editor/src/documentSession.ts:705-710 and :1013-1018 collapse to `normalized.selections[0]` \u2014 the topmost, not the original.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "In packages/editor/src/selections.ts, change `shouldMergeRanges` to take both resolved selections and apply the empty/non-empty split. In `mergeResolvedSelections`, keep the winner's `reversed` and `goal` instead of resetting them. Add an `order`/creation-index field to `Selection<T>` (or reuse a monotonic counter in the existing `createSelectionIdFactory` at :73) so `normalizeSelections` can sort for merging but restore creation order for the returned array, keeping index 0 = primary; then `documentSession.ts:705/1013` collapse to the true primary for free. A `lastAddedIndex` on `SelectionSet` set by `addSelection` (inputSelectionController.ts:931) gives the direction-priority rule.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed on every point. /Users/shaul/Desktop/D/Editor/packages/editor/src/selections.ts:209-210 `shouldMergeRanges = right.start <= left.end` \u2014 one unconditional touching test, no empty/non-empty branch, so two adjacent non-empty selections (e.g. word-select `foo` then `bar` in `foobar`) collapse into one. :233-250 `mergeResolvedSelections` builds the survivor with `goal: SelectionGoal.none(), reversed: false` \u2014 direction and goal column are both dropped; note that :226-232 `normalizeResolvedSelection` (the non-merging path) DOES preserve reversed and goal, so the loss is specific to merges. :200-207 `compareResolvedSelections` sorts by startOffset, then endOffset, then `id.localeCompare`, and :275 returns that order, so document order is the only order. No `lastAdded`/anchor-cursor concept anywhere \u2014 the only `primary*` symbols in the repo are `primarySelectionHeadOffset` (inputSelectionController.ts:1458) and find's `primarySelection` (packages/find/src/findController.ts:492), both of which just mean 'index 0'. Escape behaviour confirmed at documentSession.ts:700-712 and :1008-1022 \u2014 both `clearSecondarySelections` implementations keep `normalized.selections[0]`, the topmost, so alt-clicking a cursor below and pressing Escape teleports you up.",
  "verifierNotes": "True and this one has a user-visible bug attached, not just a fidelity difference: Escape after adding a cursor below jumps the caret to the top of the document region rather than back to where you were. Three independent fixes, all cheap and all in selections.ts: (1) make `shouldMergeRanges` take both ranges' collapsed-ness and require strict overlap when both are non-empty; (2) in `mergeResolvedSelections`, take reversed/goal from whichever input's head survives as the merged head instead of hardcoding false/none; (3) add a `lastAddedIndex` alongside the selection set so Escape and Cmd+D anchor on it. (3) is the one with real reach \u2014 Cmd+D (claim 10) depends on it too."
 },
 {
  "domain": "cursor-selection",
  "title": "Column (box) selection: a persistent from/to visual-column rectangle, driven by mouse and keyboard",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/cursor/cursorColumnSelection.ts:12-75 (columnSelect), :77-90 (columnSelectLeft), :86-105 (columnSelectRight scans for max visual column), :107-118 (columnSelectUp/Down with isPaged); references/vscode/src/vs/editor/common/cursor/cursor.ts:39,130,137-139,285-299,395 (_columnSelectData lifecycle, getCursorColumnSelectData synthesising isReal:false state from the primary cursor, invalidation on any edit); references/vscode/src/vs/editor/browser/view/viewController.ts:217-310 (dispatchMouse: middle button, alt+shift+click, alt+drag, and the `columnSelection` option all route to _columnSelect)",
  "mechanism": "Column selection is not N independent cursors; it is a single `IColumnSelectData` rectangle `{fromViewLineNumber, fromViewVisualColumn, toViewLineNumber, toViewVisualColumn}` stored on the cursor controller, from which the cursor list is *derived* on every change. `ColumnSelection.columnSelect` walks each line in the range, converts both visual columns to real columns with `columnFromVisibleColumn` (tab-aware), converts back to visible columns, and drops lines whose entire content falls outside the rectangle's column band \u2014 so short lines contribute no cursor rather than a clamped one, unless *every* line is short, in which case it falls back to a caret at each line end. The keyboard commands mutate only the `to` corner of the stored rectangle, which is why Ctrl+Shift+Alt+Right can push past the end of short lines and later re-acquire them (`columnSelectRight` computes the max visual column across the whole band to know when to stop). `_columnSelectData` is nulled by any edit (`_interpretCommandResult`, cursor.ts:395) and re-synthesised from the primary cursor with `isReal: false` when absent.",
  "whyItMatters": "Box selection over columnar data (aligned assignments, CSV-ish blocks, log columns) is one of the two or three reasons people reach for multi-cursor at all, and it is the case pure multi-cursor cannot express because short lines must be *skipped*, not clamped. We have neither the mouse nor the keyboard entry point.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/commands.ts:1-90 has no column-select command of any kind. packages/editor/src/editor/inputSelectionController.ts:915-918 \u2014 the only altKey handling in mousedown is `addCursorAtOffset`; alt+drag is never distinguished from a plain drag (`startMouseSelectionDrag`, :938-955). No middle-button branch. mouseSelection.ts (45 lines) is only auto-scroll math.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "The primitives exist: `visualColumnForOffset` and `offsetForViewportColumn` (packages/editor/src/virtualization/virtualizedTextViewLayout.ts:260-297) are exactly Monaco's `visibleColumnFromColumn`/`columnFromVisibleColumn`, and `rowForOffset` gives display lines. Add a `columnSelect: ColumnSelectData | null` field alongside `mouseSelectionDrag` in inputSelectionController.ts, cleared in `applySessionChange` on any edit. Port `ColumnSelection.columnSelect` (~60 lines) mapping (row, visualColumn) pairs through the two existing helpers, and have it produce a `setSelections` payload. Wire the entry points in `handleMouseDown` (:891-921): alt+drag and middle button start a column drag; add `columnSelectLeft/Right/Up/Down/PageUp/PageDown` command ids that mutate only the stored `to` corner.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. Repo-wide case-insensitive grep for `columnSelect|columnSelection|boxSelect|rectangularSelect|blockSelect` across packages/**/*.ts returns zero hits. commands.ts:1-89 (full EditorCommandId union) contains no column command. inputSelectionController.ts:891-921 `handleMouseDown` branches on detail>=4 / 3 / 2 / `event.altKey`, and the altKey branch calls `addCursorAtOffset` (:922-940) \u2014 alt+drag is indistinguishable from a plain drag, which then goes to `startMouseSelectionDrag` (:942-958) storing a scalar `MouseSelectionDrag {anchorOffset, headOffset, clientX, clientY}` (mouseSelection.ts:4-9). No middle-button branch anywhere (`event.button !== 0` returns early at :925 and :943). mouseSelection.ts is 45 lines of auto-scroll math only.",
  "verifierNotes": "True gap, and genuinely worth it \u2014 column selection is one of the few remaining things people leave an editor over. But be honest about effort: the win in Monaco's design is that the rectangle is the source of truth and the N cursors are DERIVED on every change, which means our `SelectionSet` would need a parallel 'column select data' field on the session that survives normalization (selections.ts:36-40 SelectionSet has only {selections, normalized, normalizedFor}). We do already have both halves of the hard math \u2014 displayTransforms.ts:221-253 bufferColumnToVisualColumn/visualColumnToBufferColumn is exactly Monaco's columnFromVisibleColumn, and virtualizedTextViewRows.ts:1119-1124 already picks the fast vs estimated path. So the geometry is free; the state plumbing is the cost."
 },
 {
  "domain": "cursor-selection",
  "title": "Cursor undo/redo as an independent bounded stack",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/cursorUndo/browser/cursorUndo.ts:16-35 (CursorState.equals), :37-43 (StackElement also captures scrollTop/scrollLeft), :66-93 (recording: cleared on model or content change, records `e.oldSelections` only when `oldModelVersionId === modelVersionId`, dedupes against the stack top, capped at 50), :96-120 (cursorUndo/cursorRedo push the current state onto the opposite stack), :113-121 (_applyState restores selections *and* scroll position under an `_isCursorUndoRedo` reentrancy guard)",
  "mechanism": "A separate 50-deep undo stack that records only pure cursor movements \u2014 the `oldModelVersionId === modelVersionId` test is what excludes selection changes caused by edits, and any content change wipes both stacks so cursor-undo can never fight document-undo. Each entry stores scroll position too, so Cmd+U returns you to where you were *looking*, not just where the caret was. The reentrancy flag stops the restore from recording itself.",
  "whyItMatters": "After a Ctrl+D run, a Go-to-Definition, or an accidental Ctrl+Home in a large file, there is currently no way back to the previous selection state except manual navigation. This is a small, self-contained feature with an outsized 'I can experiment safely' payoff, and it composes especially well with our multi-cursor and find features.",
  "ourStatus": "missing",
  "ourEvidence": "grep for cursorUndo across packages/editor/src returns nothing; packages/editor/src/editor/commands.ts:2-3 has only `undo`/`redo`. packages/editor/src/history.ts (113 lines) stores `selections` inside each *document* history entry (:3,15,49,87,105) \u2014 i.e. selection is restored as a side effect of text undo, which is the opposite of what this feature needs.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "New file packages/editor/src/editor/cursorHistory.ts holding two arrays of `{selections: EditorSelectionRange[], scrollTop, scrollLeft}`. Hook it in `applySessionChange` (packages/editor/src/editor/Editor.ts / inputSelectionController.ts) \u2014 we already pass a timing/source name per call, so 'was this an edit or a pure selection change?' is answerable without version-id comparison: clear both stacks on any change whose `edits.length > 0`, push the previous selections otherwise. Add `cursorUndo`/`cursorRedo` to commands.ts and bind Cmd+U in keymap.ts alongside the existing entries at :434.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. Grep for `cursorUndo|cursorRedo|selectionUndo|selectionHistory` across all packages returns zero hits. commands.ts:2-3 has only 'undo'|'redo'. /Users/shaul/Desktop/D/Editor/packages/editor/src/history.ts:1-5 defines `EditorHistoryEntry {snapshot, selections, transaction}` and :39-52 `commitEditorHistory` pushes the PREVIOUS snapshot+selections as one unit \u2014 selection is strictly a passenger of document history, restored only as a side effect of text undo. There is no version-id concept to distinguish 'selection changed because the user moved' from 'selection changed because an edit moved it'; documentSession.ts tracks `this.revision` (:452-458) but nothing compares it across a selection change.",
  "verifierNotes": "True gap but I'd rank it lowest-value of the missing items. It is a real, well-liked Monaco feature (Cmd+U), and the mechanism is genuinely clever \u2014 the `oldModelVersionId === modelVersionId` test is what makes it not fight document undo, and storing scroll position is what makes it feel right. We have `revision` on the session already, so that test is available to us cheaply. Keeping it, but as a low-priority nice-to-have rather than a correctness fix; if effort is constrained, claims 1, 7, 12 and 13 all beat it."
 },
 {
  "domain": "cursor-selection",
  "title": "Mouse dispatch matrix: shift-click extend, and word/line-granularity drag via a Range-valued anchor",
  "kind": "feature",
  "source": "both",
  "referenceLocation": "references/vscode/src/vs/editor/browser/view/viewController.ts:217-310 (full dispatch matrix: middle button, gutter-started line drags, count>=4 select-all, count 3 line/lineDrag, count 2 word/wordDrag/lastCursorWordSelect, single click with multicursor modifier, alt+shift = columnSelect); references/vscode/src/vs/editor/browser/controller/mouseHandler.ts:459-500 (drag-and-drop of an existing selection is preferred over re-selecting; `_dispatchMouse(position, e.shiftKey, ...)` \u2014 shiftKey *is* inSelectionMode); references/vscode/src/vs/editor/common/cursorCommon.ts:342-399 (SingleCursorState.selectionStart is a **Range**, selectionStartKind Simple|Word|Line, and _computeSelection picks whichever end of that anchor range is away from the position); references/vscode/src/vs/editor/common/cursor/cursorWordOperations.ts:741-820 (WordOperations.word: first entry sets a Word-kind anchor; subsequent inSelectionMode calls snap the moving end to whole-word boundaries and flip which anchor end is used)",
  "mechanism": "The decisive design choice is that the anchor is a *Range*, not a point, tagged with a granularity kind. Double-click sets `selectionStart` to the whole word with `SelectionStartKind.Word`; every subsequent drag position is then snapped to word boundaries and `_computeSelection` picks the far end of the anchor range, which is what makes double-click-and-drag select whole words in both directions and never shrink below the original word. Triple-click does the same with a line-sized anchor. Shift+click is not a special case at all \u2014 it is simply `inSelectionMode = e.shiftKey` fed into the same dispatch, so shift+click after a double-click extends *by word*.",
  "whyItMatters": "Three very ordinary gestures are unavailable in our editor today: shift+click to extend a selection, double-click-then-drag to select by word, and triple-click-then-drag to select by line. Shift+click in particular is muscle memory for selecting a large region without dragging through a scrolling viewport.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/inputSelectionController.ts:891-921 `handleMouseDown` \u2014 branches only on `event.detail` (>=4, 3, 2) and `event.altKey`; a grep for `shiftKey` across packages/editor/src/editor/*.ts (only keymap.ts hits) confirms no shift handling in the mouse path. The detail===2 and detail===3 branches `return` immediately after `selectWordAtOffset`/`selectLineAtOffset` (:1103-1111, :1095-1101), so no drag monitor is started at all. packages/editor/src/selections.ts:24-30 \u2014 `Selection<T>` carries `start`/`end`/`reversed`; the anchor is recoverable only as a single offset (`anchorOffset`, :181-182), so a word- or line-sized anchor is not representable. packages/editor/src/editor/mouseSelection.ts is 45 lines of auto-scroll math only.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Extend `MouseSelectionDrag` (mouseSelection.ts:4-9) from `anchorOffset: number` to `anchor: {start, end}` plus `granularity: 'char'|'word'|'line'`. In `updateMouseSelectionFromDragPoint` (:1006-1020), expand the pointer offset with the existing `wordRangeAtOffset`/`lineRangeAtOffset` (textRanges.ts:35-41, :112-131) when granularity is word/line, then union with the anchor range and set head to the far end. Have the detail===2/3 branches fall through into `startMouseSelectionDrag` with the matching granularity instead of returning. Add a shift branch at the top of `handleMouseDown` that keeps the current selection's anchor and only moves the head. Longer term, promoting `SelectionGoal`-style metadata to a `selectionStartKind` on `Selection<T>` would also make keyboard shift+arrow after a double-click behave correctly.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and the shift-click part is stronger than claimed: a repo-wide grep for `shiftKey` finds hits ONLY in packages/diff/src/DiffView.ts:1220-1221, packages/panes/src/index.ts:238, packages/find/src/findWidget.ts:207-268, and packages/editor/test/editor.test.ts \u2014 zero occurrences in packages/editor/src at all, including keymap.ts (which encodes shift via `key('L', {mod: true, shift: true})` string descriptors, keymap.ts:507). So shift-click does not extend the selection anywhere in the editor. Granularity drag: inputSelectionController.ts:906-919 \u2014 the detail===3 and detail===2 branches call `selectLineAtOffset` (:1095-1101) / `selectWordAtOffset` (:1103-1111), both of which route to `selectRange` (:1113-1126) and return with no mousemove/mouseup listeners attached, so double-click-drag and triple-click-drag do nothing. Anchor shape: mouseSelection.ts:4-9 `MouseSelectionDrag.anchorOffset` is a scalar number, and selections.ts:24-30 `Selection<T>` has no granularity tag \u2014 a word- or line-sized anchor is not representable.",
  "verifierNotes": "True and this is the highest value-per-effort item in the whole list. Shift-click alone is table stakes that we simply do not have \u2014 every native text control and every editor does it. The Range-valued-anchor design is the right thing to copy verbatim: widening `MouseSelectionDrag` from `anchorOffset: number` to `anchor: {start, end, kind: 'char'|'word'|'line'}` and snapping the drag head through the existing `wordRangeAtOffset`/`lineRangeAtOffset` (textRanges.ts:34-40, :112-131) gets shift-click, double-click-drag and triple-click-drag from one change, because all three reduce to 'anchor is a range, head is snapped to the same granularity, pick the far end'. Note the drag path also calls `session.materializeFullText()` per double/triple click (:1098, :1106) \u2014 see claim 13."
 },
 {
  "domain": "cursor-selection",
  "title": "Word-part (subword) motion clamped between the word-start and word-end candidates",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/cursor/cursorWordOperations.ts:845-853 (moveWordPartLeft = max of three candidates), :855-863 (moveWordPartRight = min of three candidates), :825-833 / :835-843 (deleteWordPartLeft/Right use the same trick on Ranges via Range.compareRangesUsingEnds/Starts), :264-303 (_moveWordPartLeft: pure camelCase/snake_case/kebab-case scan that stops at column 1 and refuses to leave the line, with the `HTTPResponse` acronym rule at :291-301)",
  "mechanism": "Monaco never lets the raw subword scanner decide alone. `moveWordPartLeft` computes three positions \u2014 `moveWordLeft(WordStart)`, `moveWordLeft(WordEnd)`, and the raw `_moveWordPartLeft` \u2014 sorts them with `Position.compare`, and takes the **last** (nearest to the caret going left); `moveWordPartRight` takes the **first** going right. The raw scanner therefore only ever gets to *shorten* a move that word navigation already bounded; it can never carry the caret past a word boundary, past whitespace, or (because `_moveWordPartLeft` returns column 1 / maxColumn at the line edges) across a line break. The delete variants apply the identical clamp to Ranges. This is a very cheap way to get correct subword behaviour without the subword scanner having to understand whitespace, punctuation, or newlines at all.",
  "whyItMatters": "Our subword scanner treats every non-alphanumeric character \u2014 including `\\n` \u2014 as a separator to be skipped, so Alt+Ctrl+Right from the end of a line silently jumps to the next line's first subword, and from a position before whitespace it skips the gap and lands at the *end* of the next word rather than its start. Users lose their place, and it makes subword motion unusable for careful editing.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/textRanges.ts:196-233 \u2014 `nextWordPartOffset`/`previousWordPartOffset` run standalone with no word-boundary clamp. :248-252 `isSubwordSeparator` is `!/[\\p{L}\\p{N}]/u.test(char)`, which is true for `\\n`, and both functions loop over separators unconditionally (:202, :222) so line breaks are traversed. Commands exist (packages/editor/src/editor/commands.ts:61-64, navigationTargets.ts:98-115) but there is no `deleteWordPartLeft`/`deleteWordPartRight` counterpart in commands.ts. Our acronym rule at textRanges.ts:236-246 is already equivalent to Monaco's.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Once `previousWordOffset`/`nextWordOffset` take a `WordNavigationType` (see the word-navigation finding), wrap the existing calls in navigationTargets.ts:98-115: `previousWordPartOffset \u2192 Math.max(previousWordOffset(WordStart), previousWordOffset(WordEnd), rawPrevious)` and the symmetric `Math.min` going right. That is ~6 lines and fixes both the newline crossing and the overshoot without touching the subword scanner. Add `deleteWordPartLeft/Right` to commands.ts applying the same clamp to the delete ranges.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. textRanges.ts:196-233 `nextWordPartOffset`/`previousWordPartOffset` take only (text, offset) and never consult `previousWordOffset`/`nextWordOffset` \u2014 no clamp. :248-252 `isSubwordSeparator` is `!/[\\p{L}\\p{N}]/u.test(char)`, true for '\\n', and the separator-skipping loops at :202 and :222 are unbounded, so Ctrl+Alt+Left from a line start walks into the previous line. navigationTargets.ts:98-115 `wordPartTarget` passes the raw scanner result straight through. Commands exist and are complete on the motion side \u2014 commands.ts:61-64 cursorWordPartLeft/Right/LeftSelect/RightSelect, wired at navigationTargets.ts:44-47 \u2014 but there is no deleteWordPartLeft/Right in the commands.ts union (:38-41 has only deleteWordLeft/deleteWordRight) and none in editActions.ts:9-10/145-146. Our acronym rule at textRanges.ts:236-246 `startsSubword` (HTTP|Response via `isUpper(previous) && isLower(text[index+1])`) is behaviourally equivalent to Monaco's, and is covered by /Users/shaul/Desktop/D/Editor/packages/editor/test/wordPart.test.ts.",
  "verifierNotes": "True. The clamp is the valuable half and it is nearly free for us: `wordPartTarget` in navigationTargets.ts:98-115 already has `context.text` and `context.resolved.headOffset`, so computing previousWordOffset/nextWordOffset alongside the raw result and taking min/max is a handful of lines \u2014 and it fixes the line-crossing bug as a side effect, since word motion would bound it. deleteWordPartLeft/Right is a straightforward addition to editActions.ts mirroring `deleteWordAction` (:194-212). Both worth doing."
 },
 {
  "domain": "cursor-selection",
  "title": "Smart-select as a ranked ladder of ranges, not a single tree walk",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/smartSelect/browser/smartSelect.ts:33-52 (SelectionRanges.mov skips ranges equal to the current one), :74-127 (run: computes the ladder once, prepends the live selection, and drops the whole state on any cursor move via onDidChangeCursorPosition), :245-276 (sort + keep only ranges that strictly contain the previous one), :277-300 (synthesised 'line without leading/trailing whitespace' and 'full line' steps inserted whenever a step crosses onto a new line); references/vscode/src/vs/editor/contrib/smartSelect/browser/wordSelections.ts:31-72 (_addInWordRanges: subword step from camelCase/snake_case/kebab-case), :74-79 (word step), :81-89 (whitespace-only-line step); references/vscode/src/vs/editor/contrib/smartSelect/browser/bracketSelections.ts:13-27,31-75,77-125 (bracket provider yields *both* inner and outer bracket ranges, and time-slices with _maxDuration=30ms across _maxRounds=2 so huge files never block)",
  "mechanism": "Providers each contribute an unordered bucket of candidate ranges; the framework filters to ranges containing the caret, sorts them, then keeps only a strictly increasing containment chain (dropping duplicates and equal ranges), and finally interpolates two synthetic steps \u2014 'the line's content without leading/trailing whitespace' and 'the whole line' \u2014 at every point where the chain jumps to a new line. The resulting ladder is computed once and cached with an index; Expand/Shrink just move the index, and `mov` skips steps that would not visibly change the selection. The state is invalidated by *cursor movement*, not by document version, and the bracket provider is written to yield inner-then-outer so `(a, b)` gives you the contents before the parens.",
  "whyItMatters": "Our expand/shrink jumps straight from the caret to the smallest named tree-sitter node, so the first press on `fooBar` selects the whole identifier \u2014 there is no subword step, no 'line without indentation' step, and no expansion at all in a buffer with no grammar (plain text, markdown, an unsupported language). Shrink is also only guarded by snapshot version, so clicking elsewhere and pressing Shrink pops a stale stack and teleports the selection.",
  "ourStatus": "partial",
  "ourEvidence": "packages/tree-sitter/src/structuralSelection.ts:45-55 (expand) and :57-71 (shrink) \u2014 a single tree-sitter provider, one range per press. packages/tree-sitter/src/treeSitter/treeSitter.worker.ts:1585-1615 \u2014 `expandedRangeAt` walks to `containingNamedNode`, so the ladder is exactly the named-node chain: no subword, no word, no trivia steps. :1600-1603 requires a parsed layer, so no grammar means no expansion. structuralSelection.ts:147-156 `rangesFromShrinkState` validates only `snapshotVersion !== options.snapshotVersion` \u2014 nothing invalidates on cursor movement. :136-142 rebuilds selections with `reversed: false`, losing direction.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Keep the tree-sitter worker as one provider but move ladder assembly to the editor package: a `selectionRangeProviders` array where the tree-sitter backend is one entry and a pure-text `WordSelectionRangeProvider` (port of wordSelections.ts, ~60 lines, reusing our `wordRangeAtOffset` at textRanges.ts:112) is another that always works. Add the containment-chain filter and the two synthetic line steps from smartSelect.ts:245-300 \u2014 they cost nothing and are what makes the ladder feel smooth. Invalidate the cached stack in structuralSelection.ts on any selection change rather than on snapshot version, mirroring the `_ignoreSelection` guard so our own writes do not self-invalidate.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, plus one finding the survey missed. structuralSelection.ts:38-42 `selectTreeSitterToken`, :45-55 `expandTreeSitterSelection`, :57-70 `shrinkTreeSitterSelection` \u2014 one tree-sitter provider, one range per press, stack kept in `TreeSitterSelectionExpansionState`. treeSitter.worker.ts:1585-1592 `selectionRangeForAction` dispatches to `tokenRangeAt` (:1594-1598, `descendantForIndex` \u2014 so we DO have one sub-named-node 'token' step, slightly better than the claim's 'exactly the named-node chain') or `expandedRangeAt` (:1600-1603) -> `containingNamedNode` (:1605-1614). No subword, word, line-content or whole-line steps. :1567-1573 returns `staleSelectionResult` when no parsed layer is cached, so no grammar means no expansion at all. structuralSelection.ts:147-156 `rangesFromShrinkState` invalidates only on `snapshotVersion` mismatch \u2014 moving the caret does not reset the ladder. :136-142 `selectionSetFromRanges` hardcodes `reversed: false`. NEW: grep for `expandTreeSitterSelection|shrinkTreeSitterSelection|selectTreeSitterToken` outside the defining file returns hits ONLY in packages/tree-sitter/test/treeSitter-workerClient.browser.test.ts \u2014 and there is no expandSelection/smartSelect/shrinkSelection command id anywhere in packages/*/src. The feature is not reachable from any keybinding or command.",
  "verifierNotes": "True, and worse than reported: the machinery is orphaned \u2014 no command id, no keymap entry, only tests call it. Ranking the work: (1) wire it to a command at all; (2) the synthetic word/line-content/whole-line steps, which are what make Cmd+Shift+Right useful in a file with no grammar and are pure string work we can do with the existing wordRangeAtOffset/lineRangeAtOffset; (3) invalidate the ladder on caret movement; (4) preserve `reversed`. The provider-bucket architecture (unordered candidates -> filter -> sort -> strictly-increasing chain) is worth adopting because it is what lets LSP selectionRange, tree-sitter, and the synthetic steps coexist \u2014 we have an LSP package that could contribute to it."
 },
 {
  "domain": "cursor-selection",
  "title": "Cmd+D from a collapsed caret is whole-word and case-sensitive, and searches from the last-added selection",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/multicursor/browser/multicursor.ts:276-325 (MultiCursorSession.create: `if (selections.length === 1 && selections[0].isEmpty()) { isDisconnectedFromFindController = true; wholeWord = true; matchCase = true; }`, and the empty-selection branch expands to `getConfiguredWordAtPosition`), :368-383 (_getNextMatch: `currentMatch` is consumed once so the first Cmd+D selects the word under the caret rather than jumping, then search resumes from `allSelections[allSelections.length - 1].getEndPosition()` \u2014 the **last added** selection, not the last in document order), :335-347 / :387-399 (addSelectionToNext/Previous), :440-452 (selectAll)",
  "mechanism": "The session captures search settings at creation time and remembers whether it owns them. Starting from a bare caret it forces wholeWord+matchCase and disconnects from the find widget's state, so Cmd+D on `foo` will not match the `foo` inside `foobar` or `Foo`. `currentMatch` makes the first press a pure 'select the word under the caret' with no search at all. Subsequent presses anchor the search at the end of the *most recently added* selection, which is what makes Cmd+D feel like it walks forward from where you last were even when earlier cursors sit later in the document.",
  "whyItMatters": "In our editor, placing the caret in `id` and pressing Cmd+D repeatedly picks up `id` inside `width`, `valid`, `hidden` \u2014 the classic wrong-rename accident, because the very next thing users do is type over all the cursors. This is a small fix with a large blast radius avoided.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/occurrences.ts:99-115 `findExactOccurrenceFrom` uses plain `text.indexOf(query)` with no boundary test; :45-58 `findAllExactOccurrences` likewise. :60-76 `findNextExactOccurrence` computes `searchStart = selected.reduce((o, r) => Math.max(o, r.end), 0)` \u2014 the maximum end across *all* selections, i.e. document order, not the last added. :20-28 `getOccurrenceQuery` takes `selections.find(s => !s.collapsed)` \u2014 the first non-collapsed in document order \u2014 as the query source. We already have the needed predicate: packages/editor/src/textRanges.ts:133-141 `isWholeWordRange`, used by packages/find/src/search.ts:205 but never by occurrences.ts.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Thread a `{wholeWord: boolean; matchCase: boolean}` session state through `addNextExactOccurrence` (packages/editor/src/editor/inputSelectionController.ts:1382-1411), set to `{true, true}` on the branch that already detects `resolved.length === 1 && primary.collapsed` (:1391), and reset it when the user starts from a real selection. In `findExactOccurrenceFrom` (occurrences.ts:99), reject candidates failing `isWholeWordRange(text, range)` when the flag is set. Change `searchStart` to derive from a tracked last-added selection index rather than `Math.max` over ends \u2014 the same `lastAdded` field proposed for the merge-rules finding.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, with one correction in our favour. occurrences.ts:99-115 `findExactOccurrenceFrom` and :44-58 `findAllExactOccurrences` use bare `text.indexOf(query)` \u2014 no word-boundary test, so Cmd+D on `foo` matches inside `foobar`. Case sensitivity is implicit (indexOf), so that half is already correct by accident. :60-76 `findNextExactOccurrence` computes `searchStart = selected.reduce((o, r) => Math.max(o, r.end), 0)` \u2014 max end over all selections. :20-28 `getOccurrenceQuery` takes the first non-collapsed selection in document order. CORRECTION to the survey: the anchor problem is only in ONE of the two paths. inputSelectionController.ts:1380-1412 `addNextExactOccurrence` (the Cmd+D path) uses `resolved[0]` and `findNextExactOccurrence`, so it is document-ordered; but :557-594 `applyMoveSelectionToNextOccurrenceCommand` already does the right thing \u2014 `resolved.at(-1)` plus `findNextExactOccurrenceFromRange` (occurrences.ts:78-88), which anchors on the last selection's range. So the last-added-anchor primitive already exists and is used; Cmd+D just does not call it. Also confirmed: inputSelectionController.ts:1425-1450 `selectCurrentWordForOccurrence` makes the first Cmd+D from a bare caret a pure word-select with no search, matching Monaco's `currentMatch`. The needed boundary predicate exists at textRanges.ts:133-141 `isWholeWordRange` and is used by packages/find/src/search.ts but never by occurrences.ts.",
  "verifierNotes": "True but smaller than presented \u2014 two of the four sub-behaviours (currentMatch first press, case sensitivity) are already right, and the last-added anchor is already implemented for moveSelectionToNextFindMatch. The remaining work is genuinely small: pass a wholeWord flag into `findExactOccurrenceFrom` gated on 'the session started from a collapsed caret' and filtered through the existing `isWholeWordRange`, and switch `addNextExactOccurrence` from `resolved[0]`/`findNextExactOccurrence` to `resolved.at(-1)`/`findNextExactOccurrenceFromRange` \u2014 i.e. make it match the sibling function 30 lines above it. The 'session remembers whether it owns the search settings' idea is the part we'd be building new, and it is the part that makes wholeWord not leak into the find widget."
 },
 {
  "domain": "cursor-selection",
  "title": "Auto-closing: token-aware quote suppression via a 'neutral character' probe, and multi-cursor surround",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/cursor/cursorTypeEditOperations.ts:171-280 (getAutoClosingPairClose), :253-262 (the neutral-character trick: `getTokenTypeIfInsertingCharacter(lineNumber, beforeColumn, neutralCharacter)` then `pair.isOK(tokenType)`), :249-252 (`isCheapToTokenize` bail-out \u2014 never force tokenization on the typing path), :234-240 (`_isBeforeClosingBrace` allows auto-close before another closer even when the char is not in autoCloseBefore), :283-306 (_findContainedAutoClosingPair for nested multi-char pairs), :395-440 (_isSurroundSelectionType: refuses when *any* selection is empty, refuses when the selected text is whitespace-only, and refuses when typing a quote over a single selected quote character)",
  "mechanism": "Two ideas worth stealing. (1) Whether a typed quote *opens* or *closes* a string is undecidable from the characters alone, so Monaco asks the tokenizer: it pretends to insert a neutral character at the position and asks what token type results. If that says 'string', the quote is closing an existing string and must not auto-close. It refuses to force tokenization (`isCheapToTokenize`) so this never stalls typing \u2014 it degrades to not auto-closing. (2) All auto-close and surround decisions are evaluated across *every* selection and only applied if all agree, so multi-cursor typing of `(` or `\"` behaves consistently; surround additionally refuses on whitespace-only selections (typing a quote over selected spaces should replace, not wrap) and on a single selected quote character.",
  "whyItMatters": "Practically: typing the closing `\"` of a string in our editor inserts a new pair rather than terminating the string when the tracked-pair record has been lost, and none of our auto-close or surround behaviour happens at all with more than one cursor \u2014 so multi-cursor wrapping of several identifiers in quotes, a very common refactor, silently replaces them instead.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/autoClose.ts:88-96 `shouldAutoClose` looks only at the immediately adjacent characters \u2014 no token/scope input, and we do have tree-sitter tokens available (packages/editor/src/editor/tokenIndex.ts, tokenProjection.ts). :105-113 `shouldTypeOverCloser` requires `trackedAtCaret`, i.e. type-over works only for pairs we inserted and still track (autoCloseStore.ts caps at MAX_TRACKED_PAIRS, :32). packages/editor/src/editor/inputSelectionController.ts:325-332 `surroundSelection` returns null unless `selections.length === 1`, and :380-390 `collapsedCaretOffset` likewise returns null for multiple selections, gating every auto-close path. No whitespace-only guard and no quote-over-quote guard in `surroundSelection`. There is no equivalent of `_isBeforeClosingBrace`, so typing `(` just before an existing `)` does not auto-close even though it should.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Lift the single-selection restriction first: make `surroundSelection` (inputSelectionController.ts:325) map over all resolved selections and emit one pair of zero-width edits per selection \u2014 the edits are already at distinct offsets so the existing batch path works \u2014 and add the whitespace-only and quote-over-quote guards from _isSurroundSelectionType (~10 lines each). Then add `_isBeforeClosingBrace` to autoClose.ts:88 so `(` closes before `)`. For the quote/token check, query the existing token index at the caret and suppress auto-closing a quote when the caret is already inside a string or comment scope; where tokens are not yet available for that row, follow Monaco and simply do not auto-close rather than blocking on the worker.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Mostly confirmed, but one sub-claim is FALSE. autoClose.ts:85-93 `shouldAutoClose` reads only `charAfter`/`charBefore` (AutoCloseContext, :70-75) \u2014 no token/scope input, while packages/editor/src/editor/tokenIndex.ts and tokenProjection.ts exist. :105-113 `shouldTypeOverCloser` requires `trackedAtCaret`, so type-over only works for pairs we inserted and still track (autoCloseStore.ts MAX_TRACKED_PAIRS cap at :32). Multi-cursor gating confirmed: inputSelectionController.ts:325-332 `surroundSelection` returns null unless `selections.length === 1`, and :380-390 `collapsedCaretOffset` likewise \u2014 so every auto-close path (insert, type-over, pair-delete, surround) is single-caret only. FALSE SUB-CLAIM: 'no equivalent of _isBeforeClosingBrace, so typing ( just before an existing ) does not auto-close'. autoClose.ts:47 `AUTO_CLOSE_BEFORE` explicitly contains ')', ']', '}', '>' alongside ';:.,= \\t\\n', so typing `(` before `)` DOES auto-close today. Also true-but-unmentioned in our favour: autoClose.ts:17-38 already varies pairs by language (PROSE_PAIRS drops the apostrophe for markdown), and :90 already blocks `''` nesting via `charBefore === pair.open`, which covers part of what Monaco's quote-over-quote guard does.",
  "verifierNotes": "Status stands at partial, but recalibrate the evidence \u2014 the closing-brace claim is wrong, and we are further along on language-specific pairs than reported. Two things genuinely worth taking: (1) multi-cursor surround/auto-close, which is a real everyday gap (select three things with Cmd+D, type a quote, nothing wraps) and is mechanically simple since `surroundSelection` at :325-352 already builds a 2-edit batch per selection \u2014 it just needs to map over all selections instead of bailing; (2) the token probe for quotes, which is the clever idea and which we can actually implement because tokenIndex.ts/tokenProjection.ts already give us token types at an offset. Copy Monaco's refusal to force tokenization \u2014 the value depends on it staying cheap. The whitespace-only-selection guard on surround is a 1-line correctness nit worth folding in."
 },
 {
  "domain": "cursor-selection",
  "title": "Vertical motion and Home/End from selection edges, wrapped lines, and first-non-blank",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/cursor/cursorMoveOperations.ts:248-263 (moveUp: with a selection and not extending, moves relative to `selection.start`), :203-227 (moveDown: relative to `selection.end`, plus a retry loop `do { ... } while (i++ < 10)` that re-runs when `normalizePosition` fails to advance past hidden/injected content), :323-338 (moveToBeginningOfLine toggles between first-non-blank column and column 1), :340-344 (moveToEndOfLine with `sticky` sets leftoverVisibleColumns to MAX_SAFE - maxColumn so End-then-Down stays at end of line), :229-243 / :266-281 (translateDown/translateUp move *both* selectionStart and position, preserving each end's leftover columns); references/vscode/src/vs/editor/common/cursor/cursorMoveCommands.ts:58-72 (_moveToLineStart: escalates from wrapped view-line start to model line start on a second press), :86-110 (_moveToLineEnd, symmetric), :18-46 (addCursorUp/Down duplicate the whole cursor *state*, and take a `useLogicalLine` flag)",
  "mechanism": "Four related behaviours our code flattens. (a) Up/Down with a non-empty selection and no Shift collapse to the *top* / *bottom* of the selection respectively before moving, matching every native text control. (b) Home escalates: first press goes to the start of the wrapped view line, and once already there (or on the first wrapped row) it goes to the model line's first non-blank column, toggling to column 1 on a further press; End mirrors this. (c) `moveDown` retries up to 10 times when position normalization does not actually change line, which is what makes Down behave across folded regions and injected text. (d) Add-Cursor-Above/Below uses `translateUp`/`translateDown`, which shift *both* endpoints \u2014 so with a non-empty selection you get a duplicated selection on the next line, not a bare caret \u2014 and takes a `useLogicalLine` flag (true for the default keybinding) so the new cursor lands on the next *logical* line rather than the next wrapped row.",
  "whyItMatters": "Home not jumping to the first non-blank character is the single most-noticed missing motion in a code editor \u2014 it is how you get to the start of an indented statement. Up/Down from a downward selection currently moves from the wrong end, which surprises anyone who drag-selects then arrows. And Add-Cursor-Below on a selection gives a caret instead of a duplicated selection, so the common 'select a token, add the same selection on the lines below' gesture does not work.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/navigationTargets.ts:117-130 `verticalTarget` always uses `context.resolved.headOffset` \u2014 no start/end collapse for non-extending moves. :152-164 `lineBoundaryTarget` calls `view.offsetAtLineBoundary(head, 'start'|'end')`, which is packages/editor/src/virtualization/virtualizedTextView.ts:650-655 \u2192 `lineStartOffset(view, row)` (virtualizedTextViewLayout.ts:300-308): the display-row start, with no first-non-blank step and no second-press escalation to the logical line. No `sticky` concept \u2014 `lineBoundaryTarget` returns no `goal`, so End-then-Down does not hold the end of line (inputSelectionController.ts:642 falls back to `SelectionGoal.none()`). packages/editor/src/editor/inputSelectionController.ts:498-520 `applyInsertCursorCommand` pushes `{anchor: selection.anchor, head: selection.anchor}` \u2014 always collapsed \u2014 and uses `offsetByDisplayRows`, i.e. wrapped rows rather than logical lines. Our goal-column plumbing itself is sound (SelectionGoal.horizontal, selections.ts:14-22; navigationGoalColumn, navigationTargets.ts:196-199) and is tab-aware via `bufferColumnToVisualColumn`.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "In navigationTargets.ts:117, mirror `horizontalTarget`'s existing collapse logic: when `!extend && !resolved.collapsed`, start from `startOffset` for rowDelta<0 and `endOffset` for rowDelta>0. For Home/End, add a `firstNonBlankOffset(row)` helper next to `lineStartOffset` (virtualizedTextViewLayout.ts:300) and make `lineBoundaryTarget` compare the current head against the display-row start and the first-non-blank column to pick the next stop in the Monaco escalation order. For Add-Cursor, change `cursorSelectionByDisplayRows` (inputSelectionController.ts:1357-1370) to translate both `anchorOffset` and `headOffset` and emit a real selection, and expose a `logicalLine` variant that steps whole buffer lines using the existing `getLineStarts` view.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed on all four sub-behaviours. (a) navigationTargets.ts:117-130 `verticalTarget` always passes `context.resolved.headOffset` to offsetByDisplayRows \u2014 no collapse to selection top/bottom. Note the contrast that makes this clearly an oversight rather than a choice: :59-77 `horizontalTarget` DOES collapse (`const collapsedOffset = direction === 'left' ? resolved.startOffset : resolved.endOffset`, used when `!extend && !collapsed`), so Left/Right behave natively and Up/Down do not. (b) :152-164 `lineBoundaryTarget` -> virtualizedTextView.ts:650-655 `offsetAtLineBoundary` -> virtualizedTextViewLayout.ts:300-303 `lineStartOffset`, which is the display-row start with no first-non-blank step and no second-press escalation; `firstNonWhitespaceOffset` DOES exist at editActions.ts:1191 but is used only by line operations (:583, :599, :803). (c) `lineBoundaryTarget` returns no `goal`, so inputSelectionController.ts:640-644 stores `SelectionGoal.none()` and End-then-Down re-derives the goal from the head's visual column (navigationTargets.ts:196-199 `navigationGoalColumn`) rather than holding 'end of line'. (d) inputSelectionController.ts:498-528 `applyInsertCursorCommand` maps through `cursorSelectionByDisplayRows` and pushes `{anchor, head: anchor}` \u2014 always collapsed, and display-row-based rather than logical-line-based. Confirmed sound: SelectionGoal.horizontal (selections.ts:9-22) and the tab-aware visual column via displayTransforms.ts:221-235.",
  "verifierNotes": "True, and the best effort-adjusted cluster after claim 7 \u2014 these are four small independent changes in two files, all with the plumbing already present. (a) is ~3 lines and mirrors code that already exists 40 lines above it in the same file. (b) smart-Home is the one users notice most and `firstNonWhitespaceOffset` is already written, just in the wrong module. (c) needs a new SelectionGoal variant ('lineEnd') alongside the existing horizontal/horizontalRange \u2014 small, but it is a type change that ripples through goal handling. (d) preserving selection shape when inserting cursors above/below is the most arguable; the display-row-vs-logical-line choice is defensible in a wrapping editor and I would not change it blindly."
 },
 {
  "domain": "cursor-selection",
  "title": "Word/character operations are line-scoped with a Uint8Array classifier; ours materialize the whole document and run regexes per character",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/core/characterClassifier.ts:11-58 (CharacterClassifier: a 256-entry Uint8Array for ASCII plus a Map for the rest, O(1) `get(charCode)`); references/vscode/src/vs/editor/common/core/wordCharacterClassifier.ts:18-40 (built once from the wordSeparators string), :99-110 (LRUCache of 10 classifiers keyed by separators+locales), :63-77 (Intl.Segmenter results memoized per line, `_cachedLine === line` short-circuit); references/vscode/src/vs/editor/common/cursor/cursorWordOperations.ts:75-78 / :143-146 (every word operation starts from `model.getLineContent(position.lineNumber)` \u2014 one line, never the document)",
  "mechanism": "Two compounding decisions. Every word operation reads exactly one line out of the piece table and scans it with `charCodeAt`, and the character classification is a direct array index into a 256-byte table rather than a predicate call. Even the expensive Intl.Segmenter path is memoized against the last line string so repeated Ctrl+Arrow within a line costs one segmentation, not one per keystroke.",
  "whyItMatters": "Every arrow keypress in our editor calls `session.materializeFullText()`. When the retained-text cache misses \u2014 which it does after any edit that does not retain (documentTextSnapshot.ts:42) \u2014 that is an O(document) string build on the keyboard path, and it is instrumented as a perf event precisely because it is known to be expensive. On top of that, each character examined during a word motion runs up to two `RegExp.test` calls with the `u` flag. On a large file, held-down Ctrl+Arrow is where this shows up as input lag.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/inputSelectionController.ts:616 \u2014 `const text = session.materializeFullText()` runs on *every* navigation command including cursorLeft/cursorRight, then passes the full string into `navigationTargetForCommand` (navigationTargets.ts:29). Same at :1099 and :1107 for triple-click and double-click. packages/editor/src/documentTextSnapshot.ts:38-54 \u2014 `materializeFullText` returns the retained string only when `materializedText?.length === snapshot.length`, otherwise calls `materializePieceTableFullText`, wrapped in `measureEditorPerformance('textSnapshot.materializeFullText')` and `recordFullTextSnapshotRead`. packages/editor/src/textRanges.ts:88-96 `characterClassAt` runs `/\\s/u.test` then `/^[\\p{L}\\p{N}_]$/u.test` per character, and is called once per character by `skipBackward`/`skipForward` (:143-164).",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Change the `NavigationTargetContext` in navigationTargets.ts:26-32 from `text: string` to a line-reader \u2014 `readRange` already exists on `DocumentTextSnapshot` (documentTextSnapshot.ts:14) and `lineRangeAtOffset` gives the bounds, so word/character operations can read one line. That removes the full-text materialization from the arrow-key path entirely (double/triple click at inputSelectionController.ts:1099,1107 can use the same reader). Separately, replace `characterClassAt` with a prebuilt classifier: a `Uint8Array(256)` for ASCII plus a `Map<number, class>` for the rest, constructed once from a configurable separators string and memoized \u2014 a direct port of characterClassifier.ts:11-58, about 40 lines, and it simultaneously delivers the configurable `wordSeparators` option the word-navigation finding needs.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "Worse than claimed \u2014 upgraded partial -> missing on the line-scoping half. inputSelectionController.ts:616 `const text = session.materializeFullText()` runs at the top of `applyNavigationCommand`, i.e. on EVERY arrow key including plain cursorLeft, and is passed whole into navigationTargets.ts:29. Same at :1098 and :1106 (triple/double click) and :539/:1383 (occurrences). Critically, the result is NOT memoized across calls: documentTextSnapshot.ts:38-55 `createDocumentTextSnapshot` retains a string only when the optional `materializedText` argument is supplied AND `materializedText.length === snapshot.length`, otherwise every call re-runs `materializePieceTableFullText(snapshot)`. documentSession.ts:414, :435 and :517 all reconstruct the snapshot after edits/undo/redo with NO text argument \u2014 so after any keystroke that edits the buffer, every subsequent arrow key walks the entire piece table and allocates a fresh full-document string. (documentSession.ts:268 and :917 pass text only at construction.) The lazy `defineLazyFullTextProperty` cache at documentTextSnapshot.ts:23-36 is per-change-object, not per-session, so it does not help the navigation path. Classifier confirmed: textRanges.ts:88-96 `characterClassAt` runs `/\\s/u.test` then `/^[\\p{L}\\p{N}_]$/u.test` per character, called once per character by skipBackward/skipForward (:143-164).",
  "verifierNotes": "Status corrected to missing: there is no line-scoping in the navigation path at all, and the absence of memoization means the cost is paid per keystroke on a dirty buffer, not once. This is the item most likely to be an actual user-visible problem on a large file \u2014 hold down ArrowRight after typing and you re-materialize the document per frame. Separate the two ideas by value: line-scoping is the real win and we already have every primitive for it (virtualizedTextViewLayout.ts:300-308 lineStartOffset/lineEndOffset, editActions.ts createLineMap, and documentTextSnapshot.ts:58 `readRange`) \u2014 `navigationTargetForCommand` should take a line-reader, not a full string. The Uint8Array classifier and the memoized-Segmenter-per-line are micro-optimizations that only matter once the O(document) allocation is gone; do not lead with them."
 },
 {
  "domain": "input-a11y",
  "title": "In-progress IME composition is invisible and the candidate window lands at the viewport corner",
  "kind": "correctness",
  "source": "both",
  "referenceLocation": "vscode: src/vs/editor/browser/controller/editContext/textArea/textAreaEditContext.ts:50-110 (VisibleTextAreaData, definePresentation), :396-425 (onCompositionStart makes the textarea visible), :703-795 (_render / _renderAtTopLeft), :112 (canUseZeroSizeTextarea); src/vs/editor/browser/controller/editContext/native/nativeEditContext.ts:502-535 (_handleTextFormatUpdate), :547-575 (_updateSelectionAndControlBounds), :577-611 (_updateCharacterBounds); codemirror5/src/input/input.js:122-134 (hiddenTextarea kept near cursor)",
  "mechanism": "Monaco's textArea path does NOT keep the textarea hidden during composition. On compositionstart it builds a VisibleTextAreaData (start line, distance to line start/end, width of hidden text before), reveals the range, sets class `ime-input`, and in _render() sizes and positions the textarea exactly over the composed span, scrolling the textarea internally (scrollLeft = widthOfHiddenLineTextBefore, scrollTop = lineCount*lineHeight) so only the composed text shows. It then copies the token's presentation (color/italic/bold/underline via definePresentation and TokenizationRegistry colour map) so the live composition is styled like the surrounding code. When the caret is off-screen it falls back to _renderAtTopLeft, and the textarea is 1x1 rather than 0x0 outside Firefox because a 0x0 textarea breaks Korean IME. On macOS (or accessibilitySupport=Enabled) the textarea is made a full lineHeight tall so the emoji/accent popup anchors correctly. The EditContext path solves the same problem differently: it never shows a textarea, but reports geometry to the OS \u2014 updateSelectionBounds/updateControlBounds from the primary selection rect, and per-character DOMRects on the `characterboundsupdate` event \u2014 and renders the IME's own underline styling by turning `textformatupdate` text formats into inline decorations (thin -> secondary, thick -> primary class).",
  "whyItMatters": "A CJK, Vietnamese or accent-key user currently types into a 1px fully-transparent textarea; they see nothing at all until compositionend, and the OS candidate list appears pinned at the top-left of the viewport instead of under the caret. This makes the editor effectively unusable for a large share of the world's developers, and it is invisible to anyone testing with a Latin keyboard.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/style.css:296-316 `.editor-virtualized-input` is `opacity:0; color:transparent; -webkit-text-fill-color:transparent; width:1px; height:1px`. packages/editor/src/virtualization/virtualizedTextViewRows.ts:1901-1902 positions the input at `scrollTop`/`scrollLeft`, i.e. the viewport's top-left corner, never at the caret. packages/editor/src/editor/inputSelectionController.ts:878-889 `handleCompositionUpdate` only stores the text in inputState; nothing is rendered, and the document is only touched at compositionend (applyCompositionText, :1320-1339). No EditContext usage anywhere (grep for `editContext` in packages/ returns nothing).",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Two-stage. (1) Cheap fix first: in VirtualizedTextView.focusInput/positionInputInViewport, position `inputElement` at the primary caret's client rect (you already have `rangeClientRect(start,end)` in inputSelectionController.ts:791-803 and `offsetToX` in virtualizedTextViewGeometry.ts) instead of the viewport corner, and give it `height: lineHeight` on macOS. That alone fixes candidate-window placement. (2) Then make it visible during composition: on `compositionstart` add a class that drops opacity:0/transparent colours and sets the textarea's font/colour from the token under the caret (tokenIndex.ts/tokenProjection.ts already resolve token styles), size it to the composed span, and restore on `compositionend`. Longer term, add a `native-edit-context` strategy behind a capability check (`'EditContext' in window`) as a sibling of InputSelectionController's DOM listeners, feeding `textupdate` into the same `applyTypedText` path and `characterboundsupdate` from `virtualizedTextView.createRange`.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED. /Users/shaul/Desktop/D/Editor/packages/editor/src/style.css:295-315 `.editor-virtualized-input { opacity:0; color:transparent; -webkit-text-fill-color:transparent; caret-color:transparent; width:1px; height:1px; overflow:hidden }`. /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewRows.ts:1896-1903 `positionInputInViewport` sets only top=scrollTop/left=scrollLeft; its two callers pass viewport scroll, never a caret rect (virtualizedTextView.ts:520-528 `focusInput`). /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/inputSelectionController.ts:873-889: compositionstart/update only mutate the reducer state; `compositionText` is stored (inputState.ts:235) and read back in exactly one place \u2014 the compositionend fallback at inputSelectionController.ts:883. Grep for every consumer of `compositionText`/`compositionActive` across packages/editor/src returns only inputState.ts and inputSelectionController.ts \u2014 zero render-side consumers, so nothing paints the preedit. No EditContext anywhere: the only `editContext` hits in packages/ are a local test helper in packages/lsp-plugin/test/completion.test.ts:24,98 (an unrelated `EditorEditContributionContext` factory).",
  "verifierNotes": "Highest-value item in this domain. Today a CJK/Korean/Japanese/Vietnamese user sees nothing while composing and the OS candidate popup renders at the viewport's top-left, disconnected from the caret. Monaco's textarea path is verified at references/vscode/src/vs/editor/browser/controller/editContext/textArea/textAreaEditContext.ts:50 (`class VisibleTextAreaData`), :396 (constructed on compositionstart), :411 (sets class `inputarea ... ime-input`) \u2014 it reveals the real textarea over the composed span rather than hiding it. Note the reference checkout also has a *native* EditContext path (src/vs/editor/browser/controller/editContext/native/), which is the cheaper modern target for us: EditContext gives characterBoundsUpdate so we position the candidate window from our own geometry (offsetToX in virtualizedTextViewGeometry.ts) without ever un-hiding the textarea. Effort: medium \u2014 geometry APIs and the row/chunk layer already exist; the work is a preedit decoration plus caret-rect-driven input placement."
 },
 {
  "domain": "input-a11y",
  "title": "Clipboard carries no editor metadata, so multi-cursor copy/paste degenerates into one blob",
  "kind": "correctness",
  "source": "both",
  "referenceLocation": "vscode: src/vs/editor/browser/controller/editContext/clipboardUtils.ts:31-71 (generateDataToCopy), :78-99 (InMemoryClipboardMetadataManager), :131-159 (ClipboardEventUtils get/setTextData, 'vscode-editor-data' MIME), :22-29 (Firefox \\r\\n normalisation); src/vs/editor/common/cursor/cursorTypeEditOperations.ts:654-705 (PasteOperation._distributePasteToCursors / _distributedPaste); src/vs/editor/common/viewModel/viewModelImpl.ts:998-1049 (getPlainTextToCopy); codemirror5/src/input/input.js:14-21 (lastCopied), :30-42 (multiPaste distribution)",
  "mechanism": "On copy Monaco writes three things to the DataTransfer: `text/plain`, optionally `text/html`, and a JSON blob under the private MIME type `vscode-editor-data` containing `{version, id, isFromEmptySelection, multicursorText, mode}` where `multicursorText` is the per-selection string array. It ALSO records the same metadata in a process-global `InMemoryClipboardMetadataManager` keyed by the exact copied text, so a paste that loses the custom MIME type (Firefox, OS clipboard round-trip, execCommand paths) can still be matched by comparing the pasted string to the last copied string. On paste, `getTextData` parses the custom MIME first and falls back to the in-memory match. `PasteOperation._distributePasteToCursors` then distributes: if `multicursorText.length === selections.length` each cursor gets its own fragment; else if `multiCursorPaste === 'spread'` and the line count happens to match the cursor count, the lines are split one per cursor; else it's a simple paste at every cursor. CM5 does the same thing more cheaply with a module-level `lastCopied = {lineWise, text: [...]}` compared by `text.join('\\n') == inserted`, and also supports N cursors being an integer multiple of the copied fragment count (`sel.ranges.length % lastCopied.text.length == 0`).",
  "whyItMatters": "Select three identifiers with three cursors, copy, put three cursors elsewhere, paste: today each cursor receives all three strings concatenated with newlines, exploding one line into three. This is the single most common multi-cursor workflow and it currently produces garbage. The in-memory fallback also matters because private MIME types silently vanish through several real clipboard paths.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/inputSelectionController.ts:1441-1456 `selectedTextForClipboard` maps every non-collapsed selection to text and returns `texts.join('\\n')` \u2014 the per-selection structure is discarded. :1214-1221 `handleCopy` sets only `text/plain`. :1163-1185 `handlePaste` reads only `text/plain` and calls `applyPasteText` -> `session.applyText(text)`, which inserts the same string at every cursor. No custom MIME type or in-memory metadata store exists (grep for `clipboardData` in packages/ returns only these two sites plus diff/src/DiffView.ts:446).",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `packages/editor/src/editor/clipboardMetadata.ts` holding a module-level `{lastCopiedText, metadata}` singleton plus `readClipboard(DataTransfer)` / `writeClipboard(DataTransfer, ...)` helpers using a private MIME (e.g. `application/x-editor-clipboard`). In `handleCopy`, keep the per-selection array instead of joining and write both plain text and metadata. In `handlePaste`, resolve metadata (MIME first, in-memory text match second) and, when `multicursorText.length === session.getSelections().selections.length`, build one TextEdit per selection through `session.applyEdits` rather than `applyText` \u2014 the batch path already exists and is used by `surroundSelection` (:325-355) for edits at distinct offsets. Normalise `\\r\\n`->`\\n` before the in-memory key comparison as Monaco does for Firefox.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/inputSelectionController.ts:1441-1455 `selectedTextForClipboard` maps non-collapsed selections to strings and returns `texts.join('\\n')`. :1214-1221 `handleCopy` calls `event.clipboardData.setData('text/plain', text)` and nothing else. :1163-1185 `handlePaste` reads only `getData('text/plain')` then calls `applyPasteText` (:1585-1590) = `session.applyText(text)`, which inserts the identical string at every cursor. Repo-wide grep for `clipboardData` outside tests: only inputSelectionController.ts:1171,1217,1219 and packages/diff/src/DiffView.ts:446. No custom MIME, no in-memory metadata store. Locked in by test: packages/editor/test/editor.test.ts:2895-2907 asserts multi-range copy yields exactly `'alpha\\ngamma'`.",
  "verifierNotes": "Real gap, but scope it precisely: our plain-text form already matches Monaco's (join with \\n). What is missing is the round-trip \u2014 copy N selections, paste with N cursors, get line i into cursor i. Verified reference: references/vscode/src/vs/editor/browser/controller/editContext/clipboardUtils.ts:134 reads and :158 writes `vscode-editor-data`. The in-memory manager matters more than the MIME for us: browser clipboard sanitisers strip unknown types, so the durable path is a module-level map keyed by the exact text, with the MIME as the cross-window bonus. Effort: low \u2014 `applyPasteText` is a 5-line function and `getSelections()` already returns an ordered array; the only new state is the metadata store. Bundle this with item 3, they touch the same two handlers."
 },
 {
  "domain": "input-a11y",
  "title": "No cut handler at all, and no empty-selection line copy/cut",
  "kind": "feature",
  "source": "both",
  "referenceLocation": "vscode: src/vs/editor/browser/controller/editContext/native/nativeEditContext.ts:135-148 (cut listener: fire event, mark screen-reader ignore window, write clipboard, then viewController.cut); src/vs/editor/common/cursor/cursorDeleteOperations.ts:235-290 (DeleteOperations.cut full-line semantics incl. last-line special case and lastCutRange de-dup); src/vs/editor/common/viewModel/viewModelImpl.ts:1004-1046 (getPlainTextToCopy expands empty ranges to whole lines, one per distinct line, each with a trailing newline); src/vs/editor/contrib/clipboard/browser/clipboard.ts:222-228 (bail out when selection empty and emptySelectionClipboard off); codemirror5/src/input/input.js:105-114 (copyableRanges), :54-55 (lineWise paste inserts at column 0)",
  "mechanism": "With `emptySelectionClipboard` on (the default), Cmd+C with a collapsed caret copies the whole line including its terminator, and the metadata records `isFromEmptySelection: true`. On paste, `pasteOnNewLine` makes the text land at column 1 of the caret's line rather than at the caret, so copy-line/paste-line reproduces the line above/at the caret instead of splicing it mid-token. `getPlainTextToCopy` also de-duplicates: several empty cursors on the same line contribute the line only once (`if (modelLineNumber !== prevModelLineNumber)`). Cut with an empty selection deletes line N plus its newline, except on the last line where it instead eats the preceding newline, and `lastCutRange` prevents two cursors on adjacent lines from double-deleting. CM5's `copyableRanges` + `lastCopied.lineWise` implements the same contract in ~15 lines.",
  "whyItMatters": "Cmd+X currently does nothing in this editor \u2014 the browser's default cut cannot work because the content is not contenteditable and the hidden textarea is empty, so the user loses a core keybinding. And Cmd+C on a caret with no selection, which experienced users rely on to duplicate a line, silently copies nothing.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/inputSelectionController.ts:109-123 `install()` registers mousedown/beforeinput/copy/drop/paste/keydown/composition*/keyup \u2014 there is no `cut` listener. :1441-1453 `selectedTextForClipboard` filters out collapsed selections and returns `null` when none remain, so an empty-selection copy is a no-op. grep for `'cut'` across packages/ finds no handler.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Register `cut` next to `copy` in `install()`/`dispose()`. Factor `selectedTextForClipboard` into `clipboardPayload(): {text, perSelection: string[], isFromEmptySelection}` that, when every selection is collapsed and the (new) `emptySelectionClipboard` option is on, expands each caret to its full line via `lineRangeAtOffset` (editor/textRanges.ts, already used by triple-click at :1095-1101) with Monaco's same-line de-dup. `handleCut` writes that payload then routes through the existing edit machinery: `session.applyEdits` with the line ranges, mirroring `applyDeleteCommand` (:397-414) so undo/typing-run behaviour matches. Add the `pasteOnNewLine` branch to `handlePaste` using the metadata from the previous finding.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED, and worse than claimed. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/inputSelectionController.ts:109-122 `install()` registers mousedown, beforeinput, copy, drop, paste, keydown, composition{start,update,end}, keyup, mouseup, selectionchange \u2014 no `cut`. Repo-wide grep for `'cut'`/`handleCut`/`deleteByCut` across packages/ (excluding dist) returns zero. The beforeinput path cannot rescue it either: `beforeInputText` (:1601-1606) returns null for every inputType except insertLineBreak/insertText/insertFromComposition, and handleBeforeInput bails on null (:1136). The keymap has no cut command \u2014 `editorCommandPackForCommand` (keymap.ts:277-289) has no branch producing the `'clipboard'` pack, and `editorKeyBindingsForCommandPack` (keymap.ts:290-302) has no `'clipboard'` case, so the declared `'clipboard'` pack (keymap.ts:21,202,213) is an empty shell. Empty-selection copy is explicitly a no-op and asserted as such: packages/editor/test/editor.test.ts:2909-2918 `'does not intercept copy for collapsed selections'` expects `formatCount() === 0` and `defaultPrevented === false`.",
  "verifierNotes": "Promote this above everything else in the domain: Cmd+X is a straight functional bug, not a missing nicety. The hidden textarea is `readOnly = true` (virtualizedTextViewHelpers.ts:166, re-asserted in virtualizedTextView.ts:517) with `value = ''`, so the native cut deletes nothing and copies nothing \u2014 Cmd+X is silently inert on a selection. Effort: trivial for the fix (one listener that reuses selectedTextForClipboard then routes through the existing delete path). The emptySelectionClipboard half is a small extension of `selectedTextForClipboard` plus a `pasteOnNewLine` flag in the metadata from item 2."
 },
 {
  "domain": "input-a11y",
  "title": "Drag-and-drop of text is non-functional: no dragover, no dragstart, no move semantics",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/editor/contrib/dnd/browser/dnd.ts:104-203 (mouse down/drag/drop lifecycle, _dragSelection capture, trigger-modifier = Alt on mac / Ctrl elsewhere for copy, dnd-target decoration), :24-30 (hasTriggerModifier), :210-216 (showAt + revealPosition); src/vs/editor/contrib/dnd/browser/dragAndDropCommand.ts:27-107 (getEditOperations emits delete-source + insert-target as one command and computes the resulting selection for every geometric case)",
  "mechanism": "Monaco does not use HTML5 DnD at all for in-editor text movement; it drives it from its own mouse pipeline (onMouseDrag/onMouseDrop). On drag it remembers which existing selection contains the press position (`_dragSelection`), paints a caret-shaped `dnd-target` decoration at the hovered position, and switches the cursor to `copy` while the trigger modifier is held. On drop it pushes an undo stop, runs `DragAndDropCommand` as a single command \u2014 delete at source (skipped when copying) plus insert at target \u2014 and, crucially, `computeCursorState` returns a `targetSelection` computed for each case (drop below the selection, above it, same line before/after) so the moved text stays selected afterwards. Dropping inside the source selection is a no-op, except that dropping exactly on the selection's start or end with the copy modifier is allowed so you can duplicate a block beside itself.",
  "whyItMatters": "Two separate problems. (a) Dragging a selection to move it \u2014 a standard editor gesture \u2014 does not exist. (b) The existing external-drop handler is almost certainly dead code: the browser only fires `drop` on an element whose `dragover` was default-prevented, and nothing in this codebase does that, nor is the editor contenteditable. So dragging a file or text from another app onto the editor does nothing, and the handler that looks like it works has probably never fired in a real browser.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/inputSelectionController.ts:115 registers only `drop`; :1187-1212 `handleDrop` inserts `dropPlainText(event)` at the hit-tested offset with no source deletion. grep for `dragover|dragstart|draggable|contentEditable` across packages/editor/src returns nothing (the only `drag` hits are the internal `mouseSelectionDrag` variables). No drop-target decoration.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add a `dragover` listener on `options.el` that calls `preventDefault()` and sets `dataTransfer.dropEffect`, plus a drop-caret indicator \u2014 the view already has a caret layer (`.editor-virtualized-caret-layer` in style.css) and `rangeDecorations.ts` for transient ranges. For in-editor move, do it in the existing mouse pipeline rather than HTML5 DnD: in `handleMouseDown`/`startMouseSelectionDrag` (:891-955), if the press offset falls inside an existing non-collapsed selection, don't start a new selection drag \u2014 arm a pending text-drag; on `mousemove` past a threshold, show the drop caret; on `mouseup` issue ONE `session.applyEdits([{from:srcStart,to:srcEnd,text:''},{from:target,to:target,text}], {selections:[...]})` (or omit the delete when Alt/Ctrl is held), reusing the batch-of-two-edits pattern already proven in `surroundSelection` (:325-355). Port `DragAndDropCommand.getEditOperations`' target-selection arithmetic \u2014 offsets make it much simpler than Monaco's line/column version.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED as partial. Drop-in from outside works: inputSelectionController.ts:115 registers `drop`, :1187-1212 `handleDrop` hit-tests via `textOffsetFromPoint`, sets the selection, inserts `dropPlainText(event)` (:1592-1599, text/plain then text) and reveals. Nothing else exists: repo-wide grep for dragover|dragstart|dragend|dragenter|draggable|dropEffect|effectAllowed across packages (ts + css, excluding dist) returns zero hits. packages/editor/src/editor/mouseSelection.ts's only `Drag` hit is the internal type `MouseSelectionDrag` (:4), i.e. rubber-band selection state, not DnD. No drop-target decoration class in packages/editor/src/style.css. Test coverage stops at the same line: packages/editor/test/editor.test.ts:388-398 `createDropEvent` only ever stubs text/plain.",
  "verifierNotes": "Keep, but rank it last in this domain. Without a `dragover` handler that preventDefaults, the browser shows a no-drop cursor for most drags, so even the working `drop` path is reached inconsistently. The genuinely useful subset for us is small: (a) dragover \u2192 preventDefault + paint a caret-shaped target decoration (rangeDecorations.ts already gives us the decoration primitive), (b) mouse-driven move-vs-copy on the existing mouseSelection pipeline. Monaco deliberately avoids HTML5 DnD for in-editor moves \u2014 references/vscode/src/vs/editor/contrib/dnd/browser/{dnd.ts,dragAndDropCommand.ts} \u2014 which is the right model for us since we already own mousedown/mousemove/mouseup. Effort: medium-high; value: moderate (most users move text with cut/paste)."
 },
 {
  "domain": "input-a11y",
  "title": "Screen readers get an empty 1x1 textarea: no paged content window, no caret relationship",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/editor/browser/controller/editContext/screenReaderUtils.ts:39-114 (SimplePagedScreenReaderStrategy.fromEditorSelection, LIMIT_CHARS=500, ellipsis U+2026 splicing), :116-136 (ariaLabelForScreenReaderContent); src/vs/editor/browser/controller/editContext/native/screenReaderContentSimple.ts:40-73 (write textContent + setBaseAndExtent), :112-158 (selectionchange listener reading the caret back from the AT), :160-174 (trailing newline fix); src/vs/editor/common/config/editorOptions.ts:479-481 (accessibilityPageSize)",
  "mechanism": "Monaco keeps a hidden but non-empty element whose textContent is a *window* of the document around the primary selection: the page (accessibilityPageSize lines, default 10) containing the selection start, plus the selection, plus the page containing the selection end, with pretext/posttext each clamped to 500 characters (`LIMIT_CHARS`) because Chromium stalls on multi-thousand-character accessible text. The DOM selection inside that element is then set with `setBaseAndExtent` at the mapped offsets, and the element's `scrollTop` is nudged so the AT's notion of position matches. The flow is bidirectional: a `selectionchange` listener maps a caret the user moved with a Braille display or NVDA review cursor back to an editor Selection (`_getEditorSelectionFromDomRange`, including a CRLF offset correction). Self-inflicted selectionchange events are filtered by a timestamp guard: `_setIgnoreSelectionChangeTime` is stamped before every programmatic write and before cut/paste, and events within 100ms of that stamp \u2014 or within 5ms of the previous event \u2014 are dropped. `accessibilitySupport === Unknown` also switches on the aggressive text trimming.",
  "whyItMatters": "Right now the accessible name of the whole editor is the literal string 'Editor input' on an always-empty textarea. A screen-reader user gets no line content, no caret position, no selection feedback, and cannot use Braille review. The paged-window trick is also the only known way to do this without freezing on large files \u2014 a naive 'put the whole document in the textarea' implementation would hang on a 50k-line file.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewHelpers.ts:161-169 `createInputElement` sets `aria-label: 'Editor input'` and nothing else; virtualizedTextView.ts:526-528 `focusInput` explicitly sets `inputElement.value = ''` and `setSelectionRange(0,0)`. inputSelectionController.ts:1307 and :1330 clear the value again on every keystroke and composition. No accessibilityPageSize-like option exists (grep for `accessibilit` across packages/ hits only diff/, panes/, find/ widget chrome).",
  "value": "high",
  "effort": "L",
  "implementationSketch": "New module `packages/editor/src/editor/screenReaderContent.ts` exporting a pure `pagedScreenReaderWindow(snapshot, resolvedSelection, linesPerPage, trim)` returning `{value, selectionStart, selectionEnd, startOffset}` \u2014 the piece table's `readPieceTableTextRange` + `offsetToPoint` make this straightforward and it is trivially unit-testable in the style of the existing inputState.test.ts. Wire it from `applySessionChange`/`syncDomSelection` to write `inputElement.value` and `setSelectionRange`, replacing the unconditional `value=''` clears (those clears exist to keep the fallback typing path clean, so gate the new content behind a `screenReaderMode` flag that starts off). Add the `ignoreSelectionChangeTime` stamp to the existing input-state machine (inputState.ts already has the phase/ownership vocabulary for this) and a `selectionchange`-on-input listener that maps back to offsets.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED, with two aggravating details the survey missed. /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewHelpers.ts:161-169 `createInputElement` sets className, autocapitalize, autocomplete, `readOnly = true`, spellcheck, and `aria-label: 'Editor input'` \u2014 nothing more. virtualizedTextView.ts:520-528 `focusInput` forces `value = ''` and `setSelectionRange(0,0)` on every focus; inputSelectionController.ts clears it again per keystroke (:1307) and per composition (:1330). Aggravator 1: the textarea is permanently `readOnly`, which most screen readers announce as a read-only field. Aggravator 2: virtualizedTextViewHelpers.ts:149-159 `createScrollElement` gives the scroll container `tabIndex = 0` with no role and no aria-label, so AT focus can land on an unlabelled generic div. The two rendering layers are `aria-hidden` (virtualizedTextViewRows.ts:174,177), correctly, but that leaves nothing accessible at all. Repo-wide aria audit: every other aria attribute in packages/ belongs to find/panes/gutters/lsp-plugin/diff chrome \u2014 none to editor content.",
  "verifierNotes": "True and important: the editor is currently opaque to a screen reader \u2014 no text, no caret position, no context. Verified reference mechanism: references/vscode/src/vs/editor/browser/controller/editContext/screenReaderUtils.ts:54 `const LIMIT_CHARS = 500` with pretext trimming at :63-64, posttext at :72-73, and a middle-ellipsis for oversized selections at :92-93; the page size lives at native/screenReaderContentRich.ts:33 `_accessibilityPageSize`. The 500-char clamp is the load-bearing trick (Chromium stalls computing accessible values on long strings), and pairing it with a paged window is what keeps arrow-key latency flat on huge files. Effort: high but bounded \u2014 we already have `readPieceTableTextRange` and resolved selections; the work is a per-selection-change content window plus dropping readOnly while focused. Item 6 is worthless without this one."
 },
 {
  "domain": "input-a11y",
  "title": "No aria-live announcement channel for editor actions",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/base/browser/ui/aria/aria.ts:16-42 (setARIAContainer builds two role=alert and two aria-live=polite containers), :44-75 (alert/status alternate between the pair), :77-87 (insertMessage: 20000-char cap, visibility hidden->visible toggle to force re-announcement); src/vs/editor/contrib/toggleTabFocusMode/browser/toggleTabFocusMode.ts:38-42 (alert on state change)",
  "mechanism": "Two subtleties make this non-obvious. First, screen readers ignore an aria-live region whose textContent is set to the same string twice in a row, so Monaco keeps *two* alert containers and *two* status containers and alternates between them, clearing the other. Second, several browser/AT combinations only announce a role=alert node after a visibility flip, so `insertMessage` sets `visibility:hidden` then `visible` immediately after writing. Messages are capped at 20000 characters because inserting a huge string into a live region freezes the browser. `alert()` is assertive (interrupts), `status()` is polite.",
  "whyItMatters": "Every non-visual state change in this editor \u2014 fold/unfold, 'added cursor above', 'selected N occurrences', rename applied, find wrapped, tab-focus toggled \u2014 is currently silent for a screen-reader user. This is the cheapest large accessibility win available, and the duplicate-message and visibility-flip workarounds are exactly the kind of thing a from-scratch implementation gets wrong and then can't debug.",
  "ourStatus": "missing",
  "ourEvidence": "grep for `aria-live|role=\"alert\"|role=\"status\"` across all of packages/ returns zero hits. The only aria attributes in the editor package are the `aria-label` on the input (virtualizedTextViewHelpers.ts:168) and two `aria-hidden` layers (virtualizedTextViewRows.ts:174,177).",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Add `packages/editor/src/editor/announce.ts` with a lazily-created container appended to `options.el.ownerDocument.body` (or the editor container so it disposes with the editor), exporting `announce(msg)` and `announceStatus(msg)` implementing the two-container alternation and visibility flip verbatim. Call it from the command handlers that already exist in inputSelectionController.ts: `applyInsertCursorCommand` (:498), `applySelectExactOccurrencesCommand` (:532), `applySelectAllCommand` (:470), plus fold/unfold in foldOperations.ts and the find widget.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED. Case-insensitive repo-wide grep for aria-live | role=\"alert\" | role=\"status\" | ariaLive | liveRegion | announce across packages/**/*.{ts,tsx,css} returns zero hits. The complete aria surface of packages/editor/src is: virtualizedTextViewHelpers.ts:168 (`aria-label` on the input), virtualizedTextViewRows.ts:174,177 (`aria-hidden` on the selection and highlight layers), and mergeConflictPlugin.ts:481 (`aria-hidden` on an svg icon). references/vscode/src/vs/base/browser/ui/aria/{aria.ts,aria.css} confirms the reference implementation exists as a standalone base module.",
  "verifierNotes": "Confirmed missing and cheap (a ~40-line module: two alert containers + two status containers, alternated so identical consecutive strings still fire, plus the visibility:hidden\u2192visible flip that some AT combinations require before announcing role=alert). But its value is entirely derivative \u2014 announcing 'replaced 3 occurrences' is pointless while the document itself is unreadable (item 5). Sequence it after 5, or implement it alongside 11 whose toggle is the canonical first consumer."
 },
 {
  "domain": "input-a11y",
  "title": "Unicode ambiguous/invisible character highlighting",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/editor/common/services/unicodeTextModelHighlighter.ts:14-98 (computeUnicodeHighlights: candidate-set regex, surrogate-pair range extension, MAX_RESULT_LENGTH=1000), :183-241 (shouldHighlightNonBasicASCII word-context heuristic, isAllowedInvisibleCharacter), :99-126 (computeUnicodeHighlightReason -> confusableWith + notAmbiguousInLocales); src/vs/base/common/strings.ts:1243-1330 (AmbiguousCharacters: generated codepoint->ascii table per locale, intersected across allowed locales and merged with _common); src/vs/editor/contrib/unicodeHighlighter/browser/unicodeHighlighter.ts:39-215 (DocumentUnicodeHighlighter in the worker vs ViewportUnicodeHighlighter inline, banner + hover explaining the reason)",
  "mechanism": "The highlighter builds a candidate code point set (all invisible code points except space/tab/newline, plus all confusables for the enabled locales), compiles it to a single character-class regex, and scans line by line, extending each match to a full code point when it lands on a surrogate. The interesting part is the false-positive suppression: before flagging a character it looks at the whole word containing it (`getWordAtText` with DEFAULT_WORD_REGEXP); if that word contains no basic-ASCII characters *and* contains an obviously non-Latin non-confusable character, the character is left alone \u2014 that is what stops every Cyrillic or Greek identifier in a genuinely non-English file from lighting up, while still catching a lone Cyrillic '\u0430' inside `p\u0430ssword`. Results are capped at 1000 ranges with a `hasMore` flag, and the whole-document variant runs in the editor worker while the viewport-only variant runs inline.",
  "whyItMatters": "This is a supply-chain security feature: homoglyph and invisible-character attacks hide malicious identifiers in code that reads correctly. It also catches accidental non-breaking spaces and zero-width joiners pasted from docs, which produce baffling syntax errors. An editor without it silently renders the attack as legitimate code.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewHiddenCharacters.ts:112-116 `hiddenCharacterKind` recognises only `' '` and `'\\t'` \u2014 the existing 'hidden characters' layer is a whitespace renderer, not a unicode analyser. grep for `2028|2029|confusable|ambiguous|invisible` across packages/editor/src returns nothing.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "The rendering hook already exists: `hiddenCharactersLayerElement` per row with absolutely-positioned markers computed from `offsetToX` (virtualizedTextViewHiddenCharacters.ts:89-110). Add `packages/editor/src/unicodeHighlight.ts` with a pure `computeUnicodeHighlights(text, options)` mirroring Monaco's scanner (port the `_common` + `_default` confusable table \u2014 it is a static JSON string, ~40KB, worth lazy-importing), then a plugin in the style of occurrenceHighlightPlugin.ts that computes ranges for the visible row range on each `notifyViewContributions('selection'|'edit')` and emits markers with a new `HiddenCharacterKind` ('ambiguous' | 'invisible'). Port the word-context heuristic \u2014 without it the feature is unusable on non-English codebases. For whole-document mode, packages/tree-sitter already proves the worker pattern.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED. /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/virtualizedTextViewHiddenCharacters.ts:112-116 `hiddenCharacterKind` returns 'space' for ' ', 'tab' for '\\t', null otherwise \u2014 the whole 'hidden characters' feature is a whitespace renderer gated on `view.hiddenCharacters === 'show' | 'show-on-selection'` (:118-124). Repo-wide grep for confusable|ambiguous|invisible|zeroWidth|u200b|unicodeHighlight across packages/**/*.ts finds nothing relevant (only an unrelated `invisible`-piece counter in packages/editor/bench/pieceTable-anchors.ts and a zero-width test name in packages/markdown/test/replacements.test.ts:161). Reference confirmed present: references/vscode/src/vs/editor/contrib/unicodeHighlighter/browser/unicodeHighlighter.ts.",
  "verifierNotes": "Genuine gap with a security payload (trojan-source / homoglyph attacks in pasted code). Two things make it cheaper for us than it looks: (1) the marker/decoration plumbing already exists \u2014 virtualizedTextViewHiddenCharacters.ts computes per-offset markers with `offsetToX` geometry and a marker-key diff (:133-140), and packages/editor/src/editor/rangeDecorations.ts gives range decorations; the new work is the code-point classifier, not the paint path. (2) The false-positive suppression is the part worth copying verbatim: check the containing word's script before flagging, so Cyrillic prose is not flagged character-by-character. Merge item 8 into this as one 'suspicious characters' feature."
 },
 {
  "domain": "input-a11y",
  "title": "U+2028/U+2029 line separators are a rendering/geometry landmine",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/base/common/strings.ts:696-702 (UNUSUAL_LINE_TERMINATORS regex, containsUnusualLineTerminators); src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeTextBufferBuilder.ts:148-165 (the flag is computed once while chunking the file, never re-scanned); src/vs/editor/common/model/textModel.ts:746-754 (mightContainUnusualLineTerminators / removeUnusualLineTerminators pushes one edit per match and preserves selections); src/vs/editor/contrib/unusualLineTerminators/browser/unusualLineTerminators.ts:66-118 (auto | prompt | off, per-model ignore state, skips on undo and on read-only)",
  "mechanism": "The text buffer records `containsUnusualLineTerminators` while the file is being chunked at load \u2014 a single boolean, so the expensive check is paid once and the common case (`false`) costs nothing on every subsequent content change. The contribution re-checks on model change and on content change (but not while undoing, to avoid fighting the user), and either silently strips the characters (`auto`) or prompts with a per-model 'ignore' flag persisted through the codeEditorService model-property bag. Stripping is done as a normal edit with `pushEditOperations(selections, ...)` so it is undoable and selections survive.",
  "whyItMatters": "This is the highest-severity-per-line-of-code item here. The piece table only treats \\n as a line break, but CSS treats LS (U+2028) and PS (U+2029) as forced line breaks inside `white-space: pre` \u2014 which is exactly how rows are rendered. A single LS character in a file therefore makes one model row render as two visual lines: the row's measured height, `offsetToX`, hit-testing, caret placement, selection rectangles and the fixed-row virtualizer's height assumptions all silently desynchronise for the rest of that row, with no error anywhere. LS/PS appear in real files from JSON string literals and from text pasted out of Word/PDF.",
  "ourStatus": "missing",
  "ourEvidence": "grep for `2028|2029` across packages/editor/src returns nothing; the only line-break scanning is on `'\\n'` (pieceTable tests at pieceTable.test.ts:31, and `pasteRevealBlock` in inputSelectionController.ts:1580-1583 checks only \\n and \\r). packages/editor/src/style.css:35,85,184,223,234,279,354 set `white-space: pre` on the row/chunk elements.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Two cheap layers. (1) Sanitise at the entry points: in `handlePaste` (:1163) and `handleDrop` (:1187), replace `[\u2028\u2029]` with `\\n` (or strip) before `applyPasteText` \u2014 matching the existing `\\r\\n` expectations. (2) Detect at load: have the piece table builder set a `containsUnusualLineTerminators` flag while chunking initial text (pieceTable/snapshot.ts), and expose an editor option `unusualLineTerminators: 'auto' | 'prompt' | 'off'` whose `auto` path issues one `session.applyEdits` removing all matches, reusing the find machinery in editor/findFeature.ts to locate them. Add a regression test asserting row geometry for a line containing U+2028 \u2014 this is exactly the kind of bug the existing virtualizedTextViewLayout.test.ts suite is set up to catch.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED, and I traced the actual failure path. Line indexing is `\\n`-only at the buffer level: /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/buffers.ts:133-137 and :168-171 both scan with `text.indexOf('\\n', ...)`, and pieceTableTypes.ts:46 documents the index as \"Lazily built '\\n' offset index per buffer\". Repo-wide grep for 2028|2029|lineTerminator|LINE_SEPARATOR across packages/editor/src returns nothing. Rows render with `white-space: pre` (packages/editor/src/style.css:35,85,184,223,234,279,354). Since U+2028 is a mandatory break in UAX#14, the browser breaks the row div into two visual lines while the virtualizer still budgets one row height \u2014 every row below is offset, and caret hit-testing through `offsetToX` desyncs for that row. The only other line-ish scan is `pasteRevealBlock` (inputSelectionController.ts:1580-1583), which tests only \\n and \\r. Reference confirmed: `containsUnusualLineTerminators` lives in references/vscode/src/vs/editor/common/model/pieceTreeTextBuffer/pieceTreeTextBufferBuilder.ts and pieceTreeTextBuffer.ts.",
  "verifierNotes": "Real correctness landmine, low frequency, near-zero cost to defend \u2014 so keep, but implement it as part of item 7 rather than as its own feature. The mechanism worth copying is the cost model, not the UI: the flag is computed once during chunking at load, so the common `false` case makes every later content-change check free. Given our architecture the cheapest defence is even simpler than Monaco's banner: normalise U+2028/2029 to \\n at the paste and setText boundaries and surface the rest through the item-7 highlighter."
 },
 {
  "domain": "input-a11y",
  "title": "Diff-based input deduction as the robust fallback, instead of a keydown timing race",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/editor/browser/controller/editContext/textArea/textAreaEditContextState.ts:117-179 (TextAreaState.deduceInput \u2014 commonPrefix/commonSuffix clamped by the selection), :181-227 (deduceAndroidCompositionInput), :50-63 (readFromTextArea carrying newlineCountBeforeSelection forward); src/vs/editor/browser/controller/editContext/textArea/textAreaEditContextInput.ts:322-364 (onInput: ignore lone high surrogates and 0x7f but keep them for next time; route insertFromPaste to onPaste), :221-262 (macOS long-press + Android compositionstart special cases), :445-462 (onBlur during composition fires an artificial compositionend)",
  "mechanism": "Monaco never guesses what a keystroke meant. It writes a known string+selection into the textarea, and on every `input` event diffs the new textarea state against the remembered one: `prefixLength = min(commonPrefixLength, prevSelStart, curSelStart)`, `suffixLength = min(commonSuffixLength, prevLen-prevSelEnd, curLen-curSelEnd)`, yielding `{text, replacePrevCharCnt, replaceNextCharCnt, positionDelta}`. Clamping the common prefix/suffix by the selection bounds is the subtle part \u2014 it prevents a repeated character adjacent to the caret from being absorbed into the prefix. The same primitive expresses ordinary typing, composition replacement, dead keys, autocorrect-style multi-character replacements and Android's word-level suggestion edits (which is why `deduceAndroidCompositionInput` exists as a variant that also computes `replaceNextCharCnt`). Guard rails: a lone high surrogate or 0x7f is dropped but deliberately left in the textarea so the next event completes it; a blur during composition synthesises a compositionend so the editor doesn't get stuck composing.",
  "whyItMatters": "Our current design instead races: on keydown it optimistically waits for a native `input` event and, if none arrives, replays the guessed character (`native-input-wait-started` -> `native-input-missing`). That produces double-insertion or dropped characters under exactly the conditions that are hardest to reproduce \u2014 autorepeat, dead keys, autocorrect, remote-desktop/synthetic key events, and any IME that doesn't fire compositionstart. A diff never double-counts because it compares against ground truth.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/inputState.ts:156-164 `canWaitForNativeTextInput` (special-cases the space key!), :275-307 the fallback generation/timeout machinery; inputSelectionController.ts:1223-1241 `handleKeyDown` guesses from `event.key`, :1280-1318 `resolvePendingKeyboardTextFallback`/`applyKeyboardTextFallback` replay the guess. editor/input.ts:1-9 `keyboardFallbackText` is a 9-line heuristic (`event.key.length !== 1` -> null). The hidden textarea's value is always forced to `''` (:1307, :1330), so there is no previous state to diff against.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Port `TextAreaState` verbatim into `packages/editor/src/editor/textAreaState.ts` \u2014 it is a pure, dependency-free class and lands directly in the existing unit-test style (see test/inputState.test.ts). Then stop clearing `inputElement.value`: keep a `previousTextAreaState`, listen to `input` on the hidden textarea (the capture listeners at inputSelectionController.ts:829-861 are already installed), and turn the deduced `{text, replacePrevCharCnt, replaceNextCharCnt}` into a `session.applyEdits` around the caret. The keydown-guess fallback and most of the `fallback-pending`/`native-input-*` half of the inputState machine can then be deleted, which is a net simplification. Bring across the lone-surrogate/0x7f guard and the blur-during-composition synthetic end.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED as partial, and the gap is larger than the claim states. The primary path is `beforeinput` (inputSelectionController.ts:1129-1160), which is sound on desktop \u2014 that is why this is partial, not missing. But `beforeInputText` (:1601-1606) returns null for every inputType except insertLineBreak / insertText / insertFromComposition, and handleBeforeInput returns immediately on null (:1136). So `insertReplacementText` (autocorrect/spellcheck accept), `insertFromPasteAsQuotation`, `insertTranspose`, and soft-keyboard `deleteContentBackward`/`deleteWordBackward` are all silently dropped. The fallback is the guessing machinery the claim describes: editor/input.ts:1-9 `keyboardFallbackText` (9 lines; `event.key.length !== 1` \u2192 null), inputState.ts:156-164 `canWaitForNativeTextInput` \u2014 which really does special-case `options.text === ' '` \u2014 and the generation/timeout replay at inputState.ts:275-307 + inputSelectionController.ts:1223-1241. The textarea value is forced to '' at virtualizedTextView.ts:526, :1307 and :1330, so there is no prior state to diff against.",
  "verifierNotes": "Keep, and raise its value above the survey's framing. The win is not 'replace beforeinput' \u2014 beforeinput is fine and modern. The win is that a non-empty textarea with a known value+selection turns every one of the dropped inputTypes above into a uniform `{text, replacePrevCharCnt, replaceNextCharCnt}` triple derived by common-prefix/suffix diffing, with no per-inputType branch and no `event.key` guessing. That single change fixes autocorrect, dead keys, voice dictation and Android soft keyboards at once, and it is the same prerequisite as item 5 (both need the textarea to hold real content). Sequence 5 and 9 together \u2014 they are one refactor of the hidden-input contract, not two."
 },
 {
  "domain": "input-a11y",
  "title": "Backspace is code-point aware but not grapheme-cluster aware",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/base/common/strings.ts:1168-1183 (getLeftDeleteOffset), :1185-1219 (getOffsetBeforeLastEmojiComponent: skip skin-tone modifiers/variation selector/enclosing keycap, require an emoji base, then absorb one preceding ZWJ), :1221-1223 (isEmojiModifier 0x1F3FB-0x1F3FF); src/vs/editor/common/cursor/cursorDeleteOperations.ts:224-232 (the only caller \u2014 left delete position)",
  "mechanism": "Monaco walks backwards with a CodePointIterator: it first skips any trailing emoji modifiers (Fitzpatrick skin tones, U+FE0F variation selector, U+20E3 enclosing keycap), then requires the next code point back to be an emoji base, then optionally absorbs one preceding ZWJ so that a ZWJ sequence collapses one component per backspace rather than leaving orphaned joiners. If the sequence doesn't look like an emoji it falls back to a single code point. Note the deliberate scoping: it does not implement full UAX#29 grapheme clusters, only the emoji case, because that is where the visible breakage is.",
  "whyItMatters": "Backspacing over \ud83d\udc68\u200d\ud83d\udc69\u200d\ud83d\udc67 or \ud83d\udc4d\ud83c\udffd today leaves behind an orphaned zero-width joiner or a bare skin-tone modifier that renders as a stray box and silently corrupts the file. Users hit this whenever emoji appear in comments, commit-message editors, or markdown.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/documentSelectionEdits.ts:326-338 `previousCodePointOffset` handles surrogate pairs only (explicit 0xD800/0xDC00 range checks) and is the sole input to `backspaceRangeForSelection` (:340-350). No ZWJ (0x200D), variation-selector or skin-tone handling anywhere in packages/.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Extend `previousCodePointOffset` in documentSelectionEdits.ts into `previousGraphemeOffset(snapshot, offset)`: read a short window backwards (`readPieceTableTextRange(snapshot, Math.max(0, offset-32), offset)`), run Monaco's `getLeftDeleteOffset` logic over that string, and map the result back to an absolute offset. Purely local change with no callers to update. Mirror it for forward delete if that path also steps by one code unit. Add cases to an existing edits test file covering \ud83d\udc4d\ud83c\udffd, \ud83d\udc68\u200d\ud83d\udc69\u200d\ud83d\udc67 and a combining-accent sequence.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Status confirmed partial, but the claim's evidence is WRONG on one point. Correct: deletion is code-point only \u2014 /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSelectionEdits.ts:326-338 `previousCodePointOffset` does explicit 0xD800-0xDBFF / 0xDC00-0xDFFF surrogate checks and feeds `backspaceRangeForSelection` (:340-350); caret movement is likewise code-point only via textRanges.ts:43-56 \u2192 navigationTargets.ts:207. FALSE: 'No ZWJ, variation-selector or skin-tone handling anywhere in packages/'. packages/editor/src/virtualization/virtualizedTextViewGeometry.ts:835-838 instantiates `new Intl.Segmenter(undefined, { granularity: 'grapheme' })`, :830-832 `textPartSegments` uses it (with a hand-written fallback at :840-863 that consumes combining marks and variation selectors), :944-950 `isCombiningMark`, :951-956 `isVariationSelector` (0xFE00-0xFE0F and 0xE0100-0xE01EF), :958-970 `isWideCodePoint` covering the emoji plane. So full grapheme machinery already exists \u2014 it is just confined to measurement (`textPartSegments` has exactly one caller, geometry.ts:484) and never reaches the edit or navigation layer.",
  "verifierNotes": "Keep; effort is now clearly low because the hard part is already written and shipped. The fix is to expose a grapheme-boundary helper from the geometry module (or lift the segmenter into a shared util) and route `previousCodePointOffset` in documentSelectionEdits.ts:326 and textRanges.ts:43 through it. Monaco's hand-rolled emoji walk (skip Fitzpatrick/FE0F/20E3, require an emoji base, absorb one ZWJ) is worth reading for the *policy* \u2014 collapse one ZWJ component per backspace rather than the whole cluster \u2014 but we should implement it on Intl.Segmenter, which we already depend on, rather than porting the table walk. Note the current asymmetry is a live bug: geometry says a family emoji is one cell wide, backspace deletes half of it."
 },
 {
  "domain": "input-a11y",
  "title": "Tab-focus mode: an accessibility escape hatch from the Tab key",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/editor/browser/config/tabFocus.ts:9-30 (global TabFocusImpl with an onDidChangeTabFocus event); src/vs/editor/contrib/toggleTabFocusMode/browser/toggleTabFocusMode.ts:13-44 (Ctrl+M / Ctrl+Shift+M on mac, and an aria alert stating the new behaviour); src/vs/editor/browser/config/editorConfiguration.ts:95,131 and src/vs/editor/browser/widget/codeEditor/codeEditorWidget.ts:2249,2262 (folds into the computed option and into an `editorTabMovesFocus` context key that keybinding `when` clauses consult)",
  "mechanism": "A single global toggle, surfaced two ways: it becomes part of the computed editor options (so the editor recomputes on change) and it sets a context key, so the Tab/Shift+Tab keybindings simply stop matching and the browser's native focus traversal takes over. The toggle announces its new state through the aria alert channel, because the only users who need it cannot see the status bar indicator.",
  "whyItMatters": "A keyboard-only or screen-reader user who tabs into an editor is otherwise trapped: Tab inserts indentation and there is no way to reach the next control. WCAG 2.1.2 (No Keyboard Trap) makes this a conformance issue, not a nicety. It is also useful to sighted users filling in forms around an embedded editor.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/keymap.ts:429-430 binds Tab -> `indentSelection` and Shift+Tab -> `outdentSelection` unconditionally; there is no context/when mechanism and no toggle command (grep for `tabFocus` across packages/ returns nothing). The scroll element is `tabIndex = 0` (virtualizedTextViewHelpers.ts:155), so focus can enter but not leave via the keyboard.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Add a `tabMovesFocus` boolean to `EditorSessionOptions`/runtime state plus a command `editor.action.toggleTabFocusMode` bound to Ctrl+M (Ctrl+Shift+M on mac) in keymap.ts. In `applyIndentCommand` (inputSelectionController.ts:416) return `false` when the flag is set so the keydown is not consumed and the browser moves focus; keymap.ts's dispatcher already distinguishes handled from unhandled commands. Announce the new state through the `announce()` helper from the aria-live finding. Also ensure Escape-then-Tab still works while a snippet session is active (:423 already gives snippets first claim on Tab).",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED missing \u2014 repo-wide grep for tabFocus|tab-focus returns zero. keymap.ts:429-430 binds Tab\u2192indentSelection and Shift+Tab\u2192outdentSelection inside `textEditingBindings`, unconditionally. The scroll element is tabIndex=0 (virtualizedTextViewHelpers.ts:155). BUT the enabling machinery is already in place and the survey missed it: keymap.ts:117-121 `shouldPreventDefault(binding, handled)` returns `handled` when the binding does not force it, and keymap.ts:101 only calls `event.preventDefault()` when that returns true \u2014 so a command handler that returns `false` leaves the native Tab traversal intact. The conditional-Tab pattern already exists too: inputSelectionController.ts:416-424 `applyIndentCommand` early-returns when an active snippet session owns the key (`moveSnippetStop`). There is also a runtime override path, commandRouter.ts:67-80 `registerCommandHandler`, and pack-level filtering at keymap.ts:261-275 `filterEditorKeymapLayersByCommandPacks`.",
  "verifierNotes": "Keep \u2014 this is the cheapest item in the domain by a wide margin. Implementation is roughly: one editor option, plus `if (this.options.tabFocusMode) return false` at the top of applyIndentCommand (inputSelectionController.ts:416), which the existing shouldPreventDefault contract turns straight into native focus traversal. No context-key system needed; our handler-returns-boolean design already does what Monaco needs a `when` clause for. Reference for the shape: references/vscode/src/vs/editor/browser/config/tabFocus.ts and contrib/toggleTabFocusMode/browser/toggleTabFocusMode.ts. Pair the toggle with item 6 so the state change is actually announced."
 },
 {
  "domain": "input-a11y",
  "title": "Copy with syntax highlighting, and the paste-provider (paste-as) pipeline",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode: src/vs/editor/common/viewModel/viewModelImpl.ts:1051-1107 (getRichTextToCopy: bail on plaintext/multi-selection, sanitise font-family for `:;\\/<>` and quote it, emit an inline-styled div), src/vs/editor/browser/controller/editContext/clipboardUtils.ts:55-62 (only when sourceText.length < 65536); src/vs/editor/contrib/dropOrPasteInto/browser/copyPasteController.ts:176-230 (on copy: mint a uuid handle, stash it in the clipboard metadata, kick off every provider's prepareDocumentPaste as a cancelable promise), :561-594 (on paste: mergeInDataFromCopy matches the handle back to the in-flight operations and merges the resulting VSDataTransfer, highest-priority provider last), :232-320 (provider selection, pasteAs pick, post-paste widget)",
  "mechanism": "Two independent capabilities that share the same metadata channel. (a) Copy-with-syntax-highlighting renders the selected range's tokens to an inline-styled HTML div written as `text/html`, guarded by a 64KB size cap and skipped for plaintext or multi-selection; pasting into Word/Slack/Confluence then preserves colours. The font-family sanitisation (rejecting names containing `:;\\/<>` and falling back to the default) is a CSS-injection guard worth copying. (b) Paste-as: on copy, providers registered for the language get to precompute a richer payload asynchronously; the clipboard carries only a uuid handle, and on paste the handle is matched against the still-running promises so the expensive work overlaps the user's think time. If the clipboard came from elsewhere the handle simply doesn't match and the providers fall back to whatever MIME types the DataTransfer actually has. Higher-priority providers overwrite lower ones by reversing the merge order.",
  "whyItMatters": "(a) is a small, self-contained win users notice immediately when pasting code into a doc or chat. (b) is the mechanism behind 'paste an image and get a markdown link', 'paste a URL over a selection and get a link', 'paste a TS symbol and get its import added' \u2014 the extension point that makes an editor feel language-aware on paste. There is already a markdown package and an LSP plugin here that would both want it.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/inputSelectionController.ts:1214-1221 `handleCopy` writes only `text/plain`; :1163-1185 `handlePaste` reads only `text/plain` and ignores `clipboardData.types`, `files`, and `text/html` entirely. The plugin system (packages/editor/src/plugins.ts, viewContributions.ts) has no paste/copy hook \u2014 grep for `paste` in plugins.ts returns nothing.",
  "value": "medium",
  "effort": "XL",
  "implementationSketch": "Split it. Ship (a) first: the token data needed is already materialised for rendering (editor/tokenProjection.ts + shiki theme colours in src/theme.ts), so `richTextForRange(snapshot, start, end)` producing an inline-styled `<div>` is maybe 60 lines; write it as `text/html` in `handleCopy` behind a `copyWithSyntaxHighlighting` option and the same 64KB cap. For (b), first widen `handlePaste`/`handleDrop` to expose the whole DataTransfer (types + files) rather than eagerly extracting `text/plain`, and add `onWillPaste`/`onWillCopy` hooks to the plugin lifecycle in plugins.ts \u2014 that alone lets packages/markdown and packages/lsp-plugin implement paste behaviours. The uuid-handle prefetch optimisation is only worth it once providers are actually async.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "CONFIRMED. inputSelectionController.ts:1214-1221 `handleCopy` writes only text/plain; :1163-1185 `handlePaste` reads only `getData('text/plain')` and never inspects `clipboardData.types` or `.files`. The plugin surface genuinely has no clipboard hook: packages/editor/src/plugins.ts:470-477 `EditorPlugin` is install/activate/update/deactivate/dispose only, and the registration API (:453 and the host events at :495-513) covers view, decoration, command, capability, edit, editor-feature, gutter, block and injected-text-row contributions \u2014 grep for paste|copy in plugins.ts returns zero. Nor is it done downstream: grep for paste|copy across packages/markdown/src and packages/lsp-plugin/src returns nothing.",
  "verifierNotes": "Keep, but split the value sharply. (a) Copy-as-HTML is a nice-to-have; moderate effort since we would render tokens from the syntax layer, and worth doing only behind the same 64KB cap and plaintext/multi-selection skips Monaco uses. (b) The paste-provider pipeline is the valuable half for this repo specifically: with packages/markdown in-tree, 'paste a URL over a selection \u2192 markdown link' and 'paste an image \u2192 placeholder + host upload' are exactly the behaviours users expect, and neither is expressible today because no extension point sees the ClipboardEvent. Recommend scoping this to (b) alone: add a `registerPasteHandler`-style contribution alongside the existing `registerEditContribution` (plugins.ts:1103-1110) so handlers can claim a paste by MIME, with our current text/plain path as the last-resort default. Depends on item 2's metadata channel for the internal-vs-external paste distinction."
 },
 {
  "domain": "decorations-widgets",
  "title": "Edit-tracked decoration identity: interval tree + 4-way stickiness + collapseOnReplaceEdit",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/intervalTree.ts:305-331 (IntervalTree.acceptReplace), :416-470 (nodeAcceptEdit), :121-138 (stickiness packed into node.metadata bitfield), :141-235 (IntervalNode with delta/maxEnd/cachedAbsoluteStart), :345-380 (normalizeDelta); references/vscode/src/vs/editor/common/model.ts:675-680 (TrackedRangeStickiness enum), :417-444 (IModelDecorationsChangeAccessor), :1081-1100 (changeDecorations/deltaDecorations/removeAllDecorationsWithOwnerId); references/vscode/src/vs/editor/common/editorCommon.ts:544-578 (IEditorDecorationsCollection); references/vscode/src/vs/editor/common/model/textModel.ts:2197-2236 (DecorationsTrees)",
  "mechanism": "A decoration is a persistent object with an id, an ownerId, and options. Ranges live in a red-black interval tree keyed by offset, where each node stores `start`/`end` relative to its parent via a `delta` field so a text edit only rewrites O(log n) deltas rather than every node (`noOverlapReplace`), plus `maxEnd` for interval search pruning. `acceptReplace(offset,length,textLength,forceMoveMarkers)` removes only the nodes intersecting the edit, shifts everything else by delta arithmetic, then re-inserts the intersecting nodes after running `nodeAcceptEdit` on each. `nodeAcceptEdit` implements four stickiness modes (AlwaysGrows / NeverGrows / GrowsOnlyWhenTypingBefore / GrowsOnlyWhenTypingAfter) by deriving `startStickToPreviousCharacter` and `endStickToPreviousCharacter` and applying `adjustMarkerBeforeColumn` three times (once at edit start, once at the common prefix of delete/insert, once at edit end) with MarkerMoveSemantics of ForceMove/ForceStay/MarkerDefined. Extra semantics: `collapseOnReplaceEdit` makes a decoration collapse to a point when an edit fully encompasses it (intervalTree.ts:437-444), and `forceMoveMarkers` pushes markers past an insertion at their exact offset. Stickiness/isForValidation/isInGlyphMargin/affectsFont/collapseOnReplaceEdit are all bit-packed into a single `metadata` number to keep nodes cache-friendly. Callers never touch the tree: they use `changeDecorations(accessor => accessor.deltaDecorations(old, new))` or an `IEditorDecorationsCollection`, which diffs and reuses ids.",
  "whyItMatters": "Today every consumer in our repo re-derives its ranges from scratch after each edit, in its own code, with its own bugs. `packages/lsp-plugin/src/diagnosticProjection.ts` hand-projects diagnostic ranges through `DocumentSessionChange.edits`; find, occurrences, document links, merge conflicts and minimap decorations each keep raw offsets and rely on their owner re-emitting a whole array. A shared tracked-range service would delete all of that duplicated projection logic, make decorations survive edits during the async gap before an LSP response arrives (so squiggles stop flickering/mis-aligning while typing), and give every future feature (bookmarks, inline chat anchors, code lens) correct behaviour for free.",
  "ourStatus": "partial",
  "ourEvidence": "`packages/editor/src/editor/types.ts:37-42` \u2014 `EditorRangeDecoration` is `{start, end, className?, style?}`: plain numbers, no id, no stickiness, no owner. `packages/editor/src/editor/Editor.ts:496` sets them wholesale from `options.rangeDecorations`; `Editor.ts:2103-2107` (`composedRangeDecorations`) just concatenates every projection's array each frame. We DO have piece-identity anchors with left/right bias and deletion liveness (`packages/editor/src/pieceTable/anchors.ts:1-261`, `anchorBefore`/`anchorAfter`/`resolveAnchor`), but a repo-wide grep shows only two consumers: `packages/editor/src/inlineMap.ts:1,191,230-231` and `packages/editor/src/foldMap.ts:1`. Nothing else tracks ranges across edits.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Don't port the red-black interval tree \u2014 we already have a stronger primitive. Build `packages/editor/src/editor/trackedRanges.ts` on top of `pieceTable/anchors`: a `TrackedRange = {id, owner, start: Anchor, end: Anchor, options}` store keyed by id, with `deltaRanges(oldIds, newSpecs)` mirroring Monaco's accessor. Map the four stickiness modes onto anchor bias pairs (AlwaysGrows = anchorBefore(start)/anchorAfter(end); NeverGrows = anchorAfter(start)/anchorBefore(end) \u2014 which is exactly what `inlineMap.ts:191` already hardcodes; the two asymmetric modes are the mixed pairs), and add `collapseOnReplaceEdit` + `forceMoveMarkers` on top since anchors alone don't express them. `resolveAnchor` already returns `liveness: 'deleted'`, which is the natural signal for dropping a decoration. Then register the store as a `rangeDecorations` display projection (`displayProjectionRegistry.ts`) so the existing render path is unchanged, and port `diagnosticProjection.ts` and `occurrenceHighlights.ts` to it as the first two consumers.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "THREE edit-tracking mechanisms exist, not zero. (1) Piece-table anchors with left/right bias and `liveness:'deleted'` \u2014 /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/anchors.ts:214-246 (`resolveAnchor`, `resolveAnchorLinear`). The claim's 'only two consumers' is flatly false; consumers are inlineMap.ts:191, foldMap.ts:116-117, editor/snippetSession.ts:36-37,87-88, editor/autoCloseStore.ts:31,47, selections.ts:126,171, documentSession.ts:761,1080, and packages/tree-sitter/src/structuralSelection.ts:138. (2) /Users/shaul/Desktop/D/Editor/packages/lsp-plugin/src/diagnosticProjection.ts:132-162 \u2014 `projectOffsetRangeThroughEdit` projects start with 'after' bias and end with 'before' bias (= Monaco NeverGrowsWhenTypingAtEdges) and `projectOffsetThroughEdit` returns null when the offset lands strictly inside a replaced span, which is exactly collapseOnReplaceEdit. (3) /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/Editor.ts:2715-2737 `projectRowDecorationMapThroughLineEdit` shifts row decorations by the edit's line delta. `EditorRangeDecoration` at editor/types.ts:37-42 is indeed id-less.",
  "verifierNotes": "Real gap is narrower than claimed: no SHARED store, no per-decoration id/owner, no interval tree with `delta`/`maxEnd` (every projection is O(n) per edit and re-supplied wholesale by its owner), and stickiness is hard-coded per feature rather than a 4-way selector. The two existing projections prove the semantics are already understood here, so this is consolidation, not invention. Note our `displayProjectionRegistry` already carries owner/layer/priority \u2014 one level too coarse, as the claim correctly says."
 },
 {
  "domain": "decorations-widgets",
  "title": "One decoration object, many surfaces \u2014 text, glyph margin, line margin, minimap, overview ruler, injected text",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model.ts:149-310 (IModelDecorationOptions: className, inlineClassName, beforeContentClassName/afterContentClassName, before/after InjectedTextOptions, glyphMarginClassName + glyphMargin lane, linesDecorationsClassName, lineNumberClassName, marginClassName, minimap, overviewRuler, zIndex, isWholeLine, showIfCollapsed, lineHeight, fontFamily/fontSize/fontWeight, blockClassName/blockPadding/blockIsAfterEnd, textDirection); references/vscode/src/vs/editor/common/model/textModel.ts:2197-2273 (DecorationsTrees splits into three trees + filters by affectsFont/lineHeight/showIfCollapsed); :2586-2641 (DidChangeDecorationsEmitter records _affectsMinimap/_affectsOverviewRuler/_affectsGlyphMargin/_affectedInjectedTextLines and defers)",
  "mechanism": "There is exactly one decoration API. The options object fans out to every rendering surface, and the model pre-partitions storage by the expensive query: `_decorationsTree0` for normal decorations, `_decorationsTree1` for overview-ruler decorations, `_injectedTextDecorationsTree` for anything with `before`/`after` \u2014 so the view-model's per-line injected-text query never walks the diagnostics. Cheap boolean flags for rarer surfaces are bit-packed into the node metadata (`IsForValidationMask`, `IsMarginMask`, `AffectsFontMask` in intervalTree.ts:280-320) so `getAllMarginDecorations` is a filtered tree walk rather than a full scan. The change event carries `affectsMinimap` / `affectsOverviewRuler` / `affectsGlyphMargin` / `affectedInjectedTextLines`, letting each view part skip re-render when a decoration change cannot possibly touch it, and `beginDeferredEmit`/`endDeferredEmit` coalesce a whole batch of decoration mutations into one event.",
  "whyItMatters": "A plugin author who wants 'error on line 12' currently has to talk to three unrelated registries in our repo and keep them in sync by hand \u2014 and there is no way at all to also mark it in the scrollbar. Unifying means a single `setDecorations` call lights up squiggle + gutter icon + minimap tick + (future) overview ruler, and the three surfaces can never drift out of sync during edits.",
  "ourStatus": "partial",
  "ourEvidence": "We have the fan-out concept but as N disjoint registries, each with its own type and its own source/owner keying: `packages/editor/src/editor/displayProjectionRegistry.ts:8-25` enumerates seven independent projection kinds (`rangeDecorations`, `rowDecorations`, `blockRows`, `blockLanes`, `injectedRows`, `gutters`, `folds`) with separate payload types; the minimap keeps a *fourth* registry entirely (`packages/minimap/src/plugin.ts:297-306` `decorationsBySource`, `EditorMinimapDecoration` from `packages/minimap/src/types.ts:60`). `VirtualizedTextRowDecoration` (`packages/editor/src/virtualization/virtualizedTextViewTypes.ts:100-103`) is just `{className?, gutterClassName?}` \u2014 no zIndex, no minimap, no lane. Positively, `displayProjectionRegistry.ts:57-64` already carries `owner`, `layer`, `priority`, and `invalidationRange` per projection, which is the ownerId/zIndex idea one level too coarse (per-array instead of per-decoration). No overview ruler exists anywhere (grep for `overviewRuler` across `packages/` returns nothing).",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Widen `EditorRangeDecoration` (`packages/editor/src/editor/types.ts:37`) into an `EditorDecoration` carrying optional `glyphMargin`, `lineDecoration`, `minimap`, `overviewRuler`, `zIndex`, `isWholeLine`, `showIfCollapsed` alongside the existing className/style, and make the tracked-range store from finding #1 the single source. Then have the existing consumers *derive* from it rather than be fed separately: `Editor.applyRangeDecorations` (Editor.ts:2065-2083) filters to the text-styling subset, the gutter contribution path filters on `glyphMargin`, and `MinimapDecorationRegistry` becomes a subscriber instead of a parallel store. Keep three separate indexes internally exactly as Monaco does so the hot per-row injected-text lookup does not scan diagnostics, and add `affects*` flags to whatever change signal we emit so `virtualizedTextView` can skip work.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed: 7 disjoint projection kinds at packages/editor/src/editor/displayProjectionRegistry.ts:8-25 plus a 4th registry in packages/minimap/src/plugin.ts:294-325 (`MinimapDecorationRegistry`). Confirmed no overview ruler (repo-wide grep for `overviewRuler` across packages/*/src and *.css returns nothing). TWO corrections: (a) minimap decorations DO carry zIndex and are sorted at paint \u2014 packages/editor/src/plugins.ts:99 (`EditorMinimapDecoration.zIndex`) and packages/minimap/src/renderer.ts:353-355 `.toSorted((l,r)=>(l.zIndex??0)-(r.zIndex??0))`. (b) Cross-projection ordering is already deterministic and priority-driven: displayProjectionRegistry.ts:226-236 `compareStoredDisplayProjections` sorts layer \u2192 priority \u2192 sequence \u2192 owner, and both `composedRangeDecorations` (Editor.ts:2367-2373) and `composedRowDecorations` (Editor.ts:1924-1931) consume that order. packages/lsp-plugin/src/diagnosticsPresenter.ts:120-185 already fans one diagnostic out to range highlight + minimap markers by hand.",
  "verifierNotes": "Keep, but scope to the storage-partitioning insight (separate trees so a per-line injected-text query never walks diagnostics), not a unification rewrite. Our per-projection owner/layer/priority is genuinely good design; the missing piece is per-decoration granularity."
 },
 {
  "domain": "decorations-widgets",
  "title": "Content widgets: an editor-managed layer for position-anchored floating UI with declarative fit preferences",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/editorBrowser.ts:126-176 (ContentWidgetPositionPreference EXACT/ABOVE/BELOW, IContentWidgetPosition with primary+secondary position), :177-232 (IContentWidget: allowEditorOverflow, useDisplayNone, suppressMouseDown, beforeRender\u2192IDimension, afterRender(position, coordinate)); references/vscode/src/vs/editor/browser/viewParts/contentWidgets/contentWidgets.ts:308-333 (_layoutBoxInViewport), :356-400 (_layoutBoxInPage with 15px lateral / 22px vertical clearance and fixedOverflowWidgets), :403-440 (_getAnchorsCoordinates), :442-456 (_reduceAnchorCoordinates), :458-520 (_prepareRenderWidget two-pass fit loop)",
  "mechanism": "A widget declares only `getPosition() \u2192 {position, secondaryPosition?, preference: [ABOVE, BELOW, EXACT]}`; the editor owns all geometry. Each render it computes the anchor's top/left/lineHeight from the *view* position (so folding and word-wrap are handled), measures the widget once (or takes `beforeRender()`'s answer, avoiding a forced reflow), then runs `_layoutBoxInViewport` or `_layoutBoxInPage` to get `{fitsAbove, aboveTop, fitsBelow, belowTop, left}` with the left clamped into the scroll viewport. The placement loop runs **twice** over the preference list: pass 1 accepts a preference only if it actually fits, pass 2 accepts the first preference regardless \u2014 so 'prefer below, fall back to above, else force below' is expressed as data, not branching. `_reduceAnchorCoordinates` handles a *secondary* anchor (e.g. suggest widget wants to touch both the cursor and the word start) by nudging left toward the secondary while guaranteeing the primary still touches. `allowEditorOverflow` + `fixedOverflowWidgets` moves the node into a separate overflow container using `position: fixed` with page coordinates, so a suggest list can escape a small editor. `afterRender(position, coordinate)` tells the widget which side it actually landed on, so it can flip its own arrow/shadow.",
  "whyItMatters": "Every floating UI we ship re-implements this badly and independently: hover tooltips, completion list, signature help, rename input, find widget. They compute their own DOMRect, clamp their own viewport, and each must remember to re-anchor on scroll/layout. Centralizing gets us consistent flip-when-it-doesn't-fit behaviour, correct behaviour when the anchor line is folded or scrolled out, and one place to fix shadow-DOM/overflow-clipping bugs \u2014 plus it makes third-party plugin widgets a two-line affair.",
  "ourStatus": "missing",
  "ourEvidence": "`packages/editor/src/plugins.ts:210-236` \u2014 `EditorViewContributionContext` gives a plugin `container`, `scrollElement`, and `getRangeClientRect(start,end): DOMRect | null` and nothing else; there is no managed widget layer. Each consumer rolls its own: `packages/lsp-plugin/src/hoverDefinitionController.ts:274`, `packages/lsp-plugin/src/completionController.ts:204`, `packages/lsp-plugin/src/signatureHelpController.ts:186`, `packages/lsp-plugin/src/plugin.ts:488` all call `getRangeClientRect` and then position by hand. `packages/lsp-plugin/src/tooltip.ts:38-41` hardcodes its own `TOOLTIP_GAP_PX`, `TOOLTIP_VIEWPORT_MARGIN_PX`, `TOOLTIP_MAX_HEIGHT_PX` and a `preferredPlacement: 'top' | 'bottom'` \u2014 a private one-off of Monaco's preference list. Each controller separately re-anchors on `'viewport' | 'layout'` update kinds (`completionController.ts:78`).",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Add `packages/editor/src/editor/contentWidgets.ts` owning a single `position: absolute` layer inside `scrollElement` (plus a `position: fixed` overflow layer appended to `document.body` for `allowEditorOverflow`). Expose `addContentWidget/removeContentWidget/layoutContentWidget` on `EditorViewContributionContext`, and drive re-layout from the existing `EditorViewContributionUpdateKind` fan-out in `viewContributions.ts:82-95` \u2014 'viewport', 'layout', 'content' and 'selection' all already fire there, which is exactly Monaco's onScrollChanged/onLinesChanged set. Reuse `inputSelection.rangeClientRect` (`Editor.ts:1979`) for the anchor rect and the virtualizer's row geometry for `lineHeight`. Port `_layoutBoxInViewport` and the two-pass preference loop verbatim \u2014 they are ~60 lines and fully self-contained. Then delete the bespoke positioning from `tooltip.ts`, `completionController.ts`, `signatureHelpController.ts` and `renameWidget.ts`.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. packages/editor/src/plugins.ts:210-236 gives plugins only container/scrollElement/getRangeClientRect/reserveOverlayWidth. Every consumer hand-rolls: lsp-plugin/src/tooltip.ts:565-652 (`placeTooltip`, `tooltipPlacement`, `tooltipAvailableHeight`, `applyTooltipPlacement`), completion.ts:342-343 (reads window.innerWidth/innerHeight directly), renameWidget.ts:40, signatureHelpController.ts, hoverDefinitionController.ts. Repo-wide grep for contentWidget/overlayWidget/widgetLayer/anchorWidget returns nothing.",
  "verifierNotes": "Value is LOWER than the claim implies. tooltip.ts already uses CSS anchor positioning \u2014 `anchor-name` (:259), `position-anchor` (:561), `position-area` (:650) \u2014 so the browser performs Monaco's `_layoutBoxInViewport` for free. What is actually duplicated across four controllers is only the flip-on-overflow decision and the max-height clamp (tooltip.ts:569-652). Build a ~100-line shared `anchoredSurface` helper, not Monaco's widget layer. Skip Monaco's `beforeRender()` forced-reflow avoidance \u2014 anchor positioning sidesteps it."
 },
 {
  "domain": "decorations-widgets",
  "title": "ZoneWidget: view zone reserves whitespace, the DOM lives in a stable overlay widget driven by onDomNodeTop \u2014 our block-row DOM is destroyed on scroll recycle",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/zoneWidget/browser/zoneWidget.ts:385-460 (_showImpl: creates an *empty* viewZoneDomNode, registers it as a view zone, then registers `this.domNode` as an OverlayWidgetDelegate), :288-300 (_onViewZoneTop sets domNode.style.top, _onViewZoneHeight re-lays out the body); references/vscode/src/vs/editor/browser/viewParts/viewZones/viewZones.ts:357-410 (render: every zone node stays in the DOM forever, only top/height/display are updated, plus onDomNodeTop(-1000000) when off-screen); references/vscode/src/vs/editor/contrib/codelens/browser/codelensWidget.ts:190-260, :291-296 (same split: CodeLensViewZone reserves px, CodeLensContentWidget renders ABOVE line n+1); references/vscode/src/vs/editor/contrib/peekView/browser/peekView.ts:98-272 (PeekViewWidget adds _fillHead/_fillBody/_doLayoutHead/_doLayoutBody/setTitle/onDidClose on top of ZoneWidget)",
  "mechanism": "Monaco never puts widget DOM inside the scrolled line list. A view zone contributes only *whitespace* to the vertical layout; its own DOM node is a stub. The real content is registered as an overlay widget (viewport-fixed, absolutely positioned), and the zone's `onDomNodeTop(top)` callback pushes the scrolled top onto that overlay node each render. Consequences: the widget's DOM is created once and never re-parented, so focus, text selection, an embedded editor's scroll position, IME composition and iframe state all survive scrolling; the widget can be clipped to the editor bounds; and resizing via a sash only changes the whitespace height, not the row list. `ZoneWidget` then layers on the reusable chrome \u2014 arrow (a dynamically generated `.arrow-decoration-N` CSS rule positioned via a decoration), frame borders, `_decoratingElementsHeight()` subtracting arrow+frame from the body height, `_getMaximumHeightInLines()` capping at 80% of the viewport, and `revealRange` after showing. `PeekViewWidget` subclasses it into title bar + action bar + body with `_doLayoutHead/_doLayoutBody`.",
  "whyItMatters": "Our block surfaces are the right *idea* but mount into recycled virtual rows, so any interactive surface (a diff comment box, an inline chat input, an embedded preview, a peek result list) loses focus and internal state the moment the user scrolls it out and back. That is a hard ceiling on what block surfaces can be used for. Splitting space-reservation from DOM hosting removes the ceiling without changing the public `EditorBlock` API, and gives us the ZoneWidget/PeekView building block for free.",
  "ourStatus": "partial",
  "ourEvidence": "We have space-reserving zones: `packages/editor/src/editorBlocks.ts:1-82` (`EditorBlock` with top/bottom `EditorBlockHorizontalSurface` and a `mount(container, ctx)` callback, `FixedSize | MinSize | MaxSize | BoundedSize`), resolved in `packages/editor/src/editor/blockSurfaceController.ts:42-115` and turned into `BlockRow` (`packages/editor/src/displayTransforms.ts:110-118`). But `packages/editor/src/virtualization/virtualizedTextViewRows.ts:697-727` mounts the surface *into the recycled row element*: `syncBlockRowMount` keys on `row.blockMountKey === blockRow.id` and otherwise calls `disposeBlockRowMount`, which invokes the mount disposable and then `resetBlockContainerElement` \u2014 `element.replaceChildren()` plus stripping every attribute. So scrolling a block surface out of the mounted window tears its DOM down and re-runs `mount` on return. There is no overlay-hosted variant and no ZoneWidget/PeekView abstraction anywhere in the repo (grep for `zoneWidget`/`peek` in `packages/*/src` returns nothing).",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add a `hosting: 'inline' | 'overlay'` field to `EditorBlockHorizontalSurface`. For `'overlay'`, `blockSurfaceController` mounts the surface once into the content-widget layer from finding #3 (or a sibling `editor-block-overlay` layer) and keeps it alive for the block's lifetime; the virtualized row then renders only an empty spacer of the measured height. `virtualizedTextViewRows.setBlockRowContent` (:667-685) already syncs a height-only container, so the spacer path is nearly free. Wire the per-frame top: the virtualizer already knows each row's `item.start`, so emit an `onTop(scrolledTop)` callback per overlay block from the same place `syncBlockContainerHeight` runs, and set `display:none` plus `onTop(-1e6)` when the block leaves the viewport (Monaco's exact trick, viewZones.ts:392-397). Once that lands, `ZoneWidget` is ~150 lines on top: an abstract class taking an `EditorBlock` with `showArrow`/`showFrame` options, `_fillContainer`, `_doLayout(height,width)`, and a `PeekViewWidget` subclass adding the title/action bar.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Verified exactly as claimed. packages/editor/src/virtualization/virtualizedTextViewRows.ts:697-727: `syncBlockRowMount` early-returns only when `row.blockMountKey === blockRow.id`, otherwise `disposeBlockRowMount` runs the mount disposable and calls `resetBlockContainerElement`, which does `element.replaceChildren()` then strips every attribute in a while loop (:721-728). Additional teardown sites at :568, :652, :983, :1613 and `disposeBlockRowMounts` at :260-262. Reference verified: references/vscode/src/vs/editor/contrib/zoneWidget/browser/zoneWidget.ts:64-85 (`_onDomNodeTop`), and diffEditor/utils.ts:257-265 uses the same pattern.",
  "verifierNotes": "Highest-value item in this domain for us. Any block surface holding component state (an editable form, a scrolled diff pane, a video, a focused input) loses it on scroll-out. Fix does not require Monaco's full ZoneWidget: keep the whitespace reservation as-is and add an opt-in 'hoisted' mode where the surface DOM lives in a viewport-fixed overlay and the row render pushes `top` onto it, mirroring onDomNodeTop."
 },
 {
  "domain": "decorations-widgets",
  "title": "Injected text as a first-class concept: phantom content at a zero-width anchor, with cursor stops and hit-test payload",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model.ts:316-360 (InjectedTextOptions: content, inlineClassName, inlineClassNameAffectsLetterSpacing, attachedData, cursorStops; InjectedTextCursorStops Both/Right/Left/None); references/vscode/src/vs/editor/contrib/inlayHints/browser/inlayHintsController.ts:536-560 (addInjectedText builds a decoration with showIfCollapsed + collapseOnReplaceEdit + AlwaysGrowsWhenTypingAtEdges and `[item.anchor.direction]: opts`), :561-566 (addInjectedWhitespace injects U+200A with a width-only class), :599-706 (per-part padding/border-radius, maximumLength trimming with '\u2026', fixed-length column alignment), :411-418 (_getInlayHintLabelPart recovers `attachedData instanceof RenderedInlayHintLabelPart` from the decoration under the mouse); references/vscode/src/vs/editor/contrib/colorPicker/browser/colorDetector.ts:203-243 (color swatch = `before: {content: noBreakWhitespace, inlineClassName: dynamicRule}`); references/vscode/src/vs/editor/contrib/inlineProgress/browser/inlineProgress.ts:23-31 + :91-98 (injected nbsp reserves the space, a content widget at EXACT draws the spinner in it)",
  "mechanism": "`before`/`after` on a decoration inject text into the *view* only \u2014 the model text, offsets and LSP positions are untouched. Crucially the anchor range may be **empty** (`showIfCollapsed: true`), which is what makes inlay hints possible at all: the hint hangs off a point, not a span. `cursorStops` controls whether the caret can land on either side of the injected run, so arrowing through `foo(\u27e8name:\u27e9 1)` skips the phantom text as one unit. `attachedData` is an arbitrary object recoverable from `getInjectedTextDecorations`/the decoration under the mouse, which is how clicking an inlay hint part resolves back to its `InlayHintLabelPart` for go-to-definition \u2014 the hit-testing story that any interactive phantom text needs. `inlineClassNameAffectsLetterSpacing` tells the layout engine the class changes advance widths so measurement is redone. The same primitive powers color swatches, ghost text, inline progress placeholders, and whitespace padding (an injected U+200A with a width-only CSS class).",
  "whyItMatters": "Inlay hints (parameter names, inferred types) are the single most-requested modern editor feature and we structurally cannot render them today. The same primitive would also give us CSS colour swatches, inline error text at end-of-line, and ghost text for AI completions \u2014 all currently impossible without faking them as whole extra rows.",
  "ourStatus": "partial",
  "ourEvidence": "`packages/editor/src/inlineMap.ts:186-190` \u2014 `inlineRangeFromSpec` does `if (endOffset <= startOffset) return null`, so an `InlineReplacementSpec` must cover at least one real character. Our inline layer *substitutes* existing text (markdown `**` \u2192 hidden, `- ` \u2192 `\u2022`, see `packages/markdown/src/replacements.ts:103-122`), it cannot *insert* phantom text. `InlineReplacement` (`packages/editor/src/displayTransforms.ts:173-180`) carries `{id, startColumn, endColumn, text, kind?, metadata?}` \u2014 no `className`, so injected content cannot be styled differently from surrounding code, and no cursor-stop control (only a global `TransformBias` at `inlineMap.ts:131-142`). `InjectedTextRow` (`displayTransforms.ts:97-106`) injects a whole extra *row* before/after a line, not inline content at a column. `metadata` exists on both, which is the `attachedData` idea already half-present.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Relax `inlineRangeFromSpec` to accept `endOffset === startOffset` when the spec carries an explicit `insert: true` (or a `showIfCollapsed` flag), anchoring both ends at the same offset with opposing biases so typing at the point pushes the hint aside rather than through it. Add `className` and `cursorStops: 'both' | 'left' | 'right' | 'none'` to `InlineReplacementSpec`/`InlineReplacement`, and thread `className` into the segment rendering in `createInlineRow` (`displayTransforms.ts:318-340`) so injected runs get their own span class. `cursorStops` then becomes an override on the `TransformBias` used by `inlineColumnToSourceColumn` for caret motion \u2014 our existing `revealInlineMap` machinery must NOT reveal insertion-kind specs, so gate reveal on `kind`. `metadata` already gives us `attachedData`; expose an `inlineReplacementAtPoint(clientX, clientY)` lookup for hit-testing. With that, an inlay-hints controller in `packages/lsp-plugin` is a straightforward LSP `textDocument/inlayHint` consumer emitting specs through the existing display-projection registry.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "The core gap is real: packages/editor/src/inlineMap.ts:186 `if (endOffset <= startOffset) return null` blocks point-anchored phantom text, so inlay hints are impossible. But two sub-claims are FALSE. (a) 'no className, so injected content cannot be styled differently' \u2014 packages/editor/src/virtualization/virtualizedTextViewRows.ts:1536-1566 `updateRowInlineKindClasses` mirrors every replacement's `kind` onto the row element as `editor-inline-<kind>` (guarded by CLASS_SAFE_REPLACEMENT_KIND at :1534), which packages/markdown already relies on for heading styling; covered by packages/editor/test/inlineReplacementPainting.test.ts:61,69. (b) 'hit-test payload missing' \u2014 `metadata` flows spec \u2192 InlineReplacementRange \u2192 InlineReplacement (displayTransforms.ts:173-180) \u2192 InlineRowSegment.metadata (:184-190) \u2192 EditorVisibleRowSnapshot.metadata (plugins.ts:~152) and EditorGutterRowContext.metadata (plugins.ts:396). That IS attachedData, already end-to-end.",
  "verifierNotes": "Correct framing: the substrate (atomic segments, total column mapping in both directions, group reveal, metadata passthrough, kind\u2192CSS) is all built; one guard and a zero-width segment kind stand between us and inlay hints. Styling caveat the claim missed in the other direction: replacement text renders into the row's plain text chunks, NOT its own span, so per-span styling (an inlay-hint chip with its own background/padding) needs a new chunk kind. `cursorStops` per side is genuinely absent \u2014 we only have the global `TransformBias` at inlineMap.ts:131-142."
 },
 {
  "domain": "decorations-widgets",
  "title": "Replacing a text range with an arbitrary DOM node, and collapsed marks spanning multiple lines",
  "kind": "feature",
  "source": "codemirror5",
  "referenceLocation": "references/codemirror5/src/model/mark_text.js:166-172 (replacedWith \u2192 marker.collapsed = true, marker.widgetNode = eltP('span',[node],'CodeMirror-widget'), cm-ignore-events unless handleMouseEvents, insertLeft), :173-179 (collapsed markers may span line boundaries; partial overlap of two collapsed markers throws), :185-198 (updateLineHeight(line,0) for fully-swallowed lines; clearOnEnter \u2192 beforeCursorEnter clears the mark), :205-217 (collapsed \u21d2 atomic \u21d2 reCheckSelection so the caret cannot land inside), :254-256 (widgetNode cloned per document for shared/linked docs)",
  "mechanism": "CodeMirror's `markText` with `replacedWith: domNode` swaps a text span for a real DOM node in the inline flow. The mark is implicitly `collapsed` (the underlying text produces no glyphs) and implicitly `atomic` (`mark_text.js:205-207`), so selection and cursor motion treat it as a single indivisible unit and `reCheckSelection` pushes the caret out if an edit would strand it inside. Because collapsing is a line-*spans* operation rather than a per-line substitution, a collapsed mark may swallow whole intermediate lines: those lines get `updateLineHeight(line, 0)` and the visual line is stitched from the surrounding fragments. `handleMouseEvents: false` stamps `cm-ignore-events` so the editor's own mouse handling ignores the widget's subtree; `insertLeft` controls which side of the mark's position an insertion lands on.",
  "whyItMatters": "Our markdown WYSIWYG is explicitly 'a view over the same piece-table buffer'. Text-to-text substitution gets us `**bold**` \u2192 bold and `- ` \u2192 `\u2022`, but it cannot render an inline image, a rendered LaTeX span, a clickable checkbox, or a colour swatch \u2014 all of which need a DOM node, not a string. The multi-line restriction also means a fenced code block or a multi-line link reference can't be collapsed into a single rendered widget.",
  "ourStatus": "missing",
  "ourEvidence": "`packages/editor/src/inlineMap.ts:207-213` (`normalizeInlineRanges`) filters `.filter(range => range.startPoint.row === range.endPoint.row)` and `.filter(range => !range.text.includes('\\n'))` \u2014 replacements are strictly single-line, single-row, text-only. `InlineReplacement` (`packages/editor/src/displayTransforms.ts:173-180`) has `text: string` and no node/element field. `InlineRowSegment` (`displayTransforms.ts:184-190`) is purely column arithmetic. On the plus side, `revealInlineMap` (`inlineMap.ts:88-118`) plus `groupId` already give us the atomic/`clearOnEnter` behaviour \u2014 the caret entering a hidden span reveals the whole group \u2014 so the hard interaction semantics are solved; only the rendering substrate is missing.",
  "value": "medium",
  "effort": "L",
  "implementationSketch": "Add an optional `render?: (container: HTMLElement) => void | EditorDisposable` alongside `text` on `InlineReplacementSpec`. In the row painter, a segment whose replacement carries `render` produces an `inline-block` span with `contenteditable=false` that the callback fills, keyed and mount-cached exactly like `syncBlockRowMount` does for block rows (`virtualizedTextViewRows.ts:697-713`) \u2014 but note finding #4: cache the mount by replacement id across row recycling or the widget will thrash. Advance-width measurement is the real cost: the segment's display width is no longer `text.length * charWidth`, so `virtualizedTextViewGeometry` needs a measured-width path per rendered replacement (reuse the ResizeObserver plumbing already in `editorBlockSurfaces.ts`). Multi-line collapsing is a separate, larger change to `foldMap`+`inlineMap` interaction and should be deferred; single-line DOM replacement alone unlocks images, checkboxes and swatches.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Verified. packages/editor/src/inlineMap.ts:207-213 `normalizeInlineRanges` filters `range.startPoint.row === range.endPoint.row` and `!range.text.includes('\\n')`. `InlineReplacement` (displayTransforms.ts:173-180) is `text: string` with no node field; `InlineRowSegment` (:184-190) is pure column arithmetic. Confirmed the positive half too: `revealInlineMap` (inlineMap.ts:88-118) + `groupId` give atomic/clearOnEnter semantics, and `normalizeInlineRanges` already drops nested replacements (:214-221).",
  "verifierNotes": "Split the two halves. Single-line replacedWith-DOM is worth it (markdown images, checkboxes, color swatches) and fits: the row already hosts a `blockContainerElement` pattern to copy. MULTI-LINE collapsed marks should be dropped \u2014 our entire virtualizer assumes rows are the unit of layout and recycling (fixedRowVirtualizer + rowHeightIndex + displayTransforms' per-visible-row expansion at displayTransforms.ts:668-700); a mark that eats line breaks breaks that invariant everywhere. Folding already covers the real use case."
 },
 {
  "domain": "decorations-widgets",
  "title": "View zone knobs we lack: ordinal tiebreak, before-first-line, render-in-hidden-areas, margin twin, min scroll width, suppressMouseDown",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/editorBrowser.ts:36-101 (IViewZone: afterLineNumber with 0 = before first line, afterColumn + afterColumnAffinity, showInHiddenAreas, ordinal, suppressMouseDown, heightInLines vs heightInPx, minWidthInPx, marginDomNode, onDomNodeTop, onComputedHeight), :104-125 (IViewZoneChangeAccessor addZone/removeZone/layoutZone); references/vscode/src/vs/editor/browser/viewParts/viewZones/viewZones.ts:143-146 (_getZoneOrdinal = ordinal ?? afterColumn ?? 10000), :148-195 (_computeWhitespaceProps: validates the position, converts model\u2192view position, and zeroes the height when the anchor line is inside a fold unless showInHiddenAreas), :198-230 (changeViewZones batches through an accessor that is invalidated after the callback), :410-412 (marginDomNode width tracks contentLeft)",
  "mechanism": "Zones are inserted into the layout's whitespace list, not the line list, so they compose with folding and wrapping. `afterLineNumber: 0` places a zone above line 1 \u2014 the only way to render a header. `ordinal` (defaulting to `afterColumn`, else 10000) deterministically orders multiple zones anchored to the same line, which matters the moment two independent features both want a zone there. `showInHiddenAreas` decides whether a zone survives its anchor line being folded; otherwise `_computeWhitespaceProps` returns `heightInPx: 0` and `isInHiddenArea: true` and the render loop skips it \u2014 the zone is *kept*, not destroyed, so unfolding restores it instantly. `marginDomNode` is a parallel node laid out in the gutter at the same top/height, which is how a peek view draws its frame across the line-number column. `minWidthInPx` raises the editor's scroll width so a wide zone gets a horizontal scrollbar. All mutations go through a short-lived `IViewZoneChangeAccessor` that is deliberately poisoned (`invalidFunc`) after the callback returns, so nobody can stash it and mutate layout outside a batch.",
  "whyItMatters": "Each of these is a bug we will hit: two plugins both adding a top surface to line 40 currently order by provider-array position (non-deterministic across plugin registration order); a block surface on a folded line has undefined behaviour; there is no way to put a banner above line 1; a wide block surface won't extend the scroll width; and a block surface can't paint into the gutter lane beside itself. Adopting the field set now is cheap and prevents API churn later.",
  "ourStatus": "partial",
  "ourEvidence": "`packages/editor/src/editorBlocks.ts:1-82` covers the core: row/range anchor, `top`/`bottom` surfaces with `FixedSize|MinSize|MaxSize|BoundedSize` (richer than Monaco's heightInPx/heightInLines), `left`/`right` lane surfaces (Monaco has no equivalent \u2014 this is ours to keep), and a `mount` callback. Missing: no `ordinal` \u2014 ordering falls out of provider iteration in `blockSurfaceController.resolveEditorBlockRows` (`blockSurfaceController.ts:117+`); `EditorBlockAnchor` is `{row}` or `{startRow,endRow}` with no 'before line 0' encoding; no `showInHiddenAreas` (grep for fold interaction in `blockSurfaceController.ts` finds none); no margin twin; no `minWidthInPx` (`BlockRow` at `displayTransforms.ts:110-118` has only height fields); no `suppressMouseDown`; and mutation is whole-array `sync(providers)` (`blockSurfaceController.ts:54-67`) rather than an add/remove/layout accessor, so a single zone's height change re-resolves every provider.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Additive changes to `packages/editor/src/editorBlocks.ts`: add `ordinal?: number` to `EditorBlock`, `showInHiddenAreas?: boolean` to the surface types, `minWidthPx?: number`, and `marginMount?: EditorBlockMount`. Use `ordinal` as the sort key in `resolveEditorBlockRows` before falling back to block id, so ordering is deterministic regardless of provider registration order. For fold interaction, `blockSurfaceController` already receives fold state indirectly via the display-projection pipeline \u2014 resolve the anchor row through `foldMap` and emit `heightPx: 0` (keeping the surface registered) when the anchor is hidden and `showInHiddenAreas` is false; that mirrors Monaco exactly and is a few lines. `minWidthPx` feeds the virtualizer's `contentWidth` computation in `virtualizedTextViewGeometry`. Encode 'before first line' as `{row: -1}` or an explicit `{beforeFirstRow: true}` variant of `EditorBlockAnchor`. The accessor-batching refactor is the largest piece and can wait.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Three of six sub-items are FALSE. (a) Ordering is NOT provider-iteration order: packages/editor/src/displayTransforms.ts:955-967 `normalizeBlockRows` sorts anchorBufferRow \u2192 placementOrder \u2192 `id.localeCompare` \u2014 deterministic. And an explicit numeric ordinal already exists on the sibling type: `InjectedTextRow.order` (displayTransforms.ts:103) used in `normalizeInjectedTextRows` (:969-979). (b) Before-first-line IS expressible: `{row: 0}` with a `top` surface maps to placement 'before' via editor/editorBlockSurfaces.ts:204-207, and displayTransforms.ts:688 `appendBlockRows(rows, blocks, bufferRow, 'before', startOffset)` emits it above line 1 \u2014 that is the header case. (c) Zones DO compose with folding: displayTransforms.ts:668-676 `appendDisplayRowsForVisibleRow` resolves `bufferRowForVisibleRow(visibleRow)`, so a zone anchored inside a fold simply does not render \u2014 which is Monaco's DEFAULT; `showInHiddenAreas` is the opt-out we lack, not basic fold correctness. Genuinely absent, confirmed: numeric ordinal on `BlockRow` (displayTransforms.ts:110-118 has none), `showInHiddenAreas`, margin twin, `minWidthInPx`, `suppressMouseDown`, and incremental mutation (blockSurfaceController.ts:54-67 re-resolves every provider on any change).",
  "verifierNotes": "Scope down hard. Of the six, only `ordinal` on BlockRow (trivial \u2014 copy InjectedTextRow.order) and the incremental add/remove/layout accessor (real: a ResizeObserver height change today re-runs every provider's getBlocks and materializes full text via blockSurfaceController.createEditorBlockProviderContext) are worth doing. `suppressMouseDown` is moot \u2014 no mousedown handler exists on the block container. Our left/right lane surfaces have no Monaco equivalent and should stay."
 },
 {
  "domain": "decorations-widgets",
  "title": "Preserve the visual anchor when content height changes above the viewport",
  "kind": "correctness",
  "source": "codemirror5",
  "referenceLocation": "references/codemirror5/src/model/line_widget.js:53-57 (adjustScrollWhenAboveVisible: if heightAtLine(line) < scrollTop, addToScrollTop(cm, diff)), :59-76 (addLineWidget computes aboveVisible before updateLineHeight and compensates by the widget height), :36-49 (LineWidget.changed() recomputes height and applies the same compensation for a resize)",
  "mechanism": "Whenever a line widget is added, removed or resized, CodeMirror checks whether the widget's line sits *above* the current scrollTop. If so it adds the height delta straight into scrollTop, so the pixels the user is looking at do not move. `LineWidget.clear()` applies the negative delta and `LineWidget.changed()` applies the delta on resize \u2014 all three mutation paths are covered, and the compensation happens inside the same operation as the height change, so there is no intermediate frame where the content has jumped.",
  "whyItMatters": "This is exactly the jank users notice: diagnostics arrive asynchronously and a plugin adds an inline error surface 200 lines above the cursor, or a code-lens row resolves and grows, or an image finishes loading inside a block surface \u2014 and the text under the user's eyes jumps. It is a small, self-contained fix with a large perceived-quality payoff, and it becomes more important the more block surfaces we add.",
  "ourStatus": "missing",
  "ourEvidence": "`packages/editor/src/virtualization/fixedRowVirtualizer.ts:796-807` \u2014 `nextScrollTopForOptions` only clamps: `if (options.scrollMode === 'static') return 0; return clampScrollTopForGeometry(currentScrollTop, scrollGeometryForOptions(options, viewportHeight))`. When `setOptions` receives new `rowSizes` (line 698-704, which is how block-row heights enter the layout via `createRowHeightIndex`), the scrollTop is carried over unchanged, so any height delta above the viewport shifts everything down. `setBlockRows` (`virtualizedTextView.ts:571-573`) and the ResizeObserver-driven measurement path in `blockSurfaceController` both go through this. The virtualizer does track a `stableVirtualWindow` and does scroll bookkeeping (`fixedRowVirtualizer.ts:240-290`) but purely for emit-throttling, not for anchoring.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "In `fixedRowVirtualizer.setOptions` (around line 168), before adopting the new normalized options, compute the old and new `rowHeightIndexStart(index, firstVisibleRow)` for the row that was at the top of the viewport (or better: for the row containing the primary caret when it is visible, else the top row) and add the difference to `scrollTop` before clamping. `rowHeightIndex.ts` already exposes `rowHeightIndexStart(index, row)` and `rowHeightIndexRowAtOffset(index, offset)`, so this is a handful of lines. Guard it behind 'the anchor row's start moved but the anchor row index is unchanged' so a genuine document replacement still resets to the top. Add a test in `virtualizedTextViewModel.test.ts` alongside the existing `blockRows` fixtures (:18) asserting that inserting a 100px block above the viewport leaves the visible first row unchanged.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. packages/editor/src/virtualization/fixedRowVirtualizer.ts:790-802 `nextScrollTopForOptions` only clamps; `setOptions` (:168-173) carries scrollTop across a `rowSizes`/`rowHeightIndex` change unchanged. Both entry points go through it: virtualizedTextView.ts:571-578 `setBlockRows` and the ResizeObserver measurement path in editor/blockSurfaceController.ts. Repo-wide grep for scroll-delta compensation (`scrollTop +=`, preserveScroll, scrollAnchor, adjustScrollTop) finds only packages/diff/src/DiffView.ts:636 (pane sync) and Editor.ts:2369 (syntax scroll-velocity bookkeeping) \u2014 neither is anchoring. Reference verified: references/codemirror5/src/model/line_widget.js:52-55 `adjustScrollWhenAboveVisible`, called from `addLineWidget` (:73), `clear()` (:29) and `changed()` (:45).",
  "verifierNotes": "Best value-to-effort ratio in this domain. Same defect hits folding a region above the viewport and any lazily-measured block. CM5's fix is 4 lines: if the changed row's top is above scrollTop, add the height delta to scrollTop. Our `rowHeightIndex` already gives O(log n) offset-of-row, so we can compute the delta exactly rather than CM5's per-widget bookkeeping."
 },
 {
  "domain": "decorations-widgets",
  "title": "Range-highlight paint order is registration-order dependent, so overlapping decorations can swap z-order after scrolling",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model.ts:200-206 (zIndex: 'A decoration with greater stack order is always in front of a decoration with a lower stack order when the decorations are on the same line'); references/vscode/src/vs/editor/browser/viewParts/glyphMargin/glyphMargin.ts:24-37 (DecorationToRender carries zIndex), :299-323 (requests sorted by lineNumber, laneIndex, zIndex DESC, type DESC, className \u2014 with an explicit 'don't change this sort unless you understand prepareRender' warning), :356-375 (only decorations sharing the winner's zIndex are merged)",
  "mechanism": "Monaco makes stacking an explicit, declared property of each decoration and sorts by it deterministically at render time, then merges only same-zIndex peers. Order never depends on when a decoration was created or whether it was momentarily empty.",
  "whyItMatters": "We paint range decorations through the CSS Custom Highlight API, where paint order for equal-priority highlights is *registration order in the `CSS.highlights` registry*. Our code never sets `Highlight.priority`, and it deregisters a group whenever it momentarily has zero mounted ranges \u2014 then re-registers it later, which moves it to the end of the registry. Concrete repro: with a find-match highlight overlapping the current selection, scroll all matches out of the mounted window (group hits `highlight.size === 0` \u2192 `unregisterRangeHighlight`), then scroll back; the group re-registers *after* the selection highlight and now paints on top of it, inverting the colours the user saw a moment ago. The same applies between any two overlapping groups \u2014 diagnostics vs occurrences, bracket match vs selection.",
  "ourStatus": "missing",
  "ourEvidence": "`packages/editor/src/virtualization/virtualizedTextViewHighlights.ts:1306-1315` \u2014 `ensureRangeHighlightRegistered` does `view.highlightRegistry.set(group.name, group.highlight)` with no `group.highlight.priority` assignment; grep for `priority` in `packages/editor/src/virtualization/*.ts` returns nothing. `renderRangeHighlight` (:260-279) calls `unregisterRangeHighlight` whenever `group.highlight.size === 0` \u2014 i.e. whenever no range of that group is currently mounted \u2014 and re-registers on return. Group names are minted as `${prefix}-range-${semanticName}-${index}` where `index` is `groups.length` at build time (`packages/editor/src/editor/rangeDecorations.ts:69-79`), and `groupedRangeDecorations` (:12-31) only coalesces *adjacent* decorations sharing a style key, so a differently-ordered input array produces differently-numbered groups. `EditorRangeDecoration` (`packages/editor/src/editor/types.ts:37-42`) has no zIndex field; the only ordering signal we have is the per-projection `layer`/`priority` in `displayProjectionRegistry.ts:62-63`, which never reaches the highlight registry.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Two-part fix. (1) Add `zIndex?: number` to `EditorRangeDecoration`, default it from the owning projection's `layer`/`priority` in `Editor.composedRangeDecorations` (`Editor.ts:2103-2107`), include it in `rangeDecorationGroupKey` (`rangeDecorations.ts:81-91`), and set `group.highlight.priority = zIndex` in `ensureRangeHighlightRegistered`. That makes paint order declared rather than incidental. (2) Stop deregistering on transient emptiness \u2014 in `renderRangeHighlight` (:272-276) keep the group registered with an empty `Highlight` when it has ranges in the model but none currently mounted, and only deregister in `clearRangeHighlight`. Sort groups by zIndex in `groupedRangeDecorations` before assigning indices so group names are stable across frames too. A regression test can assert `CSS.highlights` iteration order/priority is unchanged across a mount-out/mount-in cycle.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "The defect is REAL but the claimed cause is wrong. Refuting the 'differently-ordered input array' half: `composedRangeDecorations` (Editor.ts:2367-2373) iterates `displayProjections.values('rangeDecorations')`, which is sorted by `compareStoredDisplayProjections` (displayProjectionRegistry.ts:226-236: layer \u2192 priority \u2192 sequence \u2192 owner), so the array order and hence the group indices minted at rangeDecorations.ts:69-79 ARE deterministic. The actual bug: `ensureRangeHighlightRegistered` (virtualizedTextViewHighlights.ts:1306-1315) does `view.highlightRegistry.set(name, group.highlight)` and never assigns `group.highlight.priority` \u2014 grep for `priority` across packages/editor/src/virtualization/*.ts returns nothing. Under the CSS Custom Highlight API, equal-priority highlights paint in HighlightRegistry insertion order, and `renderRangeHighlight` (:273-278) unregisters any group whose `highlight.size === 0` \u2014 i.e. whenever none of its ranges is currently mounted \u2014 then re-registers it at the tail on return. So scrolling a find match out of and back into view flips its stacking against a diagnostic squiggle.",
  "verifierNotes": "Cheapest real bug in the list: assign `group.highlight.priority` from the owning projection's layer/priority in `ensureRangeHighlightRegistered`. Note our substrate differs from Monaco's \u2014 we do not merge same-zIndex peers into one span, we stack ::highlight() pseudo-elements, so we need priority propagation, not Monaco's sort-and-merge."
 },
 {
  "domain": "decorations-widgets",
  "title": "Refcounted dynamic CSS rule factory: arbitrary per-decoration styling without unbounded stylesheet growth",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/editorDom.ts:284-345 (DynamicCssRules: _rules keyed by JSON.stringify(properties), createClassNameRef increments a refcount and returns a disposable, a RunOnceScheduler garbage-collects unreferenced rules after 1000ms so hanging rules can be reused), :347-400 (CssProperties whitelist; RefCountedCssRule creates one <style> element per rule and resolves ThemeColor values to `var(--vscode-\u2026)` CSS variables, and targets the shadow root when the editor is in shadow DOM); usage: inlayHintsController.ts:571-706 (a class ref per hint part), colorDetector.ts:200-243 (a class ref per colour swatch)",
  "mechanism": "Features that need genuinely dynamic styling (a colour swatch whose background is the literal parsed colour; an inlay hint whose font-size/padding/border-radius derives from options) cannot use static CSS classes. `DynamicCssRules` hashes the property bag, reuses an existing rule for identical properties, hands out `{className, dispose}` and garbage-collects only after a 1s delay \u2014 which is the key trick: a re-render that disposes and immediately re-creates the same rule hits the cache instead of churning the stylesheet. `ThemeColor` values become CSS custom-property references rather than literals, so a theme switch needs no rule regeneration at all.",
  "whyItMatters": "Our decoration styling is limited to three properties applied through `::highlight()`, which cannot express borders, padding, border-radius, inline-block widths, or `::before` content \u2014 everything an inlay hint, colour swatch, or ghost-text run needs. A refcounted rule factory is the missing enabler for findings #5 and #6, and it prevents the obvious naive implementation (inline styles per span, or a stylesheet that grows monotonically) from becoming a memory leak on a long-lived editor.",
  "ourStatus": "present",
  "ourEvidence": "`packages/editor/src/editor/rangeDecorations.ts:44-56` \u2014 `rangeDecorationStyle` extracts exactly `backgroundColor`, `color`, `textDecoration`; `VirtualizedTextHighlightStyle` (`packages/editor/src/virtualization/virtualizedTextViewTypes.ts:94-98`) has the same three fields. We do dedupe by style key within a frame (`rangeDecorationGroupKey`, :81-91) and rebuild the sheet via `rebuildStyleRules(view)` (`virtualizedTextViewHighlights.ts:257,288,368,450,472`), so there is a per-frame dedupe but no cross-frame refcounted cache and no delayed GC \u2014 every style change rewrites the whole rule set. The lsp-plugin instead copies a fixed list of theme CSS variables onto its tooltip element by hand (`packages/lsp-plugin/src/tooltip.ts:44-54`), which is the ad-hoc version of Monaco's ThemeColor\u2192CSS-variable indirection.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Port `DynamicCssRules` almost verbatim into `packages/editor/src/style-utils.ts` (which already exists for style helpers) \u2014 it is ~110 lines with no VS Code dependencies beyond `DisposableStore` and a debounced scheduler, both trivial to replace. Key by `JSON.stringify` of a sorted property bag, keep the 1s delayed GC, and resolve theme tokens to the `--editor-*` custom properties we already define (`tooltip.ts:44-54` lists them). First consumers: the injected-text `className` from finding #5, and a widened `EditorRangeDecoration.style` for anything the Highlight API cannot paint. Note this only helps for *span-rendered* decorations \u2014 `::highlight()` pseudo-elements accept a narrow property set by spec \u2014 so it lands together with an inline-span rendering path, not as a replacement for the highlight path.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "FALSE GAP \u2014 the claim missed the file entirely. /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/sharedTokenHighlights.ts:39-119 is `DynamicCssRules`: `acquire(styleKey, style)` increments `refCount` and reuses the existing rule (:53-67), `release(styleKey)` decrements and deletes the registry entry and rule at zero (:69-78), rules are keyed-deduped in a Map, the `<style>` element is document-scoped and shared across every editor instance via `WeakMap<Document, SharedTokenHighlights>` (:23-37), and writes are dirty-flagged and batched into a single document mutation in `flush()` (:82-88). It is tested: packages/editor/test/sharedTokenHighlights.test.ts. Compare references/vscode/src/vs/editor/browser/editorDom.ts:288-330 \u2014 same shape. Two real deltas: no 1s delayed GC (Monaco's `_garbageCollectionScheduler` at editorDom.ts:295), and the RANGE-decoration path bypasses it \u2014 rangeDecorations.ts:44-56 and virtualizedTextViewHighlights.ts:1427-1467 rebuild the whole per-view sheet and support only 3 properties, vs `EditorTokenStyle`'s 5 (tokens.ts:1-7).",
  "verifierNotes": "Reframe entirely: the factory exists and is good; the work is routing range decorations through it and widening the property set. Also flag the real ceiling the claim mis-diagnosed \u2014 our styling substrate is `::highlight()`, and the CSS Highlight pseudo-element only honours a small property set (no border-radius, padding, or box model). So Monaco's motivating cases (an inlay-hint chip, a colour swatch) need a per-span element regardless of how rules are cached; that is claim 5/6 territory, not this one."
 },
 {
  "domain": "decorations-widgets",
  "title": "Overlay widgets: declarative corner anchoring with stacking, and widget min-width feeding the editor's scroll width",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/editorBrowser.ts:238-320 (OverlayWidgetPositionPreference TOP_RIGHT_CORNER/BOTTOM_RIGHT_CORNER/TOP_CENTER, IOverlayWidgetPositionCoordinates, IOverlayWidgetPosition with stackOrdinal, IOverlayWidget.getMinContentWidthInPx); references/vscode/src/vs/editor/browser/viewParts/overlayWidgets/overlayWidgets.ts:153-166 (_updateMaxMinWidth \u2192 viewLayout.setOverlayWidgetsMinWidth), :167-213 (_renderWidget: corner placement offset by the vertical scrollbar + minimap width, stackCoordinates accumulating widths/heights for stacked widgets, fixedOverflowWidgets switching to position:fixed with page coordinates), :219-232 (render sorts by stack ordinal before laying out); references/vscode/src/vs/editor/contrib/placeholderText/browser/placeholderTextContribution.ts:64-90 (placeholder is an overlay widget with position:null, sized from layoutInfo.contentLeft/contentWidth-verticalScrollbarWidth)",
  "mechanism": "Overlay widgets are viewport-fixed rather than document-anchored. Their position is declared as one of a few named corners (or explicit coordinates), and the editor resolves it against live layout info \u2014 subtracting `2 * verticalScrollbarWidth + minimapWidth` so a find widget never sits under the minimap. `stackOrdinal` lets several widgets claim the same corner: they are sorted by ordinal and `stackCoordinates` accumulates each one's measured width (horizontal corners) or height (TOP_CENTER) so they tile instead of overlapping. `getMinContentWidthInPx()` is aggregated into `viewLayout.setOverlayWidgetsMinWidth`, so a wide overlay widget actually increases the editor's scroll width rather than being clipped. `fixedOverflowWidgets` re-bases to page coordinates with `position: fixed` for widgets that must escape a clipped container. Even the placeholder text is just an overlay widget with `position: null`, sized from `layoutInfo`.",
  "whyItMatters": "Our find widget, and any future 'N problems' bar, breadcrumb, or read-only banner, each hand-position themselves against the scroll element and none of them know about each other. Two of them in the top-right corner will overlap. And none of them affect scroll width, so a wide overlay is silently clipped.",
  "ourStatus": "partial",
  "ourEvidence": "`packages/editor/src/plugins.ts:208` defines `EditorOverlaySide = 'left' | 'right'` and `:227` `reserveOverlayWidth(side, width)`, whose entire implementation is `packages/editor/src/virtualization/virtualizedTextView.ts:614-620`: it sets `scrollElement.style.paddingLeft/paddingRight`. That reserves *gutter* space, not a widget layer \u2014 there is no corner anchoring, no stacking, no min-content-width contribution, and no managed layer. `packages/find/src/findWidget.ts:123` creates a `.editor-find-widget` root and positions it itself; `packages/find/src/plugin.ts:138-145` owns the singleton. `packages/editor/src/style.css` has only four `z-index` declarations (2, 3, 5, 10) with no documented layering contract for plugin-supplied overlays.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Build this as the sibling of the content-widget layer from finding #3, sharing its render tick: `packages/editor/src/editor/overlayWidgets.ts` owning one `position:absolute` layer sized to the editor box. Expose `addOverlayWidget(widget)` on `EditorViewContributionContext` with `getPosition(): {preference: 'top-right' | 'bottom-right' | 'top-center' | {top,left} | null, stackOrdinal?: number}` and optional `getMinContentWidthInPx()`. Corner offsets must subtract the minimap width \u2014 `packages/minimap` already reports its width to reserve gutter space, so route that through the same layout info. Port the `stackCoordinates` accumulation verbatim (overlayWidgets.ts:167-213); it is ~40 lines. Feed the aggregated min-width into the virtualizer's content-width computation (`virtualizedTextViewGeometry.ts`) so wide overlays extend the scroll range. Migrate `packages/find/src/findWidget.ts` as the first consumer and codify the z-index bands in `style.css` while doing it.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and it produces a live defect. `reserveOverlayWidth` (packages/editor/src/plugins.ts:227 \u2192 Editor.ts:2289 \u2192 virtualizedTextView.ts:614-620) sets only `scrollElement.style.paddingLeft/paddingRight`. Its ONLY caller is the minimap (packages/minimap/src/plugin.ts:127, 280-289), which reserves minimap width + vertical scrollbar width on its side. The find widget does not participate: packages/find/src/style.css:32-35 hardcodes `position:absolute; right:18px; top:8px; z-index:20` and packages/find/src/findWidget.ts:123 just sets `.editor-find-widget`. So with the minimap mounted on the right, the find widget renders on top of the minimap. Confirmed no corner enum, no stackOrdinal, no min-content-width contribution; packages/editor/src/style.css has four bare z-index values (2, 3, 5, 10) with no layering contract, and plugins pick their own (find 20, lsp tooltip 1000, completion 1001, rename 60).",
  "verifierNotes": "Keep for the concrete bug, not the abstraction. Minimum viable fix: expose the reserved left/right widths on the view-contribution context so the find widget can offset by them, exactly as Monaco subtracts `minimapWidth + 2*verticalScrollbarWidth`. A documented z-index scale for plugin overlays is the second half and is nearly free. Full declarative corner anchoring + stackOrdinal is over-engineering for a repo with one find widget."
 },
 {
  "domain": "decorations-widgets",
  "title": "Randomized differential testing of the range store against a brute-force oracle",
  "kind": "testing",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/test/common/model/intervalTree.test.ts:25-72 (Interval + Oracle: an array kept sorted, with O(n) search), :75-140 (TestState.acceptOp applies each op to both tree and oracle, then asserts assertTreeInvariants + deepStrictEqual of getAllInOrder vs the oracle's array), :173-270 (testIntervalTree(ops) replays a recorded op list; the generator prints a ready-to-paste `testIntervalTree([...])` call on failure \u2014 line 268), :275-460 (dozens of such regressions checked in verbatim), :564-700 (assertNodeAcceptEdit: an exhaustive per-stickiness table of (nodeStart, nodeEnd, stickiness, editStart, editEnd, textLength, forceMoveMarkers) \u2192 expected (start, end))",
  "mechanism": "Two complementary layers. The randomized layer drives insert/delete/change/search operations against both the real interval tree and a dumb sorted-array oracle, asserting after *every* operation that (a) the red-black invariants still hold, (b) the full in-order listing matches the oracle exactly, and (c) interval searches match. When a random run fails, `console.log(\\`testIntervalTree(${JSON.stringify(this._ops)})\\`)` emits the exact minimal-effort reproduction as source code, and the file shows ~25 such shrunk cases pasted in as permanent regressions. The deterministic layer, `assertNodeAcceptEdit`, is a hand-written truth table covering every stickiness value crossed with every edit-vs-range topology (edit before / at start / inside / at end / after / encompassing) and both `forceMoveMarkers` values \u2014 the cases no random generator will reliably hit.",
  "whyItMatters": "Range tracking through edits is exactly the kind of code where the bugs are rare topologies (an edit whose deletion exactly equals the range, an insertion at a boundary with mixed stickiness, a surrogate-pair boundary) that only surface in production as decorations drifting one character. The oracle pattern is how you get confidence cheaply, and the printed-repro trick turns every random failure into a permanent checked-in test at zero cost.",
  "ourStatus": "present",
  "ourEvidence": "We have solid unit tests around the pieces (`packages/editor/src/virtualization/virtualizedTextViewModel.test.ts`, `packages/editor/src/editor/indentation.test.ts`, `packages/editor/test/inlineReplacementCaptures.test.ts`, `packages/editor/test/inlineReplacementPainting.test.ts`, plus `packages/editor/bench/pieceTable-anchors.ts` which already generates random anchors and resolves them at :136,:179), but nothing that differentially tests anchor/range survival against a brute-force model across a random edit stream. `inlineMap.ts`'s subtle rules \u2014 `anchorAfter(start)/anchorBefore(end)` (:191), the `endOffset <= startOffset` drop (:186), the overlap-suppression in `normalizeInlineRanges` (:214-221), and `resolveInlineRange` dropping deleted anchors (:230-233) \u2014 are exactly the surface an oracle test would pin down.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Add `packages/editor/test/trackedRanges.oracle.test.ts` using bun:test. The oracle is a plain array of `{start, end, stickiness}` updated by a ~30-line reference implementation of `nodeAcceptEdit` (port Monaco's directly from intervalTree.ts:416-470 \u2014 it is the specification, not an optimization). Drive random insert/delete/replace edits through `applyBatchToPieceTable` alongside anchor-backed tracked ranges, and after each edit assert the resolved offsets match the oracle. On mismatch, `console.log` a pasteable `testTrackedRanges([...])` call exactly as Monaco does, and check the shrunk cases back in. Separately add the deterministic `assertAcceptEdit` truth table \u2014 for our two-bias anchor model that is 4 bias pairs x ~8 topologies, roughly 32 assertions, and it directly documents the semantics that `inlineMap.ts:155-158` currently only describes in a comment. This test suite is the prerequisite for landing finding #1 safely.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "FALSE GAP. /Users/shaul/Desktop/D/Editor/packages/editor/src/pieceTable/pieceTable.test.ts:207-231 `runRandomAnchorScenario` accumulates up to 60 anchors created with BOTH biases, applies 80 random edits from a seeded LCG (`createRandom`, :33-38, so failures are reproducible by seed), and after EVERY edit asserts `expect(resolveAnchor(snapshot, anchor)).toEqual(resolveAnchorLinear(snapshot, anchor))` \u2014 the reverse-index-accelerated implementation against the brute-force linear scan. Driven over seeds 1-10 at :557-559. Companion fuzzers: `runRandomEditScenario` (:128-140, text + random range reads), `runRandomWalkerScenario` (:168-179), `runRandomDiffScenario` (:181-205), each over seeds 1-25 at :431-444. Also packages/editor/test/editChain.test.ts:106-131 fuzzes edit composition against sequential application.",
  "verifierNotes": "Two genuine follow-ups survive. (1) ORACLE WEAKNESS worth fixing: `resolveAnchor` (pieceTable/anchors.ts:235-246) FALLS BACK to `resolveAnchorLinear` when the reverse index has no entry, and both paths share `resolveAnchorAgainstEntry` \u2014 so any anchor missing the index makes the assertion vacuously true. Assert index-hit coverage, or make the oracle recompute offsets from a materialized string independently. (2) The inlineMap/foldMap layer is unfuzzed: packages/editor/test/inlineMap.test.ts is 248 lines of hand-written cases (28 `it` blocks, none an edit stream), so inlineMap.ts:186 (empty-range drop), :207-221 (single-row filter + overlap suppression) and :230-233 (deleted-anchor drop) are pinned only by examples. Reusing the existing `createRandom` harness there is cheap."
 },
 {
  "domain": "folding-brackets",
  "title": "Fold collapse state is keyed by content offsets, so any earlier edit silently expands folds",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/folding/browser/foldingModel.ts:132-186 (update / _currentFoldedOrManualRanges), 138-160 (updatePost delta-decorations), 191-245 (getMemento/applyMemento with line checksum); foldingRanges.ts:301-391 (sanitizeAndMerge)",
  "mechanism": "Monaco never identifies a folded region by its coordinates. `updatePost` writes one tracked model decoration per region (foldingModel.ts:146-157) whose range is `[startLine, maxCol] .. [endLine, maxCol+1]`. When new provider ranges arrive, `_currentFoldedOrManualRanges` (162-186) reads each decoration's *current* range back out of the text model \u2014 the decoration has already been moved by the edit \u2014 and `FoldingRegions.sanitizeAndMerge` (foldingRanges.ts:301-391) merges the fresh provider list (A) against that list of previously-collapsed/manual ranges (B) in one linear pass: equal start lines keep the provider's geometry but inherit B's collapsed flag *only if the line span is unchanged* (line 339), non-matching B entries survive as `FoldSource.recovered`, and a stack of `stackedRanges` discards anything that crosses rather than nests. Persistence across sessions uses a memento that stores a 6-digit hash of the region's first and last line content (241-245) and refuses to restore a region whose content hash changed.",
  "whyItMatters": "In our editor, collapsing a function at line 400 and then typing a single character at line 1 expands it. The fold's `startIndex`/`endIndex` shift by one, `foldRangeKey` produces a different string, and `pruneCollapsedFolds` drops the old key. This is the single most visible folding defect available to a user and it fires on ordinary typing.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/folds.ts:40-42 \u2014 `foldRangeKey` = `${languageId}:${type}:${startIndex}:${endIndex}` (byte offsets). packages/editor/src/editor/foldState.ts:135-141 `pruneCollapsedFolds` deletes any collapsed key not present in the new projection. packages/editor/src/editor/folds.ts:85-92 `projectSyntaxFoldsThroughLineEdit` returns null when `lineDelta === 0`, so a same-line insertion produces no key remap at all; the worker then reports shifted offsets and the collapse is lost. Note we already anchor the *geometry* correctly \u2014 packages/editor/src/foldMap.ts:19-63 stores `AnchorFoldRange` with piece-table anchors \u2014 so only the identity layer is wrong. No test covers collapse-survives-edit (packages/editor/test/ has foldMap.test.ts only).",
  "value": "high",
  "effort": "M",
  "implementationSketch": "In `EditorFoldState` (foldState.ts) stop storing `Set<string>` of content keys. Store `{ start: Anchor, end: Anchor }` per collapsed fold using `anchorBefore`/`anchorAfter` from packages/editor/src/pieceTable/anchors.ts \u2014 exactly what foldMap.ts already does at lines 19-28. On `setFoldProjections`, resolve each stored anchor against the current snapshot (`resolveAnchor`, dropping non-`live` ones) and match against incoming folds by resolved start offset, inheriting collapsed only when the span length is unchanged (Monaco's line-339 rule). This deletes `projectSyntaxFoldsThroughLineEdit`, `SyntaxFoldProjection.keyMap`, and `remapCollapsedFoldKeys` outright \u2014 a net simplification. Add a test: collapse a fold, insert a char at offset 0, assert `collapsedFoldCount` is unchanged.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Fully confirmed, and the failure path is tighter than claimed. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/folds.ts:40-42 `foldRangeKey` = `${languageId}:${type}:${startIndex}:${endIndex}`, pure byte offsets. folds.ts:90-91 `projectSyntaxFoldsThroughLineEdit` bails with `if (lineDelta === 0) return null` *before* computing any key remap, even though it has already computed `offsetDelta` on line 94. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/Editor.ts:2429-2447 renderSessionChange calls that projector and then `applySyntaxFoldProjection`, which at Editor.ts:2455-2460 returns early on null \u2014 so a same-line insertion remaps nothing. When the worker's next parse lands, foldState.ts:41-48 `setFoldProjections` -> :135-141 `pruneCollapsedFolds` deletes every collapsed key not present in the new (offset-shifted) fold set. The geometry layer is indeed already correct and anchor-based: /Users/shaul/Desktop/D/Editor/packages/editor/src/foldMap.ts:19-28 `AnchorFoldRange` holds `anchorBefore`/`anchorAfter` piece-table anchors and re-resolves them on every edit (foldMap.ts:127-164). Test coverage confirmed absent: /Users/shaul/Desktop/D/Editor/packages/editor/test/foldMap.test.ts only exercises `updateFoldMapForEdit` invalidations; the fold tests in packages/editor/test/editor.test.ts:5399-5495 toggle folds but never edit the document afterwards.",
  "verifierNotes": "Highest-value item in this domain and the cheapest to fix: it is a user-visible correctness bug (type one character on line 1, every fold in the file pops open once the worker reparses), not a missing feature. The fix does not need Monaco's decoration machinery \u2014 foldMap.ts already proves we can key a fold by a pair of piece-table anchors. Move `collapsedFoldKeys` from an offset string to an anchor pair (or make EditorFoldState hold `AnchorFoldRange` identities and match incoming FoldRanges by resolved anchor offset), and delete the `lineDelta === 0` early return so offset-only shifts still remap. Add the missing regression test: collapse a fold, insert a char above it, deliver a reparse with shifted offsets, assert still collapsed."
 },
 {
  "domain": "folding-brackets",
  "title": "Two range providers (syntax + indentation fallback), with #region markers and off-side rule",
  "kind": "feature",
  "source": "both",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/folding/browser/syntaxRangeProvider.ts:48-59 (fallback), 172-210 (sanitizeRanges); indentRangeProvider.ts:126-225 (computeRanges), 66-111 (RangesCollector.toIndentRanges); references/codemirror5/addon/fold/indent-fold.js:14-46; references/codemirror5/addon/fold/comment-fold.js:14-57",
  "mechanism": "`SyntaxRangeProvider.compute` awaits all folding-range providers; if *every* provider returns non-array (not just empty), it delegates to a `fallbackRangeProvider` \u2014 the `IndentRangeProvider` passed in at folding.ts:289. `computeRanges` (indentRangeProvider.ts:126-225) walks the document *bottom-up* keeping a stack of `{indent, endAbove, line}`; when the current line's indent drops below the stack top, every deeper entry pops and each pop emits a region `[line, previous.endAbove - 1]`. Two refinements matter: `offSide` (from language config `folding.offSide`, true for Python/YAML) attaches blank lines to the block *above* rather than terminating it (lines 150-158); and `folding.markers` (`#region`/`#endregion` regexes) are compiled into one alternation and handled with a sentinel indent of `-2` (lines 130-140, 178-203) so an explicit marker region overrides indentation entirely. Both collectors degrade gracefully past a limit by dropping the *deepest* levels first rather than truncating arbitrarily (indentRangeProvider.ts:82-107, syntaxRangeProvider.ts:140-165). `sanitizeRanges` (syntaxRangeProvider.ts:172-210) sorts by start then provider rank and uses a `previous` stack to compute each range's nesting level and discard crossings.",
  "whyItMatters": "Our folds exist only where a tree-sitter fold query matched. Open a .py, .yaml, .go, .rs, .sh, .css file or plain text and there is no folding at all, and even in TS there is no folding for a long `if` body or a `#region`. An indentation provider is ~100 lines and gives every language folding for free. The nesting level it computes is also the input for fold-by-level commands and for indent guides.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/syntax/session.ts:114 \u2014 folds come only from `EditorSyntaxResult.folds`; packages/tree-sitter-languages/src/queries/ has fold queries for javascript, typescript, jsx, markdown only. Editor.ts:1716-1758 registers exactly one fold projection owner, `editor.folds.syntax`. `grep -ri 'region|offSide|indentFold'` over packages/*/src returns nothing. Our FoldRange (session.ts:15-22) carries no nesting level.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Our `displayProjectionRegistry` already supports multiple owners for the `folds` kind (Editor.ts:1761 `foldProjections()` concatenates them), so this is additive: add `packages/editor/src/editor/indentFoldProvider.ts` implementing Monaco's bottom-up stack walk over a `LineMap` (the one in editActions.ts:497-506 already builds line starts), emitting `FoldRange` with `startIndex/endIndex/startLine/endLine`. Register it as owner `editor.folds.indent` and have `setSyntaxFolds` suppress it when the syntax projection is non-empty (Monaco's fallback rule). Also add a `nestingLevel` field to FoldRange while you are there \u2014 `rejectCrossingFoldRanges` (folds.ts:55-83) already maintains the `openAncestors` stack that computes it, so it is two lines.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed at the provider level. Folds enter only via `EditorSyntaxResult.folds` (/Users/shaul/Desktop/D/Editor/packages/editor/src/syntax/session.ts:106-115) and are registered under a single owner constant, Editor.ts:150 `SYNTAX_FOLD_PROJECTION_OWNER = 'editor.folds.syntax'`, set at Editor.ts:1716-1740. Grammar coverage is exactly four fold queries: /Users/shaul/Desktop/D/Editor/packages/tree-sitter-languages/src/queries/{javascript,typescript,jsx,markdown}-folds.scm. No `#region`, `offSide`, or indent-derived fold code exists \u2014 the only 'region' hits under packages/*/src are a gutter aria-label (packages/gutters/src/foldGutter.ts:165) and unrelated minimap/mergeConflict types. FoldRange (session.ts:15-22) carries no nesting level. IMPORTANT CORRECTION to the effort estimate: the *multi-provider registry already exists*. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/displayProjectionRegistry.ts:9,209-292 supports many owners for kind 'folds', collects them all (Editor.ts:1761-1762 `foldProjections()` -> `values('folds')`), and validates cross-owner non-crossing (`validateFoldProjectionSet`, displayProjectionRegistry.ts:273-292). Reference verified: references/vscode/src/vs/editor/contrib/folding/browser/indentRangeProvider.ts:126-175 is the bottom-up `previousRegions` stack walk with the `offSide` empty-line rule and the combined start/end marker regex, exactly as described.",
  "verifierNotes": "Real and worth doing: today a Python, Go, Rust, YAML, CSS or plain-text file gets zero folding, because folding is 100% grammar-gated on four queries. An indentation provider is ~120 lines and is the single change that makes folding universal. Effort is lower than the surveyor implies because the fan-in already exists \u2014 a second provider just needs its own owner string plus a public/plugin hook to register 'folds' projections (today only Editor.ts itself calls `displayProjections.set`, so the hook is the missing part, not the registry). `#region` markers are a cheap add-on to the same pass. Sequence it after the fold-identity fix (claim 2), otherwise indent folds inherit the same collapse-loss bug."
 },
 {
  "domain": "folding-brackets",
  "title": "Auto-unfold when the caret lands inside hidden lines, and selection clamping across them",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/folding/browser/folding.ts:247, 364-393 (cursorChangedScheduler + revealCursor); hiddenRangeModel.ts:44-125 (updateHiddenRanges, adjustSelections, isHidden); foldingModel.ts:162-186 (selection blocks re-collapse)",
  "mechanism": "Three cooperating rules. (1) `revealCursor` (folding.ts:370-393), fired on a 200 ms `RunOnceScheduler` after cursor movement, checks `hiddenRangeModel.isHidden(line)` and, if so, collects `getAllRegionsAtLine(line, r => r.isCollapsed && line > r.startLineNumber)` and toggles them open, then reveals. So Go-to-line / find / go-to-definition into a folded body just works. (2) `HiddenRangeModel.adjustSelections` (96-125) clamps any selection endpoint that falls inside a hidden range to the end of the line *before* the range, so arrow keys and shift-selection cannot leave a caret in invisible text. (3) `FoldingModel._currentFoldedOrManualRanges` (171-173) refuses to re-collapse a region whose interior contains the current selection when provider ranges refresh \u2014 `selection?.startsInside(...)` sets `isCollapsed = false` \u2014 so a background syntax update never swallows the line you are editing. `updateHiddenRanges` also skips ranges nested inside an already-collapsed range (61-64), keeping the hidden-range list minimal and diffable (66-76 reuses old Range objects so the emitted event is identity-stable).",
  "whyItMatters": "Without (1), find-in-file or go-to-definition can move the caret into a collapsed region and the user sees nothing move. Without (2), the caret can sit invisible. Without (3), an async syntax refresh can collapse the block you are typing in. All three are the kind of bug that reads as 'the editor is broken' rather than 'a feature is missing'.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/Editor.ts \u2014 `foldState` is only touched by explicit fold/unfold/toggle/foldAll/unfoldAll (lines 620-661, 2587-2653); no cursor-movement path consults it. `grep -n 'foldMap|hidden' packages/editor/src/editor/selectionReveal.ts packages/editor/src/editor/navigation.ts` returns nothing. packages/editor/src/foldMap.ts has `bufferPointToFoldPoint` for geometry but no selection clamping and no unfold-on-reveal. `EditorFoldState.setFoldProjections` (foldState.ts:41-48) has no notion of the current selection.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "(1) In `Editor`, on the existing selection-change path that already calls into `selectionReveal.ts`, check whether the caret row is inside a collapsed `AnchorFoldRange` in the current `FoldMap` (`foldMap.ranges` already carries resolved start/end offsets) and, if so, call `this.unfold(fold)` for each enclosing collapsed fold before revealing. (2) Add `EditorFoldState.selectionBlocksFold(offset)` and consult it in `pruneCollapsedFolds`/`setFoldProjections` so a refresh cannot collapse over the caret \u2014 this pairs naturally with the anchor-keyed rewrite in the collapse-durability finding. (3) Clamp in `mouseSelection.ts`/`navigation.ts` via a `foldMap` lookup mirroring `HiddenRangeModel.adjustSelections`.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "The unfold-on-reveal behaviour is genuinely absent \u2014 confirmed: `foldState` is touched only by the explicit fold/unfold/toggle/foldAll/unfoldAll paths (Editor.ts:620-661, 2587-2653); grep for fold/hidden across /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/{selectionReveal,navigation,scroll,navigationTargets,mouseSelection}.ts returns nothing; navigation.ts is a pure re-export of textRanges offset helpers with no view/fold awareness. BUT the claim that we have no clamping across hidden lines is wrong. /Users/shaul/Desktop/D/Editor/packages/editor/src/foldMap.ts:178-196 `pointWithFoldRowDelta` (behind `bufferPointToFoldPoint`, foldMap.ts:78-81) explicitly maps any point whose offset falls strictly inside a collapsed range onto that range's start point \u2014 that is the same job as Monaco's `HiddenRangeModel.adjustRange`, applied at display-projection level. The inverse `foldPointToBufferPoint` (foldMap.ts:83-94) and `bufferRowForVisibleRow` (packages/editor/src/virtualization/virtualizedTextViewModel.ts:95-99) complete the two-way mapping, and `foldedVisibleLineCount` (same file, :86-93) already excludes hidden rows from the scroll model.",
  "verifierNotes": "Downgraded to partial: the hidden-range coordinate mapping exists and is solid; what is missing is the *policy* layer on top of it. Two concrete behaviours to add, both small given foldMap: (1) after a programmatic reveal (go-to-definition, find-next, go-to-line \u2014 all of which already funnel through selectionReveal.ts), test the target offset against `foldMap.ranges` and unfold the enclosing collapsed ranges before revealing, otherwise the caret silently lands on a fold header with no feedback; (2) vertical navigation currently steps buffer rows, so ArrowDown into a collapsed body moves the caret through hidden text while it renders pinned to the header row \u2014 it should step *visible* rows via foldPointToBufferPoint. Do (2) even if (1) is deferred; the pinned-caret-that-does-not-move is the more confusing of the two."
 },
 {
  "domain": "folding-brackets",
  "title": "Language configuration as declarative data: onEnterRules and indentationRules",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/languages/languageConfiguration.ts:44-190 (LanguageConfiguration), 223-241 (IndentAction incl. IndentOutdent); supports/onEnter.ts:52-107; supports/indentRules.ts:8-83; autoIndent.ts:75-218 (getInheritIndentForLine), 304-358 (getIndentForEnter)",
  "mechanism": "One declarative record per language holds `comments`, `brackets`, `wordPattern`, `indentationRules {increaseIndentPattern, decreaseIndentPattern, indentNextLinePattern, unIndentedLinePattern}`, `onEnterRules [{beforeText, afterText, previousLineText, action}]`, `autoClosingPairs` (with `notIn`), `surroundingPairs`, `colorizedBracketPairs`, `autoCloseBefore`, and `folding {offSide, markers}`. `OnEnterSupport.onEnter` (onEnter.ts:52-107) evaluates in strict priority: user regex rules first, then `IndentAction.IndentOutdent` when the text before Enter matches an open-bracket regex *and* the text after matches a close-bracket regex, then plain `Indent` after a lone opener. `IndentOutdent` (languageConfiguration.ts:237) is the case that puts `}` on its own line and leaves the caret on a blank indented line. Separately, `getInheritIndentForLine` (autoIndent.ts:75-218) walks upward past lines matching `unIndentedLinePattern`, honours `indentNextLinePattern` for constructs like a bare `if (x)` whose indent is *temporary*, and returns the inherited indentation string. `IndentRulesSupport.getIndentMetadata` packs the four predicates into a 4-bit mask (indentRules.ts:8-13, 67-82) so callers branch on bits instead of re-running regexes.",
  "whyItMatters": "Our indentation is purely character-adjacency: `lineBreakIndent` looks at `charBefore`, `charAfter`, `betweenPair` and nothing else. That means Enter after `case 'a':`, after `if (x)` with no brace, inside a JSDoc block (` * ` continuation), after an unclosed JSX tag, or after a Python `def foo():` all produce the previous line's indentation verbatim. Declarative rules also move language knowledge out of three hardcoded `Record<string, ...>` tables into data an embedder can supply for a language we have never heard of.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/indentation.ts:41-53 \u2014 `lineBreakIndent` has exactly three branches (betweenPair / afterOpener / continue), driven only by adjacent characters; there is no regex, no previous-line inspection, no language input at all. Language knowledge is hardcoded and split across three files: autoClose.ts:25-40 `PAIRS_BY_LANGUAGE`, editActions.ts:94-108 `COMMENT_TOKENS_BY_LANGUAGE`, and tree-sitter fold queries. `grep -riE 'onEnterRule|indentationRule|wordPattern|electricChar'` over packages/*/src returns nothing.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `packages/editor/src/editor/languageConfiguration.ts` exporting a `LanguageConfiguration` type and a registry keyed by languageId, seeded by merging today's `PAIRS_BY_LANGUAGE`, `COMMENT_TOKENS_BY_LANGUAGE`, and `AUTO_CLOSE_BEFORE`. Expose registration on the public API (packages/editor/src/public/) so embedders can add languages. Then extend `LineBreakIndentOptions` with `previousLineText`, `lineTextAfterCaret`, and the resolved config, and reimplement `lineBreakIndent` as Monaco's three-tier `onEnter`, returning an `{indentAction, appendText, removeText}` that the caller turns into `{insert, trailing}` \u2014 our existing `trailing` field is already exactly `IndentAction.IndentOutdent`. packages/editor/src/editor/indentation.test.ts gives a regression net.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed partial, with the split slightly wider than described. Per-language knowledge lives in three unrelated tables: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/autoClose.ts:8-40 (`CODE_PAIRS`, `PROSE_PAIRS`, `PAIRS_BY_LANGUAGE`) plus a global `AUTO_CLOSE_BEFORE` set at autoClose.ts:48; /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/editActions.ts:555-580 (`DEFAULT_COMMENT_TOKENS`, `HTML_COMMENT_TOKENS`, `COMMENT_TOKENS_BY_LANGUAGE`, read at editActions.ts:1145-1149); and the .scm query files. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/indentation.ts:41-53 `lineBreakIndent` is exactly the three branches claimed (betweenPair / afterOpener / continue) and takes no languageId at all \u2014 its caller inputSelectionController.ts:194-221 derives `betweenPair`/`afterOpener` purely from `autoClosingPairForOpen(charBefore)`. No regex-based rules, no previous-line inspection. `wordPattern` has no named equivalent, though an inline `/[\\p{L}\\p{N}_$]/u` word test exists at autoClose.ts:133-137. Indent width is `options.indentText ?? '\\t'` (editActions.ts:473) and a constructor `tabSize` (Editor.ts:264,329) \u2014 configured, never detected.",
  "verifierNotes": "Correctly partial. The value is not the abstraction for its own sake \u2014 it is that a single `LanguageConfiguration` record is the prerequisite that makes claims 8, 11 and 12 each a table entry instead of a new subsystem, and it kills the current failure mode where adding a language means editing three files and forgetting one. Concretely: today `PAIRS_BY_LANGUAGE` and `COMMENT_TOKENS_BY_LANGUAGE` list the same ~14 language ids independently, so any new language silently gets `[]` pairs (autoClose.ts:53-55 returns empty for unknown ids) and `//` comments (editActions.ts:1149 falls back to DEFAULT_COMMENT_TOKENS) \u2014 an inconsistent pair of defaults. Start by merging those two tables and adding `onEnterRules`; `indentationRules` can follow with claim 12."
 },
 {
  "domain": "folding-brackets",
  "title": "Bracket-pair guides and colorization derived from the AST, including min-indentation per pair",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/model/bracketPairsTextModelPart/bracketPairsTree/bracketPairsTree.ts:247-374 (collectBrackets), 385-491 (collectBracketPairs); ast.ts:611-635 (TextAstNode.computeMinIndentation); tokenizer.ts:186-270 (lengthHeuristic); colorizedBracketPairsDecorationProvider.ts",
  "mechanism": "`collectBrackets` walks the AST for a viewport range only, carrying two counters: `level` (absolute nesting) and `nestingLevelOfEqualBracketType` tracked in a `Map<bracketText, number>` so `(` and `[` colour independently. It emits `BracketInfo(range, level-1, typeLevel-1, parentPairIsIncomplete)` \u2014 the last flag lets an unclosed pair be rendered differently. Two structural optimisations: when a child extends past the requested end there is no sibling after it, so the loop *reassigns `node` and continues the outer `whileLoop`* instead of recursing (lines 280-290, 322-334), keeping stack depth at the AST depth rather than the child count; and `level > 200` bails (line 260). `collectBracketPairs` additionally computes `minIndentation` per pair by asking every contained `TextAstNode` for the minimum visible column of its lines (ast.ts:611-635), memoised in the node. That number is exactly where a bracket-scope guide should be drawn. The tokenizer deliberately caps text-node length (`lengthHeuristic > 1000` at end of line, `> 1500` hard, tokenizer.ts:255-268) so min-indentation recomputation after an edit stays bounded.",
  "whyItMatters": "Bracket-pair colorization is the single most-requested modern editor affordance and is impossible on our flat bracket list because nothing carries a nesting level. Separately, our scope guides are derived from fold markers, so a guide only exists where a tree-sitter fold query matched \u2014 a `for` body, an `if` block, or an object literal argument gets no guide at all, and no guide is drawn in a language without fold queries.",
  "ourStatus": "partial",
  "ourEvidence": "packages/scope-lines/src/index.ts \u2014 `ScopeGuide` is built from `VirtualizedFoldMarker`, i.e. from fold ranges, with an `indentLevel` inferred rather than measured. packages/editor/src/syntax/session.ts:115 `BracketInfo` has `index`, `char`, `depth`; bracketMatching.ts:76-79 explicitly documents that the worker's `depth` is unreliable on unbalanced text and refuses to use it. packages/editor/src/bracketMatchPlugin.ts decorates one pair only. No colorization CSS or per-level class in packages/editor/src/style.css.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Gated on the bracket AST finding. Once a tree exists, add `collectBracketsInRange(startOffset, endOffset)` returning `{range, level, typeLevel, incomplete}` and have the existing rendering path in packages/editor/src/virtualization/ attach a `bracket-level-{n % 6}` class \u2014 the highlight layer already paints ranges. For guides, port `computeMinIndentation` onto Text nodes and switch packages/scope-lines to consume bracket pairs when available, falling back to fold markers. Keep Monaco's `level > 200` and text-node length caps; they are what makes it safe on generated files.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Status is right but two pieces of the evidence are wrong. (1) 'indentLevel inferred rather than measured' is false: /Users/shaul/Desktop/D/Editor/packages/scope-lines/src/index.ts:356-387 `scopeGuidePlacement` reads the actual indent column of the header line and of the first non-blank body line (`firstBodyIndentColumn`, :386-397, probing up to BODY_INDENT_PROBE_LINES=24 rows) from real line text (`uncachedLineText`, :410-423) with tab expansion (`indentColumn`, :440-450), and caches both per render pass. (2) 'No colorization CSS or per-level class' is false for the guides: index.ts:77 `SCOPE_LINE_COLOR_COUNT = 6` and :242 `element.dataset.editorScopeLineLevel = String(segment.indentLevel % 6)`, matched by twelve rules in /Users/shaul/Desktop/D/Editor/packages/scope-lines/src/style.css (`--editor-scope-line-color-0..5` and the `-active-` variants). Genuinely missing: bracket *characters* get one flat colour (`--editor-syntax-bracket`, packages/editor/src/style.css:14,60, applied via `punctuation.bracket` in packages/editor/src/syntax/captures.ts), and /Users/shaul/Desktop/D/Editor/packages/editor/src/bracketMatchPlugin.ts paints one pair only (bracketMatchPlugin.ts:99,153). Guides are keyed off `VirtualizedFoldMarker`, so a pair on one line, or in a language with no fold query, gets no guide.",
  "verifierNotes": "Keep, but scope it to what is actually absent. Two separable pieces: (a) bracket-pair *colorization* of the bracket glyphs by nesting level \u2014 this is blocked on reliable per-bracket levels, i.e. it is the real payoff of the reduced version of claim 1, and once levels are trustworthy it is a `data-bracket-level` attribute plus six CSS vars, cloned straight from scope-lines/style.css which already defines that palette; (b) Monaco's `min-indentation over the whole pair` refinement \u2014 ours takes the *first* non-blank body line's indent (index.ts:386-397), which draws the guide at the wrong column when the first body line is a continuation or is indented deeper than the rest of the block. (b) is a ten-line change to that probe loop and is worth doing regardless of (a)."
 },
 {
  "domain": "folding-brackets",
  "title": "Fold by level, fold recursively, fold all regions, manual folds from selection",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/folding/browser/foldingModel.ts:279-313 (getRegionsInside with level stack), 326-463 (toggleCollapseState, setCollapseStateLevelsDown/Up, setCollapseStateAtLevel, setCollapseStateForRest, ForMatchingLines, ForType), 472-603 (getParentFoldLine/Previous/Next); foldingRanges.ts:86-109 (parent index packing), 207-239 (findIndex/findRange); folding.ts:1034-1045 (FoldLevelAction), 1137-1186 (FoldRangeFromSelectionAction)",
  "mechanism": "`FoldingRegions` stores regions in two `Uint32Array`s and packs the *parent index* into the high 16 bits, split across the two arrays (`_startIndexes[i] |= (parent & 0xFF) << 24`, `_endIndexes[i] |= (parent & 0xFF00) << 16`, foldingRanges.ts:105-106, read back at 194-201). Computing parents is one linear pass with a stack (86-109) and is done lazily on first use. `findRange(line)` is a binary search plus a walk up the parent chain (207-239). On top of that, one filter-driven primitive `getRegionsInside(region, filter)` \u2014 which maintains a `levelStack` to supply the nesting level to a two-arg filter (284-299) \u2014 expresses every command: fold level N is `level === foldLevel && !containsBlockedLine`; fold recursively is `levels = MAX_VALUE`; fold-all-block-comments is `setCollapseStateForType('comment')`; fold-all-except is `setCollapseStateForRest`. `toggleCollapseState` (54-96) batches all toggles into a single `changeDecorations` transaction and updates only decorations whose end line falls inside the dirty span. Manual folds are first-class: `FoldRangeFromSelectionAction` (folding.ts:1152-1186) creates a `FoldSource.userDefined` range from the selection and merges it via `sanitizeAndMerge`, where user-defined entries win over provider entries (foldingRanges.ts:330-341).",
  "whyItMatters": "Fold-level-1..7 and fold-recursively are how people actually navigate a large unfamiliar file. Manual folds are how people collapse things the grammar has no node for (a config block, a data table, a licence header). We expose only fold / unfold / toggle / foldAll / unfoldAll, and foldAll collapses *every* region including nested ones, which is not what any editor's Fold All does.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/Editor.ts:625-661 \u2014 the complete fold API is toggleFold / fold / unfold / foldAll / unfoldAll. `EditorFoldState.foldAll` (foldState.ts:91-98) sets every fold's key, with no level filter. `FoldRange` (syntax/session.ts:15-22) has no parent or level field and no `source`. packages/editor/src/editor/commands.ts contains no fold command ids at all. packages/editor/src/editor/foldOperations.ts:5-23 picks a single candidate at a location; there is no 'regions inside' query.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Add a `parentIndex` array computed once per projection in `EditorFoldState` using the same stack `rejectCrossingFoldRanges` (folds.ts:143-151) already walks \u2014 no bit packing needed at our scale, a plain `Int32Array` is fine. Then add one primitive `regionsInside(fold | null, filter: (fold, level) => boolean)` and express `foldLevel(n)`, `foldRecursively`, `unfoldRecursively`, `foldAllExcept` on top, mirroring foldingModel.ts:326-429. Register the command ids in commands.ts/keymap.ts next to the existing `editor.action.*` list. Manual folds need a `source` field on FoldRange plus a second projection owner `editor.folds.manual` \u2014 the registry already supports it.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and the gap is larger than stated. The whole fold API is Editor.ts:620-661 (`setSyntaxFolds`/`fold`/`unfold`/`foldAll`/`unfoldAll`) plus `toggleFold`; `EditorFoldState.foldAll` (packages/editor/src/editor/foldState.ts:91-98) collapses every key with no level filter; `foldCandidateAtLocation` (packages/editor/src/editor/foldOperations.ts:5-23) returns a single FoldRange with no 'regions inside' query; FoldRange (packages/editor/src/syntax/session.ts:15-22) has no parent, level, or source field. Beyond the claim: there are NO fold command ids at all \u2014 grep over the full `EditorCommandId` union in /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/commands.ts:9-60 finds jumpToBracket, indentLines, joinLines etc. but nothing fold-related, and packages/editor/src/editor/keymap.ts has no fold binding. Folding is reachable only by gutter click (packages/gutters/src/foldGutter.ts:89-103) or by a host calling the Editor methods directly. Manual folds are impossible for a second reason: 'folds' projections are only ever written by Editor.ts itself (the sole `displayProjections.set({kind:'folds'})` call is Editor.ts:1726) and nothing in packages/editor/src/public/ exposes the registry. Reference verified: references/vscode/src/vs/editor/contrib/folding/browser/foldingRanges.ts:105-106 packs the parent index into the high bits of the two Uint32Arrays as described.",
  "verifierNotes": "Keep \u2014 best value-per-line in the domain, and note the correction: the missing keybindings are a bigger practical gap than the missing level operations. Ctrl+Shift+[ / ] and Ctrl+K Ctrl+0..9 are muscle memory; today no key folds anything. Nesting level does not need Monaco's Uint32Array packing: our fold list is already sorted start-asc/end-desc (`sortedFoldRangesForIngestion`, packages/editor/src/editor/folds.ts:104-117) and guaranteed non-crossing (`rejectCrossingFoldRanges`, folds.ts:55-83), so parent/level fall out of one containment-stack pass over the existing array \u2014 the same stack `innermostOpenAncestor` (folds.ts:143-151) already maintains. Manual folds want the plugin-facing 'folds' projection hook from claim 3, so the two share a prerequisite."
 },
 {
  "domain": "folding-brackets",
  "title": "Auto-close gated on token type, with the 'neutral character' tokenizer probe for quotes",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/languages/languageConfiguration.ts:297-320 (notIn parsing), 324-345 (isOK/shouldAutoClose), 348-372 (findNeutralCharacter); cursor/cursorTypeEditOperations.ts:240-280 (shouldAutoCloseBefore, scoped token check, neutral-character probe), 345 (_isBeforeClosingBrace)",
  "mechanism": "Each `autoClosingPairs` entry declares `notIn: ['string','comment','regex']`. Before inserting a closer, `_getAutoClosingPairClose` resolves the *scoped* line tokens at the caret (handling embedded languages) and calls `pair.shouldAutoClose(scopedTokens, column)`, which maps the standard token type through `isOK` (languageConfiguration.ts:324-336). For quotes this is not enough, because the token type *at* the caret cannot distinguish 'this quote opens a string' from 'this quote closes the string I am in'. Monaco's answer is a probe: `findNeutralCharacter()` (348-372) picks any character in [0-9a-zA-Z] not appearing in the pair's open/close text, then `getTokenTypeIfInsertingCharacter(line, col, neutral)` asks the tokenizer what token type would result if that harmless character were typed there (cursorTypeEditOperations.ts:273-278). If the answer is String, we are inside a string and the quote should not auto-close. Two further rules: `autoCloseBefore` is resolved per *category* \u2014 quote / comment / bracket get different allowed-following-character sets (201-214) \u2014 and `_isBeforeClosingBrace` (345) permits auto-close directly before a closing brace even though a brace is not in the generic before-set.",
  "whyItMatters": "Today, typing `(` inside a `// comment` or inside a string literal inserts a stray `)`, and typing `\"` to close a string you are in inserts a second `\"`. Both are daily-friction bugs in exactly the situations where auto-close is most annoying. We already receive per-offset syntax captures that name `string`/`comment`, so the token gate is cheap; only the neutral-character probe needs new plumbing.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/autoClose.ts:88-96 \u2014 `shouldAutoClose` consults only `charAfter`, `charBefore` and a word-character regex; there is no token, language-scope, or `notIn` input anywhere in the file. `AUTO_CLOSE_BEFORE` (line 48) is one global set, not per-category. packages/editor/src/editor/inputSelectionController.ts:258-271 calls it with no syntax context. We do have the raw material: packages/editor/src/syntax/session.ts:8-13 `EditorSyntaxCapture {startIndex, endIndex, captureName, languageId}` and :117 `injections`.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Extend `AutoCloseContext` with `tokenTypeAtCaret: 'string' | 'comment' | 'regex' | 'other'` and `AutoClosingPair` with `notIn?: readonly string[]`, then add the `isOK` gate to `shouldAutoClose`. Resolve the token type by binary-searching the current `EditorSyntaxResult.captures` for the caret offset (`captureName` prefixed `string`/`comment`) \u2014 `getEditorTokenIndex` in editor/tokenIndex.ts already does monotonic-range lookups over a similar array. The neutral-character probe needs a `tokenTypeIfInserting(offset, char)` on the tree-sitter session; if that is too invasive, approximate it by checking whether the caret sits strictly inside a `string` capture (start < offset < end) versus at its boundary \u2014 that covers the common `\"|\"` case. Existing coverage: packages/editor/src/editor/autoClose.test.ts, packages/editor/test/autoClose.test.ts.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/autoClose.ts:88-96 `shouldAutoClose` takes only `{charAfter, charBefore}` (AutoCloseContext, :72-77) \u2014 no token, scope, or `notIn`; `AUTO_CLOSE_BEFORE` (:48) is one global set. Call site /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/inputSelectionController.ts:258-268 passes exactly those two characters and nothing else. The raw material is confirmed present and closer to hand than claimed: `EditorSyntaxCapture {startIndex, endIndex, captureName, languageId}` (packages/editor/src/syntax/session.ts:8-13), captures are retained on the Editor instance (`this.syntaxCaptures`, Editor.ts:591-592) and injections are carried per-range (session.ts:37-42,111). Captures already carry the exact names needed \u2014 `comment` and `string` are first-class entries in packages/editor/src/syntax/captures.ts:32-40 \u2014 and packages/editor/src/editor/tokenIndex.ts provides a prebuilt offset index (`getEditorTokenIndex`, sortedByStart/nonOverlapping/maxEnds) for O(log n) lookup at a caret. Also confirmed present: `surroundSelection` (inputSelectionController.ts:325-345) already implements surroundingPairs behaviour for non-empty selections.",
  "verifierNotes": "Correctly partial. The concrete user-visible bug today: typing an apostrophe inside a string or a comment inserts a second quote, because nothing consults the parse. Fix is small \u2014 thread the capture at the caret into AutoCloseContext and add `notIn: ['string','comment']` to the pair table from claim 5. Monaco's 'neutral character' probe (retokenize the line with the quote replaced by a neutral char, so the tokenizer's own state does not poison the answer) is a workaround for its line-based incremental tokenizer and does NOT transfer: tree-sitter re-parses the real text and gives us the node type directly, so we can skip that trick entirely. Note `AUTO_CLOSE_BEFORE` being one global set rather than per-category is a separate, smaller wart worth fixing in the same pass (quotes and brackets want different before-sets)."
 },
 {
  "domain": "folding-brackets",
  "title": "Comment tokens resolved at the caret's embedded language, plus insertion-point column normalization",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/comment/browser/lineCommentCommand.ts:87-89, 197, 291 (getLanguageIdAtPosition), 115-190 (_analyzeLines), 438-480 (_normalizeInsertionPoint); blockCommentCommand.ts:174",
  "mechanism": "Every comment operation resolves the comment config from `model.getLanguageIdAtPosition(startLineNumber, 1)` after `tokenizeIfCheap`, not from the document's language \u2014 so `//` inside a `<script>` in HTML and `/* */` inside `<style>` are both correct, and a JS block inside Markdown comments as JS. `_analyzeLines` (115-190) then decides add-vs-remove with three careful rules: a line that is whitespace-only is ignored rather than counted as 'already commented'; if the whole selection is whitespace-only the decision flips to *add* and the ignore flags are cleared (175-185); and a trailing space after the token is removed on uncomment only when `insertSpace` is set (166-173). Finally `_normalizeInsertionPoint` (438-480) computes the minimum *visible* column across all target lines (tab-aware, via `nextVisibleColumn`), floors it to a multiple of the indent size, and inserts every `//` at that same visible column \u2014 so commenting a block of mixed-indent lines produces aligned markers rather than a ragged left edge.",
  "whyItMatters": "Two concrete defects for us: (a) commenting a JS region inside an HTML or Markdown file inserts `<!--` because our lookup is on the document language; (b) commenting a nested block inserts each `//` at that line's own first-non-whitespace column, so the markers stagger with the code instead of aligning \u2014 and uncommenting a block where one line was blank behaves inconsistently.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/editActions.ts:442, 1145-1150 \u2014 `commentTokensForLanguage(options.languageId)` where `languageId` is threaded from `Editor.ts:1557 languageId: this.languageId`, the whole-document language. editActions.ts:566-578 `lineCommentEdits` inserts at `firstNonWhitespaceOffset(map, row)` per row, with no common-column normalization. `shouldUncommentLineComments` (581-588) does correctly filter blank lines but has no all-blank flip. We already carry `EditorSyntaxResult.injections` (syntax/session.ts:37-42, 117) giving `{parentLanguageId, languageId, startIndex, endIndex}`.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Change `EditorEditActionOptions.languageId` to `languageIdAt: (offset: number) => string | null`, implemented in Editor.ts by binary-searching the current result's `injections` for the offset and falling back to `this.languageId`. Call it with the selection's start offset in `commentLineAction`/`blockCommentAction` (editActions.ts:437-460). Port `_normalizeInsertionPoint` as a helper over the existing `LineMap`: compute min visible column with a tab-aware scan, floor to `tabSize`, and emit each insertion at that column. Tests exist at packages/editor/test/editor-helpers.test.ts and editor.test.ts.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, plus one extra defect the surveyor missed. Comment config is resolved once from the document language: editActions.ts:442 and :457 call `commentTokensForLanguage(options.languageId)` (definition at editActions.ts:1145-1149), and that languageId is the whole-document one threaded from Editor.ts:1557. `lineCommentEdits` (editActions.ts:570-586) inserts at `firstNonWhitespaceOffset(map, row)` independently per row \u2014 no common column. `shouldUncommentLineComments` (editActions.ts:581-590) does filter blank rows before deciding add-vs-remove, as claimed. EXTRA FINDING: the add branch (editActions.ts:581-584) maps over *all* `rows` including blank ones, so commenting a selection that spans a blank line writes `// ` into the blank line \u2014 Monaco skips blank lines on add unless every line is blank. Injections are confirmed available for the fix: `EditorSyntaxInjection {parentLanguageId, languageId, startIndex, endIndex}` at packages/editor/src/syntax/session.ts:37-42, carried on every result (session.ts:111) and produced by the worker's injection pass (packages/tree-sitter/src/treeSitter/treeSitter.worker.ts:794-881).",
  "verifierNotes": "Keep. Directly relevant to us rather than theoretical: we ship markdown injections (packages/tree-sitter-languages/src/queries/markdown-injections.scm) and markdown is mapped to HTML comment tokens (editActions.ts:572), so commenting a line inside a fenced ```ts block in a markdown file inserts `<!-- -->` today. Resolving tokens from the innermost injection covering the selection start is a lookup over an array we already have. Bundle three fixes in one pass: injection-scoped token lookup, common-column insertion (min firstNonWhitespace over the non-blank rows), and the blank-line skip on add."
 },
 {
  "domain": "folding-brackets",
  "title": "Linked editing: mirrored ranges kept in sync with minimal prefix/suffix-trimmed edits",
  "kind": "feature",
  "source": "both",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/linkedEditing/browser/linkedEditing.ts:47-51 (AlwaysGrowsWhenTypingAtEdges decoration), 147-176 (two-stage debounce), 179-249 (_syncRanges), 276-365 (updateRanges + wordPattern capture); references/codemirror5/addon/edit/matchtags.js:34-52; references/codemirror5/addon/fold/xml-fold.js:91-182 (findMatchingOpen/Close)",
  "mechanism": "A provider returns a set of ranges that must stay identical (an HTML tag's open and close name). Each becomes a decoration with `TrackedRangeStickiness.AlwaysGrowsWhenTypingAtEdges` so typing at either boundary extends the range instead of escaping it. Two schedulers are separated on purpose (147-155): a slow `Delayer` re-asks the provider where the ranges *are*, and a fast one re-syncs their *contents*. `_syncRanges` (179-249) reads range 0 as the reference, then \u2014 this is the part that matters \u2014 for each mirror it strips the common prefix and common suffix between old and new text and emits an edit covering only the differing middle (218-236). That keeps undo granular, keeps other decorations and the caret stable inside the mirrors, and produces no edit at all when nothing changed. A guard breaks the link when the reference no longer matches `wordPattern` (193-200) \u2014 so typing `>` or a space in a tag name dissolves the linkage rather than corrupting the closing tag. Edits are wrapped in `popUndoStop` + preserved `prevEditOperationType` (242-247) so the sync merges into the user's own undo step. CM5 reaches the same UX from the other direction: `findMatchingTag` (xml-fold.js:151-178) scans with a token iterator and `matchtags.js` merely highlights, re-running on viewport change when the match failed because the partner was off-screen.",
  "whyItMatters": "Renaming a JSX/HTML tag and having the closing tag follow is one of the highest-perceived-value features in a web editor, and the same machinery gives 'highlight matching tag'. Our editor already has every prerequisite \u2014 anchors, range decorations, a snippet session that drives multiple synchronized regions \u2014 so this is assembly rather than invention.",
  "ourStatus": "missing",
  "ourEvidence": "`grep -riE 'linkedEditing|renameTag|matchTag'` over packages/*/src returns nothing. We have the parts: packages/editor/src/editor/rangeDecorations.ts, packages/editor/src/editor/occurrences.ts (document highlights, commit c23c2f9), packages/editor/src/editor/snippetSession.ts (multi-region tab stops), packages/editor/src/pieceTable/anchors.ts, and packages/lsp (rename landed in 0b91f75). packages/tree-sitter-languages/src/queries/jsx-folds.scm shows we already parse JSX.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `packages/editor/src/editor/linkedEditing.ts`. Range source: either an LSP `textDocument/linkedEditingRange` request through packages/lsp, or purely local \u2014 walk the tree-sitter tree for the JSX/HTML element containing the caret and take its opening and closing name nodes (CM5's findMatchingTag is the fallback shape if no tree is available). Hold the ranges as piece-table anchors with right/left bias matching `AlwaysGrowsWhenTypingAtEdges`. On each committed edit inside range 0, run Monaco's prefix/suffix trim and apply the minimal mirror edits through the existing `editChain.ts` so they merge into one undo unit. Break the link when the reference text stops matching the language's `wordPattern` (which the language-configuration finding introduces). `snippetSession.test.ts` is a good template for the test shape.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed absent: grep for linkedEditing/LinkedEditing/renameTag/matchTag across every packages/*/src (including packages/lsp and packages/lsp-plugin) returns zero hits. The component parts are all present as claimed \u2014 packages/editor/src/editor/rangeDecorations.ts, packages/editor/src/editor/occurrences.ts, packages/editor/src/editor/snippetSession.ts (multi-region tab stops with anchors), packages/editor/src/pieceTable/anchors.ts (`anchorBefore`/`anchorAfter`/`resolveAnchor`, already used for fold identity in foldMap.ts:1,116-117), and packages/tree-sitter-languages/src/queries/jsx-folds.scm confirming JSX is parsed. One adjacent capability the claim missed: `editor.action.changeAll` and `editor.action.selectHighlights` already exist in the command union (packages/editor/src/editor/commands.ts:31-32), i.e. we can already place carets on all occurrences and type once \u2014 the manual version of the same outcome.",
  "verifierNotes": "Genuinely missing, moderate value, moderate effort \u2014 rank it below claims 2, 7, 11. The mechanism worth copying is specifically the two-scheduler split (a slow delayer re-asking *where* the mirrored ranges are, a fast one re-syncing *what* they contain): it is what stops a stale provider answer from corrupting text mid-keystroke, and it is not obvious. Our anchors give us stickiness for free \u2014 `anchorBefore`/`anchorAfter` is the direct analogue of TrackedRangeStickiness.AlwaysGrowsWhenTypingAtEdges, and foldMap.ts already demonstrates the growth-at-boundaries semantics. Cheapest first version: a tree-sitter query returning the two tag-name nodes of a JSX element, mirrored through a snippetSession-style multi-region edit. Skip the prefix/suffix trimming until it demonstrably matters."
 },
 {
  "domain": "folding-brackets",
  "title": "Continue-list on Enter, including empty-item termination and renumbering of following items",
  "kind": "feature",
  "source": "codemirror5",
  "referenceLocation": "references/codemirror5/addon/edit/continuelist.js:14-63 (newlineAndIndentContinueMarkdownList), 67-100 (incrementRemainingMarkdownListNumbers)",
  "mechanism": "One regex triple does the work: `listRE = /^(\\s*)(>[> ]*|[*+-] \\[[x ]\\]\\s|[*+-]\\s|(\\d+)([.)]))(\\s*)/` captures indent, bullet, number, delimiter and trailing space; `emptyListRE` recognises a bullet with nothing after it. On Enter: if the line is an empty list item, the bullet is *deleted* and a bare newline inserted (43-51) \u2014 pressing Enter twice ends the list, which is the behaviour users expect and the reason a naive implementation feels broken. Otherwise the bullet is reproduced, with an ordered number incremented and a checkbox `[x]` reset to `[ ]` (line 55). It also declines to continue when the caret is before the bullet (`cursorBeforeBullet`, 38-39) or the selection is non-empty. `incrementRemainingMarkdownListNumbers` (67-100) then walks forward renumbering subsequent items at the same indent, skipping deeper-indented sublists and stopping when indentation decreases \u2014 so inserting an item in the middle of a numbered list renumbers the rest.",
  "whyItMatters": "We ship a Markdown package with inline replacements and a WYSIWYG-style view, which makes list editing a core interaction rather than a nicety. Today Enter inside `- item` yields a bare indented line and the user retypes the bullet every time.",
  "ourStatus": "missing",
  "ourEvidence": "`grep -riE 'continueList|listRE|bullet'` over packages/*/src returns nothing. packages/editor/src/editor/indentation.ts:41-53 `lineBreakIndent` copies leading whitespace only. packages/markdown/src/replacements.ts exists (and is modified in the working tree) but handles inline replacement rendering, not Enter behaviour. autoClose.ts:18-23 already distinguishes prose languages via `PROSE_PAIRS`, so the language hook point exists.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Implement as an `onEnterRules`-shaped rule for markdown once the language-configuration finding lands, or standalone first: in `inputSelectionController.insertLineBreak` (inputSelectionController.ts:191-215), before calling `lineBreakIndent`, match the current line against `listRE`; on `emptyListRE` emit a delete-of-bullet plus newline, else emit `\\n + indent + nextBullet`. Renumbering is a second, optional edit batch over following lines \u2014 our `applyEdits` already takes an array. Keep CM5's guards (collapsed caret only, caret not before the bullet) or the feature will fight multi-cursor and mid-bullet edits.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed missing as behaviour, but the claim's grep evidence is wrong on one point: `bullet` DOES appear under packages/*/src \u2014 /Users/shaul/Desktop/D/Editor/packages/markdown/src/replacements.ts:38-39 already defines `BULLET_MARKER = /^[-*+](\\s*)$/` and `QUOTE_MARKER = /^>(\\s*)$/`, used at :111-123 to substitute bullet/quote glyphs for display only. Enter behaviour is confirmed untouched by any of it: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/inputSelectionController.ts:174 routes '\\n' to `applyLineBreak` (:193-221), which calls `lineBreakIndent` (packages/editor/src/editor/indentation.ts:41-53) and copies leading whitespace only \u2014 no list awareness, no language input. There is no ordered-list or task-list regex anywhere, and no renumbering code. Language hook point confirmed to exist: autoClose.ts:18-23,33-34 already special-cases markdown/md via PROSE_PAIRS.",
  "verifierNotes": "Best value-to-effort ratio of the twelve, and the one most specific to this project: markdown here is a live editing view over the same piece-table buffer, so Enter inside a list is a core interaction, not a nicety. Two mechanisms are worth taking verbatim from the CM5 addon and are the parts people get wrong when reimplementing: (1) Enter on an *empty* list item deletes the bullet and inserts a bare newline, so pressing Enter twice ends the list \u2014 without this the feature is actively annoying; (2) blockquote `>` and task-list `[x]`/`[ ]` markers are handled by the same regex as bullets, so they continue too. Renumbering following ordered items is optional and can land later. The `[-*+]` recognizer already exists in replacements.ts:38 and should be shared rather than duplicated. Do not gate this on the full LanguageConfiguration record \u2014 one markdown-scoped onEnter rule is enough to ship it."
 },
 {
  "domain": "folding-brackets",
  "title": "Reindent lines / reindent selection driven by indentation rules",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/contrib/indentation/browser/indentation.ts:209-235 (DetectIndentation), 237-264 (ReindentLinesAction), 266-320 (ReindentSelectedLinesAction); common/indentation.ts getReindentEditOperations; common/languages/autoIndent.ts:219-302 (getGoodIndentForLine); supports/indentationLineProcessor.ts:172-225 (getProcessedTokens)",
  "mechanism": "`getReindentEditOperations` recomputes each line's indentation from `getGoodIndentForLine`, which combines the inherited indent from above with the current line's `decreaseIndentPattern`, and emits one replace-edit per line whose indentation actually changes. The correctness detail worth stealing is `IndentationLineProcessor.getProcessedTokens` (indentationLineProcessor.ts:198-225): before any indentation regex runs, it walks the line's tokens and *deletes the language's bracket characters from String, RegEx and Comment tokens*, then rebuilds the line. So `const s = \"}\"` does not trigger `decreaseIndentPattern`, and a `{` inside a comment does not increase indent. `ProcessedIndentRulesSupport` (22-67) wraps every predicate behind that filter, so no caller can forget it. `isLanguageDifferentFromLineStart` (227-235) additionally detects when the caret's scoped language differs from the line's starting language, which is how embedded-language indentation avoids using the host language's rules.",
  "whyItMatters": "Reindent-selection is the standard cleanup gesture after a paste, and paste-with-correct-indentation depends on the same primitive. Beyond the command itself, the token-masking idea is a correctness fix that applies to any indentation heuristic we ever write, including the auto-indent-on-Enter improvements: without it, brackets inside strings and comments corrupt the answer.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/commands.ts:44-45 has only `editor.action.indentLines` / `outdentLines`, which shift by a fixed unit (editActions.ts:462-476) with no rule awareness. packages/editor/src/editor/indentation.ts:67-71 `indentUnit` infers tab-vs-spaces from the current line's leading whitespace, per line, with no document-wide detection. `grep -riE 'reindent|detectIndentation'` over packages/*/src returns nothing.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Depends on `indentationRules` from the language-configuration finding. Add `editor.action.reindentlines` / `editor.action.reindentselectedlines` to commands.ts and implement in editActions.ts over the existing `LineMap`: for each row compute the target indentation from the previous non-ignored row plus the current row's decrease pattern, and emit a replace edit for the leading-whitespace range only when it differs. Implement the token mask by intersecting each row with the `EditorSyntaxCapture[]` for `string`/`comment` captures and blanking bracket characters inside them before matching \u2014 the same capture lookup the auto-close finding needs. Also worth adding `detectIndentation` over the first N lines to replace the per-line inference in indentation.ts:67-71.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. The only indent commands are `editor.action.indentLines` / `editor.action.outdentLines` (packages/editor/src/editor/commands.ts:44-45), implemented by `indentLinesAction` (packages/editor/src/editor/editActions.ts:463-477) as a fixed shift \u2014 `indentLineEdits(map, rows, options.indentText ?? '\\t')` or `outdentLineEdits(map, rows, tabSize)` \u2014 with no rule awareness. Indent width is never detected from content: `tabSize` is a constructor option normalized once (Editor.ts:264,329) and `indentText` is passed in by the caller (editActions.ts:41,473). The only content-derived indent decision in the editor is `indentUnit` (packages/editor/src/editor/indentation.ts:67-71), which picks tab-vs-spaces from the *current line's* leading whitespace, per line, exactly as claimed. No reindent or detectIndentation symbol exists under packages/*/src.",
  "verifierNotes": "Keep, but rank it last and flag the dependency: `getReindentEditOperations` is meaningless without `indentationRules` (increase/decrease patterns), so this cannot ship before claim 5 \u2014 implemented alone it would be a no-op or a guess. Two things here are worth doing independently and sooner, and both are cheap: (a) content-based indent detection at load, replacing the per-line guess in indentation.ts:67-71, which today makes a mixed-indentation file drift line by line; (b) Monaco's `IndentationLineProcessor.getProcessedTokens` idea \u2014 strip string and comment content before matching any indent pattern \u2014 which we can do for free from the captures we already retain (Editor.ts:591-592), and which is the difference between a reindent that is safe on a file containing `// }` and one that is not. If effort is constrained, take (a) and drop the rest."
 },
 {
  "domain": "find-replace",
  "title": "Search materializes the entire document as one string; neither reference ever does",
  "kind": "performance",
  "source": "both",
  "referenceLocation": "vscode/src/vs/editor/common/model/textModelSearch.ts:226-288 (_doFindMatchesLineByLine / _findMatchesInLine), :169-173 (multiline vs line-by-line dispatch), :29-66 (SearchParams.parseSearchRequest), :69-99 (isMultilineRegexSource); codemirror5/addon/search/searchcursor.js:33-43 (searchRegexpForward), :45-73 (searchRegexpForwardMultiline)",
  "mechanism": "Monaco classifies the query first: `parseSearchRequest` decides `multiline` from the *pattern source* (`isMultilineRegexSource` returns true only for a literal \\n, or an escape \\n/\\r/\\W; a non-regex query is multiline only if it contains \\n). If not multiline, `_doFindMatchesLineByLine` walks `model.getLineContent(lineNumber)` line by line and never concatenates \u2014 the piece tree is read one line at a time, and matches are produced as (line, column) ranges directly. Only when the pattern truly needs to cross lines does `_doFindMatchesMultiline` call `getValueInRange(range, EndOfLinePreference.LF)`. CM5 goes further: `searchRegexpForwardMultiline` grows its search buffer in *exponentially sized chunks* (`chunk = chunk * 2`, starting at 1 line) between retries, with the comment that this makes nearby matches fast \"and don't require concatenating the whole document\". The regex is re-run against a 1-line, then 2-line, 4-line... window until it matches.",
  "whyItMatters": "On a 20MB file our Cmd+F does a full red-black-tree walk plus a chunks.join('') allocation of the whole document before a single character is compared \u2014 and it does it again after every edit and (via findOnType) on every keystroke in the find box. Line-scoped search makes the common case (plain string, single-line regex) proportional to the number of lines actually scanned rather than to document size, and the CM5 chunking makes even multiline regex 'find next' near the cursor cheap.",
  "ourStatus": "missing",
  "ourEvidence": "packages/find/src/search.ts:30-48 `findMatches(text: string, ...)` \u2014 the whole API takes a materialized string. packages/find/src/findController.ts:370-381 `findAll()` calls `host.materializeFullText()`; packages/find/src/plugin.ts:243 wires that to `getSnapshot().fullText`. packages/editor/src/editor/Editor.ts:2152 builds a *fresh* snapshot object per `getSnapshot()` call, so `defineLazyFullTextProperty`'s cache (documentTextSnapshot.ts:23-36) does not survive between calls; packages/editor/src/documentSession.ts:414,435,517,958 create post-edit snapshots with no `retainedText`, so `materializePieceTableFullText` (pieceTable/reads.ts:30-31) really re-walks the tree. Our compileFindQuery (search.ts:89) unconditionally sets the `m` flag and always scans the full text \u2014 there is no multiline/single-line classification at all.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Change `findMatches` to take a text *source* rather than a string: `{ length, readRange(start,end), lineStartsView }`. We already have both \u2014 `TextSnapshot.readRange` (packages/editor/src/documentTextSnapshot.ts:14) and `EditorLineStartsView.at/indexForOffset/firstIndexAtOrAfter` (packages/editor/src/plugins.ts:171-177) \u2014 and `EditorViewSnapshot` already exposes `textSnapshot` and `lineStartsView`. Add a `isMultilineQuery(query)` port of isMultilineRegexSource; when false, loop lines via `lineStartsView.at(i)`/`at(i+1)` and `readRange` per line, drop the `m` flag (a single-line haystack makes ^/$ correct for free), and offset match indices by the line start. When true, use CM5's doubling-window strategy over `readRange`. Keep the current whole-string path as a fallback for the string-based public API and tests.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. /Users/shaul/Desktop/D/Editor/packages/find/src/search.ts:30 `findMatches(text: string, ...)` is the only entry point; search.ts:89 `const flags = query.matchCase ? 'gmu' : 'gimu'` \u2014 `m` is unconditional and there is no multiline classification anywhere in packages/find. findController.ts:375 `host.materializeFullText()` inside `findAll`, and the host contract itself (findController.ts:40 `materializeFullText(): string`) exposes nothing else, so a chunked search is not even expressible against the current host. plugin.ts:243 maps it to `getSnapshot().fullText`. The cache really is per-call: Editor.ts:2153 `defineLazyFullTextProperty({...})` wraps a freshly-built object literal every `getSnapshot()`, and documentTextSnapshot.ts:41-53 `createDocumentTextSnapshot` only short-circuits when `materializedText` is passed \u2014 grep shows documentSession.ts:414/435/517 pass no text, so `materializePieceTableFullText` re-walks the tree. Monaco side verified: references/vscode/src/vs/editor/common/model/textModelSearch.ts:37 `multiline = isMultilineRegexSource(this.searchString)`, :69 the classifier, :172/:226 `_doFindMatchesLineByLine`.",
  "verifierNotes": "Real and the highest-leverage item in this domain, but the fix is bigger than swapping an algorithm: `EditorFindHost` (findController.ts:38-55) only offers `materializeFullText()`. We already own the primitives \u2014 `TextSnapshot.readRange`/`forEachTextChunk` (documentTextSnapshot.ts:12-17), `LineStartsView` (virtualization/lineStartIndex.ts:82), `streamPieceTableTextChunks` (pieceTable/reads.ts:33) \u2014 so the work is (a) widen the host to expose line/range reads, (b) port `isMultilineRegexSource`, (c) a line-by-line loop. Note our whole codebase leans on full-text materialization (occurrences.ts, seedSearchString, LSP), so find alone won't remove the cost; it will remove the per-keystroke cost, which is the one users feel."
 },
 {
  "domain": "find-replace",
  "title": "Case-insensitive plain search folds the whole haystack, which both wastes memory and corrupts offsets",
  "kind": "correctness",
  "source": "both",
  "referenceLocation": "vscode/src/vs/editor/common/model/textModelSearch.ts:59-63 (canUseSimpleSearch guard); codemirror5/addon/search/searchcursor.js:125-146 (doFold/noFold + adjustPos), :148-174 (searchStringForward)",
  "mechanism": "Monaco refuses to take the fast indexOf path whenever folding could matter: `let canUseSimpleSearch = (!this.isRegex && !multiline); if (canUseSimpleSearch && this.searchString.toLowerCase() !== this.searchString.toUpperCase()) { canUseSimpleSearch = this.matchCase; }` \u2014 i.e. a case-insensitive query containing any cased character falls back to the `i`-flagged regex, which reports indices in the *original* string. CM5 instead does fold the text (`str.normalize(\"NFD\").toLowerCase()`) but then maps every folded index back through `adjustPos(orig, folded, pos, foldFunc)`, a binary search that re-folds prefixes of the original until the folded length equals the target index \u2014 with the explicit comment 'compensating for codepoints increasing in number during folding'.",
  "whyItMatters": "Case folding is not length-preserving. Verified against our exact code path: in the document `a\u0130stanbul stanbul`, `text.toLocaleLowerCase()` turns U+0130 into two code units, so a case-insensitive search for `stanbul` reports index 3, where the original text reads `tanbul ` \u2014 the highlight is off by one and Replace One writes the replacement over the wrong characters, silently corrupting the user's file. Separately, the fold allocates a full copy of the document per search *range* per research pass.",
  "ourStatus": "missing",
  "ourEvidence": "packages/find/src/search.ts:138 `const haystack = query.regex.ignoreCase ? text.toLocaleLowerCase() : text` \u2014 computed inside `appendSimpleMatches`, i.e. once per search range; lines 141-146 then use the folded index against the unfolded `text` for both the whole-word check and the emitted `{start, end}`. `compileFindQuery` (search.ts:94-95) sets `simpleSearch` for any non-regex query regardless of matchCase, so the folded path is the default for case-insensitive find.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Cheapest correct fix is Monaco's: in `compileFindQuery`, set `simpleSearch` to null when `!query.matchCase && searchString.toLowerCase() !== searchString.toUpperCase()`, letting the existing `gimu` regex path handle it \u2014 that also deletes the whole-document fold allocation. Add a regression test with `\u0130`/`\u1e9e` in the haystack asserting the returned range slices back to the query. If we later want the indexOf speed for case-insensitive ASCII, gate it on the query and the scanned window both being ASCII-only rather than reintroducing folding.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and the corruption is reproducible. search.ts:138 `const haystack = query.regex.ignoreCase ? text.toLocaleLowerCase() : text`, then :141 `haystack.indexOf(needle, ...)` and :146 `matches.push({ start: index, end: index + searchString.length })` \u2014 folded index used as an unfolded offset. search.ts:94-95 sets `simpleSearch` for every non-regex query regardless of `matchCase`, so this is the default path. Verified with node: for `t = '\u0130abc foo'`, `t.length === 8` but `t.toLocaleLowerCase().length === 9`, and `t.toLowerCase().indexOf('foo') === 6` while `t.indexOf('foo') === 5` \u2014 every match after a Turkish dotted capital I (also \ufb01-ligatures etc.) is reported one or more characters off, so find highlights and, worse, Replace All edits land at the wrong offsets. Monaco's gate verified at references/vscode/src/vs/editor/common/model/textModelSearch.ts:59-62.",
  "verifierNotes": "Cheapest high-value fix in the list: three lines in `compileFindQuery` \u2014 refuse `simpleSearch` when `!matchCase && searchString.toLowerCase() !== searchString.toUpperCase()`, letting the already-correct `i`-flagged regex path (search.ts:150) handle it. That also deletes the full-document `toLocaleLowerCase()` allocation. This is a data-corrupting bug, not a perf nit; it should be ranked above claim 1."
 },
 {
  "domain": "find-replace",
  "title": "Replace All silently stops at 19,999 matches; Monaco has a dedicated large-replace path",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/findModel.ts:515-530 (replaceAll dispatch), :532-570 (_largeReplaceAll), :572-584 (_regularReplaceAll), :586-608 (selectAllMatches); vscode/src/vs/editor/contrib/find/browser/findController.ts:424-430 (isTooLargeForHeapOperation guard)",
  "mechanism": "Monaco keeps two distinct limits. `MATCHES_LIMIT = 19999` bounds only the *decorations* used for highlighting and position display. Every mutating operation re-queries with `Constants.MAX_SAFE_SMALL_INTEGER` as the limit: `_regularReplaceAll` and `selectAllMatches` both pass it explicitly with the comment 'Get all the ranges (even more than the highlighted ones)'. And when the whole file is in scope and the count is at the limit, `replaceAll` routes to `_largeReplaceAll`, which skips match enumeration entirely: it takes `model.getValue(EndOfLinePreference.LF)`, forces the regex to `mu` flags, runs a single native `modelText.replace(searchRegex, fn)` (passing `arguments` straight into `buildReplaceString` so capture groups and preserveCase still work), and applies the result as one full-range edit via `ReplaceCommandThatPreservesSelection`. The find widget separately renders '19999+' and a tooltip saying 'Only the first N results are highlighted, but all find operations work on the entire text' (findWidget.ts:76, :457-483).",
  "whyItMatters": "A user replacing a common token across a large file gets a partial replacement with no warning \u2014 the count reads 19999, the operation reports success, and thousands of occurrences are left behind. This is the single worst kind of editor bug: silent data divergence from what the user asked for. Same for Select All Matches, which caps our multi-cursor set at the display limit.",
  "ourStatus": "missing",
  "ourEvidence": "packages/find/src/findController.ts:210-228 `replaceAll()` calls `this.findAll(...)` which hard-codes `FIND_MATCHES_LIMIT` (findController.ts:379, search.ts:8 = 19_999); there is no unlimited variant and no large-file path. `selectAllMatches()` (findController.ts:230-239) uses the already-truncated `this.matches`. Our edit merging (findController.ts:555-573 `mergeAdjacentReplaceEdits`) is a faithful equivalent of Monaco's ReplaceAllCommand merge (replaceAllCommand.ts:46-59), so only the limit is wrong. Widget shows a bare count with no '+' (packages/find/src/findWidget.ts:276-279).",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Add a `limit` parameter to `findAll(captureMatches, limit)` and pass `Number.MAX_SAFE_INTEGER` from `replaceAll` and `selectAllMatches`; keep FIND_MATCHES_LIMIT for `research()` only. Then add the large path: when `this.scopes === null && this.matches.length >= FIND_MATCHES_LIMIT`, build the replacement with a single `String.prototype.replace` over `host.materializeFullText()` and emit one `{from: 0, to: text.length, text: result}` edit. Surface the limit in the widget: `${count}+` plus a title attribute, in `resultCountText` (findWidget.ts:276).",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. `replaceAll` (findController.ts:215) calls `this.findAll(...)`, and `findAll` (findController.ts:370-381) hard-codes `FIND_MATCHES_LIMIT` (search.ts:8 = 19_999) with no override parameter; `selectAllMatches` (findController.ts:236) uses the already-truncated `this.matches`. Widget prints `${position} of ${count}` with no overflow marker (findWidget.ts:276-278 `resultCountText`). The merge logic really is equivalent (findController.ts:555-574 vs references/vscode/src/vs/editor/contrib/find/browser/replaceAllCommand.ts). Monaco's two-limit design verified: findModel.ts:81 `MATCHES_LIMIT = 19999` (decorations only), :522-526 dispatch to `_largeReplaceAll` vs `_regularReplaceAll`, :575 and :594 both pass `Constants.MAX_SAFE_SMALL_INTEGER`.",
  "verifierNotes": "Low effort given our architecture: add a `limit` argument to `findAll` and pass `Number.MAX_SAFE_INTEGER` from `replaceAll`/`selectAllMatches`. Monaco's `_largeReplaceAll` (a single regex replace over the whole text, bypassing enumeration) is a separate optimization we do not need until we care about >100k matches; the correctness half \u2014 Replace All actually replacing all \u2014 is the part worth taking. Also add the `19999+` overflow rendering in `resultCountText` so the cap is visible."
 },
 {
  "domain": "find-replace",
  "title": "Find Next past the match limit jumps to the top of the file instead of to the next match",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/findModel.ts:384-414 (_moveToNextMatch), :289-355 (_moveToPrevMatch), :416-457 (_getNextMatch); vscode/src/vs/editor/common/model/textModelSearch.ts:290-302 (findNextMatch), :331-352 (_doFindNextMatchLineByLine with modular wraparound), :368-395 (findPreviousMatch)",
  "mechanism": "Monaco treats the decoration list as a cache that is only valid below the limit: `if (this._decorations.getCount() < MATCHES_LIMIT) { ...use matchAfterPosition... return; }` and otherwise falls through to `_getNextMatch`, which runs a live incremental `model.findNextMatch(searchString, position, ...)` from the cursor. That live search is inherently unbounded \u2014 `_doFindNextMatchLineByLine` iterates `(startLineNumber + i - 1) % lineCount` so wraparound is a modular walk, and the multiline variant recurses once from Position(1,1). `_getNextMatch` also clamps the start position into the find scope and re-recurses once if the found match escaped the scope. `FindReplaceState.canNavigateInLoop()` (findState.ts:329-331) deliberately returns true when `matchesCount >= MATCHES_LIMIT` so the bounded-navigation guards don't misfire on truncated data.",
  "whyItMatters": "In any file where a token occurs more than 19,999 times (a minified bundle, a log, a large JSON), pressing Enter/F3 anywhere past the truncation point teleports the cursor to the first match at the top of the document, and keeps doing so \u2014 find becomes unusable exactly where the user most needs it.",
  "ourStatus": "missing",
  "ourEvidence": "packages/find/src/search.ts:50-60 `nextMatchAfter` does `matches.find(m => m.start >= offset)` over the truncated array and falls back to `matches[0]` when nothing is found; `previousMatchBefore` (search.ts:62-75) mirrors it with `matches.at(-1)`. packages/find/src/findController.ts:172-188 feeds it `this.matches`, which `research()` (line 363) always fills via the capped `findAll(false)`. There is no incremental single-match search anywhere in packages/find.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `findNextMatchFrom(source, query, offset, captureMatches)` and `findPreviousMatchFrom(...)` to packages/find/src/search.ts \u2014 with the line-scoped source from finding #1 these are cheap and naturally unbounded. In `EditorFindController.findNext/findPrevious`, branch: if `this.matches.length >= FIND_MATCHES_LIMIT`, call the incremental search from the selection edge (clamped into `this.scopes` when set) instead of consulting `this.matches`. Keep the array path below the limit.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. search.ts:57 `matches.find((match) => match.start >= offset)` over the capped array, falling back to `matches[0]` at :59; `previousMatchBefore` mirrors it (:69-74). findController.ts:177/186 feed it `this.matches`, which `research()` (:363) always fills from the capped `findAll(false)`. No incremental single-match search exists in packages/find. Monaco's cache-validity gate verified at references/vscode/src/vs/editor/contrib/find/browser/findModel.ts:300 and :395 `if (this._decorations.getCount() < MATCHES_LIMIT) { ... }` with a fall-through to a live model search otherwise.",
  "verifierNotes": "Correct, but rank it below claims 2/3/6 \u2014 it only bites past 19,999 matches. Worth noting we do already have a 'next exact occurrence from offset' primitive in packages/editor/src/editor/occurrences.ts:78 `findNextExactOccurrenceFromRange` / :99 `findExactOccurrenceFrom` (used by the Cmd+D-style multi-cursor commands), so the shape of an incremental search is not foreign to the codebase \u2014 it is just plain-string-only and full-text-based. A proper fix rides on claim 1's line-by-line searcher; a cheap interim fix is to detect `matches.length === FIND_MATCHES_LIMIT` and re-run `findMatches` over `[cursorOffset, textLength]` with `limit = 1`."
 },
 {
  "domain": "find-replace",
  "title": "Zero-width matches deadlock navigation; both references have an explicit escape and Monaco adds a line-stop rule",
  "kind": "correctness",
  "source": "both",
  "referenceLocation": "codemirror5/addon/search/searchcursor.js:236-274 (SearchCursor.find with afterEmptyMatch), :203-234 (constructor state); vscode/src/vs/editor/contrib/find/browser/findModel.ts:361-382 (_nextSearchPosition), :267-287 (_prevSearchPosition), :396-405 and :441-445 (the 'Looks like we're stuck at this position, unacceptable!' retries); vscode/src/vs/editor/common/model/textModelSearch.ts:513-554 (Searcher.next dedupe + surrogate-aware lastIndex bump)",
  "mechanism": "CM5's cursor records `afterEmptyMatch` after each find and, on the next call, manually advances the head by one character (wrapping across the line boundary and re-clipping) before searching, 'do not return the same 0 width match twice'. Monaco detects the same condition post-hoc \u2014 `nextMatchRange.isEmpty() && nextMatchRange.getStartPosition().equals(after)` \u2014 and retries from `_nextSearchPosition(after)`. The clever part is that `_nextSearchPosition` is *pattern-aware*: `const isUsingLineStops = this._state.isRegex && (searchString.indexOf('^') >= 0 || searchString.indexOf('$') >= 0)`; when the pattern is anchored, advancing one column is useless (the next `^` is on the next line), so it jumps to line+1, column 1. Monaco's `Searcher.next` additionally dedupes identical consecutive matches and, for empty ones, bumps `lastIndex` by 2 when `getNextCodePoint(...) > 0xFFFF` so surrogate pairs aren't split.",
  "whyItMatters": "Search `$` or `^` or `\\b` in regex mode, press Enter: the caret lands on a zero-width match, and every subsequent Enter re-selects the same zero-width range because `match.start >= selection.endOffset` is satisfied by the match itself. Find Previous deadlocks identically via `match.end <= selection.startOffset`. Anchored regex navigation \u2014 a very common power-user flow \u2014 is simply broken.",
  "ourStatus": "partial",
  "ourEvidence": "We handle the *enumeration* side: packages/find/src/search.ts:190-196 `advancePastEmptyMatch` bumps lastIndex by the code-point size, and search.test.ts:83-90 asserts `(?=)` yields 3 matches on 'ab'. But navigation has no equivalent: packages/find/src/search.ts:50-60 `nextMatchAfter` uses `match.start >= offset` with `offset = selection.endOffset` (findController.ts:175-177), and :62-75 `previousMatchBefore` uses `match.end <= offset` with `offset = selection.startOffset` (findController.ts:184-186). Both re-return a zero-width match sitting exactly at the cursor. There is no line-stop concept anywhere.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Give `nextMatchAfter`/`previousMatchBefore` an `excludeEmptyAt: number | null` argument, or simpler: in `EditorFindController.findNext`, if the returned match is empty and `match.start === startOffset`, recompute from `nextSearchOffset(text, startOffset, isRegex && /[\\^$]/.test(searchString))` \u2014 advancing to the next line start when anchored, otherwise by one code point via the existing `codePointSizeAt` (already imported in search.ts:2). Mirror for findPrevious using `lineRangeAtOffset` (packages/editor/src/textRanges.ts:35-41) for the line-stop case.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed as partial. Enumeration is handled: search.ts:190-196 `advancePastEmptyMatch` bumps `lastIndex` by `codePointSizeAt`. Navigation is not: `selectMatch` (findController.ts:395) sets the selection to `(match.start, match.end)` \u2014 collapsed for a zero-width match \u2014 and `findNext` (findController.ts:175-177) then passes `selection.endOffset` into `nextMatchAfter`, whose predicate `match.start >= offset` re-returns the very same match. Identical trap in `findPrevious` via `match.end <= offset`. Nothing in packages/find mentions a line-stop or an empty-match retry. Both references verified: references/codemirror5/addon/search/searchcursor.js:205/242/263 (`afterEmptyMatch` set at :263, consumed at :242), and references/vscode/src/vs/editor/contrib/find/browser/findModel.ts:303-306 (`prevMatchRange.isEmpty() && ...equals(before)` \u2192 retry from `_prevSearchPosition`).",
  "verifierNotes": "Genuinely reachable \u2014 `^`, `\\b`, `(?=x)`, `$` are ordinary user queries and F3 currently sticks in place forever. Small fix contained to `nextMatchAfter`/`previousMatchBefore` plus their two call sites: when the returned match is empty and sits exactly at the cursor, advance the probe offset by one code point (clamped/wrapped) and retry once."
 },
 {
  "domain": "find-replace",
  "title": "Find-in-selection scope is recomputed from the live selection, so it is destroyed by the first replace and silently widens to the whole document",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/findDecorations.ts:65-75 (getFindScopes reads back tracked decoration ranges), :219-225 (scopes stored via accessor.addDecoration), :343-347 (_FIND_SCOPE_DECORATION); vscode/src/vs/editor/contrib/find/browser/findModel.ts:199-214 (research normalizes multi-line scopes to whole lines); vscode/src/vs/editor/contrib/find/browser/findController.ts:281-303 (toggleSearchScope trims a scope ending at column 1)",
  "mechanism": "Monaco stores the find scope as a *tracked decoration* on the model, not as a selection. `getFindScopes()` reads the current ranges back out of the decoration ids, so every edit \u2014 including the find widget's own replaces \u2014 shifts the scope automatically and it survives arbitrary cursor movement. On top of that, both `toggleSearchScope` and `research` normalize a multi-line scope to whole lines: if `findScope.endColumn === 1` the last line is dropped, then the scope is rewritten as `(startLineNumber, 1) \u2192 (endLineNumber, getLineMaxColumn)`. So dragging a selection down to the start of line 4 gives a 3-line scope, and the scope highlight is whole-line.",
  "whyItMatters": "Two concrete failures in ours. (1) Replace One inside a selection scope: `replaceOne` sets a collapsed selection at the replacement point, then the resulting content update recomputes scopes from selections, gets an empty list, and `normalizedSearchRanges` treats an empty range list as 'search everything' \u2014 so the second Replace/Replace All escapes the scope and rewrites the whole file. (2) Even before that, `research()` after an edit uses the *pre-edit* scope offsets, so matches are computed against a window that has already shifted.",
  "ourStatus": "missing",
  "ourEvidence": "packages/find/src/findController.ts:311-313 in `handleViewUpdate`: `if (this.state.inSelection) this.scopes = nonEmptySelectionRanges(this.host?.getSelections() ?? [])` \u2014 and `nonEmptySelectionRanges` (findController.ts:521-527) filters out collapsed selections. packages/find/src/search.ts:107 `if (!ranges || ranges.length === 0) return [{ start: 0, end: text.length }]` is the widening step. `replaceOne` (findController.ts:201-206) hands the edit host a collapsed selection, then calls `research(true)` with the stale `this.scopes`. `toggleFindInSelection` (findController.ts:271-286) stores raw selection offsets with no whole-line normalization.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "We already have the primitive: `anchorBefore`/`anchorAfter` with bias (packages/editor/src/pieceTable/anchors.ts:191-212) plus `resolveAnchor` at :235. Store scopes as anchor pairs on the controller when `toggleFindInSelection`/`applyAutoFindInSelection` run, resolve them against the current snapshot inside `findAll`, and delete the recompute-from-selection in `handleViewUpdate`. Separately, make `normalizedSearchRanges` distinguish 'no scope' (null) from 'empty scope' (a scope that collapsed to nothing must yield zero matches, never the full document). Add the whole-line normalization from findModel.ts:202-213 when the scope spans multiple lines.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and the failure is worse than 'widens' \u2014 it is a silent whole-document Replace All. Trace: `replaceOne` (findController.ts:201-205) applies the edit with a collapsed selection; the resulting view update reaches `handleViewUpdate`, which passes the `kind === 'content'` guard (:304 only skips `selection`/`none` changes) and hits :311-312 `if (this.state.inSelection) this.scopes = nonEmptySelectionRanges(...)`; `nonEmptySelectionRanges` (:521-527) filters collapsed selections, so `this.scopes` becomes `[]`; `findAll` passes `[]` to `normalizedSearchRanges`, which at search.ts:107 treats `ranges.length === 0` identically to `null` and returns `[{start: 0, end: text.length}]`. `toggleFindInSelection` (:271-286) stores raw offsets with no whole-line normalization. Monaco stores the scope as tracked decorations (findModel.ts:199/520/591 `this._decorations.getFindScopes()`) and normalizes multi-line scopes to whole lines at findModel.ts:203-206.",
  "verifierNotes": "Highest-severity item here: 'Replace All in selection' can rewrite the whole file once the first replace lands. Two independent fixes, both cheap. (1) Never conflate empty with null \u2014 an explicitly-empty scope array should yield zero matches, not the whole document (search.ts:107). (2) Track the scope through edits: we already ship the machinery \u2014 `Anchor`/`AnchorBias`/`resolveAnchor` in packages/editor/src/pieceTable/anchors.ts, exported from the public document API \u2014 so a scope held as an anchor pair survives replaces exactly like Monaco's tracked decoration. Whole-line normalization is a nice-to-have on top."
 },
 {
  "domain": "find-replace",
  "title": "Every content change triggers a synchronous full re-search; both references debounce it and repair incrementally",
  "kind": "performance",
  "source": "both",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/findModel.ts:105-134 (RunOnceScheduler(\u2026, 100) on model content change), :156-177 (_onStateChanged with RESEARCH_DELAY=240 gated on isTooLargeForSyncing), :81-82 (constants); vscode/src/vs/editor/contrib/find/browser/findDecorations.ts:157-227 (matches held as deltaDecorations, so ranges shift without re-searching); codemirror5/addon/search/matchesonscrollbar.js:60-91 (gap-based incremental repair)",
  "mechanism": "Monaco never re-searches synchronously on typing: content changes only `schedule()` a 100ms RunOnceScheduler, and query/option changes on a model over 50MB go through a 240ms `TimeoutTimer` instead of running immediately. Between re-searches, the match ranges stay correct because they are model decorations with `NeverGrowsWhenTypingAtEdges` stickiness \u2014 the model shifts them for free. CM5's scrollbar annotation does true incremental repair: `onChange` shifts every cached match's line by `sizeChange` via `offsetLine`, records the edited region as a `gap`, and 250ms later `findMatches()` splices out only the matches inside the gap and re-runs a SearchCursor over just that region.",
  "whyItMatters": "With the find widget open, every keystroke in the document currently costs a full piece-tree materialization plus a full-document scan plus (for case-insensitive search) a full-document `toLocaleLowerCase()`, all on the main thread before the frame can paint. That is the difference between typing smoothly and typing at 4fps in a large file with find open.",
  "ourStatus": "missing",
  "ourEvidence": "packages/find/src/findController.ts:298-314 `handleViewUpdate` calls `this.research(false)` directly for every `document`/`content` update, with no scheduler. `research()` (findController.ts:360-368) calls `findAll` \u2192 `materializeFullText` \u2192 full scan, then `updateHighlights` + `updateWidget`. `setSearchString` (findController.ts:288-291) also researches synchronously per keystroke when `findOnType` is on (default true, findController.ts:505). We do have schedulers available (packages/editor/src/editor/workScheduler.ts:115 `EditorWorkScheduler`, secondaryWorkScheduler.ts:21) but find uses neither.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Two stages. Cheap first: route `handleViewUpdate`'s research through a coalescing timer (100ms) and `setSearchString` through a short debounce, keeping the current match list visible in between. Then the real win: keep matches as anchor-backed ranges (pieceTable/anchors.ts) so they shift with edits and only the edited region needs rescanning \u2014 CM5's `gap` model maps directly onto the `DocumentSessionChange` we already receive in `handleViewUpdate`, since we know the edited offset range and the length delta.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. `grep -n 'schedule|debounce|setTimeout|requestIdle' packages/find/src/*.ts` returns nothing. findController.ts:298-313 `handleViewUpdate` calls `this.research(false)` inline for every document/content update; `setSearchString` (:288-291) researches per keystroke because `findOnType` defaults true (:505). `EditorWorkScheduler` exists and is unused by find (packages/editor/src/editor/workScheduler.ts:115, with injectable timers). Monaco verified: references/vscode/src/vs/editor/contrib/find/browser/findModel.ts:93/105-110 `new RunOnceScheduler(..., 100)`, and the `TimeoutTimer` import at :7.",
  "verifierNotes": "Correct, but the survey understates a dependency: debouncing alone would be a regression for us. Monaco can defer re-search because its matches are model decorations with `NeverGrowsWhenTypingAtEdges` stickiness that the model shifts for free; our matches are raw `{start,end}` offsets pushed straight into `setRangeHighlight` (findController.ts:405), so a deferred re-search means highlights painted at stale offsets while the user types. Sequence this after claim 6's anchor-tracked ranges (or shift pending match offsets by the applied edit's delta before deferring). With that in place, `EditorWorkScheduler.schedule({key: 'find.research', ...})` is a few lines."
 },
 {
  "domain": "find-replace",
  "title": "Highlight painting is O(mounted rows \u00d7 total matches) per frame, with no visible-window index",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/findDecorations.ts:157-207 (deltaDecorations into the model's interval tree), :229-271 (matchBeforePosition/matchAfterPosition), :102-112 (getCurrentMatchesPosition via getDecorationsInRange)",
  "mechanism": "Monaco hands the match ranges to the model's decoration store (an interval tree). Rendering then asks only for decorations intersecting the rendered line range, so the per-frame cost scales with matches *in view*, not with total matches. Even the 'which match am I on' query is a range query: `getCurrentMatchesPosition` calls `getDecorationsInRange(desiredRange)` and matches on decoration identity, rather than scanning the array. Monaco explicitly caps the *other* consumer instead: when `findMatches.length > 1000`, it switches to `_FIND_MATCH_NO_OVERVIEW_DECORATION` and emits merged coarse bands for the overview ruler, 'because the overview ruler paints all the decorations in the file and we don't want to cause freezes' (findDecorations.ts:163-197).",
  "whyItMatters": "Search for `e` in a big file, get ~19,999 matches, and every scroll frame walks 19,999 ranges for each of ~60 mounted rows \u2014 over a million interval tests per frame \u2014 before any DOM range is created. This is a hard scroll stall triggered by an entirely ordinary search.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/virtualization/virtualizedTextViewHighlights.ts:1254-1260 `addMountedRangeHighlightRanges` loops `getMountedRows(view)`, and :1263-1270 `addMountedRangeHighlightRangesForRow` loops `for (const range of group.ranges)` \u2014 a full pass over every range for every row, with the cheap rejection at :1277 happening only after the inner loop is entered. The find controller pushes the whole match array in one call: packages/find/src/findController.ts:405 `host.setRangeHighlight(this.matchHighlightName, this.matches, FIND_MATCH_STYLE)`. Similarly `findMatchIndex` (search.ts:77-79) and `currentMatchIndex` (findController.ts:533-541) are linear scans run on every navigation.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Ranges arrive sorted (search.ts appends in document order per range, and `normalizeTextOffsetRanges` sorts the scopes). Store `group.ranges` as a sorted array plus a binary search, and in `addMountedRangeHighlightRangesForRow` seek to the first range with `end > row.startOffset` and stop at the first with `start >= row.endOffset` \u2014 that alone turns the inner loop from O(all) into O(in-view). Use the same binary search for `findMatchIndex`/`currentMatchIndex`.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and it is twice as bad as claimed. virtualizedTextViewHighlights.ts:1254-1260 `addMountedRangeHighlightRanges` loops mounted rows, :1263-1270 loops every range per row, and the cheap rejection is inside at :1276-1277. The *signature* pass has the same shape and additionally allocates: :1331-1338 `rangeHighlightSignature` does `group.ranges.map(range => \\`${range.start}:${range.end}\\`)` \u2014 a string per match \u2014 then `for (const row of getMountedRows(view)) appendRangeHighlightRowSignature(...)` which again loops all ranges per row, and finally `parts.join('|')`. That runs from `renderRangeHighlight` (:260-270), which `renderSnapshot` invokes for every highlight group on every rows-key change, i.e. every scroll step (virtualizedTextView.ts:822). find pushes the whole match array in one call (findController.ts:405). Monaco's interval-tree/`getDecorationsInRange` approach verified in findModel.ts / findDecorations.ts.",
  "verifierNotes": "Real, and the fix is local and does not need an interval tree. `findMatches` already emits ranges in ascending order, so `addMountedRangeHighlightRanges` and `rangeHighlightSignature` can binary-search the first range with `end > row.startOffset` and break once `range.start >= row.endOffset` \u2014 we already have a `lowerBound` helper at packages/editor/src/virtualization/rowHeightIndex.ts:46 to copy. Worth doing independent of find: this path also serves LSP diagnostics and document highlights. Kill the per-frame per-range string allocation in the signature at the same time (hash or version counter instead)."
 },
 {
  "domain": "find-replace",
  "title": "Seeding the search string from the selection does not escape regex metacharacters when regex mode is on",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/findController.ts:305-311 (setSearchString escapes when isRegex), :327-338 (_start seeding path escapes), :42-66 (getSelectionSearchString with SEARCH_STRING_MAX_LENGTH=524288 and the word-at-position fallback), :934-960 (SelectionMatchFindAction routes through setSearchString so it is escaped too)",
  "mechanism": "Monaco funnels every seeded search string through `strings.escapeRegExpCharacters` when the state is in regex mode \u2014 in `setSearchString`, in `_start`'s selection seeding, and therefore in Cmd+E / Find-Next-Selection as well. It also bounds the seed: a selection is only used if `getValueLengthInRange(selection) < SEARCH_STRING_MAX_LENGTH` (512KB), and a *collapsed* selection seeds from `getConfiguredWordAtPosition` \u2014 the language-configured word definition, not a hard-coded one. The 'seedSearchStringFromSelection: selection' mode is expressed as `seedSearchStringFromNonEmptySelection`, which suppresses the word-at-cursor fallback entirely.",
  "whyItMatters": "With regex mode on (a sticky, persisted setting in Monaco and a plausible one for us), selecting `foo(bar)` and hitting Cmd+F yields a search for a pattern with a capture group instead of the literal text \u2014 the user sees wrong or zero results with no indication why. Also, selecting a huge blob and hitting Cmd+F currently stuffs the entire selection into the input and searches for it.",
  "ourStatus": "partial",
  "ourEvidence": "packages/find/src/findController.ts:446-457 `seedSearchString` returns `selectedSingleLineText(...)` or the word at the cursor verbatim; nothing escapes it, and `this.state.isRegex` is not consulted. We do implement the multi-line suppression (`selectedSingleLineText`, findController.ts:515-519) and the 'never'/'always'/'selection' option (types.ts:3), so the option surface is right \u2014 only the escaping and the length bound are absent. We do already have `escapeRegExpCharacters` locally (search.ts:81-83) with the same character class as Monaco's strings.ts:89-91.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "In `seedSearchString` (and any future setSearchString-from-selection entry point), wrap the result in `escapeRegExpCharacters` when `this.state.isRegex`. Export that helper from search.ts. Add a `SEARCH_STRING_MAX_LENGTH` guard before `text.slice(...)` in `selectedSingleLineText`.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed as partial. findController.ts:446-457 `seedSearchString` returns `selectedSingleLineText(...)` (:452) or `text.slice(range.start, range.end)` from `wordRangeAtOffset` (:455-456) verbatim; `this.state.isRegex` is never consulted and there is no length bound. The option surface is complete (`seedSearchStringFromSelection: 'never'|'always'|'selection'`, types.ts, resolved at :504) and the multi-line suppression exists (:515-519). `escapeRegExpCharacters` already exists locally at search.ts:81-83 with Monaco's character class but is module-private. Monaco verified: references/vscode/src/vs/editor/contrib/find/browser/findController.ts:310 and :336 `strings.escapeRegExpCharacters(...)`, and the bound at :41 `SEARCH_STRING_MAX_LENGTH = 524288` / :59 `getValueLengthInRange(selection) < SEARCH_STRING_MAX_LENGTH`.",
  "verifierNotes": "Trivial and worth taking: export `escapeRegExpCharacters` from search.ts and wrap the seed when `state.isRegex`; add a length guard on the selection before slicing. Note our Cmd+D-family commands are a separate code path (packages/editor/src/editor/inputSelectionController.ts:558 `applyMoveSelectionToNextOccurrenceCommand` \u2192 occurrences.ts exact-string matching) and never touch find state, so they carry no escaping bug \u2014 the fix is genuinely one call site. The 512KB bound matters less for us since we currently materialize the whole text anyway."
 },
 {
  "domain": "find-replace",
  "title": "Replace One re-scans the whole document with capture groups enabled just to replace one match",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/findModel.ts:481-505 (replace), :416-457 (_getNextMatch with captureMatches=true); vscode/src/vs/editor/common/model/textModelSearch.ts:290-302 (findNextMatch)",
  "mechanism": "Monaco's `replace()` computes exactly one match: `this._getNextMatch(selection.getStartPosition(), /*captureMatches*/ true, /*forceMove*/ false)`, which bottoms out in `TextModelSearch.findNextMatch` \u2014 a single incremental search from the cursor, not an enumeration. It then compares `selection.equalsRange(nextMatch.range)`: if the selection is already on the match, replace it; if not, just move to that match (the classic 'first Enter selects, second Enter replaces' behavior). Note the ordering \u2014 captures are only ever computed for the one match being replaced.",
  "whyItMatters": "Holding down Replace on a large file currently costs a full materialize + full regex scan *with capture arrays allocated for every match in the document* per replacement. Capture-enabled scanning is the expensive mode (it forces the regex path even for plain strings and allocates a `matches` array per hit), so this is the worst possible way to get one match's captures.",
  "ourStatus": "missing",
  "ourEvidence": "packages/find/src/findController.ts:190-208 `replaceOne` calls `this.currentOrSelectionMatch(true)`, and that (findController.ts:475-485) does `const matches = captureMatches ? this.findAll(true) : this.matches` \u2014 a full re-enumeration with `captureMatches = true`, then a linear `.find` for the selection's range. It then calls `this.research(true)` afterwards, which is a *third* full scan.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Once `findNextMatchFrom` from finding #4 exists, `replaceOne` becomes: search once from `selection.startOffset` with captures, compare against the selection, replace or navigate. Even without it, a cheap intermediate is to re-run the compiled regex over just `[match.start, match.end]` to recover the capture groups for the already-known range, instead of scanning the document.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, and it is three full scans per Replace, not two. `replaceOne` (findController.ts:190-207) \u2192 `currentOrSelectionMatch(true)` \u2192 findController.ts:479 `const matches = captureMatches ? this.findAll(true) : this.matches` (scan #1, full enumeration with captures, then a linear `.find` at :481-484) \u2192 `applyEdits` \u2192 the resulting content update reaches `handleViewUpdate` \u2192 `research(false)` (scan #2) \u2192 and `replaceOne` itself then calls `this.research(true)` at :206 (scan #3). Each `findAll` is a fresh `materializeFullText()` (:375) because the snapshot's lazy cache does not survive across `getSnapshot()` calls. Monaco verified: findModel.ts `replace()` uses `_getNextMatch(..., /*captureMatches*/ true, /*forceMove*/ false)`, a single incremental search.",
  "verifierNotes": "Fixable today without claim 1's incremental searcher: locate the match in the already-computed `this.matches` (offsets only), then re-run the compiled regex against just `text.slice(match.start, match.end)` to recover the capture array. That removes scan #1 outright. The duplicate research (#2/#3) is removable by letting `handleViewUpdate` own the re-search after `applyEdits` instead of calling it again in `replaceOne`. Holding Cmd+Alt+Enter on a large file currently does three full-document scans per repeat."
 },
 {
  "domain": "find-replace",
  "title": "Find matches are not surfaced on the minimap or scrollbar, and Monaco's >1000-match merge is the reason that is not trivial",
  "kind": "feature",
  "source": "both",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/findDecorations.ts:161-197 (approximate overview-ruler decorations), :286-334 (decoration options carrying minimap + overviewRuler color); codemirror5/addon/search/matchesonscrollbar.js:20-58 (SearchAnnotation with MAX_MATCHES=1000), :14-18 (showMatchesOnScrollbar extension); codemirror5/addon/search/search.js:113-116 (startSearch wires the annotation), :25-43 (searchOverlay: per-visible-line lazy highlighting)",
  "mechanism": "Both give the user an at-a-glance map of where matches are. Monaco attaches `overviewRuler` and `minimap` color options to the find-match decoration itself \u2014 but only up to 1000 matches. Beyond that it swaps to a no-overview decoration and computes *merged* bands: it derives `mergeLinesDelta = Math.max(2, Math.ceil(3 / (height / lineCount)))` \u2014 the number of document lines that collapse into ~3 ruler pixels \u2014 then coalesces consecutive matches within that distance into a single band. CM5's `showMatchesOnScrollbar` is a separate lightweight annotation capped at 1000, refreshed incrementally. CM5 also highlights matches *in the text* via a tokenizer overlay (`searchOverlay`) that runs per rendered line, so text highlighting costs nothing for off-screen matches.",
  "whyItMatters": "'Where are the 40 matches in this 3000-line file' is answered instantly by a scrollbar/minimap map and not at all by a count label. We already ship a minimap with a decoration API used by diagnostics, so this is close to free \u2014 except for the density problem, which Monaco has already solved with a concrete formula worth copying rather than rediscovering.",
  "ourStatus": "missing",
  "ourEvidence": "packages/minimap/src/plugin.ts:295-311 `MinimapDecorationRegistry.setDecorations(sourceId, ...)` exists and is consumed by packages/lsp-plugin/src/diagnosticsPresenter.ts:129, but nothing in packages/find references the minimap \u2014 grep for 'minimap' across packages/find/src returns nothing. Find's only visual output is `setRangeHighlight` on the text layer (findController.ts:401-413).",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Have `EditorFindController.updateHighlights` also push to the minimap feature via a new optional host method `setMinimapDecorations(sourceId, decorations)` mirroring `setRangeHighlight`. Port the merge: convert match offsets to rows via `lineStartsView.indexForOffset`, and when `matches.length > 1000` coalesce rows within `mergeLinesDelta` computed from the minimap's own height/lineCount before handing them over.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. `grep -rn 'minimap' packages/find/src packages/find/test` returns nothing; find's only visual output is `setRangeHighlight` on the text layer plus the scope highlight (findController.ts:401-413). The consumer side exists and is proven by another package: `MinimapDecorationRegistry` (packages/minimap/src/plugin.ts:295-325) keyed by `sourceId`, the `EditorMinimapDecoration` shape (packages/editor/src/plugins.ts:90-100 \u2014 line/column based, with `color`, `position`, `zIndex`) and `EDITOR_MINIMAP_FEATURE` token at :109, already used by packages/lsp-plugin/src/diagnosticsPresenter.ts:129. There is no overview-ruler/scrollbar-mark surface at all in our repo (grep for overviewRuler/scrollbarMark across packages/*/src returns nothing). Monaco's merge verified: references/vscode/src/vs/editor/contrib/find/browser/findDecorations.ts:163 `if (findMatches.length > 1000)` and :172 `const mergeLinesDelta = Math.max(2, Math.ceil(3 / approxPixelsPerLine))`, :179 the merge condition.",
  "verifierNotes": "Keep, but scope it to the minimap only \u2014 we have no overview ruler, and building one is a much larger job than the survey implies. Half the work is already done: register find as a minimap decoration source and convert match offsets to line/column via the existing `LineStartsView`. The >1000 merge matters for us for a different reason than Monaco's: our minimap decoration list is a plain flattened array (`getDecorations()` at plugin.ts:312 does `Array.from(...).flat()`), so 19,999 unmerged entries would be re-flattened and re-rendered on every notify."
 },
 {
  "domain": "find-replace",
  "title": "Replace-pattern edge cases: out-of-range $nn should degrade digit-by-digit, and a backslash should swallow the next character",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/browser/replacePattern.ts:108-127 (_substitute), :238-287 (the backslash branch consumes the next char unconditionally, with no default case), :215-229 (the documented grammar); vscode/src/vs/editor/contrib/find/test/browser/replacePattern.test.ts",
  "mechanism": "Monaco's `_substitute` for an index beyond the capture count peels digits off the right: `while (matchIndex > 0) { if (matchIndex < matches.length) return (matches[matchIndex] || '') + remainder; remainder = String(matchIndex % 10) + remainder; matchIndex = Math.floor(matchIndex / 10); }`. So with one capture group, `$12` renders as `<group 1>` followed by a literal `2` \u2014 matching what users expect from sed/JS semantics \u2014 and only falls back to a literal `$\u2026` when even the first digit is out of range. Separately, the parser's backslash branch does `i++` before the switch and has *no* default case, so an unrecognized escape like `\\$` consumes both characters: `\\$1` stays the literal three characters and is never treated as a capture reference.",
  "whyItMatters": "Both are small, but both are the kind of thing a user hits once and then stops trusting regex replace. `$12` silently emitting the literal text `$12` into their code, or `\\$1` unexpectedly expanding to a capture, are the sort of quiet wrong-output bugs that are hard to notice in a bulk replace.",
  "ourStatus": "partial",
  "ourEvidence": "packages/find/src/replacePattern.ts:185-190 `substituteMatch` returns `` `$${index}` `` verbatim for out-of-range indices \u2014 no digit peeling. packages/find/src/replacePattern.ts:108-131 `consumeReplaceEscape` returns the unchanged index for an unrecognized escape, and the caller (parseReplaceString, :35-43) then advances only one character, so the `$` in `\\$1` is re-examined by `consumeReplaceDollar` and *is* parsed as a capture reference \u2014 the opposite of Monaco. Everything else matches closely: our `\\u/\\U/\\l/\\L` handling (replacePattern.ts:192-208) and the whole preserve-case algorithm (replacePattern.ts:48-64, 227-259) are faithful ports of Monaco's replacePattern.ts:72-101 and base/common/search.ts:8-48.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Rewrite `substituteMatch` (replacePattern.ts:185-190) as Monaco's peeling loop. In `consumeReplaceEscape`, when the character after `\\` is not a recognized escape, still advance past it (return `index + 1` after `builder.emitUnchanged`-free passthrough) so the escaped character is never re-scanned. Port Monaco's replacePattern.test.ts cases wholesale \u2014 the file is 254 lines and covers exactly these corners.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed on both counts, and both diffs verified against the reference line by line. Out-of-range: our replacePattern.ts:185-190 `substituteMatch` returns the literal `` `$${index}` `` with no peeling; Monaco's references/vscode/src/vs/editor/contrib/find/browser/replacePattern.ts:104-121 `_substitute` peels digits off the right (`remainder = String(matchIndex % 10) + remainder`). Backslash: Monaco's parser (same file, :240-286) does `i++` immediately on a backslash and `continue`s regardless of whether the next char was recognized, so in `\\$1` the `$` is consumed and never parsed as a capture; ours (replacePattern.ts:108-131) returns the index unchanged for an unrecognized escape, and the loop's `index += 1` lands directly on the `$`, which `consumeReplaceDollar` (:133-157) then happily parses as capture 1. Everything else genuinely matches \u2014 our `\\u/\\U/\\l/\\L` (:192-208) and preserve-case (:48-64, :227-259) are faithful ports.",
  "verifierNotes": "Low value but near-zero cost \u2014 roughly 15 lines across two functions, plus unit tests in the existing packages/find/test/search.test.ts. Worth folding into whatever other find work happens rather than scheduling on its own. Of the two, the backslash one is the more defensible bug: a user typing `\\$1` to get a literal `$1` currently gets the capture group instead, i.e. there is no way to escape a dollar except `$$`."
 },
 {
  "domain": "find-replace",
  "title": "Monaco runs every find test twice, the second time over a piece tree with deliberately misaligned chunk boundaries",
  "kind": "testing",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/find/test/browser/findModel.test.ts:34-68 (findTest helper), :69-99 (_getFindState / assertFindState); the suite is 2405 lines and every case goes through findTest",
  "mechanism": "`findTest(name, callback)` runs the same callback twice: once against a model built from a line array, and once against a `PieceTreeTextBufferBuilder` fed three chunks split at byte offsets 94/101/59 \u2014 boundaries chosen to fall in the middle of lines. Both runs assert the same thing via `assertFindState(editor, cursor, highlighted, findDecorations)`, which snapshots the cursor selection, the currently-highlighted match, *and* the full decoration range list in one deepStrictEqual. That single helper is why the suite can afford 2405 lines of cases: each test is three arrays.",
  "whyItMatters": "Any search implementation that reads text through a chunked buffer has bugs that only appear when a match straddles a chunk boundary \u2014 and those are exactly the bugs that a string-based test suite can never catch. If we move search onto the piece tree (findings #1 and #7), this harness is the thing that makes that safe. Our current suite tests `findMatches(string, ...)` and therefore cannot see chunk-boundary bugs at all.",
  "ourStatus": "missing",
  "ourEvidence": "packages/find/test/search.test.ts is 101 lines and calls `findMatches` with plain string literals only; packages/find/test/plugin.test.ts (181 lines) exercises the plugin wiring. Neither constructs a piece table. Our piece table does have the machinery \u2014 packages/editor/src/pieceTable/reads.ts:33-49 `streamPieceTableTextChunks` and pieceTable.test.ts exist \u2014 it is just not used by find's tests. Note pieceTableSnapshotsHaveSameText (reads.ts:72-101) already walks two chunk streams with misaligned boundaries, so the pattern is familiar in this codebase.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Add a `findTest(name, fn)` helper in packages/find/test that runs each assertion against (a) a plain string and (b) a `DocumentSession` built by feeding the same text in three arbitrary chunks that split lines, reading through `TextSnapshot.readRange`. Add an `assertFindState(controller, {cursor, current, matches})` helper so cases stay one-liners. Do this *before* the line-scoped search change, so it acts as the safety net for it.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. packages/find/test/search.test.ts calls `findMatches` with plain string literals only; packages/find/test/plugin.test.ts asserts registration/wiring against a mocked `EditorPluginContext` (spies on `registerViewContribution` etc.) and never builds a document; style.browser.test.ts is CSS. So there is no test anywhere that drives `EditorFindController` against a real buffer and asserts cursor + current-match + highlight state. Monaco's harness verified: references/vscode/src/vs/editor/contrib/find/test/browser/findModel.test.ts:33 `function findTest(testName, callback)`, :52 `const ptBuilder = new PieceTreeTextBufferBuilder();` for the second run, :92 `function assertFindState(editor, cursor, highlighted, findDecorations)`.",
  "verifierNotes": "Keep, but reframe: the chunk-boundary half only pays off once claim 1's line/chunk-based searcher exists (today `findMatches` takes a string, so buffer chunking is invisible to it). The half that pays off immediately is the `assertFindState` shape \u2014 a controller-level test driving `EditorFindController` against a real `DocumentSession` and asserting (selection, current index, highlight ranges) after each command. Claims 4, 5, 6 and 10 are all behavioural bugs that such a test would have caught and that no unit test on `findMatches` ever can. `pieceTableSnapshotsHaveSameText` (packages/editor/src/pieceTable/reads.ts:72-101) shows the misaligned-chunk-walking pattern is already idiomatic here for when it is needed."
 },
 {
  "domain": "language-features",
  "title": "LanguageFeatureRegistry: score-based, multi-provider language feature dispatch",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/common/languageFeatureRegistry.ts:56-236 (register/all/ordered/orderedGroups/_updateScores/_compareByScoreAndTime); vscode/src/vs/editor/common/languageSelector.ts score() (string '*' \u2192 5, exact language \u2192 10, scheme/pattern/notebook each 0/5/10, array \u2192 max); vscode/src/vs/editor/contrib/suggest/browser/suggest.ts:293-330 (consumes registry.orderedGroups)",
  "mechanism": "Every language feature (completion, hover, definition, code action, formatting, semantic tokens, \u2026) is one generic `LanguageFeatureRegistry<T>` instance. Providers register with a `LanguageSelector` (language id, uri scheme, glob pattern, or an array of those). On each query `_updateScores` (:163) scores every entry against the model's uri+languageId, memoized on a `MatchCandidate` (:38-54) so repeated queries for the same model do zero work; entries are then sorted by score desc, built-in selectors deprioritized, and newer registrations winning ties (`_time`, :202-223). `ordered()` returns a flat best-first list; `orderedGroups()` (:132-148) buckets providers of *equal* score, and consumers like `provideSuggestionItems` walk group by group and **stop at the first group that produced any result** (suggest.ts:293-330) \u2014 so a specific provider shadows a generic one, but two equally-specific providers are merged. `exclusive: true` (:19-27, :183-195) lets one provider blank the whole registry (score 1000).",
  "whyItMatters": "It is the single extensibility primitive that makes 'multiple language sources' work: a tree-sitter word-completer, an LSP, a snippet source and a path-completer can all answer the same request and get merged deterministically, and a markdown-in-typescript embedded region can route to a different provider. Without it every feature is hard-wired to one backend.",
  "ourStatus": "partial",
  "ourEvidence": "Our capability system (packages/editor/src/plugins.ts:31-41, Editor.ts:2256-2277) is single-owner: `registerFeature` throws `Editor feature already registered` on a duplicate token id and `getFeature` returns one value. Language features are hard-wired to exactly one LspClient: packages/lsp-plugin/src/completionController.ts:46-51, hoverDefinitionController.ts, documentHighlightController.ts, signatureHelpController.ts all take a single `client` in their options. No file in packages/ contains a registry keyed by language selector (grep for `registry|Registry` in packages/editor/src + packages/lsp-plugin/src only hits displayProjectionRegistry).",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Add `packages/editor/src/editor/languageFeatureRegistry.ts` exporting `createLanguageFeatureRegistry<T>()` with `register(selector, provider)`, `ordered(doc)`, `orderedGroups(doc)`, using our existing `EditorSyntaxLanguageId` + document uri/path as the match candidate and the same 0/5/10 scoring. Keep the `MatchCandidate` memo \u2014 our controllers query on every keystroke. Then change `packages/editor/src/plugins.ts` to add a second, multi-valued registration channel (`registerProvider(token, selector, provider)`) alongside the existing single-owner `registerFeature`, so existing capability tokens keep their semantics. Finally re-express `packages/lsp-plugin/src/*Controller.ts` as providers registered into that registry rather than direct client callers; `packages/typescript-lsp` and future tree-sitter/word providers then register alongside.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "Confirmed single-owner at the *capability* layer: /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/Editor.ts:2256-2264 `registerFeature` throws `Editor feature already registered: ${token.id}` and :2274 `getFeature` returns one value; token type at packages/editor/src/plugins.ts:31-41. Language features are hardwired to one client: packages/lsp-plugin/src/hoverDefinitionController.ts:39,:59 (`readonly client: LspClient`), completionController.ts:56, documentHighlightController.ts, signatureHelpController.ts. No selector/glob/scheme matching anywhere. BUT the claim's 'single-owner' framing is too broad \u2014 we already have two multi-provider registries: (a) packages/editor/src/plugins.ts:356-358, :678-679, :1103 \u2014 edit/view/command/decoration contributions are stored in arrays and every provider is instantiated (`createEditContributions` returns EditorEditContribution[]); (b) packages/editor/src/editor/displayProjectionRegistry.ts:56-90 \u2014 EditorDisplayProjectionRegistry keys projections by `owner` and merges them by `layer`/`priority`/`sequence` across seven kinds. Monaco side verified: references/vscode/src/vs/editor/common/languageFeatureRegistry.ts:38 MatchCandidate, :56 class LanguageFeatureRegistry, :163-181 _updateScores memoized on the last candidate.",
  "verifierNotes": "Real gap, but narrower than stated. The plugin plumbing for N providers already exists (contribution arrays, owner/layer/priority merge in displayProjectionRegistry). What is missing is (1) an aggregation layer per language feature so two LspClients (e.g. tsserver + eslint + a Copilot-style provider) can both answer completion/hover into one widget, and (2) ordered/first-non-empty vs merge semantics per feature. The VS Code half worth *skipping* is the selector scoring itself \u2014 uri scheme globs, notebook cell types, `shouldSynchronizeModel` \u2014 that is workbench multi-scheme machinery we have no analogue for; a plain languageId match plus a priority number gets us the same behaviour. Prerequisite for claims 3, 9, 10 landing cleanly (inline completions and code actions are the features that actually have multiple providers in practice)."
 },
 {
  "domain": "language-features",
  "title": "Suggest filtering: bounded fuzzy DP scoring with match positions, incremental re-filter, and filterText/label split",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/base/common/filters.ts:683-833 (fuzzyScore), :847-914 (_doScore), :631-643 (isPatternInWord pre-check), :835-845 (_fillInMaxWordMatchPos), :921-957 (fuzzyScoreGracefulAggressive), :656-667 (FuzzyScore tuple layout); vscode/src/vs/editor/contrib/suggest/browser/completionModel.ts:82-89 (Refilter.Incr), :123-242 (_createCachedState), :198-224 (filterText vs label), :244-260 (_compareCompletionItems); wordDistance.ts:52-76",
  "mechanism": "`fuzzyScore` is a Smith-Waterman-style DP over pattern\u00d7word, but with three cost controls: (a) a linear `isPatternInWord` subsequence pre-check that also records, per pattern position, the *earliest* possible word match (`_minWordMatchPos`), (b) `_fillInMaxWordMatchPos` scanning backwards for the *latest* possible match, so each DP row only iterates the [min,max] window instead of the whole word, (c) `_maxLen` truncation. `_doScore` (:847) hands out 7 for a case-exact common-prefix hit, 5 for case-insensitive or camelCase-boundary or post-separator hits, 1 otherwise, with a \u22123/\u22125 penalty for a gap before the first match and a \u22125 gap-start penalty in the DP transitions (:739,:742); the backtrace pushes the matched word positions into the same tuple as the score, so the widget gets bold highlight ranges for free. `fuzzyScoreGracefulAggressive` (:921) retries up to 7 adjacent-character transpositions of the pattern (`cnoso`\u2192`conso`) with a \u22123 penalty, which is what makes typos still match. CompletionModel then adds two systems-level wins: `Refilter.Incr` (:86) \u2014 when the user typed *more* characters, re-filter only the previously surviving items, never the full list \u2014 and a filterText/label split (:198-215) where the *rank* comes from scoring `filterText` but the *highlights* come from re-scoring the label with `anyScore`. Final sort is score \u2192 word-distance (locality bonus: `wordDistance.ts:52-76` ranks a candidate by how many enclosing bracket-scopes away its nearest textual occurrence is, keywords and never-seen words getting `2<<20`) \u2192 original provider order.",
  "whyItMatters": "Our current ranker is a 3-bucket prefix/ci-prefix/subsequence test with a `localeCompare` tiebreak, so `cthn` does not find `createThreadName`, nothing is bolded in the widget, a locally-used identifier does not beat an obscure global, and every keystroke re-scores the full server list from scratch.",
  "ourStatus": "partial",
  "ourEvidence": "packages/lsp-plugin/src/completion.ts:578-618 \u2014 `rankCompletionItems` / `completionMatchScore` returns only 3/2/1 and `isSubsequence` is a boolean; no match positions are produced. completion.ts:300-309 `completionLabelElement` sets `textContent` only, so no highlight rendering exists. completionController.ts:209-217 re-ranks the *whole* item list on every render and slices to 100. No word-distance or locality concept anywhere in packages/.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Port `fuzzyScore` + `_doScore` + `anyScore` into `packages/editor/src/editor/fuzzyScore.ts` (pure, no VS Code deps \u2014 it only needs the `_table/_arrows/_diag` module-level typed arrays and CharCode checks). Return the same `[score, wordStart, ...matchPositions]` tuple. In completion.ts, replace `completionMatchScore` with it, keep `sortText` as a third-level tiebreak, and change `completionLabelElement` to emit `<span>`/`<mark>` runs from the match positions. Add the `Refilter.Incr` distinction to `CompletionController`: keep the last filtered array and, when the new prefix extends the old one, filter that instead of the raw list. Locality bonus can reuse `packages/editor/src/editor/tokenIndex.ts` / the viewport word scan behind `occurrences.ts` to build a word\u2192offsets map for the enclosing fold/bracket scope.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed exactly as claimed. packages/lsp-plugin/src/completion.ts:597-601 `completionMatchScore` returns 3 (prefix) / 2 (case-insensitive prefix) / 1 (subsequence) / 0; :604-612 `isSubsequence` returns a bare boolean and records nothing. :586 already reads `item.filterText ?? item.label` and :615-616 `compareCompletionOrder` already falls back to `sortText ?? label` \u2014 so the filterText/label *split* is present, only the positions and the DP are not. Rendering: completion.ts:300-308 `completionLabelElement` sets `textContent` only, no per-character spans, so there is nowhere to hang highlights. completionController.ts:209-217 re-ranks the entire item array on every show and `.slice(0, 100)`. Grep for `fuzzy` across packages/ (excluding node_modules/dist) returns zero hits; no word-distance concept. Monaco side verified: references/vscode/src/vs/base/common/filters.ts:631 isPatternInWord with `fillMinWordPosArr`, :543 `_minWordMatchPos`, :683 fuzzyScore, :695-701 the pre-check then `_fillInMaxWordMatchPos` (:835) bounding each DP row; references/vscode/src/vs/editor/contrib/suggest/browser/wordDistance.ts:15-28 WordDistance.",
  "verifierNotes": "Highest-value item in this set. Two separable deliverables: (a) a scorer returning match positions \u2014 needed purely so the widget can bold the matched characters, which is the single most visible quality difference between our list and Monaco's; (b) the bounded DP itself, which only matters once lists get long. Do (a) first; it can wrap the existing 3/2/1 scorer if the DP is deferred. Note the `.slice(0, 100)` at completionController.ts:216 means the DP cost ceiling is already capped for rendering but not for scoring \u2014 scoring still touches every item, so the [min,max] window bound is the part that actually pays."
 },
 {
  "domain": "language-features",
  "title": "Incomplete completion lists: per-provider re-query with item reuse, instead of hide-and-refetch",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/suggest/browser/suggestModel.ts:717-838 (_onNewContext), specifically :770-790 (incomplete re-query with providerFilter/providerItemsToReuse), :752-767 (new-word retrigger reusing all items), :253-297 (trigger-character retrigger reusing non-triggering providers); completionModel.ts:101-110 (getIncompleteProvider), :96-99 (getItemsByProvider); suggest.ts:296-330 (providerItemsToReuse honoured per provider)",
  "mechanism": "Monaco keeps the completion list alive across typing and only re-queries what must be re-queried. On each cursor/content change `_onNewContext` classifies the delta: cursor moved left \u2192 retrigger if still on a word, else cancel; leading whitespace changed \u2192 cancel; a *new* word started \u2192 retrigger but pass the entire current item map as `providerItemsToReuse`; cursor moved right and at least one provider marked its list `incomplete` \u2192 build a `providerFilter` of *only* the incomplete providers and a `providerItemsToReuse` of all the complete ones, and retrigger with `CompletionTriggerKind.TriggerForIncompleteCompletions`; otherwise \u2192 no request at all, just update `completionModel.lineContext` and re-filter locally. Trigger characters get the same treatment (:280-296): typing `.` re-asks only the providers that declare `.` and keeps everyone else's items. There is also a 'freeze' behaviour (:800-822): when the user *manually* invoked suggest and filtering has emptied the list, the old lineContext is restored and the widget stays up rather than vanishing mid-word.",
  "whyItMatters": "This is the difference between a suggest widget that feels continuous and one that flickers. It also fixes a real correctness hole: `CompletionList.isIncomplete` from a server means 'ask me again as the user types', and a client that ignores it shows a stale, wrongly-filtered list for large member sets (TS auto-import completions are always incomplete).",
  "ourStatus": "missing",
  "ourEvidence": "packages/lsp-plugin/src/completionController.ts:69-91 \u2014 `update()` hides the widget on `selection`, `viewport`, `layout`, `document` and on any `content` change that is not itself a trigger; every accepted trigger schedules a fresh full `textDocument/completion` after an 80 ms debounce (COMPLETION_REQUEST_DEBOUNCE_MS, completion.ts:10). packages/lsp-plugin/src/completion.ts:149-155 `completionItems()` discards `CompletionList.isIncomplete` entirely \u2014 the flag is never read anywhere in packages/.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Give `CompletionController` a session object holding `{ items, isIncomplete, requestOffset, requestPrefix }` that survives content changes. On a `content` update, compute the new prefix via `completionPrefix()`; if the caret is still inside the same word and the prefix *extends* the request prefix, re-filter locally (no request) unless `isIncomplete`, in which case re-request and replace. Cancel only on the conditions Monaco cancels on: line changed, leading-whitespace changed, caret left the word. Stop hiding on `selection` updates that are the consequence of typing. Since we currently have a single provider, `providerItemsToReuse` collapses to 'keep or replace the whole list' until the registry from finding #1 lands, at which point the per-provider maps become necessary.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. packages/lsp-plugin/src/completion.ts:149-155 `completionItems()` unwraps `CompletionList` to `result.items ?? []` and never reads `isIncomplete`; grep for `isIncomplete` across packages/ hits only the *producing* side \u2014 packages/typescript-lsp/src/typescriptLsp.worker.ts:592, :634 (`isIncomplete: info.isIncomplete === true`) and its tests \u2014 so our own TS server already reports the flag and the client throws it away. packages/lsp-plugin/src/completionController.ts:68-90 `update()` calls `this.hide()` on kind `document`/`clear`/`selection`/`viewport`/`layout` and on any `content` change where `completionTriggerFromChange` returns null; completion.ts:157-172 that trigger only fires for a single-character edit that is an identifier char or a trigger char, so backspace, paste, and multi-char IME commits all close the widget. Every surviving trigger goes through `scheduleCompletion` and a fresh full `textDocument/completion`.",
  "verifierNotes": "Confirmed and worth fixing, but the framing should be inverted for us: the acute bug is not 'we do not reuse items', it is that `update()` hides the widget on *selection* changes \u2014 and typing a character produces a selection change as well as a content change, so the widget lifetime is fragile in ways a user feels immediately (backspace kills the list). Step 1: keep the session alive and re-filter locally against the current prefix (we already re-filter at completionController.ts:209 with `completionPrefix`, just at the wrong moment). Step 2: read `isIncomplete` in `completionItems()` and only re-request when it is set. Step 3 (Monaco's `providerItemsToReuse`) is only meaningful after claim 1's multi-provider aggregation exists \u2014 skip it until then."
 },
 {
  "domain": "language-features",
  "title": "Snippet engine: placeholder mirrors, transforms with regex + case-shorthands, choices, and indentation normalization",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/snippet/browser/snippetParser.ts:228-283 (Placeholder, compareByIndex), :285-317 (Choice), :318-365 (Transform.resolve / _replace with else-branch), :368-489 (FormatString: upcase/downcase/capitalize/pascalcase/camelcase/kebabcase/snakecase + if/else), :490-530 (Variable with transform), :567 (placeholders()), :599 (enclosingPlaceholders); snippetSession.ts:28-92 (OneSnippet, groupBy(placeholders, compareByIndex)), :93-190 (move(): applies placeholder transforms on leave, flips decoration stickiness so the *active* stop grows at its edges and inactive ones do not, skips stops the user deleted), :427-479 (adjustWhitespace: normalizes each snippet line's indentation against the insertion line's leading whitespace and the model's tab settings), :306-330 (merge for multi-cursor); snippetVariables.ts:79-393 (Selection/Model/Clipboard/Comment/Time/Workspace/Random resolvers)",
  "mechanism": "Snippets parse into a marker tree, not a string+ranges pair. Placeholders with the same index form a *group* (`groupBy(placeholders, Placeholder.compareByIndex)`), so `$1` appearing three times gives one tab stop with three live decorations that all update together \u2014 mirrors. Each stop's decoration options are swapped on every `move()`: the active stop uses grow-on-edge stickiness while inactive ones use never-grow, which is precisely what makes typing at a placeholder boundary do the right thing. Transforms (`${1/(.*)/${1:capitalize}/}`) run when the caret *leaves* the stop (:102-125), re-indenting any newlines the transform introduced. `adjustWhitespace` (:427) rewrites the template's Text markers before insertion so a multi-line snippet lands at the caller's indentation and in the caller's tab style. Nested placeholders are real (`enclosingPlaceholders`), and `_hasPlaceholderBeenCollapsed` (:189-210) skips a stop the user has emptied.",
  "whyItMatters": "Everything past `${1:name}` in our parser is silently lossy, and mirrors are the single most-noticed snippet feature (rename a `$1` and every copy follows). LSP servers routinely ship snippets with transforms and choices; TypeScript's own `Method Signature` completions use nested placeholders.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/snippet.ts:117-147 \u2014 nested placeholder defaults are flattened to plain text and their stops dropped (comment at :124-125); a choice `${1|a,b|}` inserts only the first option (:130-137); no `/regex/format/flags` transform branch exists at all; `variableValue` (:197-202) answers exactly two variables and returns `''` for everything else. packages/lsp-plugin/src/completion.ts:454-470 \u2014 `completionSnippetStops` explicitly keeps only `stop.ranges[0]`, with the comment 'a repeated stop's mirrors are not edited together yet'. packages/editor/src/editor/snippetSession.ts:27-40 tracks one anchor pair per stop, and bails entirely when `ranges.length <= 1`. No indentation normalization of the inserted template anywhere.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Restructure `snippet.ts` from a string-builder into a marker tree (Text | Placeholder | Choice | Variable | Transform), keeping `parseSnippet()` as a thin adapter that walks the tree to produce `{ text, stops }` so existing callers do not break. Extend `SnippetStop` to carry all its ranges and mark one as primary. In `snippetSession.ts`, track a group of anchor pairs per stop and, on each edit inside the active stop, mirror the text into the sibling ranges via `packages/editor/src/editor/textEdits.ts` in the same edit chain (our anchors already survive edits, so the tracking half is done). Port `FormatString` verbatim \u2014 it is dependency-free \u2014 and run transforms in `SnippetSession.move()` before advancing. Add indentation normalization using the existing `packages/editor/src/editor/indentation.ts` helpers.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, with one correction in our favour. packages/editor/src/editor/snippet.ts:180-188 `recordStop` already accumulates *multiple* ranges per stop index into `byStop`, and :191-195 `orderedStops` emits `{ index, ranges }` \u2014 so the parser genuinely knows about mirrors (packages/editor/src/editor/snippet.test.ts:39 tests `$1 = $1`). The loss happens downstream: packages/lsp-plugin/src/completion.ts:465-469 `completionSnippetStops` does `stop.ranges[0]` with the comment 'a repeated stop's mirrors are not edited together yet', and packages/editor/src/editor/snippetSession.ts:9-12,:33-38 tracks exactly one `{startAnchor,endAnchor}` pair per stop. Everything else in the claim checks out: :124-126 nested placeholder defaults are flattened via `parseSnippet(...).text` and their stops dropped; :130-137 a choice inserts `choice[2].split(',')[0]`; no `/regex/format/flags` branch exists in readBracedStop; :197-202 `variableValue` answers only TM_SELECTED_TEXT and TM_CURRENT_LINE_INDENT and returns '' otherwise. snippetSession.ts:27-31 bails when `ranges.length <= 1`. No indentation normalization: grep for reindent/normalizeIndent across packages/editor/src returns nothing, and packages/editor/src/editor/indentation.ts exports only `lineBreakIndent` and `leadingWhitespace` \u2014 i.e. indent-on-newline, not re-indent-of-inserted-block.",
  "verifierNotes": "Keep, and note the cheapest high-value slice: the parser already produces mirror ranges, so mirrors are a *plumbing* fix (widen SnippetStop to N anchor pairs in snippetSession.ts, drop the `ranges[0]` truncation in completion.ts:467) rather than a parser rewrite. That alone makes `for (const ${1:x} of ${2:xs}) { ${1:x} }` behave. Transforms (`${1/(.*)/${1:/upcase}/}`) are genuinely absent and are the long tail \u2014 lower value, do last. Indentation normalization matters most for multi-line server snippets inserted at depth; today they land flush-left relative to the insertion point, which is visible in any TS `for`/`try` completion."
 },
 {
  "domain": "language-features",
  "title": "Re-diffing formatter output into minimal edits before applying it",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/common/services/editorWebWorker.ts:218-298 ($computeMoreMinimalEdits); called from vscode/src/vs/editor/contrib/format/browser/format.ts:245-247 (range formatting) and the document-formatting path around :290-330",
  "mechanism": "Formatter results are never applied as given. The worker sorts the edits, merges adjacent ones (:238-248), drops no-ops (`original === text`, :264-267), normalizes EOLs to the model's, and then \u2014 for each remaining edit under a `_diffLimit` size cap \u2014 runs a character-level `stringDiff` between the *current* text in the range and the replacement, emitting one small edit per change region mapped back through `model.offsetAt/positionAt` (:276-290). A single whole-document replacement from `prettier`/`tsserver` therefore becomes a handful of tiny edits. Each produced edit is checked once more against the model before being kept (:287).",
  "whyItMatters": "Directly determines whether formatting destroys editor state. A whole-buffer replace invalidates every anchor, decoration, fold, and selection, blows out the undo stack as one giant entry, and makes the whole viewport repaint. With minimal edits, the caret stays put, folds survive, and a no-op format is genuinely a no-op. It matters more for us than for Monaco because our anchors and fold map are offset-tracked.",
  "ourStatus": "missing",
  "ourEvidence": "packages/lsp-plugin/src/formatting.ts:13-38 \u2014 `formattingEdits()` converts LSP edits, sorts, drops overlaps, reverses, and returns them raw; there is no diffing step. `formattingChangesText` (:42-44) only decides whether *anything* changed, it does not minimize. We already have the diff primitive: packages/editor/src/pieceTable/diff.ts.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Add a `minimizeFormattingEdits(text, edits)` step inside `packages/lsp-plugin/src/formatting.ts`, between the overlap filter and the reverse: for each edit whose `to - from` and `text.length` are both under a cap (say 100k), run the existing `packages/editor/src/pieceTable/diff.ts` routine over `text.slice(from, to)` vs `edit.text`, and expand into per-change-region edits offset by `from`; skip edits whose diff is empty. Merge adjacent edits first, exactly as the reference does, so a formatter that emits per-line edits does not defeat the diff. Existing tests in packages/lsp-plugin should be extended with a 'whole-document replace that changes one line' case asserting a single small edit comes out.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. packages/lsp-plugin/src/formatting.ts:13-38 `formattingEdits` converts LSP positions to offsets, sorts ascending by `from` (longest first on ties), drops any edit starting before `keptThrough`, and reverses \u2014 no minimization, no adjacent-edit merge, no no-op drop, no EOL normalization. :42-44 `formattingChangesText` is a boolean over the whole set. Crucially, the cost is documented in our own code: packages/lsp-plugin/src/plugin.ts:586-591 comments 'The caret is pinned to its offset rather than tracked through the edits: formatting moves text wholesale' and then :592-596 `applyFormattingEdits` sets `selection: { anchor: head, head }` from the pre-format offset. Primitives available: packages/editor/src/pieceTable/diff.ts:11 `diffPieceTableSnapshots` (prefix/suffix only \u2014 produces ONE edit, not a change list, so it is not sufficient alone), and packages/diff/src/inline.ts:1 already imports `diffWordsWithSpace` from the npm `diff` package, so a real change-list diff is already a monorepo dependency. Monaco side verified: references/vscode/src/vs/editor/common/services/editorWebWorker.ts:216 `_diffLimit = 100000`, :218 `$computeMoreMinimalEdits`, :270 the size cap, :276 `stringDiff(original, text, pretty)`; interface at editorWorker.ts:33.",
  "verifierNotes": "Strong keep \u2014 arguably better value/effort than anything else here, because our own source comment names the symptom. Minimizing formatter output is what makes the caret, the selection, folds, diagnostics anchors and inline-replacement anchors survive a format instead of being reset; with a whole-document edit every anchor in packages/editor/src/pieceTable/anchors.ts dies at once. Correction to the claim's evidence: packages/editor/src/pieceTable/diff.ts is NOT the right primitive (it returns a single prefix/suffix edit); use the `diff` package already vendored for packages/diff, and apply Monaco's size cap so a 5 MB minified file does not run a quadratic diff. Also worth stealing the cheap parts of editorWebWorker.ts:238-267 \u2014 merging adjacent edits and dropping `original === text` no-ops \u2014 which are ~10 lines and independent of the diff."
 },
 {
  "domain": "language-features",
  "title": "Completion insert-vs-replace ranges: we silently drop the replace range",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/suggest/browser/suggest.ts:100-120 (CompletionItem normalizes range into editStart/editInsertEnd/editReplaceEnd and validates single-line-ness); suggestController.ts:573-589 (getOverwriteInfo: picks insert or replace end per `suggest.insertMode`, adds `columnDelta` for characters typed since the request went out, and `suffixDelta` from the OvertypingCapturer); :209 (ctxHasInsertAndReplace) and :684 (AlternativeOverwriteConfig \u2014 shift+Enter inverts the mode for one acceptance); suggestOvertypingCapturer.ts:10-74",
  "mechanism": "An LSP/Monaco completion carries two end positions: `insert` (replace only up to the caret) and `replace` (replace the whole word the caret sits in). Monaco keeps both, validates that neither spans lines and that they share a start column, and at acceptance computes `overwriteBefore = caretColumn - editStart.column` and `overwriteAfter = (replace ? editReplaceEnd : editInsertEnd).column - caretColumn`, then *adds* `columnDelta` \u2014 the number of characters typed between the request and the acceptance \u2014 so a completion accepted after further typing still overwrites the right span. `suffixDelta` restores text the user overtyped over a selection. Shift+Enter flips insert/replace for a single acceptance.",
  "whyItMatters": "With `foo|Bar` in the buffer and accepting `foobaz`, insert mode gives `foobazBar` and replace mode gives `foobaz`. We always take the insert range, so accepting a completion in the middle of an existing identifier leaves the tail behind \u2014 a visible, frequently-hit bug. The `columnDelta` correction matters too: our 80 ms debounce guarantees the user has often typed more since the request.",
  "ourStatus": "partial",
  "ourEvidence": "packages/lsp-plugin/src/completion.ts:388-393 \u2014 `completionTextEdit()` for an `InsertReplaceEdit` returns `{ range: textEdit.insert, newText }`, discarding `.replace` unconditionally. `completionApplication` (:174-194) uses the offset captured at request time (`session.offset` in completionController.ts:213, :266) with no delta for characters typed since. No validation that the edit range is single-line. No insert/replace toggle key.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Change `completionTextEdit` to return both ends, and add an `insertMode: 'insert' | 'replace'` option (default 'replace', which is what most users expect and what TypeScript assumes) threaded through `CompletionControllerOptions`. In `completionApplication`, compute the replacement span from the chosen end and add `currentOffset - session.offset` to it, validating the LSP range is on one line and rejecting the item otherwise (Monaco's `isInvalid`). Bind Shift+Enter in `handleCompletionKeyDown` (completionController.ts:293-330) to accept with the mode inverted.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed, but the blast radius is smaller than stated. packages/lsp-plugin/src/completion.ts:388-393 `completionTextEdit` \u2014 for an `InsertReplaceEdit` it returns `{ range: textEdit.insert, newText }`, dropping `.replace`. However :423-433 `defaultCompletionReplacementRange` \u2014 the path taken when the item has NO textEdit \u2014 already walks identifier characters both backwards AND forwards from the offset, i.e. it already implements *replace* semantics for the common case. So the defect is confined to servers that send InsertReplaceEdit (our own tsserver bridge at packages/typescript-lsp/src/typescriptLsp.worker.ts:634-652 does not appear to). Staleness confirmed: completionController.ts:213 stores `this.completionSession = { active, offset }` at show time and :266 `completionApplication(session.active.fullText, session.offset, item)` re-uses it at accept time with no adjustment for characters typed in between. No single-line validation of the edit range, and no insert/replace toggle key (completionController.ts:293-330 handles only ArrowUp/Down, PageUp/Down, Escape, Enter, Tab).",
  "verifierNotes": "Keep, but re-prioritise inside the claim. The InsertReplaceEdit drop is a two-line fix and low impact given our default path already replaces the whole word. The *stale offset* is the real bug and is worth calling out separately: accept the completion after typing two more characters and the edit is computed against an offset that no longer matches the buffer. Monaco's `overwriteBefore = caretColumn - editStart.column` / `overwriteAfter` reformulation is the fix worth copying \u2014 it re-derives the range from the *current* caret rather than the request-time offset, which makes the whole class of races go away. The user-facing insert/replace toggle key is polish; skip it."
 },
 {
  "domain": "language-features",
  "title": "Format-on-type, gated by provider trigger characters and cancelled by any edit before the caret",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/format/browser/formatActions.ts:31-150 (FormatOnType: _update builds a CharacterSet from `support.autoFormatTriggerCharacters` at :80-93; _trigger at :97 bails on multi-cursor or non-empty selection, then installs an onDidChangeModelContent guard at :111-129 that cancels the in-flight request if any change lands at or before the caret's line, or on a model flush), :155-230 (FormatOnPaste, triggered from editor.onDidPaste with the pasted range)",
  "mechanism": "Format-on-type is not 'format the line after every keystroke'. The provider declares which characters may trigger it (`}`, `;`, newline for TS), the editor builds a `CharacterSet` once per model/option change, and only those characters fire a `textDocument/onTypeFormatting` request. Because the request is async and the user keeps typing, a listener watches for content changes while it is in flight and cancels if a change occurred at or before the caret's line \u2014 otherwise the returned edits, computed for an older document, would corrupt text the user has since typed. Format-on-paste is the same machinery keyed on the paste range instead of a character.",
  "whyItMatters": "Auto-indenting a closing brace, re-spacing a statement on `;`, and reformatting pasted code are the three highest-frequency formatting interactions; document-level formatting is the rare one. The cancellation guard is the non-obvious part and is exactly the class of bug that produces duplicated or scrambled text.",
  "ourStatus": "missing",
  "ourEvidence": "Only `textDocument/formatting` is requested anywhere in packages/ (grep over packages/lsp-plugin/src, packages/lsp/src, packages/typescript-lsp/src yields no `onTypeFormatting` and no `rangeFormatting`). packages/lsp-plugin/src/formatting.ts has no trigger-character or paste path. `documentOnTypeFormattingProvider` capability is not advertised in packages/lsp/src/capabilities.ts.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Add an `OnTypeFormattingController` in packages/lsp-plugin/src modelled on `documentHighlightController.ts`: read `serverCapabilities.documentOnTypeFormattingProvider.{firstTriggerCharacter, moreTriggerCharacter}` (already reachable via `this.client.serverCapabilities`), build a Set once per connection, and hook the same `update(snapshot, kind, change)` path CompletionController uses (`kind === 'content'` with a single-character edit) to fire the request. Reuse `latestAsyncRequest.ts` for cancellation, plus an explicit guard: capture the document version/snapshot identity at request time and drop the result if any edit landed at or before the request offset \u2014 our `DocumentSessionChange` carries edit offsets so this is a cheap comparison. Route the result through the same `formattingEdits()` (with the minimization from finding #5). Paste is a second trigger on the same controller.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. Grepping `'textDocument/` across packages/lsp-plugin/src returns exactly: completion, definition, implementation, typeDefinition, references, documentHighlight, hover, signatureHelp, rename, formatting, publishDiagnostics (plugin.ts:560 is the only formatting request). packages/typescript-lsp/src/typescriptLsp.worker.ts:143-157 handles hover/completion/definition/references/implementation/typeDefinition/didOpen/didChange/didClose \u2014 no onTypeFormatting, no rangeFormatting. packages/lsp/src/capabilities.ts:11-49 advertises only synchronization, completion and signatureHelp; `documentOnTypeFormattingProvider` is absent. Separately confirmed we have no local substitute: packages/editor/src/editor/indentation.ts exports only `lineBreakIndent` and `leadingWhitespace`, and packages/editor/src/editor/indentation.test.ts:41-64 covers 'indents one level deeper after an opener' and 'pushes a closer onto its own line' \u2014 i.e. indent-on-Enter only. Nothing re-indents a line when you type `}` on it.",
  "verifierNotes": "Keep, and note it is the *only* claim here that fixes a defect users hit on every single block they close: today typing `}` at the end of an indented body leaves it at body depth, because we have no on-type dedent from any source. Two independent routes \u2014 (a) a local, synchronous on-type reindent driven by tree-sitter (we already have brackets: packages/editor/src/editor/bracketMatching.ts, and `BracketInfo` in plugins.ts), which is cheap and has no async race at all; (b) the LSP route, which needs the capability advertised in packages/lsp/src/capabilities.ts, a CharacterSet gate, and the in-flight cancellation Monaco does. Route (a) is far better value for our architecture; take Monaco's *trigger-character gating* idea and drop the async machinery."
 },
 {
  "domain": "language-features",
  "title": "Semantic tokens: delta protocol with in-place Uint32Array splicing",
  "kind": "performance",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/semanticTokens/browser/documentSemanticTokens.ts:245 (lastResultId passed to the provider), :296-400 (_setDocumentSemanticTokens: SemanticTokensEdits branch at :325-380, empty-edits fast path at :331-337, backwards splice loop at :348-372, _copy at :288-295), plus `releaseDocumentSemanticTokens(resultId)` (:311, :421) and the pending-changes reschedule (:296-305); provider interface at vscode/src/vs/editor/common/languages.ts:2488-2499",
  "mechanism": "The provider is asked `provideDocumentSemanticTokens(model, lastResultId, token)` and may answer either a full `SemanticTokens` (flat Uint32Array of 5-tuples: deltaLine, deltaStartChar, length, tokenType, tokenModifiers) or a `SemanticTokensEdits` \u2014 a list of `{ start, deleteCount, data }` splices *against the previous array*. Applying them allocates one destination `Uint32Array` sized `src.length + \u03a3(data.length - deleteCount)` and fills it back-to-front (:348-372), copying the untouched tail, then each edit's data, then the untouched head \u2014 one allocation, no intermediate arrays, no per-token objects. Invalid edit starts are detected and the whole result discarded rather than producing garbage (:350-354). `releaseDocumentSemanticTokens(resultId)` tells the provider it may drop its cached previous result. Content changes that arrive while a request is in flight are accumulated and force a reschedule rather than being applied to stale tokens (:296-305).",
  "whyItMatters": "Semantic tokens are the layer that distinguishes a type from a variable from a parameter \u2014 the thing tree-sitter alone cannot do for TypeScript. The delta encoding is what makes them affordable on a large file: a one-character edit produces a few-hundred-byte splice instead of a megabyte array, and the flat-array representation composes with the packed token format we already use.",
  "ourStatus": "missing",
  "ourEvidence": "No `semanticTokens` anywhere in packages/ (grep across all .ts files returns nothing). Highlighting comes from tree-sitter (packages/tree-sitter, packages/editor/src/syntax/packedTokens.ts) and shiki (packages/editor/src/shiki/). packages/lsp/src/capabilities.ts:11-49 does not advertise `textDocument.semanticTokens`.",
  "value": "medium",
  "effort": "L",
  "implementationSketch": "We already have the right substrate: `packages/editor/src/syntax/packedTokens.ts` is a flat packed-token representation and `packages/editor/src/editor/tokenProjection.ts` merges token sources for rendering. Add a `SemanticTokensController` in packages/lsp-plugin that advertises `textDocument.semanticTokens` with `full: { delta: true }` in capabilities.ts, holds `{ resultId, data: Uint32Array }`, and ports the backwards splice loop verbatim. Decode the 5-tuples into our packed format keyed by the server's legend, and register the result as a second token layer in `tokenProjection.ts` so it overlays tree-sitter tokens rather than replacing them (which is exactly how VS Code layers semantic over TextMate). Debounce and reschedule on pending changes as the reference does.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed absent: grep for `semanticToken|SemanticToken` across packages/ (all .ts) returns zero hits, and packages/lsp/src/capabilities.ts:11-49 does not advertise `textDocument.semanticTokens`. Highlighting is tree-sitter (packages/tree-sitter/*, packages/editor/src/syntax/session.ts) plus shiki (packages/editor/src/shiki/). Monaco side verified: references/vscode/src/vs/editor/common/services/semanticTokensDto.ts:18,:80,:136-145 encodes `{start, deleteCount, data}` deltas into a flat buffer; references/vscode/src/vs/editor/contrib/semanticTokens/browser/documentSemanticTokens.ts:340 computes `deltaLength += (edit.data ? edit.data.length : 0) - edit.deleteCount` for one destination allocation and :358 copies the tail. IMPORTANT counter-evidence for the *mechanism* half: we already have the exact transport idiom \u2014 packages/editor/src/syntax/packedTokens.ts:8-50 `PackedEditorTokens` is structure-of-arrays (`starts`/`ends`/`styleIds` as Uint32Array plus an identity-deduped style palette) and :52-54 `packedEditorTokenTransfers` returns the ArrayBuffers so postMessage transfers rather than clones.",
  "verifierNotes": "Lowest priority of the twelve for us. Two things are conflated: the *feature* (type-aware colouring that tree-sitter cannot do \u2014 distinguishing a type from a value, readonly, a namespace) and the *delta protocol* (an over-the-wire optimisation). Our tree-sitter + shiki stack already delivers most of the visible colour, so the feature is incremental; and the delta protocol only pays when a server re-sends tokens for a large file on every keystroke, which is a scenario we do not have yet. The genuinely transferable insight is the single-allocation splice at documentSemanticTokens.ts:340-358 \u2014 and we already do the SoA/transferable half in packedTokens.ts, so if we ever add this the integration cost is low. Rank it below claims 2, 3, 5, 7."
 },
 {
  "domain": "language-features",
  "title": "Inline completions / ghost text: deriving renderable inline parts from an arbitrary text edit",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/inlineCompletions/browser/model/computeGhostText.ts:19-118 (computeGhostText), :36-62 (indentation-touching special case), :120-152 (cachingDiff with a smartness retry), :162-215 (smartDiff: bracket-aware LCS by remapping '(' and ')' to synthetic char codes so `if ()` aligns against `if (f() = 1)` correctly); model/ghostText.ts:16-160 (GhostText/GhostTextPart/GhostTextReplacement); model/inlineCompletionIsVisible.ts:12-30",
  "mechanism": "An inline suggestion arrives as a plain `TextReplacement` (range + text), which may replace text, not just append. `computeGhostText` first strips the common prefix, rejects multi-line edits whose first lines are not a pure prefix, then handles the indentation case explicitly (a suggestion that starts inside the line's leading whitespace is re-based so ghost text never re-renders existing indentation). It diffs the replaced text against the suggestion with `smartDiff` \u2014 an LCS where parentheses are remapped to depth-and-group-unique synthetic character codes, so brackets align structurally instead of greedily \u2014 and turns every pure-insertion change region into a `GhostTextPart` at a specific column. Any change region with `originalLength > 0` (a real deletion) aborts: ghost text can only *add*. `previewSuffixLength` splits each part into committed vs italic 'preview' text for partial acceptance. `mode: 'subwordSmart'` refuses to place ghost text before the caret. The diff result is memoized on the last (original, new) pair since it is recomputed on every render.",
  "whyItMatters": "This is the entire rendering contract for AI/LSP inline completions, and it is the hard part \u2014 the request plumbing is trivial by comparison. The bracket-aware diff is what stops ghost text from looking scrambled when a suggestion completes a call expression.",
  "ourStatus": "partial",
  "ourEvidence": "No `inlineCompletion` or `ghostText` anywhere in packages/. However the *rendering* substrate exists: `packages/editor/src/displayTransforms.ts:51-72` defines `DisplayInjectedTextRow` with before/after placement, className and display columns, and `packages/editor/src/inlineMap.ts:26-49` defines anchored `InlineReplacementSpec`/`InlineReplacementRange` \u2014 plus the uncommitted packages/editor/test/inlineReplacementPainting.test.ts shows this path is actively being worked on.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Port `computeGhostText` + `smartDiff` into `packages/editor/src/editor/ghostText.ts` operating on our offsets rather than line/column (drop the ColumnRange machinery; our `TextEdit {from,to,text}` maps directly). Feed the resulting parts into `displayTransforms.ts` as injected text at an offset with a dim className \u2014 no new rendering primitive needed. Keep the `cachingDiff` memo, it is called per frame. The provider side then rides on the registry from finding #1 (`textDocument/inlineCompletion` for LSP, or a local provider), with Tab-to-accept coordinating with `snippetSession.ts` and the completion widget for key precedence.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "The *feature* is absent (grep for `inlineCompletion|ghostText` across packages/ returns nothing) but the rendering substrate is materially more complete than 'missing' implies, and the claim's own evidence understates it. packages/editor/src/displayTransforms.ts:51-71 DisplayInjectedTextRow (before/after placement, className, gutterClassName, display columns, wrapSegment) and :95-106 InjectedTextRow \u2014 these are whole injected *rows*, already driven end-to-end by packages/diff/src/liveProjection.ts:21,:47,:95 and packages/diff/src/editorDiffPlugin.ts:71-73 via `registerInjectedTextRowProvider`. For the *inline* (mid-line) case: displayTransforms.ts:173-180 `InlineReplacement {startColumn, endColumn, text}` and packages/editor/src/inlineMap.ts:26-49 anchored InlineReplacementSpec/InlineReplacementRange, with inlineMap.ts:162-163 clamping `Math.min/max(spec.startIndex, spec.endIndex)` \u2014 a zero-width spec (startIndex === endIndex) is a pure insertion, which is exactly the ghost-text shape. Plus the two uncommitted tests packages/editor/test/inlineReplacementCaptures.test.ts and inlineReplacementPainting.test.ts show this path is being actively worked. Monaco side verified: references/vscode/src/vs/editor/contrib/inlineCompletions/browser/model/computeGhostText.ts exists alongside ghostText.ts, singleTextEditHelpers.ts, inlineCompletionIsVisible.ts.",
  "verifierNotes": "Downgraded to partial: we have the hard half. Rendering anchored, wrap-aware, theme-able inline text that the cursor and column arithmetic understand (inlineColumnToSourceColumn / sourceColumnToInlineColumn in displayTransforms.ts) is the expensive part of ghost text and it is built and tested. What is missing is the ~200 lines of `computeGhostText` logic \u2014 strip the common prefix, reject multi-line edits whose first line is not a pure prefix, re-base a suggestion that starts inside leading whitespace so indentation is not re-rendered \u2014 plus a provider and an accept/partial-accept command. That is a genuinely small increment on top of existing machinery and should be ranked much higher than a 'missing' label suggests. One caveat: verify zero-width InlineReplacementSpec actually survives inlineMap's dedup/grouping before committing to that route."
 },
 {
  "domain": "language-features",
  "title": "Code actions: hierarchical kind filtering, isPreferred ordering, and 'auto fix'",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/codeAction/browser/codeAction.ts:43-96 (ManagedCodeActionSet: comparator ranks diagnostic-bearing actions above others, then isPreferred; `hasAutoFix` = a valid action whose kind is under `quickfix` AND isPreferred), :98-160 (getCodeActions: builds a `CodeActionContext` with `only`, queries every matching provider in parallel, filters each result with `filtersAction`, reports slow providers after 1250 ms); vscode/src/vs/editor/contrib/codeAction/common/types.ts (HierarchicalKind `contains`); codeActionModel.ts:35-80 (CodeActionOracle: 250 ms debounce on cursor position AND on marker changes, suppressed when the selection is whitespace-enclosed); codeActionCommands.ts:274-301 (AutoFixAction \u2192 CodeActionAutoApply.IfSingle); lightBulbWidget.ts:103-250 (state machine, distinct icon when hasAutoFix)",
  "mechanism": "Code actions are kinds in a dotted hierarchy (`quickfix`, `refactor.extract.function`, `source.organizeImports`), and filtering uses prefix containment on that hierarchy, not equality \u2014 so a `refactor` request matches `refactor.extract.function`. The lightbulb is driven by an oracle debounced at 250 ms that listens to *both* cursor movement and diagnostic changes, and refuses to trigger when the selection is enclosed in whitespace (which is why the bulb does not flicker while you type). Ranking puts actions that reference a diagnostic first, then `isPreferred` ones \u2014 and `hasAutoFix` (a preferred quickfix) is what lets Cmd+. apply a single obvious fix with no menu at all, and what changes the bulb's icon.",
  "whyItMatters": "Quick fix is the highest-value LSP feature we do not have; it is how you add a missing import, install a missing type, or apply an eslint fix. We already have diagnostics rendered and `workspaceEdit.ts` to apply the results, so this is mostly the trigger/selection/UI layer.",
  "ourStatus": "missing",
  "ourEvidence": "No `codeAction` anywhere in packages/ (grep). We do have diagnostics end-to-end (packages/lsp-plugin/src/diagnostics.ts, diagnosticProjection.ts, diagnosticsPresenter.ts), a workspace-edit applier (packages/lsp-plugin/src/workspaceEdit.ts:1-85, used by rename), and a floating widget pattern to copy (renameWidget.ts:1-110). `textDocument/codeAction` is not in the request list and `codeAction` is absent from packages/lsp/src/capabilities.ts.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `packages/lsp-plugin/src/codeActionController.ts`: advertise `textDocument.codeAction` with `codeActionLiteralSupport` + `isPreferredSupport` + `resolveSupport` in capabilities.ts; an oracle debounced at 250 ms on selection change and on the diagnostics-updated event that diagnosticsPresenter already emits, passing the diagnostics overlapping the selection as `context.diagnostics`. Sort with the two-level comparator (has-diagnostics, then isPreferred) and implement `HierarchicalKind.contains` as dotted-prefix matching. Render the menu with the renameWidget.ts pattern, and put the lightbulb in `packages/gutters`. Apply results through `workspaceEdit.ts` (resolve the action first when it arrives with only a `command`/no `edit`). Wire `editor.action.autoFix` to apply the single preferred quickfix directly, no menu.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed absent: grep for `codeAction|CodeAction` across packages/ returns zero, as does `quickfix|quick fix|lightbulb`. `textDocument/codeAction` is not among the requests in packages/lsp-plugin/src (full list: completion, definition, implementation, typeDefinition, references, documentHighlight, hover, signatureHelp, rename, formatting) and `codeAction` is absent from packages/lsp/src/capabilities.ts:11-49. The prerequisites the claim cites all check out: packages/lsp-plugin/src/workspaceEdit.ts:26 `workspaceEditPlan`, :35 `workspaceEditTouchesOtherDocuments`, :41 `workspaceEditForDocument` (already exercised by rename at plugin.ts:497 and by the formatting path at plugin.ts:547); packages/lsp-plugin/src/renameWidget.ts:33 `createRenameWidgetController` is a complete floating-widget-plus-input pattern to clone; diagnostics are end-to-end via diagnostics.ts / diagnosticProjection.ts / diagnosticsPresenter.ts.",
  "verifierNotes": "Keep \u2014 highest feature-value item, and unusually cheap for us because every prerequisite exists (workspace-edit applier, diagnostics with ranges, a floating widget pattern, an edit-feature transaction boundary). The two mechanisms worth copying precisely are (a) prefix containment on the dotted kind hierarchy rather than string equality, so a `refactor` request matches `refactor.extract.function` \u2014 get this wrong and every filtered request returns nothing, and (b) the 250 ms oracle debounced on *both* cursor movement and diagnostic changes, since a naive cursor-only trigger misses the case where diagnostics arrive after the cursor settled. Skip VS Code's lightbulb-in-the-gutter UI decisions; our gutters package (packages/gutters) has its own model."
 },
 {
  "domain": "language-features",
  "title": "Commit characters: accepting the focused suggestion on the next typed character",
  "kind": "feature",
  "source": "monaco",
  "referenceLocation": "vscode/src/vs/editor/contrib/suggest/browser/suggestCommitCharacters.ts:14-70 (CommitCharacterController: rebuilds a CharacterSet from the *focused* item's commitCharacters on every focus change, and on editor.onWillType \u2014 before the character reaches the buffer \u2014 accepts if the character is in the set and the widget is not frozen); wired at suggestController.ts:169 with `InsertFlags.NoAfterUndoStop` so the commit character and the completion share one undo step. Contrast CM5: codemirror5/addon/hint/show-hint.js:118-127 (cursorActivity closes the widget when the character before the caret matches `closeCharacters`, default `/[\\s()\\[\\]{};:>,]/`) and :508-513 (`completeSingle`, `closeOnPick`, `updateOnCursorActivity` defaults) \u2014 a coarser, close-instead-of-commit variant of the same idea.",
  "mechanism": "Servers attach `commitCharacters` to a completion item (TypeScript sends `.`, `,`, `(` for most items). Monaco listens on `onWillType` \u2014 i.e. the character is intercepted *before* insertion \u2014 and if it is in the focused item's set, accepts the completion instead, then lets the character through. The set is recomputed only when the focused item changes, and is cleared when the widget hides or the model goes idle. `NoAfterUndoStop` means a single Ctrl+Z undoes both the accepted completion and the typed character together.",
  "whyItMatters": "It is the interaction that makes typing `arr.fil(` produce `arr.filter(` without ever pressing Enter or Tab, and it is essentially free for us because our TypeScript worker already produces the data and throws it away.",
  "ourStatus": "partial",
  "ourEvidence": "packages/typescript-lsp/src/typescriptLsp.worker.ts:652 already emits `commitCharacters: entry.commitCharacters ?? info.defaultCommitCharacters`, but no consumer exists: grep for `commitCharacters` in packages/lsp-plugin/src returns nothing, and completionController.ts:293-330 only handles ArrowUp/Down, PageUp/Down, Escape, Enter and Tab.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "In `CompletionController`, track the currently selected item (the widget already exposes `selectedItem()`, completion.ts:115) and derive a `Set<string>` of its commit characters whenever selection changes. In `handleCompletionKeyDown`, before the Enter/Tab branch, check single-character `event.key` against that set; if it matches, call `acceptCompletion()` and let the keystroke proceed rather than preventing default \u2014 our edit chain (`packages/editor/src/editor/editChain.ts`) should coalesce the two into one undo entry, which is worth asserting in a test. Gate it behind an option, since a wrong commit character is the most annoying possible completion bug.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed exactly. Producer side exists: packages/typescript-lsp/src/typescriptLsp.worker.ts:652 `commitCharacters: entry.commitCharacters ?? info.defaultCommitCharacters`. Consumer side is empty: `commitCharacters` appears nowhere else in the repo (single grep hit across all of packages/), and packages/lsp-plugin/src/completionController.ts:293-330 `handleCompletionKeyDown` handles only the manual-trigger chord, ArrowDown, ArrowUp, PageDown, PageUp, Escape, and then `if (event.key !== 'Enter' && event.key !== 'Tab') return`. The pre-insertion hook Monaco's `onWillType` needs does exist on our side: packages/editor/src/editor/inputSelectionController.ts:112 registers a `beforeinput` listener, :1138 `beforeInputText(event)` extracts the pending text and :1147 transitions to `beforeinput-pending` \u2014 so a character can be inspected before it reaches the buffer.",
  "verifierNotes": "Keep \u2014 genuinely small and the ingredients are on both ends already (server emits the set; a beforeinput hook exists to intercept). Two mechanism details from Monaco are load-bearing and easy to get wrong: the set must be recomputed only when the *focused item* changes (not per keystroke), and it must be cleared when the widget hides or the model changes, otherwise a stale set commits a suggestion that is no longer on screen. Also: the character must still be inserted after the accept, in the same transaction, or `foo` + `.` yields `foo` with a lost dot. Rank just below claim 3, since commit characters on a widget that hides on every selection change would rarely fire."
 },
 {
  "domain": "api-perf-infra",
  "title": "Typed option registry: validate \u2192 compute \u2192 per-option change diff",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/config/editorOptions.ts:1087-1210 (IEditorOption / BaseEditorOption / ComputedEditorOption / SimpleEditorOption / applyUpdate), :1028-1038 (ConfigurationChangedEvent.hasChanged), :5796-5801 (editorOptionsRegistry + register()); references/vscode/src/vs/editor/browser/config/editorConfiguration.ts:287-352 (EditorOptionsUtil.validateOptions/computeOptions/checkEquals/applyUpdate), :103-114 (_recomputeOptions), :165-175 (updateOptions), :258-285 (ValidatedEditorOptions / ComputedEditorOptions dense arrays)",
  "mechanism": "Every option is an object with `{id (dense int), name, defaultValue, validate(input): V, compute(env, computedSoFar, value): V, applyUpdate(old, update)}`, registered into a dense array indexed by the id enum. Three layers of state are kept separate: `_rawOptions` (what callers passed, deep-cloned and migrated), `_validatedOptions` (each raw value pushed through `validate`, which clamps/coerces \u2014 `clampedInt` at editorOptions.ts:1246-1259, `boolean()` treating the string 'false' as false at :1215-1225), and `options: ComputedEditorOptions` (each validated value pushed through `compute(env, ...)`, where env carries fontInfo, outerWidth/Height, pixelRatio, viewLineCount, lineNumbersDigitCount, accessibilitySupport). Because `computeOptions` iterates the registry in id order and passes the partially-filled result in, a later option can legally read an earlier one \u2014 that's how `layoutInfo` (EditorLayoutInfoComputer at :2627-2860) is derived purely arithmetically from fontInfo + outer dimensions with zero DOM reads. Recompute is a pure function of (validated options, env); `checkEquals` deep-compares old vs new computed values slot-by-slot and produces a `boolean[]` indexed by option id, wrapped in `ConfigurationChangedEvent` so a consumer writes `if (e.hasChanged(EditorOption.lineHeight))`. If nothing changed, no event fires at all. `applyUpdate` (editorOptions.ts:1147-1167) recursively merges nested objects and reports `didChange`, so `updateOptions({minimap:{enabled:false}})` merges instead of clobbering, and a no-op update short-circuits before validation even runs. Every environmental input is wired to `_recomputeOptions` (editorConfiguration.ts:94-100): zoom, tab-focus, container resize, font measurement change, pixel ratio, accessibility, input mode.",
  "whyItMatters": "One `editor.updateOptions(partial)` entry point replaces N hand-written setters, and every consumer downstream gets exact change granularity for free instead of re-deriving 'did this matter to me?'. Callers get input coercion (a string '4' for tabSize, a NaN lineHeight, an out-of-range value) instead of silent corruption. And derived state like layout/wrap columns is recomputed exactly when one of its real inputs moved, not on every render.",
  "ourStatus": "missing",
  "ourEvidence": "packages/editor/src/editor/types.ts:76-99 \u2014 `EditorOptions` is a flat readonly plain-object type with no per-option metadata. packages/editor/src/editor/Editor.ts:325-503 consumes it once in the constructor; the only validation anywhere is `normalizeTabSize(options.tabSize)` at Editor.ts:329. There is no `updateOptions`; instead there are 21 hand-written setters (Editor.ts:503,516,545,551,560,568,577,620,667,826,871,875,889,898,908,920,941,960,971,982,1001), each of which re-derives its own 'did this change / what do I need to redo' logic. The cost has already leaked into the bindings: packages/react/src/index.ts:565-590 `useControlledOptionSync` hand-writes one `useEditorLayoutEffect` per option (`syncThemeOption`, `syncHiddenCharactersOption`, `syncEditabilityOption`, `syncKeymapOption`, `syncRangeDecorationsOption`, \u2026), and packages/solid will need the same list again. No option carries a default, a validator, or a change signal.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "New `packages/editor/src/editor/optionRegistry.ts`. Define `type EditorOptionDef<K extends keyof EditorOptions, V> = { key: K; defaultValue: V; validate(input: unknown): V; compute?(env: EditorEnvironment, get: <T>(k: T) => ..., value: V): V }` and an ordered `EDITOR_OPTION_REGISTRY` array. Keep the public `EditorOptions` type exactly as-is (it is the raw input shape) and add `ResolvedEditorOptions` = the computed record. In `Editor`, replace the 21 setters' bodies with a single `updateOptions(partial: Partial<EditorOptions>)` that deep-merges into `rawOptions`, revalidates, recomputes, diffs, and dispatches through one `applyOptionChange(changed: ReadonlySet<keyof EditorOptions>)` switch that calls the existing per-concern code paths \u2014 the setters stay as thin back-compat wrappers delegating to `updateOptions`. The env object should carry `{ textMetrics, viewportWidth, viewportHeight, devicePixelRatio, lineCount }`, which we already have on `view.getState()` (Editor.ts:2486-2503), so `lineHeight`, `rowGap`, and wrap columns become computed options rather than imperatively pushed values. Then collapse packages/react/src/index.ts:565-590 into a single effect that diffs the previous options object and calls `controller.updateOptions(diff)`.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/types.ts:75-95 `EditorOptions` is 18 flat optional fields with no metadata. /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/Editor.ts:325-345 constructor destructures it directly; the ONLY validation in the whole file is `normalizeTabSize(options.tabSize)` at Editor.ts:329 (grep for `normalize[A-Z]` across packages/editor/src returns normalizeSelectionSet/normalizeTextEdits/normalizeAnchorFoldRanges/normalizeInlineRanges/normalizeTokenStyle \u2014 all document-model normalizers, zero option normalizers besides tabSize). No `updateOptions`, no registry: grep for `updateOptions|OptionRegistry|defaultValue` across packages/*/src hits only fixedRowVirtualizer.ts / virtualizedTextViewLayout.ts / virtualizedTextView.ts (unrelated `defaultValue` locals). Setters confirmed: Editor.ts:551 setWordWrap, :875 setTheme, :908 setEditability, :960 setLineHeight, each re-deriving its own change check.",
  "verifierNotes": "The claim UNDERSTATES the cost \u2014 I found the drift it predicted has already happened. /Users/shaul/Desktop/D/Editor/packages/react/src/index.ts:565-599 `useControlledOptionSync` wires 11 per-option effects (document, theme, hiddenCharacters, editability, keymap, rangeDecorations, rowGap, scrollMode, selection, scrollPosition, plugins). /Users/shaul/Desktop/D/Editor/packages/solid/src/index.ts:220-230 wires only 6 (document, theme, hiddenCharacters, editability, rangeDecorations, selection, scrollPosition) \u2014 solid silently does NOT live-update rowGap, scrollMode, or keymap. Same editor, two bindings, different sets of reactive options, and nothing in the type system notices. That divergence is the concrete user-visible bug this claim fixes, and it is the argument to keep it. Recommend the narrow version: a descriptor array (id, name, default, validate, equals, applyTo(editor)) that both bindings iterate, rather than Monaco's full compute/ComputedEditorOptions layer \u2014 we have no derived options (Monaco needs compute for wrappingInfo/layoutInfo; our layout derivation already lives in the virtualizer). Effort: moderate-low. Value: high, it deletes the drift class permanently."
 },
 {
  "domain": "api-perf-infra",
  "title": "Operation batching: coalesce DOM reads/writes across nested and repeated edits",
  "kind": "performance",
  "source": "both",
  "referenceLocation": "references/codemirror5/src/display/operations.js:23-53 (startOperation/endOperation), :57-69 (endOperations R1\u2192W1\u2192R2\u2192W2\u2192finish), :71-170 (phase bodies), :173-206 (runInOp/operation/methodOp/docMethodOp); references/codemirror5/src/util/operation_group.js:5-41 (pushOperation/finishOperation group nesting), :43-72 (signalLater / fireCallbacksForOps); references/vscode/src/vs/editor/common/viewModelEventDispatcher.ts:86-108 (beginEmitViewEvents/endEmitViewEvents with _collectorCnt), :42-53 (_addOutgoingEvent with attemptToMerge), :122-152 (_emitMany / _doConsumeQueue)",
  "mechanism": "CM5 wraps every public mutating method in `methodOp`, which is a re-entrancy-aware guard: if `cm.curOp` already exists the call just runs inline, otherwise it opens an operation and closes it in a `finally`. Nested calls therefore contribute to one operation instead of each doing a full display update. The payoff is `endOperations` (operations.js:57-69), which runs five explicit phases across *all* ops in the group: R1 reads (clip scrollbars, find max line, decide `mustUpdate`), W1 writes (updateDisplayIfNeeded), R2 reads (updateHeightsInViewport, measureForScrollbars, measureChar for the widest line, prepareSelection), W2 writes (set sizer minWidth, showSelection, updateScrollbars, setDocumentHeight, reset input), then `finish` (scroll into view, fire `changes`). Every layout-forcing read is hoisted above the writes that would invalidate it, so a batch of N edits costs a bounded number of forced reflows rather than N. `operation_group.js:43-72` adds the second half: `signalLater` never dispatches a handler synchronously from inside a half-updated editor \u2014 it queues into `group.delayedCallbacks` and `fireCallbacksForOps` drains with a `do\u2026while` that keeps going while handlers append more callbacks. Monaco reaches the same shape differently: `beginEmitViewEvents`/`endEmitViewEvents` refcount a collector so nested emitters batch, outgoing events are *merged* pairwise via `attemptToMerge` before dispatch, and `_doConsumeQueue` clones the handler list and loops while the queue refills, making handler-triggered re-entrancy safe.",
  "whyItMatters": "Any command or plugin that performs several edits, or an edit plus a selection change plus a decoration update, currently pays full render + DOM-selection-sync + notify per step, with reads and writes interleaved so the browser is forced to reflow between them. Batching makes multi-cursor edits, sort/dedupe line ops, snippet expansion, and refactor-style multi-edit applications cost one visual update. It also removes a whole class of bugs where a listener fired mid-update observes an editor whose view and model disagree.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/Editor.ts:2388-2421 `applySessionChange` synchronously does `renderSessionChange` \u2192 `revealOffset` \u2192 `inputSelection.syncDomSelection()` \u2192 `sessionOptions.onChange` \u2192 `notifyViewContributions` \u2192 `notifyChangeWithTiming`, with no guard for an already-in-progress operation and no phase separation. `EditorEditInput = TextEdit | readonly TextEdit[]` (types.ts:118) lets a *single* call carry a batch, but two consecutive `edit()` calls, or an edit followed by `setSelection`, each run the full pipeline. Layout-forcing reads are scattered through the write path: virtualizedTextViewGeometry.ts:689,697,700,709 (`getBoundingClientRect`), inputSelectionController.ts:800,1045,1050,1057 (rect + `scrollHeight`/`clientHeight`), interleaved with writes at virtualizedTextViewRows.ts:1913,1939,1952 and Editor.ts:1145 (`scrollTop =`). Grep for `runInOp|startOperation|beginUpdate|batch` in packages/editor/src returns only packages/react/src ReactEditorStore.batch, which batches React notifications, not DOM work.",
  "value": "high",
  "effort": "L",
  "implementationSketch": "Add `packages/editor/src/editor/operation.ts` with an `EditorOperation` accumulator on `Editor` (`private currentOp: EditorOperation | null`) and `runInOperation<T>(fn: () => T): T` using the exact CM5 re-entrancy guard (`if (this.currentOp) return fn()`). Wrap the public mutators (`edit`, `setText`, `setSelection`, `setRangeDecorations`, `setRowDecorations`, `setSyntaxFolds`, and `commandRouter.execute`) in it. Move the body of `applySessionChange` (Editor.ts:2388-2421) into the operation flush, split as: R phase \u2014 read `view.getState()`, current scroll metrics, and any `getBoundingClientRect` the selection layer needs (inputSelectionController.ts:800,1045); W phase \u2014 `renderSessionChange`, scroll writes, `showSelection`; finish phase \u2014 reveal, then drain the queued notifications. Add a `signalLater`-equivalent: instead of calling `sessionOptions.onChange` / `notifyViewContributions` / `notifyChangeWithTiming` inline, push them onto `currentOp.deferredCallbacks` and drain with the `do\u2026while` refill loop from operation_group.js:19-29, merging consecutive same-kind view-contribution notifications the way `_addOutgoingEvent` does. Existing `appendTiming` calls map cleanly onto the phases.",
  "verified": true,
  "statusCorrected": true,
  "verifierEvidence": "The core mechanism is genuinely absent, but the claim missed two pieces of equivalent machinery, so 'missing' is too strong.\n\nPRESENT: (a) /Users/shaul/Desktop/D/Editor/packages/editor/src/editor/viewContributions.ts:51-84 is a real re-entrancy-aware guard in CM5's shape \u2014 `notify()` sets `this.notifying`, a re-entrant `notify('layout')` is folded into a `pendingLayout` flag (`queueReentrantUpdate`, :70-76) and flushed once after the outer update (`flushPendingLayout`, :78-83). That is exactly `methodOp`'s 'if an operation is already open, contribute to it' semantics, for the contribution-notification half. (b) /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/fixedRowVirtualizer.ts:476 and :493 coalesce scroll and resize into one rAF via `requestFrame` (:1048), so the *scroll/resize* path is already frame-batched.\n\nMISSING: the edit path. Editor.ts:2388-2421 `applySessionChange` runs render \u2192 reveal \u2192 syncDomSelection \u2192 onChange \u2192 notifyViewContributions \u2192 notifyChangeWithTiming synchronously with no operation guard. Confirmed no batching primitive: grep for `runInOp|startOperation|beginUpdate|transact` under packages/editor/src returns only history/documentSession `transaction` (undo grouping, a different concept \u2014 documentSession.ts:503-548). Confirmed no read/write phase separation and no rect memo: 44 `getBoundingClientRect|offsetWidth|clientHeight|scrollHeight` sites under packages/editor/src, and grep for `rectCache|measureCache|cachedRect|invalidateMeasure` returns ZERO hits \u2014 e.g. virtualizedTextViewGeometry.ts:689-701 `measuredRangeRect`/`measuredElementRect` each call `row.element.getBoundingClientRect()` again per range, uncached, inside the render path.",
  "verifierNotes": "Keep, but rescope to the two cheap halves rather than porting CM5's five-phase endOperations. (1) An operation depth counter around the public mutating methods so `edit(); setSelection()` produces ONE reveal + ONE syncDomSelection + ONE onChange instead of two \u2014 reuse the exact `notifying`/`pendingLayout` pattern already proven in viewContributions.ts:51-83, hoisted to Editor. (2) A per-operation rect cache keyed on the row element, cleared at operation close \u2014 that alone kills the repeated `row.element.getBoundingClientRect()` in measuredRangeRect/measuredElementRect, which is the worst offender because it re-measures the SAME row rect once per range on a row. The full R1/W1/R2/W2/finish phase split is not worth it: our writes are already funnelled through the virtualizer rather than scattered across independent addons the way CM5's are."
 },
 {
  "domain": "api-perf-infra",
  "title": "Debounces with no maximum wait starve syntax refresh under continuous typing",
  "kind": "correctness",
  "source": "both",
  "referenceLocation": "references/codemirror5/src/util/misc.js:31-55 (class Delayed \u2014 `set(ms,f)` only re-arms when `!this.id || time < this.time`), used by references/codemirror5/src/display/highlight_worker.js:10-13 (startWorker) and re-armed mid-slice at :44-47; references/vscode/src/vs/base/common/async.ts:499-522 (ThrottledDelayer = Delayer \u2218 Throttler), :421-486 (Delayer), :267-345 (Event.debounce with `leading` and `flushOnListenerRemove`)",
  "mechanism": "CM5's `Delayed` is deliberately *not* a debounce. `set(ms, f)` replaces the callback but only touches the timer when there is no pending one, or when the new deadline is *earlier* than the pending one. A repeated `set(100, \u2026)` therefore fires ~100ms after the *first* request, not 100ms after the last \u2014 the deadline can only be pulled in, never pushed out. That is what makes the highlight worker make forward progress while the user types continuously: `highlightWorker` slices for `workTime` ms, and when it runs out it calls `startWorker(cm, cm.options.workDelay)` (highlight_worker.js:44-47), which cannot be starved by incoming edits. Monaco expresses the same guarantee compositionally: `ThrottledDelayer` runs the delayer's trailing edge through a `Throttler`, so a new request arriving while a run is in flight is queued rather than cancelling it, and `Event.debounce` takes a `leading` flag so the first event in a burst always gets through.",
  "whyItMatters": "A fast typist sustains ~100-120ms between keystrokes. Our syntax refresh debounce is 150ms with no ceiling, so during a continuous typing run the editor never re-tokenizes at all \u2014 highlighting stays frozen on stale token positions until the user pauses, and the longer the run the worse the visible drift. The same applies to feature contributions (LSP diagnostics, occurrence highlights) on the same key.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/editor/workScheduler.ts:133-143 \u2014 `schedule` cancels the existing entry for the key (`this.cancel(options.key, 'replaced')`) and starts a fresh `delayMs` timer at :189, so re-scheduling always pushes the deadline out. There is no `maxDelayMs`. Consumers on that path: Editor.ts:2544-2553 schedules `editor.syntaxRefresh` with `delayMs: RAPID_INPUT_SECONDARY_WORK_DELAY_MS` (= 150, Editor.ts:136) on every rapid-input change, and Editor.ts:2554+ does the same for `editor.featureContributions`; syntaxController.ts:497-505 and :516-524 re-schedule with `syntaxRefreshDelay(change)`, :452 with 50ms, :360 with 120ms. The gap is already understood at one call site and worked around by hand rather than fixed in the primitive: packages/minimap/src/workerClient.ts:349-367 schedules the *same* flush under two keys \u2014 a quiet debounce at `MINIMAP_UPDATE_QUIET_DELAY_MS` plus a second entry with `replace: false` at `MINIMAP_UPDATE_MAX_DELAY_MS` \u2014 precisely to bound the wait. `budgetMs` (workScheduler.ts:334-340) bounds how long a task may *run*, not how long it may be deferred.",
  "value": "high",
  "effort": "S",
  "implementationSketch": "Add `maxDelayMs?: number` to `EditorScheduleWorkOptions` (workScheduler.ts:56-70). Carry a `firstScheduledAt` on `ScheduledEditorWork` that survives replacement: in `schedule` (:133-143), before `this.cancel(key,'replaced')`, read the outgoing work's `firstScheduledAt` and reuse it. In `scheduleStart` (:182-190) compute `effectiveDelay = Math.min(delayMs, Math.max(0, firstScheduledAt + maxDelayMs - now))`. That is CM5's 'deadline may only be pulled in' rule expressed for a replace-on-key scheduler, and it makes the two-key minimap workaround (packages/minimap/src/workerClient.ts:349-367) deletable. Then set `maxDelayMs` at the starvation-sensitive sites: Editor.ts:2544/2554 (~400ms) and syntaxController.ts:497/516. Also consider a leading-edge option so the first keystroke of a burst refreshes immediately, matching `Event.debounce(\u2026, leading)`.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed exactly as claimed. workScheduler.ts:133-143 `schedule` calls `this.cancel(options.key, 'replaced')` then creates a fresh work item, and scheduleStart (:182-190) arms a brand-new `delayMs` timer \u2014 so every re-schedule pushes the deadline out. No `maxDelayMs`: grep for `maxDelayMs|maxWait|MAX_WAIT` across packages/*/src returns only /Users/shaul/Desktop/D/Editor/packages/minimap/src/workerClient.ts:33 `MINIMAP_UPDATE_MAX_DELAY_MS = 300` and its use at :363. `budgetMs` (workScheduler.ts:334-346) arms `timeoutWork` which aborts the AbortController \u2014 a run-duration kill switch, not a defer bound, as claimed. Consumers confirmed: Editor.ts:2544-2562 both schedule at 150ms and are re-scheduled from `applySessionChange` on every change (Editor.ts:2420). CM5 counterpart verified: /Users/shaul/Desktop/D/Editor/references/codemirror5/src/util/misc.js:31-52 \u2014 `set(ms,f)` re-arms only `if (!this.id || time < this.time)`, so the deadline can only be pulled in, never pushed out.\n\nThe one thing the claim missed, which makes this MUCH cheaper than it looks: the primitive already has the needed escape hatch. workScheduler.ts:135 `if (existing && options.replace === false) return this.handleFor(existing)` \u2014 a `replace: false` schedule is a no-op when an entry for that key already exists, i.e. its deadline is immovable. That is precisely CM5's `Delayed` semantics, already implemented, already exercised, and it is exactly what minimap uses at workerClient.ts:349-367 to hand-roll the bound.",
  "verifierNotes": "Highest value-per-effort item in this batch. The user-visible symptom is concrete: with a 150ms pure debounce on `editor.syntaxRefresh`, a fast typist typing a long line never pauses 150ms, so syntax colouring does not update at all until they stop \u2014 colours freeze mid-line. Implementation is small because the primitive is already 90% there: add `maxDelayMs` to EditorScheduleWorkOptions and have `schedule` internally do what minimap does by hand \u2014 arm the normal replaceable entry under `key`, plus a `replace: false` entry under a derived key (e.g. `${key}#max`) at maxDelayMs, whichever fires first cancels the other. Then delete the hand-rolled pair at packages/minimap/src/workerClient.ts:349-367 and set `maxDelayMs` on Editor.ts:2544/2554. Note NOT to convert the whole scheduler to CM5's pull-in-only semantics: a pure `Delayed` would make every keystroke's syntax refresh fire on the FIRST keystroke's deadline, which is wrong for coalescing; maxDelayMs on top of a debounce is the right shape and is what minimap already proved out."
 },
 {
  "domain": "api-perf-infra",
  "title": "Font measurement: what to measure, how to key the cache, and when to throw it away",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/browser/config/fontMeasurements.ts:167-245 (_actualReadFontInfo), :128-158 (readFontInfo + Bug 14341 guard), :72-98 (_writeToCache / _evictUntrustedReadings), :103-123 (serialize/restoreFontInfo); references/vscode/src/vs/editor/browser/config/charWidthReader.ts:112-136 (_render repeats the char 256\u00d7 ; fulfill = offsetWidth/256); references/vscode/src/vs/editor/common/config/fontInfo.ts:104-107 (getId cache key includes pixelRatio); references/vscode/src/vs/base/browser/pixelRatio.ts:39-48 (matchMedia(`(resolution: Xdppx)`) re-armed on every change); wired at references/vscode/src/vs/editor/browser/config/editorConfiguration.ts:94-100",
  "mechanism": "Monaco measures by rendering each probe character repeated 2^8 = 256 times (charWidthReader.ts:112-128, using \u00a0 for space so it isn't collapsed) into an off-screen container, then dividing `offsetWidth` by 256 \u2014 averaging away sub-pixel rounding that a single-glyph measurement bakes in. It measures far more than one width: `typicalHalfwidthCharacterWidth` ('n'), `typicalFullwidthCharacterWidth` ('\uff4d'), `spaceWidth`, `maxDigitWidth` (max over '0'-'9', which is what makes line-number gutter width exact), middot and word-separator-middot widths for whitespace rendering, and a monospace probe: '|/-_ilm%' measured in regular *and* bold *and* italic, all compared against a reference width within \u00b10.001 (fontMeasurements.ts:196-215) \u2014 bold/italic breaking monospace is a real and common failure. `canUseHalfwidthRightwardsArrow` is a derived rendering decision (:217-225). Cache correctness is the other half: the key is `BareFontInfo.getId()` = pixelRatio + family + weight + size + featureSettings + variationSettings + lineHeight + letterSpacing (fontInfo.ts:104-107) \u2014 note pixelRatio is *in the key*, and PixelRatio fires whenever `matchMedia('(resolution: Ndppx)')` flips, which covers browser zoom and dragging the window to a different-DPI monitor (pixelRatio.ts:39-48). Readings taken before the webfont loaded produce garbage; the `isTrusted` flag plus a 5s eviction timer (fontMeasurements.ts:76-98) re-reads them and fires `onDidChange`, which routes straight into `_recomputeOptions`. And a defensive guard treats any width \u2264 2px as a failed read and floors it at 5 (fontMeasurements.ts:133-153).",
  "whyItMatters": "Every column-to-pixel mapping in a virtualized editor rides on character width. A measurement taken before a webfont finishes loading, or kept across a monitor/zoom change, silently misplaces the caret, selection rectangles, and horizontal scroll extent for the whole session with no error and no way for the user to recover short of a reload. `maxDigitWidth` and `isMonospace` additionally determine whether the gutter is wide enough and whether fast fixed-width positioning is even legal for the configured font.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/virtualization/browserMetrics.ts:11-30 measures a single 16-char probe ('mmmmmmmmmmmmmmmm') with one `getBoundingClientRect()` and divides by 16 (:57-60) \u2014 better than one glyph, but 16\u00d7 less averaging than 256, and 'm' is the widest ASCII glyph, so on a non-monospace fallback the derived width is systematically too large. We derive only `{rowHeight, characterWidth}` (browserMetrics.ts:1-4): no maxDigitWidth, no spaceWidth, no monospace verification, and no bold/italic probe at all. The cache key (browserMetrics.ts:104-119) is built from computed style (family/size/style/stretch/variant/weight/letterSpacing/lineHeight/textTransform/whiteSpace) and correctly excludes nothing font-related, but omits devicePixelRatio. Invalidation is entirely manual \u2014 `clearBrowserTextMetricsCache()` (:32-34) is exported and nothing subscribes to anything: grep for `document.fonts|fonts.ready|loadingdone|FontFaceSet` across packages/*/src returns zero hits, and grep for `matchMedia` returns only packages/decode/src/index.ts:191 (prefers-reduced-motion). packages/minimap reads `globalThis.devicePixelRatio` at snapshot time (workerClient.ts:664, :1480) but never subscribes to changes either.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Extend `BrowserTextMetrics` (browserMetrics.ts:1-4) to `{rowHeight, characterWidth, spaceWidth, maxDigitWidth, isMonospace, devicePixelRatio}`. In `measureBrowserTextMetrics`, build one probe container holding regular/bold/italic child divs and append repeated-256\u00d7 spans per requested char, mirroring charWidthReader.ts:65-136 \u2014 one appendChild and one batch of `offsetWidth` reads, still a single layout pass. Add `devicePixelRatio` to `browserTextMetricsCacheKey` (:104-119). Add a module-level invalidation source: a `DevicePixelRatioMonitor` copied from pixelRatio.ts:39-48 (re-arm `matchMedia` after each change \u2014 that detail is load-bearing) plus `document.fonts.addEventListener('loadingdone', \u2026)` and a `document.fonts.ready.then(\u2026)`, both calling `clearBrowserTextMetricsCache()` and notifying mounted editors to re-measure. That notification is the natural first consumer of the option-registry recompute from finding 1: metrics become an env input, and `lineHeight`/wrap columns fall out as computed options. Finally, port the \u22642px sanity floor (fontMeasurements.ts:133-153) and use `maxDigitWidth` for gutter sizing in packages/gutters.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "All specifics verified in /Users/shaul/Desktop/D/Editor/packages/editor/src/virtualization/browserMetrics.ts: PROBE_TEXT is 16 chars of 'm' (:8), one `getBoundingClientRect()` (:23), width divided by 16 (:47-50), `BrowserTextMetrics` is exactly `{rowHeight, characterWidth}` (:1-4) \u2014 no maxDigitWidth, no spaceWidth, no bold/italic probe, no monospace verification. Cache key (:104-119) covers 10 computed-style properties and omits devicePixelRatio. `clearBrowserTextMetricsCache()` (:32-34) is the only invalidation and nothing calls it automatically: grep across packages/*/src for `document.fonts|FontFaceSet|fonts.ready|loadingdone` returns ZERO hits, and `matchMedia` returns only packages/decode/src/index.ts:191 (prefers-reduced-motion) \u2014 no dppx query anywhere. devicePixelRatio is read unsubscribed at packages/minimap/src/workerClient.ts:664 and :1480 and packages/diff/src/canvasGutter.ts:353. Monaco counterpart verified at /Users/shaul/Desktop/D/Editor/references/vscode/src/vs/editor/browser/config/charWidthReader.ts:112-128 \u2014 doubling loop `for (let i = 0; i < 8; i++) testString += testString` = 256 repeats, with \u00a0 for space.\n\nMitigating context the claim missed: `characterWidth` is a monospace-grid assumption we make deliberately (packages/editor/src/style.css:34 and :83 set `font-family: monospace`), and it is consumed as a grid cell size in ~25 places across editor/diff/decode/minimap (e.g. virtualizedTextViewRows.ts:2065, packages/diff/src/gutters.ts:169). Where exactness matters we do NOT trust it \u2014 virtualizedTextViewGeometry.ts:681-711 falls back to real `Range.getClientRects()` measurement.",
  "verifierNotes": "Keep, but split \u2014 the two halves have wildly different value. HIGH VALUE / LOW EFFORT: automatic invalidation. Subscribe to `document.fonts.ready` and the `loadingdone` FontFaceSet event and call `clearBrowserTextMetricsCache()` + re-measure; add `devicePixelRatio` to the cache key at browserMetrics.ts:104-119 and subscribe to `matchMedia('(resolution: Xdppx)')` change to invalidate. This fixes a real, reproducible bug we have today: mount before a webfont finishes loading, measure the fallback font, and every column position in the editor is wrong forever with no event that fixes it \u2014 plus the same on a drag-to-external-monitor DPR change. LOW VALUE: the 256-char probe and the extra metrics. 16 'm's already averages sub-pixel rounding 16 ways; the residual error is well under a pixel on a real monospace font, and maxDigitWidth/typicalFullwidth only earn their keep in a proportional-font renderer, which we are not. Do the invalidation half; skip the measurement-fidelity half."
 },
 {
  "domain": "api-perf-infra",
  "title": "Theme scope matching should be a trie with inheritance, not exact-match plus first-segment fallback",
  "kind": "correctness",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/editor/common/languages/supports/tokenization.ts:339-410 (ThemeTrieElement.match / .insert \u2014 split on the first '.', recurse on the tail, unknown head falls back to `_mainRule`), :103-150 (resolveParsedTokenThemeRules \u2014 lexicographic sort then index, so specificity is insertion-independent; '' rules become the defaults), :193-245 (TokenTheme.match with a per-token memo cache), :277-337 (ThemeTrieElementRule.acceptOverwrite \u2014 children clone the parent rule so unspecified attributes inherit)",
  "mechanism": "Theme rules are inserted into a trie keyed on dot-separated scope segments. Each node clones its parent's resolved rule when created (`new ThemeTrieElement(this._mainRule.clone())` at :404) and then `acceptOverwrite`s only the attributes the rule actually specifies \u2014 so a rule on `keyword` supplies the colour and a rule on `keyword.control` overrides only what it names, inheriting the rest. Lookup walks the trie segment by segment and, the moment a segment has no child, returns the deepest rule reached (:363-383). That means `keyword.control.conditional` resolves against `keyword.control` if that exists, else `keyword`, else the theme default \u2014 correct longest-prefix semantics at arbitrary depth. Rules are sorted lexicographically with the source index as tiebreak *before* insertion (:105-111), so a theme's declaration order can't produce specificity surprises. Results are memoized per token string in `TokenTheme._cache` (:226-244), and the resolved colours are interned through a `ColorMap` into small integer ids, which is what makes packing style into token metadata bits possible.",
  "whyItMatters": "Our capture\u2192style resolution is a two-level lookup, so anything deeper than two segments loses all intermediate specificity. `keyword.control.conditional`, `keyword.control.import.from`, `variable.parameter.function`, `punctuation.definition.string.begin` all fall through to their first segment, discarding a level of styling the grammar deliberately provided. That also caps how faithfully we can render an imported VS Code/TextMate theme, and every new distinction someone wants requires editing a hardcoded table in our source rather than shipping theme data.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/syntax/captures.ts:32-53 `EXACT_CAPTURE_STYLES` (21 hardcoded full scope names) and :55-70 `PREFIX_CAPTURE_STYLES` (14 first-segment names), resolved at :76-82: exact hit, else `captureName.split('.')[0]` (:80) \u2014 nothing in between. So `keyword.control` is styled (exact entry at :37) but `keyword.control.conditional` collapses to plain `keyword`. The theme vocabulary is a closed 17-member union (packages/editor/src/theme.ts:1-19 `EditorSyntaxThemeColor`), and importing a real theme means hand-writing scope lists into those 17 slots: packages/editor/src/shiki/theme.ts:37+ `EDITOR_SHIKI_SYNTAX_SCOPE_MAPPINGS` enumerates e.g. `['entity.other.attribute-name','meta.attribute','support.type.property-name'] \u2192 'attribute'`. No trie, no inheritance, no memo cache \u2014 `sharedStyleForTreeSitterCapture` does two object lookups and a `split` per capture, per token.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `packages/editor/src/syntax/themeTrie.ts` porting ThemeTrieElement (tokenization.ts:339-410) essentially verbatim, but with `EditorTokenStyle` as the rule payload and an `acceptOverwrite` that merges only the defined fields of `{color, backgroundColor, fontStyle, fontWeight, textDecoration}`. Seed the trie from the existing `EXACT_CAPTURE_STYLES` + `PREFIX_CAPTURE_STYLES` tables so current behaviour is preserved exactly for the scopes they cover while deeper scopes start inheriting correctly; the tests in packages/editor/test can pin that. Add the per-capture-string memo Map from TokenTheme.match (:226-244) \u2014 `styleForTreeSitterCapture` (captures.ts:71-74) currently clones a fresh object per call, and interning the resolved style also makes the `styleKey` dedup in virtualization/sharedTokenHighlights.ts:52-66 hit more often. Then make `EditorTheme.syntax` accept arbitrary scope\u2192style rules alongside the existing 17 named keys (theme.ts:21), and rewrite packages/editor/src/shiki/theme.ts to feed a theme's `tokenColors` straight into the trie instead of funnelling through EDITOR_SHIKI_SYNTAX_SCOPE_MAPPINGS.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "The tree-sitter path is as described: /Users/shaul/Desktop/D/Editor/packages/editor/src/syntax/captures.ts:32-52 has 21 exact entries, :55-70 has 14 first-segment entries, and :76-83 `sharedStyleForTreeSitterCapture` does exact-lookup then `captureName.split('.')[0]` with nothing between \u2014 so `keyword.control` hits the exact entry at :37 but `keyword.control.conditional` collapses to bare `keyword`. Closed 17-member `EditorSyntaxThemeColor` union confirmed at theme.ts:1-18. Monaco's trie verified at /Users/shaul/Desktop/D/Editor/references/vscode/src/vs/editor/common/languages/supports/tokenization.ts:277-345 (ThemeTrieElementRule.clone at :296, acceptOverwrite at :300, ThemeTrieElement children map at :343).\n\nBUT the claim reads the codebase as having ONE scope-resolution path when it has two, and the second one is already correct:\n(1) /Users/shaul/Desktop/D/Editor/packages/editor/src/shiki/tokenizer.ts:2 imports `createHighlighter` from `shiki` (a real dependency, packages/editor/package.json:103 `shiki: ^4.2.0` + `@shikijs/themes: 4.2.0`), and packages/editor/src/shiki/editor-tokens.ts:10-58 maps shiki's already-themed `ThemedToken.color/bgColor/fontStyle` straight onto EditorTokenStyle. For anything tokenized through the shiki path we are running vscode-textmate's own theme trie, with full arbitrary-depth scope inheritance, in a worker. That is the reference implementation, in-process, today.\n(2) /Users/shaul/Desktop/D/Editor/packages/editor/src/shiki/theme-extract.ts:105-121 already implements prefix-with-specificity scope matching in our own code: `scopeTargetMatchScore` scores exact match 300+depth and `target.startsWith(scope + '.')` 100+depth, with tie-break on later-rule-wins (`isBetterScopeColorMatch`, :124-131). So a theme rule on `keyword` correctly applies to `keyword.control.conditional`.",
  "verifierNotes": "Keep, heavily rescoped \u2014 the deliverable is NOT 'build Monaco's theme trie', it is 'stop the tree-sitter capture table from being the odd one out'. Concretely: replace EXACT_CAPTURE_STYLES + PREFIX_CAPTURE_STYLES in packages/editor/src/syntax/captures.ts with a single segment-keyed trie whose nodes inherit the parent's resolved style and override only the properties they name, built once at module load from the same 35 entries. That is maybe 40 lines and gets `keyword.control.conditional`, `variable.parameter.builtin`, `punctuation.bracket.angle` etc. correct instead of collapsing them. It also removes the per-token `split('.')` at captures.ts:80 (currently once per capture per token) in favour of a walk that terminates at the first missing child. Do NOT widen `EditorSyntaxThemeColor` past 17 as part of this \u2014 the shiki path already covers 'I want a real VS Code theme rendered faithfully', and theme-extract.ts is the documented bridge for the 17-slot vocabulary. Effort: low. Value: moderate, and it is entirely self-contained in one file."
 },
 {
  "domain": "api-perf-infra",
  "title": "Extensible color registry with per-theme-type defaults and derived colors",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/platform/theme/common/colorUtils.ts:168-192 (ColorRegistry.registerColor), :216-224 (resolveDefaultColor), :46-66 (ColorTransform ops: Darken/Lighten/Transparent/Opaque/OneOf/LessProminent/IfDefinedThenElse/Mix), :256-300 (executeTransform/resolveColorValue), :34-44 (asCssVariableName / asCssVariable); references/vscode/src/vs/editor/standalone/browser/standaloneThemeService.ts:382-421 (_updateThemeOrColorMap: theming participants emit rules, all registry colors emitted as CSS vars in one rule, one style element write)",
  "mechanism": "Any module registers a themable color by id with a description and defaults that vary per theme type \u2014 `{light, dark, hcDark, hcLight}` \u2014 and crucially a default may be a *reference to another color id* or a *transform* over one: `registerColor('x', { dark: transparent(editorForeground, 0.3), light: \u2026 })`. `resolveColorValue` (:256-270) dispatches on the value's shape: '#rrggbb' \u2192 literal, any other string \u2192 recursive theme lookup by id, object \u2192 `executeTransform`, which itself recurses. So a theme that defines only a handful of base colors still yields a coherent full palette, and `IfDefinedThenElse`/`OneOf` express 'derive from this if the theme set it, otherwise fall back'. Emission is uniform: every registered id becomes `--vscode-<id with dots\u2192dashes>` written into one CSS rule on the editor container (standaloneThemeService.ts:394-402), plus contributed rules from theming participants, assembled into a single string and assigned to `styleElement.textContent` once (:419-421). `StandaloneTheme.getColor` memoizes resolved defaults per id (:105-114) and lazily builds the color map only on first access (:77-93).",
  "whyItMatters": "Today only the core editor can be themed, and only through 23 fixed properties. A plugin \u2014 find highlights, diff gutter, minimap overlay, merge-conflict bands, scope lines \u2014 has no way to declare a themable color, so its colors are either hardcoded or require a core `EditorTheme` change. Derived defaults are the other half: a consumer supplying just a background and foreground should get sensible selection, current-line, and find-match colors without enumerating them, and today they must supply every value or accept whatever the stylesheet hardcodes.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/theme.ts:21-30 `EditorTheme` is 6 fixed color properties plus `syntax`; :32-40 and :44-66 map them one-to-one onto a closed list of CSS custom properties, and `applyEditorTheme` (:74-87) writes each with `element.style.setProperty` \u2014 inline styles on the host element, so there is no place for a plugin to contribute and no cascade. `mergeEditorThemes` (:89-105) is a shallow per-known-key merge and `editorThemesEqual` (:107-121) compares only the known keys, so an unknown/extension color would be invisible to both. There is no registry, no notion of light/dark/high-contrast defaults, and no color-derivation (no darken/lighten/transparent helpers anywhere under packages/editor/src). Compare our existing token-style path, which already got this right \u2014 packages/editor/src/virtualization/sharedTokenHighlights.ts:39-90 ref-counts one Highlight + one CSS rule per distinct style per document and flushes the stylesheet once per batch (:84-90); the theme layer just never grew the same structure.",
  "value": "high",
  "effort": "M",
  "implementationSketch": "Add `packages/editor/src/themeRegistry.ts`: `registerEditorColor(id, defaults, description)` where `defaults` is `{light, dark} | ColorValue`, `ColorValue = string | ColorId | ColorTransform`, and a `resolveColorValue` port of colorUtils.ts:256-300 (start with Darken/Lighten/Transparent/OneOf/IfDefinedThenElse \u2014 the eight-op set is not needed on day one). Keep the current 23 keys as pre-registered ids so `EditorTheme` stays source-compatible, mapping them through `asCssVariableName`-style id\u2192`--editor-*` naming; the existing EDITOR_THEME_VARIABLES/EDITOR_SYNTAX_THEME_VARIABLES tables (theme.ts:32-66) become the seed registrations. Replace `applyEditorTheme`'s per-property `setProperty` loop (:74-87) with one generated rule containing every resolved variable, written to a shared per-document `<style>` element \u2014 reuse the ref-counted, batched-flush machinery already in sharedTokenHighlights.ts:39-90 rather than inventing a second one. Then expose `registerEditorColor` through `EditorPluginContext` (packages/editor/src/plugins.ts:440-455, alongside `registerHighlighter`/`registerSyntaxProvider`) so packages/find, diff, minimap, gutters, scope-lines register their own ids and drop their hardcoded colors.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Status is right but the claim's evidence is substantially wrong on three of its four points, and the corrections shrink the gap a lot.\n\nWRONG \u2014 'no place for a plugin to contribute and no cascade': there are ~80 distinct `--editor-*` custom properties defined across 11 stylesheets shipped by 8 different packages (packages/editor/src/style.css, gutters/src/{style,foldGutter,lineGutter}.css, diff, find, minimap, panes, scope-lines, markdown, decode). A plugin contributing a themable color is an established pattern here; the cascade is CSS's own, which is strictly more composable than Monaco's JS registry.\nWRONG \u2014 'no color-derivation (no darken/lighten/transparent helpers)': we derive colors natively. packages/scope-lines/src/style.css:4-44 derives 12 scope-line colors as `color-mix(in srgb, var(--editor-syntax-type) 34%, transparent)` etc.; packages/diff/src/style.css:9-24 and :168 derive 5 more; packages/find/src/style.css has 6. That is exactly Monaco's `transparent(colorId, factor)` (verified at /Users/shaul/Desktop/D/Editor/references/vscode/src/vs/platform/theme/common/colorRegistry.ts:265-271, darken/lighten/transparent in resolveColorValue) \u2014 expressed declaratively so it re-resolves automatically when the base var changes, which the JS version has to recompute by hand.\nWRONG \u2014 'no notion of light/dark defaults': packages/find/src/style.css:180 is a `@media (prefers-color-scheme: light)` block redefining its fallbacks.\nRIGHT: theme.ts:21-29 EditorTheme is 6 fixed colors + syntax; EDITOR_THEME_VARIABLES (:32-41) and EDITOR_SYNTAX_THEME_VARIABLES (:44+) are closed lists; applyEditorTheme (:70-84) writes inline via setProperty and clearEditorTheme (:121+) removes only known vars; mergeEditorThemes (:87-104) and editorThemesEqual (:106-119) iterate only the known keys, so an unknown color id is invisible to both.",
  "verifierNotes": "Keep, but the real deliverable is much narrower than the claim implies. We do not need resolveColorValue/darken/lighten \u2014 CSS color-mix already does that better, and we should keep derivation in CSS. What we actually lack is DISCOVERABILITY and TYPED PASS-THROUGH: ~80 vars exist, none is declared anywhere in TypeScript, so (a) nobody can enumerate what a theme author may set, (b) `EditorTheme` can carry only 6+17 of them, and (c) editorThemesEqual will report two themes equal when they differ on any of the other ~57. Minimal fix: a `registerEditorColor(id, cssVar, {light, dark})` table that packages append to at import time, have applyEditorTheme/mergeEditorThemes/editorThemesEqual iterate the registry instead of two frozen arrays, and let EditorTheme carry an open `colors?: Record<string,string>` bag alongside the 6 named ones. The light/dark defaults belong in `:root` + `@media (prefers-color-scheme)` in CSS as find/src/style.css already does, NOT in the JS table. Skip high-contrast entirely \u2014 that is workbench/accessibility-settings territory we have no consumer for. Effort: low-moderate. Value: moderate."
 },
 {
  "domain": "api-perf-infra",
  "title": "Emitter/Event: lazy wiring, listener-error isolation, re-entrancy-safe delivery, leak detection",
  "kind": "api-design",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/base/common/event.ts:1170-1230 (Emitter construction + options), :1260-1350 (get event \u2014 leak monitor, listener containers, onWillAddFirstListener/onDidAddFirstListener/onDidRemoveLastListener), :1376-1435 (_deliver with per-listener try/catch, _deliverQueue, fire), :1441-1480 (EventDeliveryQueuePrivate \u2014 index-based cursor so re-entrant fire() finishes the outer dispatch first), :1002+ (LeakageMonitor with most-frequent-stack attribution and hard refusal past threshold\u00b2), :267-345 (Event.debounce), :347-368 (Event.accumulate), :373-430 (Event.throttle)",
  "mechanism": "Four things a bare callback field doesn't give you. (1) Lazy wiring: `onWillAddFirstListener`/`onDidRemoveLastListener` let an emitter attach its own upstream subscription only while someone is listening \u2014 that's how `Event.debounce` (:267-345) sets up and tears down its timer and how DOM listeners get attached on demand. (2) Error isolation: `_deliver` (:1376-1395) wraps each listener in try/catch routed to `onListenerError`, so one throwing subscriber cannot prevent the remaining subscribers from seeing the event. (3) Re-entrancy: `fire` with multiple listeners enqueues into a shared `EventDeliveryQueuePrivate` holding `{current emitter, value, i, end}` and `_deliverQueue` increments `dq.i` *before* invoking the listener (:1398-1404) \u2014 if a listener re-enters `fire()`, the outer dispatch is drained to completion first (:1411-1413) rather than interleaving. (4) Leak detection: past a threshold the emitter records a stack per listener, reports the most frequent one as 'dominated' or 'popular' (:1264-1277), and past threshold\u00b2 refuses new listeners outright. On top of this, `Event` is a set of composable operators \u2014 debounce/throttle/accumulate/once/filter/map/runAndSubscribe \u2014 that any producer gets for free because the producer is just a function `(listener) => IDisposable`.",
  "whyItMatters": "We already have the *disposable* half of this contract everywhere; what's missing is a producer primitive. Concretely: today one throwing `onChange` handler or one throwing view contribution can abort the rest of the notification pass mid-edit; there is no way to hand a consumer a debounced or throttled view of an editor event without them writing their own timer; and a plugin that leaks subscriptions across mount/unmount is invisible until memory grows.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/plugins.ts:27-29 defines `EditorDisposable {dispose(): void}` and it is used consistently as the *subscription* return type (:106, :417, :447-453), but there is no `Emitter`/`Event<T>` producer type anywhere \u2014 grep for `class .*Emitter` under packages/*/src returns nothing. Producers are ad-hoc optional callbacks: `options.onChange` invoked bare at Editor.ts:2515 and :2522, `sessionOptions.onChange?.(finalChange)` at Editor.ts:2412, `provider.onDidChangeBlocks?.(\u2026)` at plugins.ts:1207, `onDidChangeInjectedTextRows?.(\u2026)` at :1255, `EditorTextBufferChangeListener` at documentSession.ts:110, `ConflictListener` at mergeConflictPlugin.ts:56. Error handling is per-site and inconsistent \u2014 plugins.ts:1033-1038 wraps a registration notification and rolls back on throw, but the change-notification path (Editor.ts:2412-2419) has no try/catch around individual consumers. No debounce/throttle/accumulate operators exist over these callbacks; each consumer that wants one reaches for `EditorWorkScheduler` directly.",
  "value": "medium",
  "effort": "M",
  "implementationSketch": "Add `packages/editor/src/editor/emitter.ts`: `type EditorEvent<T> = (listener: (e: T) => void, disposables?: EditorDisposable[]) => EditorDisposable` and `class EditorEmitter<T> { readonly event: EditorEvent<T>; fire(e: T): void; dispose(): void }`. Port three pieces from event.ts and skip the rest: per-listener try/catch routed to an injectable `onListenerError` (:1376-1395), the index-cursor delivery queue for re-entrancy (:1441-1470 \u2014 the array-snapshot-plus-index shape, not the shared cross-emitter queue), and `onWillAddFirstListener`/`onDidRemoveLastListener` hooks. Add `EditorEvent.debounce/throttle/once/map` as free functions over the type, implemented on top of our existing `EditorWorkScheduler` rather than raw timers so debounced events inherit priority, cancellation, and the `maxDelayMs` fix from finding 4. Migrate incrementally: `Editor.onChange` and the plugin-host `onDidChange*` hooks first (Editor.ts:2513-2523, plugins.ts:1207/1255), keeping the existing option-callback signatures as thin adapters. In test builds only, wire a listener-count threshold that logs the dominant registration stack (event.ts:1264-1277), which would catch plugin subscription leaks across the mount/unmount cycles in pluginLifecycle.test.ts.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Confirmed. No Emitter type exists: grep for `class .*Emitter|createEmitter|EventEmitter|type Event<` across packages/*/src returns zero producer abstractions (all `addEventListener` hits are raw DOM). Producers are hand-rolled Set-of-callbacks or bare optional fields, and I found three independent re-implementations: /Users/shaul/Desktop/D/Editor/packages/editor/src/documentSession.ts:249 `listeners = new Set<EditorTextBufferChangeListener>()` + :490-492 subscribe + :645 dispatch; /Users/shaul/Desktop/D/Editor/packages/editor/src/mergeConflictPlugin.ts:100 + :119; /Users/shaul/Desktop/D/Editor/packages/react/src/index.ts:463 `subscriptions = new Set<...>()`. Plus bare-callback producers at Editor.ts:2412 `sessionOptions.onChange?.(finalChange)`, Editor.ts:2515/2522 `this.options.onChange?.(...)`, plugins.ts:1207 `onDidChangeBlocks?.`, packages/diff/src/editorDiffPlugin.ts:73-99 (a single-slot listener with its own hand-written disposable).\n\nThe sharpest concrete defect, which the claim states abstractly, is at documentSession.ts:645: `for (const listener of this.listeners) listener(event)` \u2014 no try/catch. One throwing subscriber aborts the loop, so every listener registered after it silently never receives that buffer change, and the exception propagates back up into whatever edit call is in flight. Monaco's counterpart isolates per listener in `_deliver`. Confirmed the subscription half is fine \u2014 EditorDisposable (plugins.ts:27-29) is used consistently as the return type at plugins.ts:106, :417, :447-453, documentSession.ts:490.",
  "verifierNotes": "Keep, but buy only the parts that pay. WORTH IT: (1) error isolation \u2014 a `notifyAll(listeners, arg)` helper wrapping each call in try/catch and routing throws to the existing logger (packages/editor/src/logging) instead of aborting the fan-out; that is ~15 lines and fixes a real silent-data-loss path at documentSession.ts:645. (2) iterate a snapshot copy so subscribe/unsubscribe during dispatch is well-defined. (3) collapse the three hand-rolled Sets onto one tiny `EditorEventSource<T>` returning EditorDisposable, so the subscription+dispose contract is written once. NOT WORTH IT: Monaco's onWillAddFirstListener/onDidRemoveLastListener lazy wiring (our producers are already cheap fields on objects that exist anyway \u2014 there is no expensive upstream to attach), and Event.debounce/throttle/accumulate operators (we already have a far more capable scheduler in workScheduler.ts with task classes, budgets and abort; adding a second, weaker deferral mechanism would be a step back \u2014 consumers reaching for EditorWorkScheduler is the right answer, not a workaround). Effort: low for the kept part. Value: moderate."
 },
 {
  "domain": "api-perf-infra",
  "title": "DisposableStore and a pluggable disposable tracker for leak detection",
  "kind": "testing",
  "source": "monaco",
  "referenceLocation": "references/vscode/src/vs/base/common/lifecycle.ts:229-233 (setDisposableTracker), :85-180 (DisposableTracker \u2014 records a Stacktrace per disposable, computeLeakingDisposables, removePrefix noise-trimming of the captured frames), :49-83 (GCBasedDisposableTracker using FinalizationRegistry), :271-284 (trackDisposable/markAsDisposed/setParentOfDisposable), :327-360 (dispose() overloads over single/array/iterable), :380-400 (Disposable base class registering itself with the tracker)",
  "mechanism": "`IDisposable` is a one-method interface, exactly like ours, but every construction and disposal routes through two global hooks \u2014 `trackDisposable(x)` and `markAsDisposed(x)` \u2014 that are no-ops unless a tracker is installed. `DisposableStore.add` also calls `setParentOfDisposable(child, this)`, so the tracker learns the ownership tree, not just a flat set. In tests a tracker is installed that captures a stack per live disposable; `computeLeakingDisposables` walks the graph and reports the ones that were constructed, parented, and never disposed, with the *construction* stack attached and boilerplate frames stripped (:178). `GCBasedDisposableTracker` (:49-83) is the zero-overhead production variant: it registers each disposable with a `FinalizationRegistry` and reports only those collected without ever having been disposed. The ergonomic layer on top \u2014 `toDisposable(fn)`, `MutableDisposable` (swap-and-dispose-previous in one assignment), `DisposableMap`, and the `dispose()` overloads that accept a single value, an array, or any iterable \u2014 is what makes the pattern cheap enough that people actually use it consistently.",
  "whyItMatters": "We have a large plugin surface where every contribution point hands back an `EditorDisposable`, and correctness depends on every host and every plugin disposing every one on unmount. Right now that is enforced only by hand-maintained arrays and code review; a plugin that forgets one leaves a listener, a decoration, or a scheduler entry alive across editor remounts with no test that can catch it.",
  "ourStatus": "partial",
  "ourEvidence": "packages/editor/src/plugins.ts:27-29 `EditorDisposable`, with `disposeAll(registrations)` and manual arrays repeated at :791, :796, :855, :873, :1036, :1062, :1088, :1112, :1138, :1164, :1300 and a `disposeAll(result)` aggregator at :1311 \u2014 the same store-shaped logic re-implemented per contribution kind. `trackLifecycleRegistration` (plugins.ts, used at :1029 and :1055) and `createEditorLifecycleSummary` (Editor.ts:263) show we already want lifecycle visibility, but there is no construction-site attribution and nothing that can answer 'which disposables from this mount are still alive'. There is no `MutableDisposable` equivalent \u2014 swap-and-dispose is hand-written, e.g. plugins.ts:1226 and :1274 (`this.\u2026InvalidationDisposables.get(provider)?.dispose()` before re-registering). packages/editor/src/editor/pluginLifecycle.test.ts exists, so there is already a place for the assertion to live.",
  "value": "medium",
  "effort": "S",
  "implementationSketch": "Add `packages/editor/src/editor/disposables.ts` with `EditorDisposableStore` (add/clear/dispose, disposes immediately if the store is already disposed \u2014 lifecycle.ts:410-460), `toEditorDisposable(fn)`, and `MutableEditorDisposable`. Route creation and disposal through two module-level hooks `trackDisposable`/`markAsDisposed` with a `setEditorDisposableTracker(tracker | null)` setter, defaulting to null so production cost is zero (lifecycle.ts:229-284). Replace the eleven ad-hoc arrays in plugins.ts with stores and the two manual swap sites (:1226, :1274) with `MutableEditorDisposable`. Then in packages/editor/test add a helper that installs a stack-capturing tracker around a mount/unmount cycle and asserts nothing survives \u2014 wire it into pluginLifecycle.test.ts first, then make it a shared fixture so every plugin package (find, minimap, gutters, diff, lsp-plugin) gets the same assertion for free.",
  "verified": true,
  "statusCorrected": false,
  "verifierEvidence": "Status is right, but the claim's central assertion \u2014 'there is no construction-site attribution and nothing that can answer which disposables from this mount are still alive' \u2014 is largely refuted. /Users/shaul/Desktop/D/Editor/packages/editor/src/plugins.ts:556 declares `lifecycleRegistrationStack: EditorDisposable[][]`; installPlugin (:843-861) and activatePlugin (:864-879) each push a fresh array, run the plugin body inside try/finally, and pop; every one of the 13 `register*` methods routes its disposable through `trackLifecycleRegistration` (:1279-1284) which pushes onto `lifecycleRegistrationStack.at(-1)` (:1286-1288). So each registration is AUTOMATICALLY attributed to the owning plugin lifecycle with no `_register(...)` call at the site \u2014 implicit ownership, which is stronger than VS Code's explicit DisposableStore.add. `lifecycleDisposableFromResult` (:1291-1300) returns a `disposableOnce` (idempotent, :1319-1329) that disposes the plugin's own result then `disposeAll(registrations)`, and disposeAll (:1315-1317) uses `.toReversed()` \u2014 reverse-order teardown, same as DisposableStore. Failure rollback exists too (:855, :873 dispose the partial set on a throwing install/activate). The `disposeAll` + manual-array sites the claim lists at :791/:796/:1036/:1062/... are those same registration arrays, not 11 independent re-implementations.\n\nWhat IS genuinely missing: a global trackDisposable/markAsDisposed hook, construction-site stack capture, and a MutableDisposable (swap-and-dispose is hand-written at plugins.ts:1226 and :1274).",
  "verifierNotes": "Keep \u2014 but for a hole I found while refuting it, not for the tracker. plugins.ts:1279-1284 reads `if (registrations) registrations.push(disposable)` and SILENTLY DROPS the disposable when `currentLifecycleRegistrations()` returns null (:1286-1288). That is null for every registration made outside the synchronous install/activate body \u2014 i.e. anything a plugin registers from an async continuation, a timer, or an event handler after activation. Those registrations are never auto-disposed on unmount and never appear in the lifecycle summary (Editor.ts:263 createEditorLifecycleSummary / :2665). That is a real leak class in a plugin system where async registration is normal (LSP capabilities arriving after initialize, for instance), and it is invisible today. Minimal fix: have the plugin context carry its own owning store so late registrations attribute to the plugin rather than to nothing, and make the null branch loud (log or throw in dev) instead of silent. Add MutableDisposable while there \u2014 it is ~10 lines and deletes the hand-written swap at plugins.ts:1226/:1274. SKIP the global tracker + stack capture: with implicit per-lifecycle attribution already in place, the leak surface it would find is almost entirely the null branch above, which is cheaper to close directly. Assertion belongs in the existing packages/editor/src/editor/pluginLifecycle.test.ts."
 }
]